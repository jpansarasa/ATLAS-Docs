# SentinelCollector — architecture [agent read-first]

NOTE: this card covers the news->matrix pipeline which spans SentinelCollector AND MacroSubstrate (see MacroSubstrate/README for the shared write path).

PURPOSE: news-article -> `(signal x sector)` matrix tilt via GPU vLLM JSON-CoD extraction (Extraction:Backend=VllmJson, Qwen2.5-32B-AWQ) + `:sig:` rows in macro_observations. no gRPC cell push; digest + extraction/CoVe/CoD paths not addressed here. (CPU llama-server DSL extraction = rollback path.)

DATA MODEL + INVARIANTS:
  INV `:sig:` infix: news row identified SOLELY by literal `:sig:` in source_id (`{rawContentId}:sig:{signalId}`). not schema-enforced — string contract; FOUR artefacts move together: producer (MacroObservationRouter) + projector const (ObservationCellProjector) + consumer (NewsMomentumQueryService) + digest reader (DigestQueryService.ArticleSignalsSql + TryParseRawContentId). change-all-or-none.
  INV signal⊥sector: signal_identity_id=SIGNAL dim gates projection; atlas_sector_code=SECTOR dim does NOT gate. null-signal->never projects (qualitative rows skipped permanently).
  INV |v|<=1: `:sig:` rows carry |value|~<=1; projector excludes |v|>1.5 (raw-value leak guard); drops non-`:sig:` rows from decay sum even if same group (G2 mixed-group drop).
  INV heal-on-rewrite: macro_observations = authoritative cell source; projector re-projects in place (DO UPDATE), never DO NOTHING.
  INV sector-dim⊥entry: resolve-entities sectorless/fail -> row still written (sector=null or fallback); prepass feeds sector-pick inside classify, not an entry gate.
  INV incomplete-SectorWeights: group whose pattern's SectorWeights missing any sector -> ArgumentException caught -> group skipped, 0 cells, logged Warning not Error.
  INV decay: news magnitude = K*tanh(S/K) over half-life-weighted decay sum (freshness=1.0, no floor). K+H from Matrix:NewsDecay. Not the hard-data mean formula.
  review_status AutoClosed = terminal NoResolution disposition (Stage 1): NoResolution rows never auto-approve/reject -> would pile up Pending forever. AutoClosed exits the human queue (ReviewUiEndpoints query = Pending AND InstrumentId!=null AND ResolutionState==Resolved — NoResolution rows are excluded from the human view by the ResolutionState==Resolved filter whether or not they carry a stray instrument_id, see review-UI GOTCHA; AutoClose = the terminal/digest transition) while the digest KEEPS it (DigestQueryService !=Rejected; NOT Rejected — that would strip legit macro/qualitative digest content). Set by NoResolutionSweepWorker (age-gated, NoResolutionCloseGraceDays default 7) + POST /admin/review/close-noresolution. No D-entry — terminal-state janitor, not an exception path.

PATHS (distinct code — do not conflate):
  NewsSignalClassifier [in-proc · inside ExtractionProcessor]
    does: structured vLLM call; inlines signal_identities catalog + 11 sector codes; validates ids; drops <0.5 confidence / NaN / hallucinated.
    does NOT: throw (fail-soft->Empty); parse free-form; touch DB.
    on-miss: empty catalog / empty excerpt / timeout / unstructured -> NewsSignalClassification.Empty; extraction proceeds.
    ⚠ excerpt capped before call — signal in tail -> Empty, not failure. Sub-floor confidence empties feed silently (counted, not errored).
  MacroObservationRouter writer [in-proc · MacroSubstrate]
    does: upsert value_numeric=tilt*confidence; source_collector="sentinel"; `:sig:` source_id.
    does NOT (NEWS caller): fall back to extracted_observations — Failed=row LOST (by design); count IdempotentSkip as progress.
    ⚠ CollectedAt stored as UTC instant of its offset, not source wall-clock; matters for idem-key matching.
    ⚠ NUMERIC caller DOES fall back to extracted_observations two-phase. Fallback behavior diverges by CALLER not method.
  ObservationCellProjector [ThresholdEngine background · DB-poll]
    does: read Kind=Numeric DESC; group by (signal_identity_id,source_collector) -> 11 sector rows/group; decay sum -> K*tanh(S/K) * trust * temporal * confidence * sectorWeight clamped +/-3; upsert matrix_cells.
    does NOT: gate on sector; project null-signal rows; project unknown/disabled patterns; write partial cell-set for incomplete SectorWeights.
    does not run when Matrix:ObservationProjectorMode=Off -> zero matrix_cells + no error. CHECK this config FIRST before concluding pipeline broken.
    ⚠ Shadow is not dry-run: runs identical read+project+write as Authoritative (same cells written); only Off suppresses writes.
    ⚠ read DESC + hard cap -> sustained ingest silently drops oldest (cap-induced staleness, metered).

PROCESSING ORDER (per article): normalize -> EntityResolutionPrepass(spaCy NER->SecMaster,fail-soft) -> V2 pipeline -> TryClassifyNewsSignalsAsync -> per-signal preferredSector=grounded??classifier.Sector -> plan -> write -> addRange.
MAXIM: prepass output FEEDS classify as parameter -> router derives grounded sector. Prepass=sector input, not an entry gate.

DISTINCTIONS:
  `:sig:` news row ≠ raw-legacy sentinel row — same table/group; only `:sig:` feeds projector decay sum; legacy raw values dropped (G2).
  signal-dim (signal_identity_id, gates projection) ≠ sector-dim (atlas_sector_code, does NOT gate).
  matrix feed (macro_observations->projector->matrix_cells) ≠ digest consumer (matrix_cells+sector_regimes+sentinel rows, separate read; theme taxonomy retired — sector is the only digest axis) ≠ extracted_observations (legacy numeric fallback sink, not read by matrix or digest).
  digest news-momentum SQL filters source_collector='sentinel' AND signal_identity_id IS NOT NULL AND value_numeric BETWEEN -1.5 AND 1.5 — not a `:sig:` infix filter; splits window at midpoint for early->late per-signal tilt trend (replaces former bias-vs-FRED-matrix view, removed: FRED matrix too stale/sparse -> fake divergence). Legacy non-`:sig:` sentinel numeric rows WITH signal_identity_id enter digest momentum avg but ARE dropped by projector (G2). Two consumers see DIFFERENT effective inputs.
  news magnitude=decay-weighted SUM(K*tanh(S/K)) ≠ hard-data mean(sum(o_i)/n * freshness scalar). Unexpected news cell magnitude -> CHECK Matrix:NewsDecay K/H.
  ThresholdEngine projector(DB-polled) ≠ gRPC MatrixUpdateStream/ObservationEventStream (separate event surface; matrix feed is DB-polled not gRPC).

CROSS-SERVICE:
  IN: SecMaster HTTP :8080 /api/signal-identities (catalog warmup) + /api/resolve-entities (sector grounding, fail-soft). vLLM GPU (classify).
  IN: ThresholdEngine gRPC-stream :5001 ObservationEventStream (ValidationEventConsumerWorker — sector-crossing events -> validation triggers).
  OUT: macro_observations via in-proc DI, not network. FEEDS: ThresholdEngine projector (DB-poll) -> matrix_cells; digest reads matrix_cells separately.
  EXPOSES gRPC :5001: ObservationEventStream (ThresholdEngine consumes) + SecMaster gRPC :5001 RegisterSeries (f-a-f on extracted observations).

GOTCHAS:
  ✗ gate-news-entry-on-resolve-entities/sector ✗ route-news-via-gRPC-MatrixUpdateStream
  ✗ assume-CandidateSurfaceFilter-catches-all-junk: STRUCTURAL junk + curated non-entity classes are rejected at ingress (D-1 #824 markup/slug/money/country/institution/crypto; D-5 govt-agency/regional-Fed/byline/garbled-fragment #823 recurrence; D-11 curated non-tradeable media-outlet set — a market-INDEX class was rejected: US indices resolve to real tracked instruments) and markup stripped at the normalizer last-resort (D-2, #825), but SEMANTICALLY-ambiguous surfaces (media ISSUER names like BuzzFeed=BZFD/NYT, market-index names, no-digit slugs, bare public-official SURNAMES) Keep BY DESIGN -> SecMaster resolve -> its budget-gated Gemini last-resort (#823, company-name->ticker ONLY; ungated it drained $100/6d 2026-06-30). Defense-in-depth, not redundancy: ingress cleans the source, SecMaster gates the $. TWO filter SOURCES, both now cleaned: the NER-candidate ingress (D-1/D-5, feeds the SecMaster->resolver path) AND the LLM-extracted V2 SubjectEntity (D-6 — DeterministicResolver.TryGeminiResolveAsync calls :9300 DIRECT, a SEPARATE source that BYPASSED the ingress filter until D-6 ran the SAME filter over it before the paid call); only ambiguous-Keep is uncaught, by design. [[GIGO]] [[INTENT_FIDELITY]]
  ✗ extracted_observations-fallback-for-news ✗ assume-digest-sees-only-`:sig:`-rows ✗ Shadow=dry-run
  ✗ qualitative(null-signal)-rows-reach-matrix_cells ✗ zero-matrix_cells-no-error=broken(check-Mode-first)
  ✗ change-`:sig:`-infix-in-one-artefact ✗ CollectedAt-as-wall-clock ✗ numeric-fallback-applies-to-news
  ✗ incomplete-SectorWeights-writes-partial-cells ✗ prepass-as-entry-gate
  review-UI actionable filter: `/ui/review` list AND Pending count share ONE query = ReviewStatus==Pending AND InstrumentId!=null AND ResolutionState==Resolved (ReviewUiEndpoints.ActionablePending — one predicate so list + count agree). Human approves/rejects RESOLUTIONS; a row with no instrument, OR a NoResolution row carrying a stray instrument_id (a ticker co-mentioned with an unrelated subject — 69% of the raw queue), has NOTHING to action -> auto-disposed by the background sweep/drain, NOT human-reviewed. No data hidden — those rows persist + still age out, just not in the human view. ✗ drop-EITHER-filter: dropping InstrumentId!=null lets null-instrument rows leak; dropping ResolutionState==Resolved lets NoResolution-with-stray-symbol garbage leak — both re-flood the queue and mask the actionable rows. GOTCHA-note not a D-entry — display-side invariant, not a scarce-resource/exception path.

DECISIONS:
  D-1 ingress-junk-filter: INTENT reject non-entity surfaces at the ingress where garbage is BORN (#824) — junk poisoned FRED search (#818), paid Gemini (#823) AND resolution correctness for FREE (wrong-ticker -> corrupt matrix); one source fix covers every consumer / PRECOND reject STRUCTURALLY-invalid only (markup-chars|dotted-id|slug+digit|money/number|metric-abbrev|country|institution|crypto); semantically-ambiguous names Keep -> SecMaster decides (dropping a real equity = the expensive error); destination gates stay defense-in-depth / GUARD EntityResolutionPrepass.ApplySurfaceFilter @ src/Services/EntityResolutionPrepass.cs:386 / TEST EntityResolutionPrepassFilterTests.enforce_mode_removes_junk_candidates_before_secmaster_call
  D-2 html-strip-last-resort: INTENT the last-resort path (normalization failed: trafilatura empty/erroring on text/html, markitdown on the binary+rss legs) must NEVER emit markup as content (#825) — raw DOCTYPE/tags/script bodies reach the LLM extractor + spaCy NER as bogus entities / PRECOND strip failure -> empty not raw (fail-closed); OptionMaxNestedChildNodes=1000 converts an UNCATCHABLE StackOverflow (deep nesting; ~4.5k tags crashed the 1MB worker stack) into a catchable load failure / GUARD ContentNormalizer.StripHtmlFallback @ src/Services/ContentNormalizer.cs:330 / TEST ContentNormalizerTests.should_return_visible_text_not_markup_when_both_normalizers_fail_on_html
  D-3 llm-signal-validation: INTENT LLM output is never trusted into macro_observations — hallucinated ids/NaN/sub-floor dropped + counted per reason (feed must never silently re-zero); tilt clamp [-1,1] = SOURCE enforcement of INV |v|<=1 (projector 1.5 gate = destination backstop) / PRECOND classifier is additive enrichment: fail-soft->Empty, never breaks extraction / GUARD NewsSignalClassifier.Validate @ src/Semantic/NewsSignalClassifier.cs:192 / TEST NewsSignalFeedWiringTests.should_drop_hallucinated_id_so_only_valid_signals_route
  D-4 gpu-prompt-budget: INTENT refuse an over-budget prompt BEFORE the GPU call (throw PromptTooLargeException, zero HTTP) — context-overflow 400s waste GPU round-trips and mis-log as extraction errors / PRECOND callers assembling prompts client-side derive budgets from PromptTokenAllowance/ExactPromptTokenAllowance (guard+caller can never disagree); structured path verifies vLLM /tokenize exact count — chars/4 under-counts ticker-dense prose (#692) / GUARD VllmClient.EnforcePromptBudget @ src/Services/VllmClient.cs:597 / TEST VllmClientPromptSizeGuardTests.should_not_send_http_request_when_prompt_too_large_for_generate
  D-5 ingress-non-entity-classes: INTENT companion to D-1 (supersedes none) — extend the SAME ingress cleanup to the non-entity surface classes that flooded the paid Gemini resolver AFTER D-1 shipped (govt agency/regional-Fed, journalist byline, garbled ticker-fragment); gating only the resolver destination (#823) is the recurring disease — the junk re-appears at the next consumer, so reject at the SOURCE and every consumer (FRED, Gemini, storage, matrix) is covered once / PRECOND reject only UNAMBIGUOUS non-instruments: curated federal-agency/stats-bureau set + trailing-"Fed" regional rule; political parties (trailing-"Party" only — "Party City" leads); govt institutions (standalone keyword Institute/Ministry/Directorate/Parliament/Politburo anywhere, corp-suffix-exempt); byline = embedded newline + all-caps dateline; garbled = >=3 tokens containing a digit-bearing dotted uppercase ticker code. Bare/full public-official PERSON names (Carney/Warsh/Chuck Schumer) NOT rejected — no precise structural discriminator, a blocklist would nuke person-named issuers (Morgan Stanley, Charles Schwab); ambiguous names Keep -> SecMaster resolve + resolver daily-cap stay defense-in-depth. credential/degree class DROPPED 2026-07-02 — 9/22 tokens were live US tickers (DDS=Dillard's/CPA/ESQ equities, DBA/CFA ETFs, MPA/SCD/EDD CEFs, MSC ADR); rejected real instruments while firing ~never; precision > recall means an ambiguous degree Keeps / GUARD CandidateSurfaceFilter.Classify @ src/Services/CandidateSurfaceFilter.cs:408 / TEST EntityResolutionPrepassFilterTests.ingress_rejects_agency_byline_and_garbled_but_keeps_real_entity
  D-6 v2-direct-gemini-surface-filter: INTENT companion to D-5 (supersedes none) for the SEPARATE V2 Sentinel-direct source — the LLM-extracted SubjectEntity reaches the paid :9300 Gemini resolver via DeterministicResolver WITHOUT the prepass ingress filter, so the same junk classes D-5 cleans at the NER-candidate source still flood the shared 1500/24h cap here (the outcome=client_null 429 storm, ~146/hr, starves genuine last-resort resolutions); GIGO cleans at EVERY source not per-destination, so run the SAME ICandidateSurfaceFilter (reuse only — no new classification) over the SubjectEntity BEFORE the call / PRECOND identical to D-5 — EntityCandidate(SubjectEntity, CompanyName) so behaviour matches the NER-candidate path EXACTLY; reject only unambiguous non-instruments, ambiguous names Keep (a false-positive silently drops a real resolution — worse than junk the cap already refuses for $0); a junk SubjectEntity skips the call == non-resolution (caller falls through to Rule 2/3), counted outcome=surface_filtered on sentinel_gemini_resolver_calls_total (specific reason rides activity/log, NOT a metric tag) / GUARD DeterministicResolver.TryGeminiResolveAsync @ src/Services/DeterministicResolver.cs:546 / TEST DeterministicResolverTests.gemini_direct_path_filters_junk_subject_but_calls_for_real_entity · V1 MIRROR (added w/ D-12): the V1 leg GeminiSymbolFallbackService.LookupAsync never ran this filter — it now runs the SAME Classify over the SubjectEntity BEFORE its paid :9300 call (a junk subject skips the call for $0 == NoMatch) / GUARD +GeminiSymbolFallbackService.LookupAsync @ src/Services/GeminiSymbolFallbackService.cs:85 / TEST +GeminiSymbolFallbackServiceTests.v1_gemini_surface_filters_junk_subject_before_paid_call
  D-7 near-dup-collapse: INTENT companion to D-1/D-5/D-6 (supersedes none) — collapse near-duplicate NER spans of the SAME entity from the SAME article to ONE candidate at the SOURCE (the Sentinel prepass set, AFTER the junk gate, BEFORE the SecMaster POST), so every downstream consumer (SecMaster resolve, the paid :9300 Gemini last-resort, the matrix) sees one candidate and burns ONE slot of the shared 1500/24h cap, not N, and cannot double-count the entity; the existing exact-surface+kind dedup at the SecMaster DESTINATION (EntityResolutionService.cs:144 GroupBy (Surface.Trim, Kind)) does NOT catch article/function-word variants — "Cuban Institute of Friendship" vs "...for Friendship" = two paid resolutions / PRECOND merge ONLY when unambiguously the same entity: identical ORDERED content-token key within ONE Kind — lowercase, strip a leading article, strip punctuation (. , ' & quotes parens) per token, DROP curated function words {of,for,and,the,a,an,at,in,on,to}+bare &, join remaining ORDERED tokens; per group keep the LONGEST surface (tie->first, stable), survivors stay in original order. Precision over recall — a FALSE merge silently DROPS a distinct entity's resolution (strictly worse than the minor waste of resolving a true dup twice): "Bank of America"!="Bank of China", "Morgan Stanley"!="Stanley Black & Decker", "First Solar"!="First Republic" (different content key); different Kind never merges (key includes Kind). NO fuzzy similarity; NO corp-suffix stripping ("Apple"!="Apple Inc" — left to exact-dedup+resolver cache, suffix stripping widens the false-merge surface). ACCEPTED MISS: concatenation variants ("JPMorgan"!="JP Morgan", different token count) stay distinct — a miss costs one resolver slot, a false merge loses a real entity. Always-on (provably safe: identical content key = same entity; no shadow mode) / GUARD EntityResolutionPrepass.CollapseNearDuplicates @ src/Services/EntityResolutionPrepass.cs:484 / TEST EntityResolutionPrepassNearDupTests.near_dup_variants_collapse_but_distinct_token_sharing_entities_survive
  D-8 exact-candidate-recovery: INTENT recover REAL instruments the surface-form-sensitive fuzzy confirm misses on colloquial/bare names ("Nike"/"Tesla" fall below the ~0.85 fuzzy threshold and are filtered, discarding the already-correct EXACT-catalog ticker) — the ~27% of NoResolution that are genuine misses / PRECOND an id-less candidate's Symbol is an EXACT primary-symbol catalog hit (GetInstrumentBySymbolAsync, verbatim symbol equality — NEVER fuzzy, no similarity score) AND the OBSERVATION SUBJECT (input.SubjectEntity) shares >=1 content token with the catalog hit's Name (SubjectNameNormalizer.SharedTokenCount(SubjectEntity,hit.Name)>=1). Subject-overlap, NOT candidate-name overlap: the candidate is catalog-sourced so c.Name==hit.Name, and a Math.Max(candidate,subject) term is TAUTOLOGICAL (always >=1) and BYPASSES the subject check — a merely co-mentioned company then resolves to the WRONG instrument -> matrix corruption (the CRITICAL this refines). NOT per-observation LLM selection AT THIS LEG — and since #913 that is STRUCTURAL, not incidental: V2 passes ARTICLE-WIDE Candidates (GpuJsonExtractionService -> DslToMergedExtractionAdapter builds ONE candidate list from ALL doc ENTs; index stays null unless a per-NUM source_entity exact-matches an ENT verbatim_name), and a row that DOES carry a pick fires Rule 1 and RETURNS BEFORE this leg once its ResolutionConfidence clears ResolverCandidatePickThreshold. Reaching TryExactCandidateMatchAsync therefore means no USABLE pick existed (index null, OR its confidence sat below the threshold — a sub-threshold pick does arrive here), so subject-overlap is still the enforceable guard. Empty/whitespace subject -> overlap 0 -> refused (a subjectless observation must not resolve via this leg). Does NOT contradict SecMaster D-2 (fuzzy-proposes-authoritative-confirms): exact symbol existence + subject-name overlap IS authoritative confirmation, not a fuzzy proposal, so the wrong-neighbour failure mode is not reintroduced — new D-entry, NOT a D-2 supersession / GUARD DeterministicResolver.TryExactCandidateMatchAsync @ src/Services/DeterministicResolver.cs:353 / TEST DeterministicResolverTests.exact_match_leg_refuses_comention_when_subject_mismatches_catalog_name
  D-9 method-aware-review-drain: INTENT drain the Pending review queue DOWNSTREAM of the V2 write (the V2 path keeps writing everything Pending — DD-4=b PRESERVES the 2026-05 cutover "downstream owns approval": this scheduled drainer IS that downstream approver, so NO cutover reversal / NOT a supersession). AutoApprovePolicy.Evaluate runs on a schedule, NON-dry-run, over review_status=Pending rows only, batched + interval-bounded, the SAME single-source gate the backfill endpoint + live + re-extract paths use / gate = Resolved AND instrument non-null AND (resolution METHOD in AutoApproveMethods {ticker_in_quote (D-10), llm_candidate_exact (D-8)} OR extracted_at < now - ReviewGraceDays). METHOD, not confidence, is the quality signal: confidence is useless for sizing the queue (94% of Resolved rows are ticker_in_quote at a FLAT 0.900 — theater); the errors cluster by method (gold->BEA index, "$30T debt"->30Y yield, Bitcoin->ETH were ALL cove_VectorSearch). Exact subject-verified legs auto-approve; fuzzy/semantic methods (cove_*, hybrid_subject, gemini_fallback, llm_candidate_pick, null/unknown) are HELD for human review (~2-5/day, owner reviews <=10/day) then DEFAULT-ACCEPT after ReviewGraceDays (default 3) so the queue can NEVER wedge. Review-hold applies to FRESH fuzzy (recent extracted_at); the re-extract path reprocesses the ~80K aged backlog whose extracted_at is OLD, so agedPastGrace already fires and its fuzzy re-resolutions DEFAULT-ACCEPT with no review window — DELIBERATE (Evaluate keys agedPastGrace off original ExtractedAt): those rows are NOT in the current recent-news digest (low review value), and holding ~80K would FLOOD the bounded <=10/day queue. Low-stakes either way since extracted_observations feed DigestQueryService !=Rejected, NOT the WS3 matrix. auto-reject still fires at extraction < 0.5 (keeps garbage extractions out of the digest); NoResolution / null-instrument rows stay Pending (Stage-1 sweep owns them). SUPERSEDES the #856 "trust all Resolved" refinement (it auto-approved fuzzy rows too, surfacing wrong-instrument garbage) / GUARD AutoApproveDrainWorker.RunOneBatchAsync @ src/Workers/AutoApproveDrainWorker.cs:265 / TEST AutoApproveDrainWorkerTests.Drain_approves_exact_method_holds_fresh_fuzzy_and_keeps_noresolution_pending AND Drain_default_accepts_fuzzy_row_aged_past_review_grace AND AutoApprovePolicyTests.guard2_fuzzy_method_resolved_instrument_recent_stays_pending
  D-10 ticker-in-quote-subject-guard: INTENT mirrors D-8's wrong-ticker principle at BOTH ticker_in_quote resolution sites — the LIVE ExtractionProcessor.ApplyLocalResolutionAsync leg (PRIMARY co-mention source) and the ReExtractResolutionAdapter re-extract leg (secondary) attach a ticker found in the quote/filing to the observation, but a quote frequently CO-MENTIONS a ticker unrelated to the SUBJECT (verified live: Gilbert Cisneros [congressman's trade disclosure]->LLY, Banxico [Bank of Mexico]->EWW, Jewett Cameron Trading->EWTX); the ticker exact-matches a catalog symbol (IsExactSymbolMatch passes) so it attached as a WRONG-ticker -> polluted the digest; the review-UI filter (#856) only HID this garbage, it did not stop it being generated / PRECOND before attach require TickerInQuoteGuard.SubjectSharesInstrumentToken(subject, <attached instrument's catalog Name>) i.e. SubjectNameNormalizer.SharedTokenCount >= 1 (subject-overlap, same normalizer + principle as D-8) — ONE shared predicate wired at both sites so they cannot drift. ResolveAsync returns no Name -> fetch it via GetInstrumentBySymbolAsync/InstrumentLookupDto.Name (same client the D-8 leg uses; no new dependency). Overlap 0 — OR unavailable Name OR subjectless observation — REFUSES: set NoResolution + return (NOT fall-through: the description+CoVe path would re-ground the very same in-quote symbol, whose Symbol is literally in the quote, and re-attach the co-mention). Subject spans v1 (SourceEntity) and v2 (SubjectEntity) — each caller passes whichever is present: live + full re-extract read the FRESH extraction (`result.SubjectEntity ?? result.SourceEntity`; the hot-path observation never populates SubjectEntity so `result` is authoritative), resolve-only reads the existing row. New D-entry NOT a D-8 supersession (distinct sites/guard/tests) / GUARD TickerInQuoteGuard.SubjectSharesInstrumentToken (shared predicate) @ src/Extraction/TickerInQuoteGuard.cs:74 — called @ ExtractionProcessor.ApplyLocalResolutionAsync src/Workers/ExtractionProcessor.cs:1139 AND ReExtractResolutionAdapter.ResolveInternalAsync src/Workers/ReExtractResolutionAdapter.cs:182 / TEST ExtractionProcessorResolutionTests.live_ticker_in_quote_refuses_comention_when_subject_mismatches_catalog_name AND ReExtractBackgroundServiceTests.resolve_only_ticker_in_quote_refuses_comention_when_subject_mismatches_catalog_name
  D-11 ingress-media-outlet-class: INTENT companion to D-5/D-6 (supersedes none) — extend the SAME shared ICandidateSurfaceFilter over one more curated non-entity class that floods the paid Gemini resolver: non-tradeable news/media outlets (Rolling Stone/Eater/Indiewire/TechCrunch/Politico/Axios/The Verge/Ars Technica + the BuzzFeed Celeb section label). ONE edit to Classify covers BOTH the D-5 NER-candidate prepass AND the D-6 V2-direct path (both call Classify) — clean at the SOURCE, not per-destination / PRECOND reject ONLY unambiguous NON-TRADEABLES via EXACT case-insensitive curated match; a false-positive SILENTLY DROPS A REAL RESOLUTION (worse than junk the daily cap refuses for $0), so real media ISSUERS stay ABSENT and Keep (BuzzFeed=BZFD, News Corp=NWSA, NYT, Gannett=GCI, Fox=FOXA, Warner Bros Discovery=WBD, Paramount, Netflix, Disney, Comcast). Match uses NO "the " stripping ("The Verge" verbatim so bare "Verge"=crypto XVG never trips). A market-INDEX reject class was considered and REMOVED (2026-07-07): US indices resolve to REAL tracked instruments (S&P 500->SP500 [SentinelCollector-sourced]+SPX, DJIA, Nasdaq Composite->IXIC, Russell 2000->RUT) and even bare "Nifty" fuzzy-matches live tickers (NIFTY Lifestyle 4262.T + Nifty ETFs) — a name-based index reject VIOLATES the D-5 PRECOND (silent false-drop of a legitimate resolution); index junk ("Nifty Smallcap") is left to the Gemini daily cap + D-1 server gate. PERSON names stay OUT of scope (D-5 PRECOND — no structural discriminator, a blocklist would nuke person-named issuers) / GUARD CandidateSurfaceFilter.Classify @ src/Services/CandidateSurfaceFilter.cs:408 / TEST CandidateSurfaceFilterTests.should_reject_media_outlets_as_MediaOutlet AND EntityResolutionPrepassFilterTests.ingress_rejects_media_outlet_but_keeps_real_issuer
  D-12 gemini-selfseed-authoritative-confirm: INTENT companion to D-8/D-10 (supersedes none) — "fuzzy proposes, AUTHORITATIVE confirms" for the Gemini auto-register legs (BOTH the V2 DeterministicResolver leg AND the V1 ExtractionProcessor leg), the ONE resolution path that self-seeded a BRAND-NEW SecMaster instrument on a bare Gemini answer (conf>=0.7, NO authoritative id, NO subject corroboration), so a person/state/country subject grounds to a real-but-irrelevant EQUITY ticker and pollutes the catalog + matrix (#872 cleaned; recurred same-day). D-8 (exact-candidate) + D-10 (ticker-in-quote) gave the OTHER legs subject-grounding; this Gemini self-seed leg never had ANY confirmation. A NEW instrument is self-seeded ONLY after an authoritative identifier source confirms the Gemini ticker; attach-to-existing (secmaster_match) is unaffected — ONLY new-instrument CREATION is gated / PRECOND before RegisterInstrumentAsync require BOTH gates (SelfSeedRefusalReason returns null): (1) AUTHORITATIVE — ConfirmTickerAsync -> SecMaster GET /api/identifiers/resolve?ticker={sym} (local catalog + ticker-alias, then OpenFIGI /v3/mapping; authoritative-ONLY, NO fuzzy/vector tier) yields a TRI-STATE TickerConfirmationOutcome (EXTENDED, not superseded, by the confirm-seam PR): Confirmed (200) | Unconfirmed (404 = authoritative MISS, both sources ANSWERED and declined) | Unavailable (503/transport = NO verdict; SecMaster's resolve endpoint 503s — never 404s — on an OpenFIGI outage, so a 404 is never an outage in disguise). EVERY non-confirmed state -> NoResolution, fail-closed (an outage suppresses self-seeding, never opens it), but with DISTINCT bounded refusal reasons {unconfirmed|subject_mismatch|confirm_unavailable} tagged reason= on the selfseed_refused increments of BOTH legs' counters (sentinel_gemini_resolver_calls_total / sentinel_gemini_fallback_calls_total) + gemini.selfseed_refused_reason activity tag on BOTH legs — pre-extension the outage was laundered into "miss", silently disabling self-seeding during outages while the metric read as the-gate-working (Information refusal logs are filtered in prod; the tag is the prod-visible signal); AND (2) SUBJECT-CORROBORATION — the confirmed instrument's canonical Name shares >=1 content token with the observation subject (TickerInQuoteGuard.SubjectSharesInstrumentToken, the SAME D-8/D-10 predicate): OpenFIGI proves the ticker is REAL, not that it belongs to THIS subject, so a person/country subject Gemini maps to a real-but-irrelevant ticker (#872) is a wrong-ticker even when equity-shaped -> refused. A confirmation carrying an InstrumentId is a ticker-alias hit the primary by-symbol lookup missed -> ATTACH (no duplicate), still subject-corroborated. REUSE SecMaster's existing endpoint — do NOT reinvent OpenFIGI in SentinelCollector. NOTE the SecMaster register guard (RegistrationService.EvaluateGuard) is asset-class-keyed (rejects Economic-from-untrusted + sub-2-char equity) and does NOT stop an equity-shaped junk row — so the old "GeminiFallback collector tag makes the guard reject junk" comments were FALSE COMFORT (corrected in this PR); THIS two-gate check, not the tag, is the protection. SENDER CLAMP (same confirm-seam PR, UPHOLDS SecMaster D-4 — the allowlist is NOT widened): both register sites send SelfSeedAssetClass.Clamp(assetClass) — case-insensitive member of the D-4 allowlist {Equity,ETF,Index,Currency,Crypto,Commodity} -> canonical spelling, anything else incl null ("Stock"/"ADR"/"REIT"/"Fund") -> "Equity" — because a confirmed, subject-corroborated instrument carrying Gemini's free-form label was silently rejected at register (macro_class_from_untrusted); GIGO fixes the sender, never the allowlist. ONE shared PURE gate wired at both legs so they cannot drift (D-8/D-10 pattern) / GUARD GeminiSelfSeedGate.SelfSeedRefusalReason (shared gate) @ src/Services/GeminiSelfSeedGate.cs:60 + SelfSeedAssetClass.Clamp (shared clamp) @ src/Services/SelfSeedAssetClass.cs:36 — called before self-seed @ DeterministicResolver.TryGeminiResolveAsync src/Services/DeterministicResolver.cs:675 AND ExtractionProcessor.TryGeminiFallbackAsync src/Workers/ExtractionProcessor.cs:1481 / TEST DeterministicResolverTests.gemini_selfseed_refuses_unconfirmed_symbol AND DeterministicResolverTests.gemini_selfseed_refuses_confirmed_ticker_when_subject_mismatches AND DeterministicResolverTests.gemini_selfseed_persists_openfigi_confirmed_symbol AND DeterministicResolverTests.gemini_person_subject_unconfirmed_yields_noresolution AND DeterministicResolverTests.gemini_selfseed_attaches_to_existing_when_confirm_returns_instrument_id AND DeterministicResolverTests.gemini_selfseed_refusal_reason_visible_as_bounded_metric_tag (unavailable row REDs if the outage is laundered into "unconfirmed") AND DeterministicResolverTests.gemini_selfseed_sends_only_d4_allowlist_asset_class_to_register AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_refuses_unconfirmed_symbol AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_persists_confirmed_symbol AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_refuses_unavailable_confirm_with_its_own_reason AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_clamps_sent_asset_class_to_d4_allowlist AND SecMasterClientTests.confirm_ticker_returns_unavailable_not_unconfirmed_on_503 AND (SecMaster side) IdentifierResolutionServiceTests.should_signal_unavailable_when_openfigi_fails_never_a_null_miss + IdentifiersEndpointsTests.should_return_503_not_404_when_resolution_unavailable
  D-13 runtime-log-level: INTENT a WARN-quiet-by-design collector must be raisable to Information/Debug AT RUNTIME (on-demand deep debugging) with NO restart or redeploy, while steady state stays Warning (prod-quiet). A LoggingLevelSwitch is the SOLE level authority; the operator edits Serilog:MinimumLevel:Default in the RUNNING container and reloadOnChange picks it up live. Serilog.Settings.Configuration 9.0.0 does NOT re-apply MinimumLevel on IConfiguration reload, so an explicit ChangeToken re-parse is what makes the edit take effect; the MEL floor sits at Debug so the bridge does not pre-filter below the switch. The per-source Overrides (DigestService etc. at Information) are independent of the switch and survive — raising Default to Debug lifts every un-overridden source; the raised level lives ONLY in the container's writable layer — revert by editing the key back to Warning (live) OR RECREATE (not restart) the container to reinstate the image's baked-in default; a restart PRESERVES the writable layer and does NOT revert. No compose/mount change (baked-in appsettings is the assumed, acceptable case). / PRECOND operator runs `nerdctl exec sentinel-collector` to edit /app/appsettings.json Serilog:MinimumLevel:Default — no env override for that key exists, so the JSON is authoritative and reloadable. / GUARD RuntimeLogLevel.BuildLogger (ControlledBy = sole authority) + RuntimeLogLevel.CreateSwitch (ChangeToken re-parse on reload) + RuntimeLogLevel.AddRuntimeControlledSerilog (MEL Debug floor) @ src/Telemetry/RuntimeLogLevel.cs:34 / TEST RuntimeLogLevelTests.Runtime_appsettings_edit_raises_then_lowers_serilog_level
  D-14 ingress-structural-junk-classes: INTENT companion to D-5/D-11 (supersedes none) — a full census of the paid-resolver stream (7,177 dispatches / 4,361 distinct surfaces, 2026-08-01T17:35Z->08-05T11:35Z) measured the D-1 SecMaster gate refusing only 1.5% while STRUCTURALLY-impossible names are a 25.0% floor (58.5% of the top-150 by volume); pre-outage `gemini_resolver_live_calls_24h` sat pegged at exactly 1500 = the entire daily cap for days, so genuine last-resort resolutions were competing for slots against `LLC`/`Tech Stock`/`CFO`. Extend the SAME shared ICandidateSurfaceFilter (ONE edit to Classify covers the D-5 prepass, the D-6 V2-direct leg AND its V1 mirror) over SHAPE classes only: bare legal-form suffix; embedded-newline multi-section fragment (runs AFTER IsByline so a byline keeps its specific tag) — the gate is INVERTED: COLLAPSE-and-recheck BY DEFAULT, reject ONLY on a positive fusion TELL, because ContentNormalizer PRESERVES source line breaks (InlineWhitespace `[^\S\n]+` excludes `\n`, rejoined on `\n`) and spaCy passes spans through verbatim, so a hard-wrapped real issuer arrives multiline and an outright reject SILENTLY DROPS it; the 143-multiline census CANNOT refute that class — it sampled only surfaces that REACHED Gemini (ones SecMaster had already failed to resolve), so a hard-wrapped name that resolved fine is invisible to it (selection bias), and the sibling regex extractor already canonicalises the same shape (CompanyNameCandidateExtractor.CollapseWhitespace, "Apple\n  Inc"). Gating the RESCUE on a corporate suffix was that SAME selection bias one level down (corrected 2026-08-05): most real issuers carry NO suffix, so 21 of 23 tested hard-wrapped issuers still dropped — "Home\nDepot"->HD, "Bank of\nAmerica"->BAC, "Texas\nInstruments"->TXN, Morgan Stanley, Berkshire Hathaway, Wells Fargo, Eli Lilly all silently lost (HD/BAC/TXN verified resolving CORRECTLY 2026-08-05; "Morgan Stanley" is kept because MS is a REAL issuer, NOT because its resolution is good — it returns CAF, Morgan Stanley China A Share Fund, despite an exact MS catalog entry, a SecMaster RANKING defect tracked separately and NOT evidence for this rescue), while only the suffix-bearing minority ("Vertiv\nHoldings", "Taiwan Semiconductor\nManufacturing Company") was rescued. The TELLS, each measured on the 2,458-row / 4,141-occurrence review-queue multiline census: a transcript SECTION label ANYWHERE in the span (`Research Division` + `Conference Call Participants`, UNION 516 rows / 1,774 occ — `Research Division` anywhere 304 rows / 1,503 occ, `Conference Call Participants` anywhere 223 rows / 282 occ; the rows OVERLAP so the union is NOT the sum, and the prior `1,655 occ` wrongly added an ANYWHERE count to a MARGINAL one — trailing-only TruncatedSpan cannot see a LEADING label) and the SPACED-dash attribution separator (614 rows / 856 occ), matched as a dash RUN; an unspaced hyphen is ordinary inside a brand ("Coca-Cola"). The dash is a RUN (`+`) because a single-dash test structurally CANNOT match the wire-copy "<outlet>\nInvesting.com -- <firm>" attribution — the same fusion with a DOUBLE dash (21 rows / 23 occ admitted by the single-dash form; the catalog already carries one as an instrument NAME, EROC = "Investing.com - JPMorgan"). Widening is measured strictly additive: those 23 occurrences and nothing else. Post-widening the census admits 1,527 rows / 1,806 occ. The label tell is what rejects "Research Division\nJPMorgan Chase & Co" — the UNDASHED fusion where the WRAP REPLACES THE DASH, which carried `Co`, passed the suffix rescue and resolved the analyst’s EMPLOYER to JPM (wrong-instrument corruption, NOT a harmless over-keep). Fragment-count >2 (75 occ) and a person-name comma shape (32 occ, and most remaining commas sit in REAL names — "Culp, Inc.", "SCREEN Holdings Co., Ltd.") were measured and REJECTED as tells: marginal $0 junk against a real false-drop class the census structurally cannot bound. ACCEPTED: a tell-less section fusion ("Key Takeaways\nMarkets") collapses and Keeps — per the PRECOND an over-keep costs $0 at the capped resolver, a silent drop costs a resolution. EXCEPTION [a LIMIT of the stated PRECOND, not a supersession — the DECISION stands]: that trade does NOT hold for a tell-less ANALYST/BROKER ATTRIBUTION, where the over-keep is NOT free but writes a WRONG TICKER — measured 2026-08-05, "Aditya Infotech\nEquirus Securities" collapses to INFY.NS (Infosys) at 77% and "Henrique Morello\nMorgan Stanley" to KOF at 80%, because KOF's catalog name ALREADY IS "Research Division\nHenrique Morello - Morgan Stanley". The pollution is SELF-REINFORCING: corrupted catalog names act as embedding attractors, so each newly-admitted attribution-shaped surface lands on a previously-corrupted instrument. A future reader applying the bare PRECOND to THIS subclass would re-introduce exactly that bug. NOT CLOSED at this layer, deliberately: the proposed discriminator "BOTH fragments are independently complete multi-word entities" was MEASURED against the census and REJECTED — it drops confirmed-good resolutions ("Taiwan Semiconductor\nManufacturing Company"->TSMC, "Executive Summary\nAdobe Inc"->ADBE, "Axis Bank\nAxis Bank"->Axis Bank Ltd) at ~5% precision (148 rows caught for ~7 true fusions), because a 2-token Title-Case section HEADING ("Executive Summary") is SHAPE-IDENTICAL to a 2-token entity ("Aditya Infotech"); its premise is false anyway — "Taiwan Semiconductor" IS independently a complete entity (resolves to 2330) inside a GENUINE hard wrap. Closing it needs the COLLAPSED-FORM CATALOG check (SecMaster inside Classify) — an architectural change, not another shape rule; `|` section separator (added to CodeChars); truncated span = dangling JOINER as the FINAL char (`&` `-` `/` plus en/em dash — wire copy punctuates typographically) or a trailing `Research Division` transcript label. A trailing `(` is EXCLUDED from that set and STRIPPED-and-rechecked instead (corrected 2026-08-05 on the FULL review-queue census — pinned snapshot 2026-08-05T14:18Z, 71,075 rows / 232,097 occ; the queue is a LIVE accreting table, observed growing 71,049 -> 71,075 -> 71,085 within one session and never shrinking since every row is `is_open`, so a bare row COUNT is not a checkable citation and the DELTAS below are what reproduce): the ubiquitous `Company Name (TICKER)` wire format is NER-cut AT the paren, and a paren is a DELIMITER that FOLLOWS a complete name rather than a joiner inside one, so 68 rows / 99 occ ended in one and 67 / 98 recovered a surface the chain Keeps — `Meta (` (9 occ) -> META, `Rubrik, Inc. (` (4) -> RBRK, `Northrop Grumman (` (3) -> NOC, `Fortinet, Inc. (` (2) -> FTNT, `The J. M. Smucker Company (` -> JM2.FRK, each verified via hybrid_resolve; `main` has no TruncatedSpan class and keeps/resolves every one, so rejecting them was a NEW false-drop versus main. The other four connectors were measured the SAME way and stay outright rejects — they are JOINERS occurring INSIDE a name ("Procter & Gamble", "Coca-Cola", "AC/DC"), so a cut there IS the mid-name cut, and their recovered forms are dominated by role fragments and mid-WORD cuts rather than issuers: `&` 151 recoverable rows led by `the Board` 31 / `President` 23 / `Treasurer` 20 (only ~5 marginal 1-occurrence issuers in 274 occ), `-` 42 led by `sci` 5 / `Academy Award` 3 / `Alphabet co` 2, `/` 25 led by newswire boilerplate (`Press Release - Banking` 8 plus seven `ACCESS Newswire` rows), en/em dash 1 row EACH and neither an issuer — stripping those would re-admit that junk to buy a handful of 1-occurrence names. The strip re-runs the WHOLE chain rather than returning a bare Keep, so the delimiter cannot become a BYPASS (`the Federal Reserve (` -> Institution, `LLC (` -> BareCorporateSuffix, `Tech Stock (` -> MarketJargon, `Bitcoin (` -> Crypto — all of which merely REMOVING `(` from the joiner set would have let slip). Truncation rejects go 332 -> 264 -> 261 rows (the third step is the EDGAR exemption below; `Research Division` and the trailing-`&` role fragments untouched throughout) and the vs-main new-drop set 1,474 rows / 12,729 occ -> 1,407 / 12,631 -> 1,404 / 12,624, with new-keeps still 0 at every step. RECONCILED 2026-08-05 against the pinned snapshot, superseding the earlier `1,455 / 12,703 -> 1,388 / 12,605`: the truncation counts reproduce EXACTLY (that class is saturated — trailing-connector junk is a closed shape), the new-drop ABSOLUTES do not and cannot, because they include the multiline and institution classes that grow with the live queue. Cite the deltas (-68/-3 truncation rows, -67/-3 new-drop rows), never the absolutes; and possessive NORMALISATION — strip `'s`/`’s` and re-run the chain ONCE over the possessor, so "the Federal Reserve's" reaches the existing Institution stop-list it was missing while "Macy's"/"Lowe's"/"The Trade Desk's" still Keep, which a possessive REJECT rule would have silently dropped. SCOPE NARROWED by D-20 (named, NOT a supersession — the arm STANDS): D-20 normalises the MULTI-token possessive at the prepass ingress BEFORE Classify runs, so from THAT path only the SINGLE-token illustrations ("Macy's", "Lowe's") still reach this arm — "the Federal Reserve's" and "The Trade Desk's" are both multi-token and now arrive already stripped, so they no longer demonstrate it. The arm remains live for every DIRECT Classify caller and for the D-6 V2-direct/V1-mirror leg, which D-20 deliberately does not normalise. ONE shape reaches NEITHER rule, pre-existing and unchanged: a SINGLE-token PLURAL ("Barclays'", "Lands'") is refused by D-20's multi-token discriminator AND returns null from StripPossessive, which requires `surface[^1]=='s'` and so never fires on a trailing apostrophe. Recorded here so neither entry is read as covering it / PRECOND shape-based, NEVER name-based — a surface that is only a suffix, or is cut MID-NAME, cannot identify an issuer whichever issuer it came from, so D-11's precondition (a false-positive silently DROPS A REAL RESOLUTION, worse than junk the $0 cap refuses) holds without adjudicating any name. That mid-name-cut justification does NOT reach a cut landing ON a trailing DELIMITER: there the text BEFORE the delimiter is a COMPLETE name, so the shape carries no evidence of incompleteness and rejecting it is precisely the silent false-drop the precondition forbids — the `(` regression, found by differential measurement against main and corrected 2026-08-05. The dividing line is therefore JOINER vs DELIMITER, and it is ENUMERATED, never inferred — STRIPPED-and-rechecked: `(` ONLY; REJECTED outright: `&` `-` `/` en-dash em-dash, plus a trailing `Research Division` label; with ONE carved exemption inside the `/` reject, the EDGAR incorporation slug (below). Moving a connector into the strip set requires the same full-census differential that admitted `(` — positive evidence that REAL issuers are being lost, not merely that something recovers; a strip that rescues nothing is complexity for nothing, and one that rescues role fragments re-opens the junk floor this decision exists to close. VALIDATION SET [the rule that was missing, added 2026-08-05]: a connector decision needs BOTH corpora. The review queue answers "what junk ARRIVES" and BY CONSTRUCTION holds only surfaces that FAILED to resolve, so it structurally CANNOT contain a real-issuer counter-example — clearing a connector on queue evidence alone is unfalsifiable. The CATALOG (`instruments` name+symbol, 43,693 at the snapshot) and `edgar_filers` (4,055) answer "what REAL names look like". `/` was cleared on the queue alone and that is exactly what it hid: EXEMPTION — EDGAR appends a state-of-incorporation slug to the canonical filer name, so `edgar_filers.company_name` for BAC is verbatim `BANK OF AMERICA CORP /DE/` and for TJX `TJX COMPANIES INC /DE/`; the slug FOLLOWS a complete name, making that `/` a DELIMITER on the `(` side of the line, and rejecting it was 110 silently-dropped real issuers (BAC TJX AMT APH CHTR CHD CLX MCO NOC DHR MAR NEM PGR SCCO TRI TM BMO CM LHX BN among them) — the PRECOND violation this decision exists to prevent, invisible to the queue. TWO forms occur and both are exempt: slug PRESENT (`/DE/` `/CAN/` `/DEL/` — 2-3 letters, the COMPLETE set over all 4,055 rows; the class is case-insensitive because the same column already emits a lowercase slug, `Core Scientific, Inc./tx`) and slug EMPTY, a bare trailing solidus — `SOUTHERN COPPER CORP/` (SCCO), `TOYOTA MOTOR CORP/` (TM), and BWIN's LIVE catalog name `BALDWIN INSURANCE GROUP INC/`. A `/[A-Z]{2,3}/$` pattern ALONE would still have dropped the empty-slug issuers, so the empty form is recognised by the legal-form SUFFIX preceding it; that suffix is the whole discriminator, since a bare solidus is otherwise indistinguishable from the newswire boilerplate measured above (`Press Release - Banking/` 8 occ, `News/Reuters/`, the `ACCESS Newswire /` rows), all of which stay rejected. At least one word must PRECEDE the suffix so a trailing solidus cannot BYPASS the bare-suffix reject (`LLC/` stays junk) — the same bypass concern the `(` strip answers by re-running the whole chain. Measured across all three corpora: rescues 109/110 edgar_filers names + the one catalog name, and re-admits 3 queue rows / 7 occ that are THEMSELVES real issuers (two Form-144 headers quoting the EDGAR name verbatim, and `Minerva SA/` = Minerva S.A.) — no junk. STILL dropped, accepted: `TRICO BANCSHARES /`, whose last word is not a legal-form suffix (`Bancshares` is deliberately NOT added — CorporateSuffixes is shared with the institution generic-last-word exemption, so widening it there would move an unrelated rule). The OTHER connectors were re-validated the SAME way and stand UNCHANGED: `&` 10 catalog hits / 0 edgar, `-` 2 / 0, en-dash and em-dash 0 / 0 — and the `&`/`-` hits are NOT canonical names but pre-existing catalog POLLUTION, 8 of the 12 written by the junk-resolution pipeline itself (`discovery_source=entity_resolution:gemini`: `Managing Partner &`, `the Executive Board &`, `Parker Co-`) and the rest truncated vendor names (IEZ `ISHARES U.S. OIL EQUIPMENT &`, `SPORTING CLUBE DE PORTUGAL -`), which is the self-reinforcing corruption described above, not evidence for a rescue. The `(` strip is corroborated from the same side: the 2 catalog names ending in `(` are real issuers (MYGN, RLGT) and both recover. Curated members were verified 2026-08-05 via SecMaster hybrid_resolve and OMITTED on an exact symbol/name hit: PLC->PLC RESOURCES LTD, ASA->ASA Gold & Precious Metals, SA->SEABRIDGE GOLD, AG->FIRST MAJESTIC SILVER, CORP->PIMCO INV GRADE CORP BD ETF all Keep; no punctuation folding ("S.A." listed, bare "SA" NOT). `LLC` is the ONE deliberate inclusion despite exact symbol LLC=Lendlease Group (ASX): a CompanyName NER span reading "LLC" in a US macro/equity feed is the legal suffix, so admitting it resolves by SYMBOL to an unrelated issuer — the wrong-ticker corruption D-1/D-8/D-10 exist to stop, not a lost resolution. That precondition is KIND-SCOPED and the guard enforces it: the bare-suffix reject fires on Kind==CompanyName ONLY, because a cashtag reaches the filter as Kind==Ticker with the `$` already stripped (`$LLC` -> surface "LLC", CompanyNameCandidateExtractor.CashtagPattern) — an EXPLICIT authoritative symbol the author typed, where resolving by symbol is the CORRECT answer, so a kind-agnostic reject would silently drop it. That scope now covers EVERY curated NAME set, not just bare-suffix: MediaOutlets and MarketJargon are Kind==CompanyName too, because `$VALUE`/`$SELL`/`$BOARD`/`$AXIOS`/`$EATER` arrive as bare Ticker surfaces that hit those sets case-insensitively. Harmless today (no current member has an exact SecMaster symbol) but the invariant is now ENCODED and TESTED, so a future member carrying a live symbol cannot silently reopen the hole. The D-6 V2-direct leg and its V1 mirror build EntityCandidate(SubjectEntity, CompanyName) and stay gated (the catalog is ALREADY polluted this way: instruments ANIOY/ANYYY/BRRLY carry transcript-attribution names like "Research Division\nElodie Rall - JPMorgan Chase & Co"). Trailing COMMA and mid-string unbalanced paren are deliberately NOT truncation tells — "Super Micro Computer (SMCI" is a real issuer kept since #761, and the trailing-delimiter strip cannot disturb it because it fires on the FINAL char only. PLUS one CURATED class in the D-11 mould (exact case-insensitive match, NOT a shape rule): non-tradeable market jargon / analyst ratings / TA indicators (`Tech Stock` 95, `Merger & Acquisition` 89, `Value` 82, `Dividend News`, `Finance`, `Healthcare`, `Software`, `Board`, `Risk/Reward`, `EV/EBITDA`, `Overweight`, `Outperform`, `Sell`, `SuperTrend`, `Fibonacci`, `Ichimoku`) — each term verified to have NO exact SecMaster symbol or name; bare `Tech` is deliberately ABSENT (TECH = Bio-Techne), and `Consolidated`/`State`/`House` were REJECTED as truncations of real names rather than jargon. OUT OF SCOPE and left to the cap + D-1 server gate: BARE ACRONYMS (17.5%, the single largest class — CFO/PMI/RSI — many collide with live tickers, needs a catalog-aware discriminator not a shape rule), market INDICES (D-11 litigated and removed that class), PERSON names (D-5 PRECOND) / GUARD CandidateSurfaceFilter.Classify @ src/Services/CandidateSurfaceFilter.cs:408 / TEST CandidateSurfaceFilterTests.should_reject_structural_junk_shapes AND CandidateSurfaceFilterTests.should_keep_real_issuers_that_shadow_the_structural_rules AND CandidateSurfaceFilterTests.should_keep_hard_wrapped_issuer_names_that_collapse_to_one_name (REDs if the collapse-and-recheck is removed OR the gate is re-narrowed to require a company tell — the suffix-less rows revert to MultilineFragment) AND CandidateSurfaceFilterTests.should_reject_dashed_attribution_fusion_that_carries_no_section_label (the ONLY guard on the dash tell — every dashed row elsewhere also carries a section label, so the label tell alone kept them green; mutation-verified 2026-08-05: deleting the AttributionSeparator clause REDs exactly its 3 rows, dropping the `+` REDs exactly the Investing.com row) AND CandidateSurfaceFilterTests.should_keep_bare_suffix_surface_when_kind_is_an_authoritative_ticker AND CandidateSurfaceFilterTests.should_keep_curated_set_surface_when_kind_is_an_authoritative_ticker (both RED if the Kind==CompanyName condition is dropped from its curated set) AND CandidateSurfaceFilterTests.should_keep_issuer_names_cut_at_the_paren_delimiter (the ONLY guard on the delimiter strip — its rows are the real census surfaces `main` keeps and resolves; mutation-verified 2026-08-05: reverting to pre-fix behaviour, i.e. `(` back in the joiner set AND the strip clause deleted, REDs the 8 rescue rows, the delimiter-bypass rows and the ingress real-flow test, while deleting the strip clause ALONE REDs exactly the 6 bypass rows — which is what proves the strip does work beyond merely removing `(` from the joiner set. The bypass set was 5 and is now 6: `TruncatedSpan` is the class the strip DISPLACES and was the one bypass class with no row, so `Treasurer & (` — cut mid-name AND then at a paren — was working but untested; re-measured after adding it) AND EntityResolutionPrepassFilterTests.ingress_rejects_structural_junk_but_keeps_real_issuer (now also asserts `Meta (` AND the EDGAR-canonical `BANK OF AMERICA CORP /DE/` SURVIVE to the SecMaster POST, while `ACCESS Newswire /` — a trailing solidus with no incorporation slug — does not) AND CandidateSurfaceFilterTests.should_reject_market_jargon_as_MarketJargon AND CandidateSurfaceFilterTests.should_keep_real_issuers_that_shadow_the_market_jargon_rule AND EntityResolutionPrepassFilterTests.ingress_rejects_market_jargon_but_keeps_real_issuer AND CandidateSurfaceFilterTests.should_keep_edgar_canonical_issuer_names_that_end_in_the_incorporation_slug (the ONLY guard on the EDGAR exemption — every row is a verbatim `edgar_filers.company_name`; mutation-verified 2026-08-05: removing the exemption REDs exactly its 11 rows plus the ingress real-flow test, removing the corporate-suffix branch REDs exactly the 4 empty-slug issuers [SCCO, TM, BWIN, Minerva SA], and widening it to ANY trailing solidus REDs exactly the 4 newswire/bare-suffix reject rows plus the ingress test — the three mutations pin the rescue, the second form, and the non-nullification independently)

  D-15 num-source-entity-ent-grounding: INTENT the per-NUM `source_entity` slot restored in #913 is LLM FREE TEXT under a `{string, maxLength:80}` schema — the CoD prompt tells the model to copy an `entities[]` name character-for-character but NOTHING enforces it, and DslToMergedExtractionAdapter took the value verbatim into SubjectEntity, which DeterministicResolver keys Rule 2 (`hybrid_subject`, NO subject guard) and Rule 2.5 (PAID Gemini) off. PARAPHRASE, not hallucination, is the likely failure and it misses identically — "CrowdStrike" against an emitted "CrowdStrike Holdings, Inc." — and D-6's shared ICandidateSurfaceFilter KEEPS plausible company names BY DESIGN (D-1/D-5 PRECOND: a false-positive silently drops a real resolution), so NO destination gate can catch this class: what it resolves to is wrong-but-plausible and is written as if confirmed. GIGO says clean at the SOURCE where the surface is BORN — this adapter is the FIRST C# code to see it — so one fix covers SecMaster, Gemini, extracted_observations.source_entity AND the matrix. Destination gates D-6/D-8/D-10/D-12 stay defense-in-depth. PRODUCER ALIGNED in the SAME PR (the string contract spans a process boundary, so the halves change together or not at all): the v8 DSL ROLLBACK prompt (cod-prompts/cod_dsl_v8_baseline.txt) explicitly PERMITTED the violation — "you may emit it for NUMs whose entity you did not also declare as a full ENT (the resolver consults SecMaster regardless of local ENTs)" — which is the pre-GIGO assumption that SecMaster is authority enough; that permission is WITHDRAWN (the ENT `name:` match is now MUST), because leaving it would instruct the rollback producer to emit precisely what the consumer discards. The same stanza's "analyst-firm name on price targets" was corrected to the COVERED issuer for the reason D-15 exists: the analyst firm is a real, plausible, resolvable company, so anchoring the target to it is wrong-but-confirmed-looking, not a visible failure. Does NOT contradict D-8: that entry's parenthetical ("index stays null unless a per-NUM source_entity exact-matches an ENT verbatim_name") describes the SelectedCandidateIndex contract this gate TIGHTENS, never relaxes — new D-entry, NOT a D-8 supersession / PRECOND a non-blank `source_entity` must equal (OrdinalIgnoreCase, BOTH sides Trim'd) a verbatim_name OR local_id the SAME DocumentAst declared — the FULL ENT list, deliberately NOT the candidate shortlist, which excludes NonInstrumentEntTypes and would refuse a NUM genuinely owned by a macro_indicator or analyst_firm. Trim on BOTH sides because the v8 DSL rollback is a DIFFERENT producer whose generic-slot rule can carry a trailing space: discard-on-doubt is right for an undeclared surface and WRONG for whitespace, which would discard a legitimate anchor AND report the artefact as a rising ent_ungrounded rate, i.e. corrupt the very signal the counter exists to carry. Grounding is the ONLY question asked; tradability is not. On failure the surface is DISCARDED, never repaired: SubjectEntity falls back to the doc-lede subject (the documented v8 no-slot contract), SourceEntity persists NULL, and the row FORFEITS candidate pre-selection — the model asserted an owner other than the lede, so pre-selecting the lede at DslPreselectionConfidence (0.85, above ResolverCandidatePickThreshold) would fire Rule 1 on an answer the adapter invented. A blank/whitespace slot is absent, not ungrounded (unchanged normalisation). Counted subject_source=ent_ungrounded on sentinel_dsl_adapter_row_decision_total (cardinality 9->12, then ->36 when description_source {slot|context|raw} joined the same counter in this PR — description_source=raw is the residual bare-numeral fallback, still REACHABLE because `context` is schema-required WITHOUT minLength, so "" is a legal emission that lands the row back on num.Raw; dashboarded as its own share panel); the rejected surface rides the Information log, NOT a metric tag (D-6 precedent) — a climbing ent_ungrounded share means the prompt is losing its grip on the copy-verbatim instruction. Prod sits at Warning per D-13, so the surface is DELIBERATELY absent from the default stream: the counter carries the RATE (the alertable signal), and D-13's runtime level raise — appsettings edit inside the running container, no restart, no redeploy — is how an operator recovers the surfaces once the rate says to look. Promoting the per-row refusal to Warning would put thousands/day of log lines in prod carrying no signal the counter lacks / GUARD DslToMergedExtractionAdapter.BuildExtractionsFromNums @ src/Extraction/DslToMergedExtractionAdapter.cs:351 (the grounding check) + :410 (the pre-selection forfeit), set built by BuildDeclaredEntSurfaces @ :1193 / TEST DslToMergedExtractionAdapterTests.should_refuse_a_source_entity_the_document_never_declared (mutation-verified: deleting the grounding check REDs it; the grounded counterpart should_prefer_per_num_source_entity_over_doc_level_subject_for_subject_entity stays green, so the gate is proven to discriminate rather than reject everything) AND DslToMergedExtractionAdapterTests.should_ground_a_source_entity_whose_slot_carries_surrounding_whitespace (pins the Trim: drop either side and a legitimate anchor is refused as ungrounded)

  D-16 thin-content-gpu-gate: INTENT refuse a GPU extraction call whose input cannot carry an observation -- rss-fallback persists "title\n\nsummary" as a WHOLE article and empty strings reached the model outright, so the call is pure waste AND whatever the model returns for it is invented, which is the worse half (a fabricated row is indistinguishable downstream from a real one) / PRECOND normalized content, trimmed, >= ThinContentGate.MinArticleChars (200). 200 is the measured knee, not a round number: over 1,333 real trafilatura extractions and 416 known-thin payloads drawn from the live raw-data tree it refuses 68.3% of thin at a 0.38% cost to real, where 300 costs 1.35% for +5.3pp; the five real refusals were hand-read (three "X schedules Q3 earnings call" stubs, one earnings-deck title, one article stub) and none carries a quantitative claim. Widening = re-run the curve, never nudge the constant. Skip is counted on sentinel_extraction_skipped_total with reason=empty_content|thin_content -- two tags not one, because "the normalizer produced nothing" and "the source only ships a headline" are different upstream defects. DEAD-TAG CLOSED 2026-08-12: outcome="empty_content" on sentinel_article_total_duration_seconds was documented live (here AND at SentinelMeter's "4 sources x 5 outcomes = 20 series") with NO WRITER on the v2 leg -- every caller of GpuJsonExtractionService pre-filtered empties (LoadNormalizedContentAsync, GpuJsonShadowRunner), so anything empty exited before the histogram and ReasonFor could only ever answer thin_content there. The empty exit was itself silent (no counter, no histogram), excused by a comment reading "consistent with the v1 no-content skip (also silent)" -- FALSE, v1 meters exactly that case. Resolved by METERING rather than deleting the tag: 22/201 corpus docs take that exit, so a corpus-wide normalizer failure was invisible, and deleting the tag would have preserved the blind spot. LoadNormalizedContentAsync now returns (Content, SkipReason) and the v2 leg has ONE refusal point covering thin_content|empty_content|file_not_found|non_extractable_content_type -- v1's vocabulary verbatim so one query spans both legs, NOT merged onto a single tag (a missing body and an emptied one are different upstream defects). Histogram outcome cardinality is now 4 sources x 7 outcomes = 28. WHERE the gate sits is part of the guard, not a placement detail: it runs at the TOP of ExtractionProcessor.RunV2ProductionAsync, BEFORE EntityResolutionPrepass. Sitting at the GPU legs alone (until 2026-08-12) meant a thin document had already paid spaCy NER + SecMaster /api/resolve-entities + whatever last-resort that cascade reaches, so the gate saved the GPU call and NOTHING else -- the GIGO shape one layer up, junk reaching a paid boundary that merely is not the one being guarded. Hoisting loses NO resolution, structurally not just empirically: V2ExtractionPipeline calls IMergedExtractionService.ExtractAsync as its FIRST statement and returns on !Succeeded WITHOUT ever reading preResolvedEntities, so that bag was computed and discarded one statement later; and the pipeline passes normalizedContent through VERBATIM, so the hoisted gate refuses exactly what the GPU-leg gate already refused. SIDE-EFFECT CLAIM CORRECTED 2026-08-12, same PR, by this entry's own author -- NOT a supersession: this entry and the guard comment both said "the prepass is a pure lookup with no persisted side effect". That is FALSE, and it is the worst sentence a READ-FIRST card can carry, because POST /api/resolve-entities is BOTH paid AND catalog-mutating -- a future agent would have trusted it without checking. Verified in SecMaster at HEAD, six durable effects on the skipped path: (a) upstream-discovery instrument INSERT @ SecMaster CatalogService.cs:237, reached from EntityResolutionService.cs:828 with discover:true and NOT confirmation-gated -- ConfirmAsync runs LATER @ :870 and the !Confirmed drop @ :877 leaves the already-inserted row behind; (b) self-seed instrument INSERT @ EntityResolutionService.cs:1029 (EnableSelfSeed defaults true @ EntityResolutionOptions.cs:76; prod compose.yaml overrides neither it nor ReviewQueueEnabled); (c) instrument_embeddings write @ :1037 -> EmbeddingService.EmbedInstrumentAsync, committed @ EmbeddingService.cs:133; (d) openfigi_lookup_cache upsert @ UpsertCacheBestEffortAsync :1188, reached from the OpenFIGI leg @ :553, written on BOTH the negative :1160 and positive :1179 paths; (e) unresolved-surface review-queue enqueue @ :659 -> the `ctx.ReviewRepository.EnqueueAsync` CALL SITE @ :691 -- BOTH line numbers are in EntityResolutionService.cs like every other citation in this list, NOT in the repository; the implementation is EntityResolutionReviewRepository.EnqueueAsync @ src/Data/Repositories/EntityResolutionReviewRepository.cs:30 (ReviewQueueEnabled defaults true @ EntityResolutionOptions.cs:168); (f) the confirmation cascade can reach the PAID gemini leg @ IdentifierConfirmationService.cs:177. Skipping the prepass is therefore a STATE CHANGE, and the ACCEPTED COST is stated rather than implied: for the thin population the catalog no longer grows from those documents, and their unresolved surfaces never register as coverage gaps -- no review-queue row, so the depth gauge under-reports coverage holes by exactly the thin population. SIZE OF THAT POPULATION, sample and extrapolation stated SEPARATELY because collapsing them is the error this sentence carried until 2026-08-12 ("~8 thin docs/day" -- 8 is the CORPUS COUNT, never a daily rate; the 2026-08-12 rewrite that caught that went on to assert the figure was LARGER than the v1 leg, which was the unstandardized product and is itself withdrawn below -- standardized, v2 IS the smaller leg, so the ordering "~8/day" implied was accidentally right for the wrong reason): SAMPLE = 8 of the 171 v2 documents that reach the prepass in the frozen 201-doc corpus, 4.68%. ESTIMATE = a BOUND, not a range: POINT ~0.6-2.2 thin documents/day IF thinness is fed-specific as the corpus shows, UPPER BOUND ~20/day. Volume anchored 2026-08-12T16:59:18Z via `sum by (source, outcome) (increase(sentinel_article_total_duration_seconds_count[24h]))`: v2-enabled 1179.2/day (rss 1106.2 + rss-mirror 54.0 + searxng-content 19.0), v1 61.0/day (rss-fallback 60.0 + tsa-checkpoint 1.0), every outcome=ok. Scaling the corpus rate onto that TOTAL gives 1179.2 x 8/171 = 55/day, and 45/day on the 7-day mean (951.6/day) -- and THAT ARITHMETIC IS UNSTANDARDIZED, which is why "~45-55/day" stood here until 2026-08-12 and is WITHDRAWN by this entry's own author, no supersession (prose figure, no guard and no test touched): it applies a rate measured on a fed-dominated sample to a production volume that is at least 99.7% NON-fed, the identical error this paragraph already declines to make for the EMPTY axis one clause below. STANDARDIZED on production's mix (re-derived 2026-08-12T17:40:12Z, same expression at [7d] and [30d]): ALL 8 thin documents are fed-*, so the corpus observed 0 thin among the other 163 that reached the prepass. fed-* at 100% thin contributes its whole volume and no more -- 4.0 prepass-reaching documents/7d = 0.57/day, 65/30d = 2.17/day, and fed is structurally low-volume (9 raw_content rows/7d, 75/30d, all-time single-day max 16). Everything else is 0/163 -> RULE OF THREE (the 95% upper bound on a zero count is 3/n), 3/163 = 1.84%, across 945.2/day of non-fed v2 volume = AT MOST 17.4/day; the rss-only form agrees, 0/120 -> 2.47% exact one-sided x 888.7/day = 21.9/day, so the bound is ~20/day either way. The withdrawn 45-55 sits 2.5-3x ABOVE that bound and 20-95x the point estimate -- the corpus does not support it, and note the failure mode: an unstandardized product reads like a measurement, so nothing about the sentence flagged that it had left the evidence. Still not MEASURABLE today because the gate is unshipped (ThinContentGate is absent from origin/main), so sentinel_extraction_skipped_total{reason="thin_content"} has no series to read at all; and re-run the volume query before quoting any of it: the corpus RATE is frozen, the VOLUME is not. LIMIT OF THE SAMPLE -- the soft spot the arithmetic does NOT bracket, and the one that matters more than the volume: the frozen corpus is NOT a proportional draw from current traffic, so the RATE it freezes need not be production's rate at all. Re-measured 2026-08-12T17:24:14Z: the corpus's no-content exit rate is 22/193 = 11.4% against production's 2.9% over 7d (6847 v2-enabled rows in sentinel.raw_content vs 6651.2 observations on the histogram -- the deployed build's pre-prepass exit is SILENT, so that difference IS the population), a ~4x gap that is entirely MIX rather than per-source disagreement (rss-mirror 40.0% corpus vs 32.0% prod, searxng-content 33.3% vs 33.8%, rss 0/120 vs ~0/6252) because the corpus is 31.1% rss-mirror+searxng-content where production is 8.5%. The THIN axis is skewed the same way and harder: ALL 8 thin documents are fed-*, 4.1% of the corpus against 0.13% of production volume (9 rows/7d, every fed source under 1/day), while rss carries 91% of production volume and contributed 0/120 -- so the sample never resolved the rate of the source that dominates the extrapolation. BOTH ARMS ARE BOUNDED, which is why the figure above is a BOUND and not the "DIRECTION OF ERROR UNKNOWN" this sentence declared until 2026-08-12 -- an unknown direction was the wrong framing, and what it actually bought was the preservation of a number the evidence excludes. The DOWN arm is MEASURED, not merely suspected: it IS the 8/8-fed vs 0/163-non-fed split, and standardizing on it is what drags the figure to ~0.6-2.2/day. The UP arm is BOUNDED BELOW THE WITHDRAWN RANGE rather than open, and this entry's own parenthetical already said so while the conclusion walked past it: 1% on rss alone is ~8.9/day, nowhere near 45; clearing 55/day on rss ALONE needs a 6.19% thin rate, which 0/120 excludes at 99.95% under a binomial ((1-p)^120 = 4.7e-4), and even the bottom of that range, 45/day, needs 5.06% and is excluded at 99.80%. So the up arm cannot reach the withdrawn range, let alone pass it. WHAT WOULD MOVE THE NUMBER is a non-zero rss thin rate somewhere in 1-5% -- precisely the band this corpus cannot resolve, since 0/120 is consistent with anything up to 2.47% and rss carries 94% of non-fed volume. Narrowing it is a NEW draw against production's source mix, never a re-reading of this corpus. That histogram count IS the prepass-reaching population STRUCTURALLY rather than by assumption -- on the deployed build RunV2ProductionAsync records the article histogram on BOTH of its post-prepass exits (ok and error) and its ONLY histogram-free exit is the empty-content return ABOVE the prepass, so a v2-source observation on that histogram at outcome=ok|error is exactly a document that reached the prepass: the prod analogue of the corpus's 171. SCOPED to those two outcomes deliberately -- outcome=age_cutoff is recorded @ ExtractionProcessor.cs:532, ABOVE the v1/v2 branch @ :593 (the same precedent this entry cites at :515), so an age-dropped v2-source row lands on the SAME histogram having never reached the prepass; it measures ZERO over both the 24h and 7d windows so the estimate is untouched, but unscoped the sentence is false. The converse needs no scoping: a throw past the prepass unwinds to ProcessSingleArticleAsync's catch @ :953 -> outcome=error @ :958, so nothing that reached the prepass leaves the histogram silently. Line cites here are HEAD, as everywhere in this entry; this PR edits none of those blocks (only the catch's line number moved, 923 -> 953), so the structure described is the DEPLOYED one the volume query reads. TWO-LEG CHECK: the ~40/day v1 figure below falls out of the SAME query and window (rss-fallback 60.0/day x the 66.2% under-floor share = 39.7/day), and the same query reproduces this entry's own baseline (61.0/1240.2 = 4.9% v1 share, v2 = 95.1% of volume), so both legs now scale from ONE measured volume on ONE window. The legs are consistent, but the ORDERING claim this sentence carried until 2026-08-12 ("v2 refuses MORE documents absolutely, ~55/day vs ~40/day") travelled with the withdrawn 45-55 and falls with it: STANDARDIZED, v2 refuses FEWER absolutely (~0.6-2.2/day point, <=~20/day bound, against v1's ~40/day) at a FAR lower rate (0.06-0.25% standardized, against the 4.68% unstandardized, vs 66.2%) despite carrying ~19x the volume. v1's ~40/day survives the correction untouched because it is not the same KIND of figure -- a rate measured directly on rss-fallback payloads, on the leg those payloads actually take, never transported across a source mix. The trade is the POINT of the gate (a headline stub is precisely the input that should not seed the catalog or bill a frontier model) but it IS a trade, not a free win: anyone widening, moving or deleting this gate is moving catalog growth and review-queue coverage with it. WHERE-not-WHETHER, SCOPED to the production backend: prod runs Extraction__Backend=VllmJson (/opt/ai-inference/compose.yaml:1078) and GpuJsonExtractionService -- bound @ DependencyInjection.cs:781 -- is the ONLY IMergedExtractionService implementation carrying a ThinContentGate call, so on the PROD leg the hoist changes WHERE a document stops, never WHETHER. On the other two bindings it is a STRENGTHENING, not a relocation: CpuDslExtractionService (:777, Backend=LlamaServerDsl) and MergedExtractionService (:785, every other Backend) contain no ThinContentGate reference at all, so under those the hoisted gate adds refusal coverage that did not previously exist. KNOWN, NOT CLOSED HERE: the qualitative dispatch leg (TryDispatchQualitativeAsync) calls the SAME prepass @ ExtractionProcessor.cs:2448 behind an empty-content skip but NO thin gate -- the identical GIGO shape one leg over, so a thin qualitative document still pays the catalog-mutating boundary above. Pre-existing, NOT introduced by this PR and deliberately outside its scope; closing it means the same hoist on that leg. Measured on the 201-doc corpus: 193 v2-enabled, 22 empty (they exit earlier still), 171 reach the prepass, 8 of those (4.7%) thin -- Fed headline stubs ("Federal Reserve Board announces approval of the application by FS Bancorp, Inc.") that DO carry resolvable issuers, which is why "no entities to lose" is NOT the argument. GPU-leg gates STAY: GpuJsonShadowRunner calls ExtractAsync directly (pre-filtering only empties) so a headline reaches the model on the shadow leg without them. ✗ NOT hoisted to ProcessSingleArticleAsync before the v1/v2 branch (the reviewer's proposal, declined and recorded): on v1 there is no prepass between the branch and the existing gate, so it saves nothing, while costing a SECOND ContentNormalizer pass per article (ExtractionService normalizes internally from RawFilePath), and putting a GPU-call gate in front of the tsa-checkpoint deterministic branch @ ExtractionService.cs:72 -- which sits ABOVE the v1 gate @ :109 and makes NO model call (0/8 corpus TSA docs are thin, so that one is a structural hazard, not a live break). A THIRD leg was cited here and is now DROPPED as stale (2026-08-12): "collapsing v1's file_not_found|non_extractable_content_type reasons into empty_content" described the PRE-PR helper. Post-PR LoadNormalizedContentAsync @ ExtractionProcessor.cs:2692 distinguishes them -- file_not_found :2699, non_extractable_content_type :2705, empty_content :2712 -- so that leg no longer holds. The decision stands unchanged on the two remaining legs. OUTCOME CLASSIFICATION: a TERMINAL NON-ERROR -- processed_at set (so the row leaves the queue and is never retried), sentinel_extraction_skipped_total{reason}, outcome=thin_content|empty_content on sentinel_article_total_duration_seconds, Information log, NO error-counter increment. Shape follows the age_cutoff precedent (ExtractionProcessor.cs:515) which is likewise excluded from SentinelHighExtractionErrorRate. processed_at NOT processing_error: writing a skip into a column named "error" re-creates the same conflation for every query reading processing_error IS NOT NULL. The !Succeeded LAUNDERING IS GONE (ThinContentGate.IsSkipReason deleted, unused): with one gate above the pipeline a skip can no longer arrive on the failure channel, and a thin document that somehow reaches the pipeline on the prod leg now means the upstream gate FAILED -- recording that as an error is correct, not a regression. v1 ASYMMETRY CLOSED 2026-08-12 (was "accepted"): the v1 leg returned a bare empty ExtractionOutput, indistinguishable from "the article had no numbers", so a v1 thin skip recorded outcome=ok AND incremented sentinel_extraction_success_total -- the DENOMINATOR of SentinelHighExtractionErrorRate, so a burst of thin documents DILUTED that alert during exactly the upstream degradation the skip counter exists to flag. 66.2% of rss-fallback payloads (median 149 chars) are under the floor, and rss-fallback is a v1 source, so this was the common case not the corner. Fixed by the skip channel D-16 said it would need: ExtractionOutput.SkipReason, set at every no-LLM-call exit in ExtractionService (file_not_found | non_extractable_content_type | empty_content | thin_content -- all four were counted as successes), consumed in ProcessSingleArticleAsync as a TERMINAL NON-ERROR returning ProcessOutcome.Skipped, which the batch path already treats as neither. ✗ NEVER key this on Extractions.Count==0: an empty list is ALSO what a real article the model read and found no numbers in returns, and that IS a success -- only the explicit SkipReason separates them. ALERT BASELINE (checked, not absorbed): success_total feeds SentinelExtractionDead (24h dead-man's switch) and SentinelHighExtractionErrorRate (denominator) plus 2 dashboard panels. Denominator shrinks by at most v1's share -- 61/1243 = 4.9% of 24h successes, of which the thin part is ~40/day (rss-fallback 60.0/day x its 66.2% under-floor share, derived in TWO-LEG CHECK above) -- so the ratio gets ~3% more sensitive in the INTENDED direction (de-dilution) and stays far under the 0.1 threshold + >=5-error floor. SentinelExtractionDead is the one real semantic change: a 24h window in which EVERY document was skipped would now fire where it previously stayed quiet. That is correct (no document reached the model = the pipeline is dead for extraction purposes) and it needs both legs silent at once, v2 being ~95% of volume. ONE shared pure predicate wired at BOTH GPU legs so they cannot drift (D-8/D-10/D-12 pattern) / GUARD ThinContentGate.IsTooThin @ src/Extraction/ThinContentGate.cs:44 -- called FIRST @ ExtractionProcessor.RunV2ProductionAsync src/Workers/ExtractionProcessor.cs:1828 (ahead of the prepass, one exit for thin|empty|file_not_found|non_extractable; terminal record @ ExtractionProcessor.RecordV2SkipAsync src/Workers/ExtractionProcessor.cs:2018), and again as defense-in-depth @ GpuJsonExtractionService.ExtractAsync src/Services/GpuJsonExtractionService.cs:118 (the ONLY gate on the shadow leg) AND ExtractionService.ExtractAsync src/Services/ExtractionService.cs:109 -- whose refusal leaves via ExtractionOutput.SkipReason, consumed @ ExtractionProcessor.ProcessSingleArticleAsync src/Workers/ExtractionProcessor.cs:643 / TEST ThinContentGateTests.should_send_no_http_request_to_vllm_when_content_is_headline_only AND ThinContentGateTests.should_send_no_http_request_to_vllm_when_content_is_empty AND ThinContentGateTests.should_still_call_vllm_when_content_is_a_real_article (REDs if the floor is raised past real articles) AND -- the V1 half, which shipped UNTESTED: deleting the v1 gate block -- ExtractionService.cs:98-120 at HEAD, the INTENT(D-16) comment at :98 through the closing brace at :120; a NARRATIVE record of a mutation run, NOT a guard pointer, and the ":98-113" cited here until 2026-08-12 was a stale pre-edit range that does not span a compilable block -- left the whole suite green (2071/2071 unit + 31/31 integration, reproduced twice) even though v1 is the leg the motivating `rss-fallback` source takes (absent from Extraction__V2EnabledSources) -- ThinContentGateTests.should_send_no_http_request_to_vllm_when_v1_content_is_headline_only AND ThinContentGateTests.should_send_no_http_request_to_vllm_when_v1_content_is_empty (mutation-verified 2026-08-12 by the reviewer's exact deletion: both RED naming the boundary, "found 6"/"found 14" requests -- CoD and CoVe each retry, so one headline is 6-14 GPU calls, not 2) AND ThinContentGateTests.should_still_call_vllm_on_v1_when_content_is_a_real_article (stays GREEN under that deletion, so the pair discriminates rather than refusing everything) AND ExtractionProcessorThinContentOutcomeTests.should_record_thin_article_as_a_skip_not_an_extraction_error (real pipeline + real gate, vLLM transport the only mock; REDs if the skip is reclassified as an error) AND ExtractionProcessorThinContentOutcomeTests.should_still_record_a_genuine_pipeline_failure_as_an_extraction_error (REDs if a real v2 failure stops being an error) AND -- the PLACEMENT half, which is what makes the gate save more than GPU -- ExtractionProcessorThinContentOutcomeTests.should_not_call_the_entity_resolution_prepass_for_a_thin_article (fixture is a thin doc that WOULD resolve, "FS Bancorp, Inc.", so passing cannot mean "nothing to resolve"; REDs if the gate moves back below the prepass or is deleted) AND ExtractionProcessorThinContentOutcomeTests.should_still_call_the_entity_resolution_prepass_for_a_real_article (the discriminating half: REDs if the gate refuses everything) AND -- the v1 success-counter half -- ExtractionProcessorV1SkipOutcomeTests.should_not_count_a_thin_v1_article_as_an_extraction_success (real ExtractionService over the real gate; REDs if a skip is counted as success again) AND ExtractionProcessorV1SkipOutcomeTests.should_still_count_an_extraction_that_found_no_numbers_as_a_success (REDs if the skip branch keys on Extractions.Count==0 instead of SkipReason) AND -- the dead-tag half -- ExtractionProcessorThinContentOutcomeTests.should_meter_a_v2_document_the_normalizer_emptied_instead_of_exiting_silently (REDs if the empty exit goes back to being silent) AND ExtractionProcessorThinContentOutcomeTests.should_tell_a_missing_file_apart_from_a_document_the_normalizer_emptied (REDs if the load failures collapse onto one tag)

  D-17 html-normalizer-absent-routing: INTENT `Trafilatura__Enabled=false` USED to mean "fall back to the other normalizer"; once markitdown left the text/html path (#951) it means NO article extractor runs and EVERY html document takes the whole-page visible-text strip. Keeping that is a DELIBERATE choice, recorded because it would otherwise read as the leftover of removing a branch: restoring markitdown re-admits what was MEASURED and rejected (whole-page chrome, ~7.4x trafilatura's chars, 40 nav-link lines/doc, ZERO article prose recovered over 78 empty-trafilatura documents) and adds a call to a SECOND sidecar commonly down for the same reason as the first; the strip is no worse on content (also whole-page, but no markup, no link URLs, no script/style bodies) and is in-process so it cannot fail with the sidecar; and REFUSING is worse than both -- ExtractionProcessor marks the row processed (or errored past MaxRetries) and NEVER revisits it, so a switched-off normalizer would permanently DROP those articles rather than degrade them. What the strip must not be is INVISIBLE: an outage still increments TrafilaturaExtractionCounter{status=exception|http_error} because a call is made, but with Enabled=false NO call happens and NOTHING increments -- the disabled regime was the blind spot / PRECOND the degraded path is OBSERVABLE before it is acceptable: the disabled regime carries its own bounded `site` tag (trafilatura-disabled, distinct from raw-html = a document that defeated a RUNNING extractor), and the strip meters the text it SERVES (outcome=stripped_text_served) not only the times it yields nothing -- whole-page text is indistinguishable downstream from a real extraction, so without a rate a corpus-wide degradation is a burst of identical Warnings and nothing else. LIMIT OF THE EVIDENCE: the A/B corpus was drawn with a WORKING sidecar, so it measures trafilatura-vs-alternatives on documents trafilatura SAW and says NOTHING about the population reaching the strip when it never runs -- which is every html document, including the ones it would have extracted cleanly. Widening this is a NEW measurement, never a re-reading of that one. Does NOT supersede D-2: that entry governs what the last-resort strip EMITS (visible text, never markup, fail-closed to empty) and still receives every document-level miss; this entry governs which failures REACH it and how the regime is told apart / GUARD ContentNormalizer.NormalizeAsync htmlStripSite @ src/Services/ContentNormalizer.cs:181 -- passed @ :291; served-text meter @ ContentNormalizer.StripHtmlFallback src/Services/ContentNormalizer.cs:379 / TEST ContentNormalizerTests.should_strip_to_visible_text_and_not_call_markitdown_when_trafilatura_is_disabled (REDs if the disabled path is re-routed to markitdown or made to refuse) AND ContentNormalizerTests.should_meter_the_disabled_regime_apart_from_a_document_that_defeated_trafilatura (REDs if the two regimes collapse onto one site tag, or if serving stripped text goes back to being log-only)

  D-18 sentinel-series-key-ownership: INTENT a series key belongs to whoever MEASURED the number, and Sentinel is on BOTH sides of that line. For a series someone else measures, a Sentinel value is LLM-EXTRACTED opinion about it: ThresholdEngine files every SeriesCollectedEvent into ONE ObservationCache keyed by SeriesId (EventProcessor.HandleSeriesCollectedAsync -> AddObservation(evt.SeriesId,...)), that cache is the singleton also registered as IObservationRepository, and pattern signalExpressions read it via ctx.GetLatest(mnemonic) -- so an extracted number published under a FRED mnemonic does not merely sit beside the real series, it BECOMES the value the matrix computes on, and it always wins (cache keys on date.Date, GetLatestValueAsync takes max Date then max AsOf, and Sentinel stamps EXTRACTION time, structurally newer than any monthly/weekly FRED vintage). MEASURED in prod 2026-08-12: oil-price at signal -3 (max bearish) off `(wti-70)/15` implying WTI=25 against DCOILWTICO=84.77 (correct ~ +0.98); 30d publications under first-party mnemonics CPIAUCSL 96 (-32.5 .. 5.8e10), DGS10 42, DCOILWTICO 36, UNRATE 30, PAYEMS 29, ICSA 26; clean control natgas-price reads correctly and Sentinel has NEVER published DHHNGSP. But Sentinel is ALSO the primary source for six alternative-data series no structured collector measures (ADP_EMPLOYMENT, BDIY, CHALLENGER_JOB_CUTS, INDEED_POSTINGS, REDBOOK_SALES, TRUFLATION_CPI -- SecMaster resolves all six to PrimarySource=SentinelCollector, none appears in fred_observations, and Sentinel has published all six: BDIY 625 events, CHALLENGER_JOB_CUTS 412, ADP_EMPLOYMENT 82, TRUFLATION_CPI 79, INDEED_POSTINGS 43, REDBOOK_SALES 35). There is no other publisher to displace there, so namespacing them severs the only feed they have -- measured on the live engine when this shipped blanket: challenger-layoff-surge 0.98 -> 0 and truflation-vs-cpi -3/Triggered -> ~0, with challenger-vs-payroll and baltic-freight-recession reading the same keys. OWNERSHIP, not collision-detection, and the polarity is the whole entry: asking "does this key collide with a first-party series?" defaults to publishing BARE, so it must answer a question about a registry that CHANGES -- a symbol that is not a FRED series today is one tomorrow, every event published before that day is already wrong, nothing re-checks them, and a SecMaster outage has to mean something (fail-open restores the defect, fail-closed drops all Sentinel events on one blip). Asking "is this a series Sentinel OWNS?" defaults to NAMESPACING, needs the same knowledge, and costs a namespaced key rather than a corrupted one when it is wrong. The owned set is a literal frozen list in SentinelSeriesKey, not a lookup: the check runs on every outgoing event so it must be a pure string predicate, no I/O and no staleness. Drift is the residual risk and it is pinned, not hoped at -- owned_series_must_match_the_secmaster_instrument_declarations reads SecMaster/config/instruments/*.json (the repo-side declaration of instruments catalogued independently of any collector registering them -- the same population) and REDs on either direction of disagreement. What that pin does NOT catch: a first-party collector newly registering a series that SHARES one of the six mnemonics; that is the one change which silently restores the defect, and it is accepted because it requires another collector to claim a name Sentinel already publishes under. Namespacing PRESERVES the observation instead of deleting it: a pattern that genuinely wants an extracted value for a series it does not own references SENTINEL:NUM:X explicitly, opt-in by an author who knows the value is LLM-derived, never silent inheritance. That hatch is OPEN, not merely promised: ThresholdEngine PatternMnemonicFormatValidator is the only gate a pattern mnemonic passes (there is no catalog validator despite what its doc-comment used to claim) and none of its five conventions admitted a colon, so a pattern taking this path failed PatternLibraryTests until a namespace row was added for the two shapes SentinelSeriesKey actually mints. One caveat, in the hatch's own regex comment: DataWarmupService routes RequiredSeries per series to ONE collector (SecMaster PrimarySource, else prefix-fallback to FredCollector) and a SENTINEL:-prefixed key resolves to neither, so cold-start warmup for it misses -- the live gRPC feed still fills the cache, so a signalExpression reading one works, but the restart window is thinner than for a first-party mnemonic. Extends the convention CreateSectorEvent already used (SENTINEL:SECTOR:{sector}:{symbol}); sector keys are ALWAYS namespaced (a sector roll-up is Sentinel's own construct, so the owned-series case cannot arise) and that call routes through the same authority so the two cannot drift. GIGO at the SOURCE: ThresholdEngine's consumption is deliberately NOT changed -- gating the destination is the recurring disease (#818, #823) and Sentinel is the only producer that can mint these keys / PRECOND every SeriesId Sentinel emits is either inside `SENTINEL:` or one of the six owned mnemonics (Ordinal, case-SENSITIVE on both arms -- the namespace is a literal Sentinel itself writes and the owned keys are exact mnemonics, so a case-insensitive match would admit a first-party key that merely spelled one of them differently), and a BLANK identity is refused rather than namespaced (`SENTINEL:NUM:` identifies nothing and would pool every unidentifiable observation into one series; unreachable today -- 0 blank symbols across all 652,462 extracted_observations rows, measured 2026-08-12 -- because the birth site treats a blank Symbol as ABSENT and falls through, which is the call site that keeps it unreachable). Enforced at TWO sites for two different populations: the birth site namespaces unless Sentinel owns the series (no observation is ever refused there -- the resolved symbol becomes the identity SEGMENT, so nothing is lost), and the gRPC egress REFUSES anything Sentinel may not publish, which is what makes it structurally impossible rather than merely fixed-going-forward -- 78,736 rows over 4,725 keys are already stored in sentinel.events, TryConvertToProto is the ONE place a stored payload becomes an outbound event for EVERY RPC this service serves (SubscribeToEvents, GetEventsSince, GetEventsBetween-which-delegates), and the stored row is DISCARDED there, never repaired -- rewriting it to SENTINEL:NUM:{mnemonic} would mint a new key out of a value nothing asked for (D-15 precedent: discard the ungrounded surface, do not repair it). The refusal logs at WARNING, not Information: prod runs Serilog Default=Warning with no override for SentinelCollector.Grpc.*, so at Information the refused key -- the only record of WHICH series was dropped -- reaches Loki never, and a refusal at a correctness boundary is not routine traffic. REQUIRED COMPANION, not a drive-by: SubscribeToEvents advanced `lastEventTime` only after a successful write while the repository pages on `occurred_at > since`, so a page the converter rejects WHOLESALE is re-read forever at the 100ms poll -- harmless when only malformed events skipped, a hot spin once D-18 makes whole-page rejection routine and ThresholdEngine is always subscribed; the cursor now advances for every event EXAMINED. Does NOT contradict D-3: that entry governs LLM values entering macro_observations (tilt clamp, hallucinated-id drop) on the `:sig:` DB-poll path the projector reads; this entry governs the SEPARATE gRPC SeriesCollected path into the live-eval ObservationCache, which D-3's clamp never saw -- new entry, NOT a D-3 supersession / GUARD SentinelSeriesKey.ForNumeric applied @ src/Publishers/EventPublisher.cs:112 (birth) + SentinelSeriesKey.IsPublishable @ src/Grpc/Services/EventStreamService.cs:260 (egress refusal) + cursor advance @ src/Grpc/Services/EventStreamService.cs:95; ownership authority = src/Publishers/SentinelSeriesKey.cs (OwnedSeries) / TEST SentinelSeriesKeyNamespaceTests.should_refuse_to_stream_an_extracted_value_under_a_first_party_series_key (mutation-verified: deleting the egress check REDs it) AND SentinelSeriesKeyNamespaceTests.should_publish_a_resolved_first_party_symbol_into_the_sentinel_namespace (REDs if the birth site stops namespacing) AND SentinelSeriesKeyNamespaceTests.should_still_stream_events_inside_the_sentinel_namespace (proves the gate DISCRIMINATES rather than streaming nothing) AND SentinelSeriesKeyNamespaceTests.should_publish_a_sentinel_owned_series_under_its_own_key + should_still_stream_a_sentinel_owned_series_stored_before_the_namespace_existed (RED if ownership collapses back to blanket namespacing -- the six mnemonics, birth and egress) AND SentinelSeriesKeyNamespaceTests.owned_series_must_match_the_secmaster_instrument_declarations (REDs on drift between the frozen set and SecMaster's instrument configs) AND SentinelSeriesKeyNamespaceTests.should_not_re_read_a_refused_page_on_every_poll (the CURSOR guard, and the ONLY test in this service that drives SubscribeToEvents -- it fakes the repository's `occurred_at > since` paging and counts how often one refused row is served, so it REDs at 4-served-instead-of-1 if the advance moves back inside the write branch; mutation-verified, and it is the only test of the 2,099 that REDs on that revert. The pre-correction entry claimed this of a test driving GetEventsSince, which has no cursor at all -- that claim was refuted by mutation and is withdrawn here by its own author, not superseded) AND SentinelSeriesKeyNamespaceTests.should_stream_a_good_event_that_follows_a_refused_one_in_the_same_page (refusal is per-event, not per-page) AND SentinelSeriesKeyNamespaceTests.should_refuse_to_mint_a_key_from_a_blank_identity + should_fall_back_past_a_blank_symbol_rather_than_mint_a_bare_prefix (blank-identity guard and the call site that keeps it unreachable)

  D-19 semantic-signal-shadow: INTENT the macro NUMERIC route is an exact dictionary lookup of LLM free text against 358 normalized alias keys, and it drops 99.945% of what reaches it -- measured 2026-08-12 UTC, 9,080 numeric claims / 7,784 DISTINCT descriptions -> 5 rows in macro_observations (08-10:6, 08-11:3, 08-13:1, so 5 is typical not an outlier). MacroSignalIdentityCatalog.cs states the choice outright ("LLM extractions describe the observation differently every time; alias matching is the authority") and that comment IS the funnel. The stage was ALSO UNMETERED, which is why it ran unseen: with no counter at the miss, "the route is dead" and "no matching input arrived" are the SAME observable -- silence. This entry governs the SHADOW that measures the recovery, NOT a routing change: nothing here writes, and D-3 is untouched (supersedes none) because D-3's trust boundary is a WRITE boundary and this path never reaches one; D-3's PRECOND (additive enrichment, fail-soft, never breaks extraction) is the standard this meets. WHAT IS EMBEDDED on the signal side is most of the quality and was MEASURED, not assumed: label + "label. description" + PROSE aliases, MAX-POOLED per surface, mnemonics EXCLUDED. The catalog `description` ALONE is the WORST option available -- 26 of 7,784 descriptions reach cosine 0.70 against it vs 395 for the composed set -- because it is editorial prose about the signal's ROLE ("Dr Copper", "published monthly by BLS") while an extraction is a short noun phrase naming a QUANTITY; max-pool not average, because aliases are heterogeneous ("wti"/"brent"/"opec") and averaging blurs every member; mnemonics (DCOILWTICO, FPF-ALLQHF_LEVERAGE_GAV_P50) are character soup to a text embedder AND already have an exact home in the alias map. THE GATE IS DIFFERENTIAL AGAINST THE MATCHED SIGNAL'S OWN SURFACES, NEVER A DENYLIST -- that is load-bearing, not stylistic: "probability of a rate hike" is junk against fed-funds-rate and CORRECT against recession-probability, identical words, opposite verdicts, and only a comparison with the signal separates them (mutation-verified: rejecting on the operator word alone REDs the accept half). Two axes, both confirmed real on 240 hand-adjudicated matches stratified over six score bands: entity_scope (subject mismatch) + derived_quantity (an operator applied to the signal). CORRECTION to the framing that commissioned it -- axis 1 is NOT "entity-scoped where the signal is an aggregate": the COMPANY case ("Senco Gold retail sales") is ABSENT from the operating range altogether. POPULATION = the 240-row adjudication, not a live day: of the 23 subject-labelled rows at cosine >=0.75, ZERO name a company -- JURISDICTION 9 ("Italy 10-year yield"), TENOR 5 ("30-year Treasury yield"), COMMODITY 4 ("Spot silver price"), a bare-subset form 3 ("market cap" against the buffett-indicator aggregate) and a CPI variant 2 ("SuperCore CPI YoY"); every named-company form in the file ("ConocoPhillips forward P/E", "Atalaya Mining Copper share price", "petrol price at Pinnacle", "EnerSys EPS") sits BELOW 0.75 and mostly below 0.70, where adjudicated precision is already 0%. So a gate built on the company exemplar alone keeps AT LEAST 20 of 23 of the class where a threshold would sit, and all 23 read strictly. The earlier "~3 of 22 ... ~86%" reproduced from nothing the PR ships -- the TSV gives 23 subject rows and 18 entity_scope refusals at >=0.75, and no reading gives 22. MEASURED, and EVERY figure carries its population because these do not share one: over ALL 240 adjudicated rows the gate refuses 72.4% of junk (126/174) keeping 93.9% of correct matches (62/66); at cosine >=0.80, where an operating point would actually sit, it refuses 83.9% keeping 95.9% and precision rises 61.3% (49/80) -> 90.4% (47/52), at >=0.75 50.8% -> 82.9%. The 0.80 row is stated to the ROW because it did not reproduce: `Brent oil per barrel price` scores 0.799523 and the shipped adjudication rounded it to "0.800", so re-deriving from the artefact put it in the band above and gave 53/90.6% against the body's 52/90.4%. The TSV now ships 6dp and the row re-derives where it belongs; "the scorer agrees to the row" was false at exactly that row. NO OPERATING THRESHOLD IS SET HERE and ShadowScoreFloor=0.60 is NOT one -- it is a metric-cardinality floor; the curve is flat-dead below 0.70 (adjudicated precision 0% in both bands under 0.70; estimated correct rows/day saturates at ~154 and buys nothing further). KNOWN LIMITS, measured, deliberately open: a LOWER-CASE out-of-catalog subject is invisible to the capitalisation tell ("spot silver price", "average gas price per gallon") and wants a CATALOG-COMPLETENESS answer -- the catalog carries no gold/silver/gasoline signal, so argmax has nowhere correct to land -- or an abstain margin, NOT another shape rule; a description that is a strict SUBSET of the signal name passes ("market cap" vs "Buffett Indicator (Market Cap / GDP)") because a differential rule cannot fire on a novel token that does not exist; and EVERY false reject measured is a CATALOG SURFACE GAP not a rule defect ("US crude price" refuses against oil-price because NO surface of that signal contains "US"), so enriching the surface fixes it and loosening the rule would not. SAMPLE LIMIT [do not re-derive without it]: sentinel.extracted_observations is BY DEFINITION the rows that did NOT take the macro route, so replaying the alias map over it returns 0 matches TAUTOLOGICALLY -- a property of the sink, never evidence about a matcher -- and the 5 rows/day that DID match are in macro_observations and absent from it; the harness therefore measures RECALL OVER THE MISSES only. / PRECOND the shadow WRITES NOTHING -- not macro_observations, not extracted_observations, not the row it is handed -- and runs AFTER the caller's durable write, swallowing every failure; promoting it to a write path is a NEW decision that supersedes this entry, never a config change. Metric tags stay bounded (outcome x score-band bucket -- a TWENTILE, Floor(score*20)/20 in steps of 0.05, NOT the decile three places used to call it; reason in {entity_scope,derived_quantity}); the triggering token and the signal id are UNBOUNDED and ride the log, never a tag. "OFF THE CRITICAL PATH" IS A BOUND, NOT AN ADJECTIVE: the scoring loop is awaited INSIDE ObservationRepository.AddRangeAsync, which ExtractionProcessor wraps in its timed Stage.Persist span, one embed per missed row, sequentially, against a client with a 30s per-call timeout and deliberately no circuit breaker -- so unbounded it adds up to N x 30s per article and inflates the very histogram that exists to compare persist latency across the v1/v2 pipelines. A per-batch budget (ShadowBatchBudget) caps the whole loop; rows it cannot reach are counted as outcome=budget_exhausted, because a shadow that silently SAMPLES reads exactly like one that OBSERVES and its accept rate means something different. AN OUTAGE MUST NOT READ AS A DISPROVED HYPOTHESIS: the empty-snapshot check runs BEFORE the embed call, so the likeliest failure of this build (dead embedder) used to meter as `no_candidate` -- the outcome that means "the catalog held nothing close", i.e. evidence AGAINST the recall hypothesis the shadow was built to test. The outcome axis now separates not_primed | embed_failed (the embedder) from gate_failed (the rule set, which used to surface as embed_failed because _gate.Evaluate sat outside every try) from budget_exhausted (the shadow's own clock) from no_candidate (the catalog genuinely holding nothing). ONE emission point, the router, always carrying BOTH tags: the matcher used to emit a second bucket-less increment, so one outage row counted twice under two outcomes. sum() over the counter is rows HANDED, never rows SCORED: budget_exhausted adds one per row the batch budget could not reach, so the two agree only while the budget never fires -- i.e. everywhere except the degraded period an operator is investigating. The duration histogram records ONLY attempts that reached the embedder -- recording the nanosecond short-circuits pulled p95 DOWN during precisely the stall it is watched for. / GUARD MacroObservationRouter.ObserveCatalogMissAsync @ src/Services/MacroObservationRouter.cs:103 (writes nothing; single metric emission point) + SignalScopeGate.Evaluate @ src/Services/SignalScopeGate.cs:165 (differential rules -- and Rule 1 reads AllText where Rule 1b reads Name DELIBERATELY: the editorial prose is evidence of what the series IS but only publication CADENCE for periodicity. The description donates a class the name lacks for 24 of 85 signals, so narrowing Rule 1 to Name was measured rather than argued -- it costs 2 of 66 correct matches to buy 3 junk refusals over ALL 240 rows (126 -> 129 of 174) and ZERO at >=0.80, where an operating point would sit and where it merely LOWERS gated precision from 90.4% to 90.2%. Declined on that evidence; revisit with a measurement, never with a tidiness argument) + ObservationRepository.ObserveCatalogMissesAsync @ src/Data/Repositories/ObservationRepository.cs:236 (the per-batch budget that makes "off the critical path" true) + MacroObservationRouter.TryPlanMacroWrite @ src/Services/MacroObservationRouter.cs:276 (the miss counter -- the denominator that made the funnel visible) / TEST SemanticSignalShadowTests.should_never_write_when_shadow_scores_a_catalog_miss (asserts the writer substitute received NOTHING, so it REDs when ANY write is injected -- mutation-verified through both members of IMacroObservationWriter, accepted and refused verdicts alike. The prior version named WriteAsync only and a real macro_observations write via WriteBatchAsync left it GREEN with all 2,160 tests passing; that control was re-run here and still passes under the old assertion, which is what makes this a fix rather than a rewording) AND ObservationRepositoryShadowBudgetTests.should_never_write_to_the_substrate_while_scoring_a_batch_of_catalog_misses (the same property through the REAL persistence flow -- routing decision, legacy commit and shadow loop together) AND SemanticSignalShadowTests.should_count_a_catalog_miss_so_a_dead_route_is_distinguishable_from_no_input AND SignalScopeGateTests.should_refuse_a_probability_of_the_signal_but_keep_one_for_a_probability_signal (the ONLY guard on differential-not-denylist -- the ACCEPT half REDs if Rule 1 rejects on the operator word alone) AND SignalScopeGateTests.should_refuse_a_jurisdiction_the_signal_does_not_carry AND SignalScopeGateTests.should_refuse_a_tenor_the_signal_does_not_carry AND SignalScopeGateTests.should_refuse_a_conflicting_periodicity_however_the_compound_is_spelled (now EVERY spelling in the lexicon, not the two compounds it covered. The earlier entry read as though this axis were closed and it was not: the token shape was letter-initial and slash-free, so `y/y` tokenized to a bare `y`, `m/m` to `m`, `q/q` to `q` and `12-month` to `month`, and FOUR of the lexicon's own entries could never match anything. Measured through the real gate: "US CPI month over month" refused, "US CPI m/m" ACCEPTED -- same claim, same day's stream, opposite verdicts decided by punctuation. Periodicity now matches on a phrase that preserves `/` and `-`; the `daily` class also gained the spaced twin every other class already had) AND SignalScopeGateTests.should_keep_the_signals_own_periodicity_however_it_is_abbreviated (the accept half, so a lexicon that merely refused everything containing a slash cannot pass) AND SignalScopeGateTests.should_not_read_a_quarter_marker_as_a_rival_subject (`q1`..`q4` were not merely inert, they INVERTED: "Q1" tokenized to a bare capitalised `Q` in no catalog surface, so Rule 2b refused it as a rival SUBJECT -- measured at 85 of 85 catalog signals, i.e. every quarter-qualified description in the stream landed junk on the wrong axis of the metric this shadow exists to produce. The reason tag IS the measurement, so a right verdict under a wrong reason is a defect) AND SignalScopeGateTests.should_not_read_a_measured_value_as_a_tenor + SignalSurfaceSetTests.should_not_read_a_digit_that_qualifies_no_duration_as_a_tenor (Rule 2a armed off a bare \d+ in the signal's own NAME, so the 6 of 85 signals carrying incidental digits -- "U-3", "M2", "S&P 500", "Nasdaq 100", "99pctl" -- then refused any description carrying a measured VALUE: "US unemployment rate at 4.1%" refused against unemployment-rate as tenor:4,1, a correct match lost to the decimal point in its own number. A tenor now requires a duration unit) AND SignalScopeGateTests.should_keep_the_signals_own_jurisdiction_however_it_is_punctuated (REDs if NormalizeToken stops folding internal periods; "U.S." vs "US" was 7 of 11 false rejects on the first iteration) AND SignalSurfaceSetTests.should_extract_a_tenor_even_when_a_letter_follows_the_digits (REDs if the tenor pattern regains a trailing \b -- the catalog writes "10Y"/"2s10s", so a bounded pattern extracts NOTHING and the rule no-ops on every rate signal while LOOKING present) AND SignalSurfaceSetTests.should_embed_prose_surfaces_and_exclude_source_system_mnemonics AND SemanticSignalShadowTests.should_meter_an_unprimed_snapshot_as_an_outage_not_as_no_candidate + should_meter_a_failing_query_embed_as_embed_failed + should_meter_a_gate_fault_separately_from_an_embedder_fault (the three ways this build fails, each landing on its own outcome instead of all three on the one that reads as a refuted hypothesis) AND SemanticSignalShadowTests.should_emit_exactly_one_match_measurement_per_scored_row (ONE increment per row handed -- NOT sum() == rows scored, see above) AND SemanticSignalShadowTests.should_not_record_a_latency_sample_for_a_call_that_never_reached_the_embedder (REDs if p95 can fall during an outage) AND ObservationRepositoryShadowBudgetTests.should_bound_the_shadow_so_a_stalled_embedder_cannot_stall_extraction + should_count_the_misses_the_budget_could_not_reach · HARNESS tools/semantic-signal-shadow/score_semantic_match.py + adjudication-2026-08-12.tsv reproduce the curve independently of the service and ship the 240-row adjudication; it prints a curve and does NOT choose a threshold

  D-20 ingress-possessive-normalisation: INTENT companion to D-7 (supersedes none) — NORMALISE the candidate surface at the NER ingress, do NOT reject it: a trailing possessive clitic ("Rocket Lab's", "Ariel Investments'") is GRAMMAR belonging to the sentence the span was cut from, not to the entity, and it is not a junk CLASS — the entity behind it is real and wanted. The cost is not merely a dirty search string: SecMaster's candidacy filter is a SUBSTRING test — InstrumentRepository.SearchAsync admits a row only when the query is a substring of its SYMBOL, NAME OR DESCRIPTION (three `ILIKE '%query%'` tests, ORed; an earlier `name ILIKE '%query%'` spelling here UNDERSTATED the filter) — so the POSSESSIVE FORM OF A NAME THE CATALOG HOLDS WITHOUT ONE ("Rocket Lab's" against `ROCKET LAB USA INC`) matches no catalog row on any of the three, returns ZERO candidates, and the resolution dies before any ranking runs. SCOPED on purpose — the GENERAL claim "a possessive surface matches NO catalog row" is FALSE, and an earlier revision of this entry made it (corrected 2026-08-13, measured live): 788 ACTIVE catalog names CONTAIN `'s` CASE-INSENSITIVELY — the catalog is UPPERCASE (`MACY'S INC`), so the backticked lowercase literal matches only 736 and 788 is the count that governs, SearchAsync's test being `ILIKE`; the basis went unstated until 2026-08-14 — so a surface whose possessive IS the catalog spelling matches perfectly well — `Papa John's` -> PZZA `PAPA JOHN'S INTL INC`, `Macy's` -> M `MACY'S INC`, `Moody's` -> MCO `MOODY'S CORP` (5 rows) — and 15 possessive queue surfaces ALREADY match unstripped today, of which the NAME-ONLY 7 are led by `Moody's` 33 occ, `McDonald's` 16, `Wendy's` 7. A SECOND blocker sits on top of the possessive, an ENCODING one: active instrument names carry 830 STRAIGHT apostrophes and ZERO curly ones, while 58% of the possessive queue (2,182 surfaces / 3,928 occ) uses the CURLY `’`. So a curly possessive can never match a catalog NAME — but that is a claim about ONE column, and what decides the resolution is whether SearchAsync returns ZERO CANDIDATES, which all THREE decide: 771 active rows carry a curly apostrophe in `description`, and 4 curly surfaces reach a row through it today. Re-measured 2026-08-14 on the three-column definition, those 15 unstripped matches split 11 STRAIGHT / 4 CURLY and the LARGEST is CURLY — `the Federal Reserve’s` 215 occ, `The Federal Reserve’s` 41, `Federal Reserve’s` 5, `Bachelor’s` 1, every one of them through DESCRIPTION. "All straight" is true only of the NAME-ONLY subset, which is 7 (`Moody's` 33, `McDonald's` 16, `Wendy's` 7, `Reddy's` 5, `Lowe's` 3, `Fannie Mae's` 2, `Consumers'` 1); an earlier revision asserted it of all 15, mixing two bases in ONE sentence — the same defect corrected in the top-20 list below (corrected 2026-08-14). Encoding is NOT "the real mechanism", and an earlier revision of this entry called it that: it is an ABSOLUTE blocker on the curly majority, while the POSSESSIVE FORM is the DOMINANT term. On the name column 0 of 2,182 curly surfaces match against 7 of 1,586 straight (0.44%) — one 2026-08-14 measurement, whose 3,768 total is the 3,761 below plus a night's accretion, not a second basis — so encoding accounts only for the gap between 0% and 0.44% — 1,579 of those 1,586 STRAIGHT possessives fail on the possessive form alone. Say it that way, because "the real mechanism is encoding" points the next agent at normalising the APOSTROPHE, which would only move curly surfaces into a population matching 0.44% of the time, against the 607 surfaces / 1,228 occ the possessive rule below actually gains. That is not a counter-example to the rule, it is WHY the trailing-only + multi-token contract is load-bearing; and it is the SAME fact the "only re-ranks" clause below rests on, since re-ranking PRESUPPOSES candidates. The two clauses cannot both be read generally, which is how the contradiction got in. The class doc at `src/Services/CandidateSurfaceNormalizer.cs` and the test comments were already correctly scoped to "Rocket Lab's"; only this entry generalised, and a generalisation is exactly what a future agent would quote to justify loosening ("stripping is free, the possessive gets nothing anyway"). It does NOT then reach SearXNG: `ISearxngClient` has THREE callers and not one is fed a candidate surface — MirrorSearchService queries the article HEADLINE (`attempt.Title`), SearxngCollectionScheduler an operator-configured `sentinel.search_queries` row, ValidationQueryExecutorWorker a template-substituted release/threshold string. An earlier draft of this entry named a "free SearXNG probe" consumer that DOES NOT EXIST (corrected 2026-08-13). What the dead surface does cost downstream is real but singular: the SecMaster `/api/resolve-entities` cascade behind the POST, whose last-resort leg is the PAID :9300 Gemini call. Measured on the frozen 6h capture (sha256 79c4016410…, 1,504 records / 980 distinct, 2026-08-11): 42 of 872 prepass surfaces carry a trailing clitic and 33 normalise; 12 go from ZERO catalog candidates to a real instrument, 11 at HIGH confidence (RKLB WDC VG BAC CAH NEE COF BKD HPK MG FMCC DISHTV.NS) — recovered resolutions, the D-8 "genuine miss" class, not a $-saving. CAVEAT [figures superseded, rule unaffected — read before citing any 2026-08-11 CATALOG number above or below]: they predate #961's catalog repair, run against 1,171 names at 2026-08-13T22:14Z. The rewrite COUNT is reported two ways and this entry no longer picks one: #961's report says 1,095, while stamping says 1,090 — the gap is rows the repair "rewrote" to the SAME string, which the report counts and the row does not show. Cite the mechanism, not the number. What IS independently measurable in `instruments.updated_at` and re-verified here 2026-08-13: 945 rows stamped in that single minute, and 1,053 rows carrying `discovery_source=entity_resolution:gemini` stamped across the whole repair window (22:00-23:00Z). They are NOT re-derived, because this rule is SHAPE-based and depends on no catalog fact; and the re-measurement against the repaired catalog moved every number the RIGHT way — wrong-issuer down from 29 groups to ~13, and the tier-1 "certain to land" WRONG class ELIMINATED, 17 -> 0. The post-repair GAIN is re-derived from the live queue below (607 surfaces / 1,228 occ, 0 losses) rather than from the frozen capture, and the two counts are NOT interchangeable: the capture is a 6h paid-boundary window, the queue is the whole open backlog. A further 9 land on a surface the capture ALREADY carries, so the D-7 collapse can merge them (D-7's content key strips punctuation PER TOKEN — "Rocket Lab's" keys as `rocket labs`, NOT `rocket lab` — so D-7 provably does NOT already catch this; normalisation is what makes those keys agree). Runs BEFORE the junk gate so the whole chain (classify -> collapse -> POST) sees ONE canonical form; normalising AFTER would hand SecMaster a surface the gate never classified. SCOPED to the NER-candidate ingress and deliberately NOT run over the D-6 V2-direct/V1-mirror SubjectEntity: measured 0 of 120 v2-direct surfaces carry the clitic (the class is spaCy-span-specific, not LLM-specific), and D-15 REQUIRES SubjectEntity to equal a declared ENT verbatim_name — normalising there would break that grounding equality, so the two must not be merged into one call site / PRECOND strip ONLY a TRAILING clitic (`'s` `’s`, or a bare `'`/`’` whose preceding char is `s`) AND ONLY when the possessor is MULTI-TOKEN, Kind==CompanyName. The multi-token requirement IS the discriminator and it is not a tie-breaker: a single-token possessive is structurally IDENTICAL to a name that simply ends that way ("Macy's" "Moody's" "Kohl's" vs "Meta's" "Dell's" "Teck's") — no shape rule separates them, so this one refuses. Measured 2026-08-11 against the live catalog, the refusal is not merely polite to real names, it is what prevents a REWRITE INTO A DIFFERENT ENTITY: the possessor of a single-token possessive is usually a bare common noun that then HIGH-CONFIDENCE matches an unrelated issuer — "Universal's"->"Universal"->UVV (Universal Corp, tobacco), "Channel's"->"Channel"->PTHS (Channel Therapeutics), "Bailey's"->"Bailey"->a FRED county series — the wrong-ticker corruption D-8/D-10/D-12 exist to stop. WHAT THE REFUSAL COSTS [measured 2026-08-13; the earlier wording, "the handful of single-token wins it also forgoes", was false by two orders of magnitude and is the kind of claim a future agent revisits]: over `entity_resolution_review_queue` (79,437 open surfaces / 259,297 occ — a LIVE ACCRETING table per D-14, so the SHAPE of this split reproduces and the ABSOLUTES do not; cite it that way) 3,761 CompanyName surfaces / 6,622 occ carry a trailing clitic, splitting 2,766 / 4,768 multi-token against 995 / 1,854 single-token. Loosening the discriminator to accept a single-token possessor would hand 572 surfaces / 1,198 occ a catalog candidate they do not have today — MORE than the multi-token rule itself buys on the SAME corpus (607 surfaces / 1,228 occ; 0 losses in BOTH directions, i.e. stripping never removes a candidate a surface already had). The refusal STANDS anyway, because that population is MIXED and not clean: in the top 20 by occurrence — BASIS: PER-ROW (one queue row per apostrophe spelling), which is the basis EVERY figure in this sentence now uses; the earlier text quoted Alphabet at its AGGREGATED 59 beside per-row figures for everything else, which is exactly why it read as "Trump 50 is the largest" next to a 59 — the genuine single-token issuers (Alphabet->GOOGL 40 and 19 across the two spellings, Meta->META 34, Moody->MCO 33 and 27, Broadcom->AVGO 24, Dell->DELL 19, McDonald->MCD 19 and 16, Disney->DIS 14, MSCI 13, Oracle->ORCL 13 — both Alphabet rows sit inside the top 20 and both are inside the 446 denominator, but an earlier revision named only the 40 while spelling BOTH rows out for Moody and McDonald; corrected 2026-08-14) sit beside a PERSON ("Trump", 50 occ — the largest single ROW on this basis; on the AGGREGATED basis it is THIRD behind Moody 60 and Alphabet 59, so name the basis before calling anything "largest". "Largest WRONG hit" is true on BOTH and is the claim worth making), two further PERSONS ("Warsh" 28, "Mamdani" 9), a CITY ("Kyiv"->KYIVSTAR GROUP 21) and a known corruption ("Universal"->UVV 11); below the top 20 at 9 occ each sit the QUARTER LABEL ("Q1"->Q1.MX), the MAGAZINE ("Variety"->BJ, matched through the DESCRIPTION column) and "Mizuho"->4595.T Mizuho Medy — real, but OUTSIDE the volume this share is computed over, which the earlier text did not say. A THIRD class was omitted entirely: 3 of the top 20 have NO catalog candidate AT ALL ("Barron", "Burnham", "Homer" — 19/19/18 per-row, 27/19/18 aggregated), so they are neither a gain nor a corruption and belong in NEITHER half. Re-measured 2026-08-13 on BOTH bases, the share of top-20 VOLUME resolving to a WRONG entity is ~27% — 119 of 446 occ per-row, 136 of 508 aggregated — and it is notably STABLE across the two bases even though their membership differs. The "roughly 40%" this replaces was NOT an arithmetic error and must not be read as one: it is exactly what you get counting the NO-CANDIDATE entries AS wrong (175/446 = 39.2% per-row, 200/508 = 39.4% aggregated). The two numbers answer DIFFERENT questions, and this entry now takes the position that a possessor resolving to NOTHING is neither a gain nor a corruption and belongs in neither half — which is the substantive claim, the percentage merely follows from it. Cite either as a measured approximation, NEVER as a counter: per-row correctness is a JUDGEMENT and the classification is the disputable part, not the arithmetic — judging "Lloyd" the other way (context is "Lloyd's of London", the insurance MARKET, not Lloyds Banking Group) alone moves the aggregated figure to 24.4%. GOTCHA surfaced by the re-derivation and NOT fixed here: "Trump"/"Warsh"/"Mamdani" match catalog rows whose NAME IS THE PERSON — `GASREGW`="Donald Trump", `CUUR0300SA0`="Kevin Warsh", `CUURA101SEHA`="Zohran Mamdani", SEVEN rows in all, every one `discovery_source=GeminiFallback`. An earlier revision said SIX "stamped 2026-07-18 onto a FRED/BLS series": that FILTERED to 2026-07-18 and then reported 2026-07-18 as the finding, and the filter dropped `DHOILNYH`="President Donald Trump" (2026-07-08), so the date qualifier is gone (corrected 2026-08-14). The FOLLOW-UP is WIDER than those seven and is NOT confined to FRED/BLS series — scope it on PROVENANCE, never on series, or it fixes 17 rows and misses the equities entirely: hand-classifying all 210 `GeminiFallback` rows 2026-08-14 finds 31 person-named, 16 of them ACTIVE and all series ("Mark Rutte", "Austan Goolsbee", "Aleksandar Vucic", "Andrew P. Napolitano" and more beyond the three above), 15 INACTIVE and mostly EQUITIES (`ECHO`="Elon Musk", `VSXY`="Ghislaine Maxwell", `PPRUY`="Salma Hayek Pinault", `XE`/`RXRX`="Cathie Wood", `AMCX`="Yoshimura Kana" common_stock, `SOX`="Lewis Krauskopf" index). "Person-named" is a JUDGEMENT over 210 names, not a mechanical predicate — cite 31 as a hand count, and note that only the 16 ACTIVE ones are reachable through SearchAsync at all — reachability RANKS this repair, it does NOT scope it: the population is every person-named row of this PROVENANCE, an inactive row holding the polluted name exactly as an active one does and reactivation restoring its reach, which is why the series-scoped miss above counts 17 (those 16 plus the one inactive person-named series, `MCRFPC1`="Justin Trudeau") and not 16. #961 did NOT repair them: its population allowlist keyed on `entity_resolution:gemini` and these carry a DIFFERENT provenance string (210 `GeminiFallback` rows against 1,395 `entity_resolution:gemini`; 3 GeminiFallback rows were touched in the repair window and none is person-named), so this pollution class is still live and is its own follow-up. Both halves are recorded here on purpose: the win is real AND the damage is real, and an entry that kept only the flattering half is what invites the loosening. The real names that SHADOW the multi-token rule were measured on both corpora and are UNHARMED: re-measured 2026-08-13 post-repair, 0 of 25,307 active catalog names (0 of 25,398 including inactive) and 0 of 4,392 `edgar_filers` names END in a possessive. The earlier phrasing said zero and forty in ONE sentence ("0 of 25,105 … (the 40 that do …)") — those 40 were `discovery_source=entity_resolution:gemini` self-seeded pollution, this pipeline's own damage and the same self-reinforcing corruption D-14 measured for `&`/`-`, and #961 repaired them away, so the count is now LITERALLY zero on both corpora and the parenthetical is moot. "Papa John's"/"Standard & Poor's"/"Trader Joe's" keep the SAME top-1 catalog row after stripping because `%query%` candidacy is monotonically WIDENING — removing trailing characters can never drop the true row OUT of the candidate set, only re-rank it, and a multi-token possessor stays specific enough to hold tier 2 (name-prefix). NOT a denylist entry and NOT a D-1/D-5/D-11/D-14 reject class: this STEP drops nothing, and the risk it guards is the INVERTED one (rewriting a surface into a DIFFERENT entity). BUT "nothing is dropped" is true of the STEP and FALSE of the PIPELINE, so the D-1/D-11 precondition (a false-positive silently DROPS a real resolution) is not entirely out of play and this entry does NOT get to wave it away. Normalisation runs at EntityResolutionPrepass.cs:169, immediately BEFORE ApplySurfaceFilter, so Classify receives a DIFFERENT string and can reach an EXACT-match reject set the raw surface missed. The delta is exactly the PLURAL `s'` subset, and it is a GAP IN D-14 rather than a new rule here: D-14's normalise-and-recheck arm calls CandidateSurfaceFilter.StripPossessive, which requires `surface[^1]=='s'` and returns null on a trailing apostrophe, so a plural possessive EVADED that arm entirely. Worked case: "United States'" was Kept and POSTed pre-change (the country class is EXACT-match and "United States'" is not "United States"); it now normalises to "United States" and is rejected as `gpe_country` — the answer D-14 would already have given had its arm been reachable. Direction is benign and the singular form ("the Federal Reserve's") was ALREADY rejected pre-change, so it is not part of the delta. Two things are nonetheless owed to a future reader: the drop is metered as `reason=gpe_country` with NOTHING tying it to normalisation (read `sentinel_candidate_surface_normalized_total` alongside the filter counter when auditing a volume change), and it is TESTED — see the composed-path test below rather than inferring it from the step's own invariant. INTERACTION [named, not a supersession]: D-14's Classify now receives the NORMALISED surface, so its internal possessive normalise-and-recheck arm is unreachable for the multi-token subset; the arm STANDS and still fires for the single-token SINGULAR subset this rule refuses ("Macy's" -> "Macy"), for any direct Classify caller, and for the D-6 V2-direct/V1-mirror leg this rule deliberately does not normalise; D-14's own tests exercise it unchanged. The earlier wording said "the single-token subset" and that was TOO BROAD: a single-token PLURAL ("Barclays'", "Lands'") is handled by NEITHER rule — D-20's token discriminator refuses it AND StripPossessive returns null on it, requiring `surface[^1]=='s'`. Pre-existing and NOT introduced here, but this entry must not assert coverage that does not exist. FOLLOW-UP [named, and NOT a cost of this change]: the wrong-issuer groups that survive the repaired-catalog re-measurement are `Abu Dhabi`->ADCB.AD (18 occ), `Goldman Sachs`->GSBD (14 occ), and — omitted from an earlier revision of this entry, which named only the first two and so understated the class — `Saudi Aramco`->2223.SR (`Saudi Aramco Base Oil Company - Luberef SJSC`, a SUBSIDIARY, not the parent) and `Social Security`->M318151Q027NBEA (a BEA/NIPA series whose description carries the phrase). All four are equally real and NONE is gated by `IsInstitution`: verified 2026-08-13, neither `Aramco` nor `Security` is in GenericLastWords or GovernmentBodyKeywords, and neither surface is in the InstitutionStoplist. They are tier-2 PREFIX/description matches against REAL catalog names that this rule merely makes REACHABLE — a PLACE resolving to one bank headquartered in it, and SecMaster's shortest-name trigram tie-break preferring `Goldman Sachs BDC Inc` (0.636) over `Goldman Sachs Group Inc` (0.609), which is the SAME ranking defect D-14 records for "Morgan Stanley"->CAF. Closing them needs an issuer-vs-place/segment discriminator in SecMaster RANKING, not another string rule at this ingress — a string rule here would be the destination-gating GIGO forbids. OUT OF SCOPE, reported not fixed: exchange-qualified `EXCHANGE:TICKER` surfaces (SecMaster never splits the prefix, so `NASDAQ:TLT` misses a catalog holding bare `TLT`) is a SecMaster-side contract defect, and the `ResultsCherry Hill…` fusion is trafilatura 2.0.0 emitting a `<head>` inside a `<cell>` with no separator — both are producer bugs / GUARD CandidateSurfaceNormalizer.TryStripTrailingPossessive (the multi-token discriminator) @ src/Services/CandidateSurfaceNormalizer.cs:68 AND CandidateSurfaceNormalizer.Normalize (the Kind gate, the SECOND refusal — a Ticker/TickerInQuote surface is an author-typed SYMBOL and is never rewritten) @ src/Services/CandidateSurfaceNormalizer.cs:57, both reached through EntityResolutionPrepass.NormalizeSurfaces @ src/Services/EntityResolutionPrepass.cs:333 / TEST EntityResolutionPrepassNormalizationTests.refuses_single_token_possessive_so_a_name_is_never_rewritten_into_another_entity (the discriminator guard — REDs on 7 of its 8 rows if the multi-token requirement is loosened) AND EntityResolutionPrepassNormalizationTests.never_rewrites_an_authoritative_ticker_surface (the Kind-gate guard — REDs if the Kind check is deleted, on the two MULTI-token rows `("Rocket Lab's", Ticker)` / `("Rocket Lab’s", TickerInQuote)` added 2026-08-13; before them BOTH fixtures were inert, "LANDS'" being refused by the token rule and "NASDAQ:TLT" carrying no apostrophe, so the advertised guard could not fail) AND EntityResolutionPrepassNormalizationTests.strips_trailing_possessive_from_multi_token_issuer_spans (the transformation, both apostrophe forms) AND EntityResolutionPrepassNormalizationTests.normalisation_lets_the_near_dup_collapse_merge_a_possessive_with_its_clean_sibling (ordering — REDs if normalisation stops running before the D-7 collapse) AND EntityResolutionPrepassNormalizationTests.leaves_apostrophes_that_sit_inside_a_name_untouched (NARROWER than the other four and labelled so in the test body: it kills a "cut at the last apostrophe anywhere" implementation, and reaches NEITHER the Kind gate nor the discriminator) AND EntityResolutionPrepassNormalizationTests.normalisation_exposes_a_plural_possessive_the_D14_recheck_arm_never_reached (the COMPOSED path through the REAL CandidateSurfaceFilter — the disclosure above, asserted rather than described: "United States'" must NOT reach the POST, while the singular "the Federal Reserve's" is the pre-change control and "Rocket Lab’s" is the must-survive control that stops a drop-everything implementation passing; REDs if normalisation stops running before the gate, and REDs if the plural branch of TryStripTrailingPossessive is deleted) · MUTATION-VERIFIED 2026-08-11: loosening the discriminator to accept a single-token possessor REDs exactly 5 rows — the refusal guard, its two Theory rows ("Macy's", "Universal’s"), the counter row (2 rewrites become 3), AND the PRE-EXISTING D-14 ingress test EntityResolutionPrepassFilterTests.ingress_rejects_structural_junk_but_keeps_real_issuer, which already asserts "Macy's" reaches the SecMaster POST verbatim and now reads "Macy". That independent RED is the corroboration: the damage direction was already pinned by an existing guard before this rule existed, and the strict form leaves it green · MUTATION-VERIFIED 2026-08-13 (the Kind gate, which had NO failing test until this round): deleting `if (kind != EntityCandidateKind.CompanyName) return surface;` REDs never_rewrites_an_authoritative_ticker_surface alone, and restoring it returns the file GREEN

SEE: README.md §Reference · Events/src/Events/Protos/observation_events.proto (ObservationEventStream — EXPOSES to ThresholdEngine + CONSUMES from ThresholdEngine for validation) · Events/src/Events/Protos/secmaster.proto (SecMasterRegistry — registration) · src/Semantic/NewsSignalClassifier · src/Services/MacroObservationRouter+EntityResolutionPrepass · src/Workers/ExtractionProcessor · ThresholdEngine src/Workers/ObservationCellProjector · MacroSubstrate MacroObservationRepository · NewsMomentumQueryService (digest)
