# SentinelCollector — architecture [agent read-first]

NOTE: this card covers the news->matrix pipeline which spans SentinelCollector AND MacroSubstrate (see MacroSubstrate/README for the shared write path).

PURPOSE: news-article -> `(signal x sector)` matrix tilt via GPU vLLM JSON-CoD extraction (Extraction:Backend=VllmJson, Qwen2.5-32B-AWQ) + `:sig:` rows in macro_observations. no gRPC cell push; digest + extraction/CoVe/CoD paths not addressed here. (CPU llama-server DSL extraction = rollback path.)

DATA MODEL + INVARIANTS:
  INV `:sig:` infix: news row identified SOLELY by literal `:sig:` in source_id (`{rawContentId}:sig:{signalId}`). not schema-enforced — string contract; FOUR artefacts move together: producer (MacroObservationRouter) + projector const (ObservationCellProjector) + consumer (NewsMomentumQueryService) + digest reader (DigestQueryService.ArticleSignalsSql + TryParseRawContentId). change-all-or-none.
  INV signal⊥sector: signal_identity_id=SIGNAL dim gates projection; atlas_sector_code=SECTOR dim does NOT gate. null-signal->never projects (qualitative rows skipped permanently).
  INV |v|<=1: `:sig:` rows carry |value|~<=1; projector excludes |v|>1.5 (raw-value leak guard); drops non-`:sig:` rows from decay sum even if same group (G2 mixed-group drop).
  INV heal-on-rewrite: macro_observations = authoritative cell source; projector re-projects in place (DO UPDATE), never DO NOTHING.
  INV sector-dim⊥entry: resolve-entities sectorless/fail -> row still written (sector=null or fallback); prepass feeds sector-pick inside classify, not an entry gate.
  INV incomplete-SectorWeights: group whose pattern's SectorWeights missing any sector -> ArgumentException caught -> group skipped, 0 cells, logged Warning not Error.
  INV decay: news magnitude = K*tanh(S/K) over half-life-weighted decay sum (freshness=1.0, no floor). K+H from Matrix:NewsDecay. Not the hard-data mean formula.
  review_status AutoClosed = terminal NoResolution disposition (Stage 1): NoResolution rows never auto-approve/reject -> would pile up Pending forever. AutoClosed exits the human queue (ReviewUiEndpoints query = Pending AND InstrumentId!=null AND ResolutionState==Resolved — NoResolution rows are excluded from the human view by the ResolutionState==Resolved filter whether or not they carry a stray instrument_id, see review-UI GOTCHA; AutoClose = the terminal/digest transition) while the digest KEEPS it (DigestQueryService !=Rejected; NOT Rejected — that would strip legit macro/qualitative digest content). Set by NoResolutionSweepWorker (age-gated, NoResolutionCloseGraceDays default 7) + POST /admin/review/close-noresolution. No D-entry — terminal-state janitor, not an exception path.

PATHS (distinct code — do not conflate):
  NewsSignalClassifier [in-proc · inside ExtractionProcessor]
    does: structured vLLM call; inlines signal_identities catalog + 11 sector codes; validates ids; drops <0.5 confidence / NaN / hallucinated.
    does NOT: throw (fail-soft->Empty); parse free-form; touch DB.
    on-miss: empty catalog / empty excerpt / timeout / unstructured -> NewsSignalClassification.Empty; extraction proceeds.
    ⚠ excerpt capped before call — signal in tail -> Empty, not failure. Sub-floor confidence empties feed silently (counted, not errored).
  MacroObservationRouter writer [in-proc · MacroSubstrate]
    does: upsert value_numeric=tilt*confidence; source_collector="sentinel"; `:sig:` source_id.
    does NOT (NEWS caller): fall back to extracted_observations — Failed=row LOST (by design); count IdempotentSkip as progress.
    ⚠ CollectedAt stored as UTC instant of its offset, not source wall-clock; matters for idem-key matching.
    ⚠ NUMERIC caller DOES fall back to extracted_observations two-phase. Fallback behavior diverges by CALLER not method.
  ObservationCellProjector [ThresholdEngine background · DB-poll]
    does: read Kind=Numeric DESC; group by (signal_identity_id,source_collector) -> 11 sector rows/group; decay sum -> K*tanh(S/K) * trust * temporal * confidence * sectorWeight clamped +/-3; upsert matrix_cells.
    does NOT: gate on sector; project null-signal rows; project unknown/disabled patterns; write partial cell-set for incomplete SectorWeights.
    does not run when Matrix:ObservationProjectorMode=Off -> zero matrix_cells + no error. CHECK this config FIRST before concluding pipeline broken.
    ⚠ Shadow is not dry-run: runs identical read+project+write as Authoritative (same cells written); only Off suppresses writes.
    ⚠ read DESC + hard cap -> sustained ingest silently drops oldest (cap-induced staleness, metered).

PROCESSING ORDER (per article): normalize -> EntityResolutionPrepass(spaCy NER->SecMaster,fail-soft) -> V2 pipeline -> TryClassifyNewsSignalsAsync -> per-signal preferredSector=grounded??classifier.Sector -> plan -> write -> addRange.
MAXIM: prepass output FEEDS classify as parameter -> router derives grounded sector. Prepass=sector input, not an entry gate.

DISTINCTIONS:
  `:sig:` news row ≠ raw-legacy sentinel row — same table/group; only `:sig:` feeds projector decay sum; legacy raw values dropped (G2).
  signal-dim (signal_identity_id, gates projection) ≠ sector-dim (atlas_sector_code, does NOT gate).
  matrix feed (macro_observations->projector->matrix_cells) ≠ digest consumer (matrix_cells+sector_regimes+sentinel rows, separate read; theme taxonomy retired — sector is the only digest axis) ≠ extracted_observations (legacy numeric fallback sink, not read by matrix or digest).
  digest news-momentum SQL filters source_collector='sentinel' AND signal_identity_id IS NOT NULL AND value_numeric BETWEEN -1.5 AND 1.5 — not a `:sig:` infix filter; splits window at midpoint for early->late per-signal tilt trend (replaces former bias-vs-FRED-matrix view, removed: FRED matrix too stale/sparse -> fake divergence). Legacy non-`:sig:` sentinel numeric rows WITH signal_identity_id enter digest momentum avg but ARE dropped by projector (G2). Two consumers see DIFFERENT effective inputs.
  news magnitude=decay-weighted SUM(K*tanh(S/K)) ≠ hard-data mean(sum(o_i)/n * freshness scalar). Unexpected news cell magnitude -> CHECK Matrix:NewsDecay K/H.
  ThresholdEngine projector(DB-polled) ≠ gRPC MatrixUpdateStream/ObservationEventStream (separate event surface; matrix feed is DB-polled not gRPC).

CROSS-SERVICE:
  IN: SecMaster HTTP :8080 /api/signal-identities (catalog warmup) + /api/resolve-entities (sector grounding, fail-soft). vLLM GPU (classify).
  IN: ThresholdEngine gRPC-stream :5001 ObservationEventStream (ValidationEventConsumerWorker — sector-crossing events -> validation triggers).
  OUT: macro_observations via in-proc DI, not network. FEEDS: ThresholdEngine projector (DB-poll) -> matrix_cells; digest reads matrix_cells separately.
  EXPOSES gRPC :5001: ObservationEventStream (ThresholdEngine consumes) + SecMaster gRPC :5001 RegisterSeries (f-a-f on extracted observations).

GOTCHAS:
  ✗ gate-news-entry-on-resolve-entities/sector ✗ route-news-via-gRPC-MatrixUpdateStream
  ✗ assume-CandidateSurfaceFilter-catches-all-junk: STRUCTURAL junk + curated non-entity classes are rejected at ingress (D-1 #824 markup/slug/money/country/institution/crypto; D-5 govt-agency/regional-Fed/byline/garbled-fragment #823 recurrence; D-11 curated non-tradeable media-outlet set — a market-INDEX class was rejected: US indices resolve to real tracked instruments) and markup stripped at the normalizer last-resort (D-2, #825), but SEMANTICALLY-ambiguous surfaces (media ISSUER names like BuzzFeed=BZFD/NYT, market-index names, no-digit slugs, bare public-official SURNAMES) Keep BY DESIGN -> SecMaster resolve -> its budget-gated Gemini last-resort (#823, company-name->ticker ONLY; ungated it drained $100/6d 2026-06-30). Defense-in-depth, not redundancy: ingress cleans the source, SecMaster gates the $. TWO filter SOURCES, both now cleaned: the NER-candidate ingress (D-1/D-5, feeds the SecMaster->resolver path) AND the LLM-extracted V2 SubjectEntity (D-6 — DeterministicResolver.TryGeminiResolveAsync calls :9300 DIRECT, a SEPARATE source that BYPASSED the ingress filter until D-6 ran the SAME filter over it before the paid call); only ambiguous-Keep is uncaught, by design. [[GIGO]] [[INTENT_FIDELITY]]
  ✗ extracted_observations-fallback-for-news ✗ assume-digest-sees-only-`:sig:`-rows ✗ Shadow=dry-run
  ✗ qualitative(null-signal)-rows-reach-matrix_cells ✗ zero-matrix_cells-no-error=broken(check-Mode-first)
  ✗ change-`:sig:`-infix-in-one-artefact ✗ CollectedAt-as-wall-clock ✗ numeric-fallback-applies-to-news
  ✗ incomplete-SectorWeights-writes-partial-cells ✗ prepass-as-entry-gate
  review-UI actionable filter: `/ui/review` list AND Pending count share ONE query = ReviewStatus==Pending AND InstrumentId!=null AND ResolutionState==Resolved (ReviewUiEndpoints.ActionablePending — one predicate so list + count agree). Human approves/rejects RESOLUTIONS; a row with no instrument, OR a NoResolution row carrying a stray instrument_id (a ticker co-mentioned with an unrelated subject — 69% of the raw queue), has NOTHING to action -> auto-disposed by the background sweep/drain, NOT human-reviewed. No data hidden — those rows persist + still age out, just not in the human view. ✗ drop-EITHER-filter: dropping InstrumentId!=null lets null-instrument rows leak; dropping ResolutionState==Resolved lets NoResolution-with-stray-symbol garbage leak — both re-flood the queue and mask the actionable rows. GOTCHA-note not a D-entry — display-side invariant, not a scarce-resource/exception path.

DECISIONS:
  D-1 ingress-junk-filter: INTENT reject non-entity surfaces at the ingress where garbage is BORN (#824) — junk poisoned FRED search (#818), paid Gemini (#823) AND resolution correctness for FREE (wrong-ticker -> corrupt matrix); one source fix covers every consumer / PRECOND reject STRUCTURALLY-invalid only (markup-chars|dotted-id|slug+digit|money/number|metric-abbrev|country|institution|crypto); semantically-ambiguous names Keep -> SecMaster decides (dropping a real equity = the expensive error); destination gates stay defense-in-depth / GUARD EntityResolutionPrepass.ApplySurfaceFilter @ src/Services/EntityResolutionPrepass.cs:300 / TEST EntityResolutionPrepassFilterTests.enforce_mode_removes_junk_candidates_before_secmaster_call
  D-2 html-strip-last-resort: INTENT the last-resort path (both normalizers failed) must NEVER emit markup as content (#825) — raw DOCTYPE/tags/script bodies reach the LLM extractor + spaCy NER as bogus entities / PRECOND strip failure -> empty not raw (fail-closed); OptionMaxNestedChildNodes=1000 converts an UNCATCHABLE StackOverflow (deep nesting; ~4.5k tags crashed the 1MB worker stack) into a catchable load failure / GUARD ContentNormalizer.StripHtmlFallback @ src/Services/ContentNormalizer.cs:267 / TEST ContentNormalizerTests.should_return_visible_text_not_markup_when_both_normalizers_fail_on_html
  D-3 llm-signal-validation: INTENT LLM output is never trusted into macro_observations — hallucinated ids/NaN/sub-floor dropped + counted per reason (feed must never silently re-zero); tilt clamp [-1,1] = SOURCE enforcement of INV |v|<=1 (projector 1.5 gate = destination backstop) / PRECOND classifier is additive enrichment: fail-soft->Empty, never breaks extraction / GUARD NewsSignalClassifier.Validate @ src/Semantic/NewsSignalClassifier.cs:192 / TEST NewsSignalFeedWiringTests.should_drop_hallucinated_id_so_only_valid_signals_route
  D-4 gpu-prompt-budget: INTENT refuse an over-budget prompt BEFORE the GPU call (throw PromptTooLargeException, zero HTTP) — context-overflow 400s waste GPU round-trips and mis-log as extraction errors / PRECOND callers assembling prompts client-side derive budgets from PromptTokenAllowance/ExactPromptTokenAllowance (guard+caller can never disagree); structured path verifies vLLM /tokenize exact count — chars/4 under-counts ticker-dense prose (#692) / GUARD VllmClient.EnforcePromptBudget @ src/Services/VllmClient.cs:597 / TEST VllmClientPromptSizeGuardTests.should_not_send_http_request_when_prompt_too_large_for_generate
  D-5 ingress-non-entity-classes: INTENT companion to D-1 (supersedes none) — extend the SAME ingress cleanup to the non-entity surface classes that flooded the paid Gemini resolver AFTER D-1 shipped (govt agency/regional-Fed, journalist byline, garbled ticker-fragment); gating only the resolver destination (#823) is the recurring disease — the junk re-appears at the next consumer, so reject at the SOURCE and every consumer (FRED, Gemini, storage, matrix) is covered once / PRECOND reject only UNAMBIGUOUS non-instruments: curated federal-agency/stats-bureau set + trailing-"Fed" regional rule; political parties (trailing-"Party" only — "Party City" leads); govt institutions (standalone keyword Institute/Ministry/Directorate/Parliament/Politburo anywhere, corp-suffix-exempt); byline = embedded newline + all-caps dateline; garbled = >=3 tokens containing a digit-bearing dotted uppercase ticker code. Bare/full public-official PERSON names (Carney/Warsh/Chuck Schumer) NOT rejected — no precise structural discriminator, a blocklist would nuke person-named issuers (Morgan Stanley, Charles Schwab); ambiguous names Keep -> SecMaster resolve + resolver daily-cap stay defense-in-depth. credential/degree class DROPPED 2026-07-02 — 9/22 tokens were live US tickers (DDS=Dillard's/CPA/ESQ equities, DBA/CFA ETFs, MPA/SCD/EDD CEFs, MSC ADR); rejected real instruments while firing ~never; precision > recall means an ambiguous degree Keeps / GUARD CandidateSurfaceFilter.Classify @ src/Services/CandidateSurfaceFilter.cs:273 / TEST EntityResolutionPrepassFilterTests.ingress_rejects_agency_byline_and_garbled_but_keeps_real_entity
  D-6 v2-direct-gemini-surface-filter: INTENT companion to D-5 (supersedes none) for the SEPARATE V2 Sentinel-direct source — the LLM-extracted SubjectEntity reaches the paid :9300 Gemini resolver via DeterministicResolver WITHOUT the prepass ingress filter, so the same junk classes D-5 cleans at the NER-candidate source still flood the shared 1500/24h cap here (the outcome=client_null 429 storm, ~146/hr, starves genuine last-resort resolutions); GIGO cleans at EVERY source not per-destination, so run the SAME ICandidateSurfaceFilter (reuse only — no new classification) over the SubjectEntity BEFORE the call / PRECOND identical to D-5 — EntityCandidate(SubjectEntity, CompanyName) so behaviour matches the NER-candidate path EXACTLY; reject only unambiguous non-instruments, ambiguous names Keep (a false-positive silently drops a real resolution — worse than junk the cap already refuses for $0); a junk SubjectEntity skips the call == non-resolution (caller falls through to Rule 2/3), counted outcome=surface_filtered on sentinel_gemini_resolver_calls_total (specific reason rides activity/log, NOT a metric tag) / GUARD DeterministicResolver.TryGeminiResolveAsync @ src/Services/DeterministicResolver.cs:476 / TEST DeterministicResolverTests.gemini_direct_path_filters_junk_subject_but_calls_for_real_entity · V1 MIRROR (added w/ D-12): the V1 leg GeminiSymbolFallbackService.LookupAsync never ran this filter — it now runs the SAME Classify over the SubjectEntity BEFORE its paid :9300 call (a junk subject skips the call for $0 == NoMatch) / GUARD +GeminiSymbolFallbackService.LookupAsync @ src/Services/GeminiSymbolFallbackService.cs:72 / TEST +GeminiSymbolFallbackServiceTests.v1_gemini_surface_filters_junk_subject_before_paid_call
  D-7 near-dup-collapse: INTENT companion to D-1/D-5/D-6 (supersedes none) — collapse near-duplicate NER spans of the SAME entity from the SAME article to ONE candidate at the SOURCE (the Sentinel prepass set, AFTER the junk gate, BEFORE the SecMaster POST), so every downstream consumer (SecMaster resolve, the paid :9300 Gemini last-resort, the matrix) sees one candidate and burns ONE slot of the shared 1500/24h cap, not N, and cannot double-count the entity; the existing exact-surface+kind dedup at the SecMaster DESTINATION (EntityResolutionService.cs:144 GroupBy (Surface.Trim, Kind)) does NOT catch article/function-word variants — "Cuban Institute of Friendship" vs "...for Friendship" = two paid resolutions / PRECOND merge ONLY when unambiguously the same entity: identical ORDERED content-token key within ONE Kind — lowercase, strip a leading article, strip punctuation (. , ' & quotes parens) per token, DROP curated function words {of,for,and,the,a,an,at,in,on,to}+bare &, join remaining ORDERED tokens; per group keep the LONGEST surface (tie->first, stable), survivors stay in original order. Precision over recall — a FALSE merge silently DROPS a distinct entity's resolution (strictly worse than the minor waste of resolving a true dup twice): "Bank of America"!="Bank of China", "Morgan Stanley"!="Stanley Black & Decker", "First Solar"!="First Republic" (different content key); different Kind never merges (key includes Kind). NO fuzzy similarity; NO corp-suffix stripping ("Apple"!="Apple Inc" — left to exact-dedup+resolver cache, suffix stripping widens the false-merge surface). ACCEPTED MISS: concatenation variants ("JPMorgan"!="JP Morgan", different token count) stay distinct — a miss costs one resolver slot, a false merge loses a real entity. Always-on (provably safe: identical content key = same entity; no shadow mode) / GUARD EntityResolutionPrepass.CollapseNearDuplicates @ src/Services/EntityResolutionPrepass.cs:407 / TEST EntityResolutionPrepassNearDupTests.near_dup_variants_collapse_but_distinct_token_sharing_entities_survive
  D-8 exact-candidate-recovery: INTENT recover REAL instruments the surface-form-sensitive fuzzy confirm misses on colloquial/bare names ("Nike"/"Tesla" fall below the ~0.85 fuzzy threshold and are filtered, discarding the already-correct EXACT-catalog ticker) — the ~27% of NoResolution that are genuine misses / PRECOND an id-less candidate's Symbol is an EXACT primary-symbol catalog hit (GetInstrumentBySymbolAsync, verbatim symbol equality — NEVER fuzzy, no similarity score) AND the OBSERVATION SUBJECT (input.SubjectEntity) shares >=1 content token with the catalog hit's Name (SubjectNameNormalizer.SharedTokenCount(SubjectEntity,hit.Name)>=1). Subject-overlap, NOT candidate-name overlap: the candidate is catalog-sourced so c.Name==hit.Name, and a Math.Max(candidate,subject) term is TAUTOLOGICAL (always >=1) and BYPASSES the subject check — a merely co-mentioned company then resolves to the WRONG instrument -> matrix corruption (the CRITICAL this refines). NOT per-observation LLM selection: V2 passes ARTICLE-WIDE Candidates with a null SelectedCandidateIndex (GpuJsonExtractionService -> DslToMergedExtractionAdapter builds ONE candidate list from ALL doc ENTs; index stays null unless a per-NUM source_entity exact-matches an ENT verbatim_name), so an LLM pick is unavailable — subject-overlap is the enforceable guard. Empty/whitespace subject -> overlap 0 -> refused (a subjectless observation must not resolve via this leg). Does NOT contradict SecMaster D-2 (fuzzy-proposes-authoritative-confirms): exact symbol existence + subject-name overlap IS authoritative confirmation, not a fuzzy proposal, so the wrong-neighbour failure mode is not reintroduced — new D-entry, NOT a D-2 supersession / GUARD DeterministicResolver.TryExactCandidateMatchAsync @ src/Services/DeterministicResolver.cs:286 / TEST DeterministicResolverTests.exact_match_leg_refuses_comention_when_subject_mismatches_catalog_name
  D-9 method-aware-review-drain: INTENT drain the Pending review queue DOWNSTREAM of the V2 write (the V2 path keeps writing everything Pending — DD-4=b PRESERVES the 2026-05 cutover "downstream owns approval": this scheduled drainer IS that downstream approver, so NO cutover reversal / NOT a supersession). AutoApprovePolicy.Evaluate runs on a schedule, NON-dry-run, over review_status=Pending rows only, batched + interval-bounded, the SAME single-source gate the backfill endpoint + live + re-extract paths use / gate = Resolved AND instrument non-null AND (resolution METHOD in AutoApproveMethods {ticker_in_quote (D-10), llm_candidate_exact (D-8)} OR extracted_at < now - ReviewGraceDays). METHOD, not confidence, is the quality signal: confidence is useless for sizing the queue (94% of Resolved rows are ticker_in_quote at a FLAT 0.900 — theater); the errors cluster by method (gold->BEA index, "$30T debt"->30Y yield, Bitcoin->ETH were ALL cove_VectorSearch). Exact subject-verified legs auto-approve; fuzzy/semantic methods (cove_*, hybrid_subject, gemini_fallback, llm_candidate_pick, null/unknown) are HELD for human review (~2-5/day, owner reviews <=10/day) then DEFAULT-ACCEPT after ReviewGraceDays (default 3) so the queue can NEVER wedge. Review-hold applies to FRESH fuzzy (recent extracted_at); the re-extract path reprocesses the ~80K aged backlog whose extracted_at is OLD, so agedPastGrace already fires and its fuzzy re-resolutions DEFAULT-ACCEPT with no review window — DELIBERATE (Evaluate keys agedPastGrace off original ExtractedAt): those rows are NOT in the current recent-news digest (low review value), and holding ~80K would FLOOD the bounded <=10/day queue. Low-stakes either way since extracted_observations feed DigestQueryService !=Rejected, NOT the WS3 matrix. auto-reject still fires at extraction < 0.5 (keeps garbage extractions out of the digest); NoResolution / null-instrument rows stay Pending (Stage-1 sweep owns them). SUPERSEDES the #856 "trust all Resolved" refinement (it auto-approved fuzzy rows too, surfacing wrong-instrument garbage) / GUARD AutoApproveDrainWorker.RunOneBatchAsync @ src/Workers/AutoApproveDrainWorker.cs:265 / TEST AutoApproveDrainWorkerTests.Drain_approves_exact_method_holds_fresh_fuzzy_and_keeps_noresolution_pending AND Drain_default_accepts_fuzzy_row_aged_past_review_grace AND AutoApprovePolicyTests.guard2_fuzzy_method_resolved_instrument_recent_stays_pending
  D-10 ticker-in-quote-subject-guard: INTENT mirrors D-8's wrong-ticker principle at BOTH ticker_in_quote resolution sites — the LIVE ExtractionProcessor.ApplyLocalResolutionAsync leg (PRIMARY co-mention source) and the ReExtractResolutionAdapter re-extract leg (secondary) attach a ticker found in the quote/filing to the observation, but a quote frequently CO-MENTIONS a ticker unrelated to the SUBJECT (verified live: Gilbert Cisneros [congressman's trade disclosure]->LLY, Banxico [Bank of Mexico]->EWW, Jewett Cameron Trading->EWTX); the ticker exact-matches a catalog symbol (IsExactSymbolMatch passes) so it attached as a WRONG-ticker -> polluted the digest; the review-UI filter (#856) only HID this garbage, it did not stop it being generated / PRECOND before attach require TickerInQuoteGuard.SubjectSharesInstrumentToken(subject, <attached instrument's catalog Name>) i.e. SubjectNameNormalizer.SharedTokenCount >= 1 (subject-overlap, same normalizer + principle as D-8) — ONE shared predicate wired at both sites so they cannot drift. ResolveAsync returns no Name -> fetch it via GetInstrumentBySymbolAsync/InstrumentLookupDto.Name (same client the D-8 leg uses; no new dependency). Overlap 0 — OR unavailable Name OR subjectless observation — REFUSES: set NoResolution + return (NOT fall-through: the description+CoVe path would re-ground the very same in-quote symbol, whose Symbol is literally in the quote, and re-attach the co-mention). Subject spans v1 (SourceEntity) and v2 (SubjectEntity) — each caller passes whichever is present: live + full re-extract read the FRESH extraction (`result.SubjectEntity ?? result.SourceEntity`; the hot-path observation never populates SubjectEntity so `result` is authoritative), resolve-only reads the existing row. New D-entry NOT a D-8 supersession (distinct sites/guard/tests) / GUARD TickerInQuoteGuard.SubjectSharesInstrumentToken (shared predicate) @ src/Extraction/TickerInQuoteGuard.cs:74 — called @ ExtractionProcessor.ApplyLocalResolutionAsync src/Workers/ExtractionProcessor.cs:1139 AND ReExtractResolutionAdapter.ResolveInternalAsync src/Workers/ReExtractResolutionAdapter.cs:174 / TEST ExtractionProcessorResolutionTests.live_ticker_in_quote_refuses_comention_when_subject_mismatches_catalog_name AND ReExtractBackgroundServiceTests.resolve_only_ticker_in_quote_refuses_comention_when_subject_mismatches_catalog_name
  D-11 ingress-media-outlet-class: INTENT companion to D-5/D-6 (supersedes none) — extend the SAME shared ICandidateSurfaceFilter over one more curated non-entity class that floods the paid Gemini resolver: non-tradeable news/media outlets (Rolling Stone/Eater/Indiewire/TechCrunch/Politico/Axios/The Verge/Ars Technica + the BuzzFeed Celeb section label). ONE edit to Classify covers BOTH the D-5 NER-candidate prepass AND the D-6 V2-direct path (both call Classify) — clean at the SOURCE, not per-destination / PRECOND reject ONLY unambiguous NON-TRADEABLES via EXACT case-insensitive curated match; a false-positive SILENTLY DROPS A REAL RESOLUTION (worse than junk the daily cap refuses for $0), so real media ISSUERS stay ABSENT and Keep (BuzzFeed=BZFD, News Corp=NWSA, NYT, Gannett=GCI, Fox=FOXA, Warner Bros Discovery=WBD, Paramount, Netflix, Disney, Comcast). Match uses NO "the " stripping ("The Verge" verbatim so bare "Verge"=crypto XVG never trips). A market-INDEX reject class was considered and REMOVED (2026-07-07): US indices resolve to REAL tracked instruments (S&P 500->SP500 [SentinelCollector-sourced]+SPX, DJIA, Nasdaq Composite->IXIC, Russell 2000->RUT) and even bare "Nifty" fuzzy-matches live tickers (NIFTY Lifestyle 4262.T + Nifty ETFs) — a name-based index reject VIOLATES the D-5 PRECOND (silent false-drop of a legitimate resolution); index junk ("Nifty Smallcap") is left to the Gemini daily cap + D-1 server gate. PERSON names stay OUT of scope (D-5 PRECOND — no structural discriminator, a blocklist would nuke person-named issuers) / GUARD CandidateSurfaceFilter.Classify @ src/Services/CandidateSurfaceFilter.cs:273 / TEST CandidateSurfaceFilterTests.should_reject_media_outlets_as_MediaOutlet AND EntityResolutionPrepassFilterTests.ingress_rejects_media_outlet_but_keeps_real_issuer
  D-12 gemini-selfseed-authoritative-confirm: INTENT companion to D-8/D-10 (supersedes none) — "fuzzy proposes, AUTHORITATIVE confirms" for the Gemini auto-register legs (BOTH the V2 DeterministicResolver leg AND the V1 ExtractionProcessor leg), the ONE resolution path that self-seeded a BRAND-NEW SecMaster instrument on a bare Gemini answer (conf>=0.7, NO authoritative id, NO subject corroboration), so a person/state/country subject grounds to a real-but-irrelevant EQUITY ticker and pollutes the catalog + matrix (#872 cleaned; recurred same-day). D-8 (exact-candidate) + D-10 (ticker-in-quote) gave the OTHER legs subject-grounding; this Gemini self-seed leg never had ANY confirmation. A NEW instrument is self-seeded ONLY after an authoritative identifier source confirms the Gemini ticker; attach-to-existing (secmaster_match) is unaffected — ONLY new-instrument CREATION is gated / PRECOND before RegisterInstrumentAsync require BOTH gates (SelfSeedRefusalReason returns null): (1) AUTHORITATIVE — ConfirmTickerAsync -> SecMaster GET /api/identifiers/resolve?ticker={sym} (local catalog + ticker-alias, then OpenFIGI /v3/mapping; authoritative-ONLY, NO fuzzy/vector tier) yields a TRI-STATE TickerConfirmationOutcome (EXTENDED, not superseded, by the confirm-seam PR): Confirmed (200) | Unconfirmed (404 = authoritative MISS, both sources ANSWERED and declined) | Unavailable (503/transport = NO verdict; SecMaster's resolve endpoint 503s — never 404s — on an OpenFIGI outage, so a 404 is never an outage in disguise). EVERY non-confirmed state -> NoResolution, fail-closed (an outage suppresses self-seeding, never opens it), but with DISTINCT bounded refusal reasons {unconfirmed|subject_mismatch|confirm_unavailable} tagged reason= on the selfseed_refused increments of BOTH legs' counters (sentinel_gemini_resolver_calls_total / sentinel_gemini_fallback_calls_total) + gemini.selfseed_refused_reason activity tag on BOTH legs — pre-extension the outage was laundered into "miss", silently disabling self-seeding during outages while the metric read as the-gate-working (Information refusal logs are filtered in prod; the tag is the prod-visible signal); AND (2) SUBJECT-CORROBORATION — the confirmed instrument's canonical Name shares >=1 content token with the observation subject (TickerInQuoteGuard.SubjectSharesInstrumentToken, the SAME D-8/D-10 predicate): OpenFIGI proves the ticker is REAL, not that it belongs to THIS subject, so a person/country subject Gemini maps to a real-but-irrelevant ticker (#872) is a wrong-ticker even when equity-shaped -> refused. A confirmation carrying an InstrumentId is a ticker-alias hit the primary by-symbol lookup missed -> ATTACH (no duplicate), still subject-corroborated. REUSE SecMaster's existing endpoint — do NOT reinvent OpenFIGI in SentinelCollector. NOTE the SecMaster register guard (RegistrationService.EvaluateGuard) is asset-class-keyed (rejects Economic-from-untrusted + sub-2-char equity) and does NOT stop an equity-shaped junk row — so the old "GeminiFallback collector tag makes the guard reject junk" comments were FALSE COMFORT (corrected in this PR); THIS two-gate check, not the tag, is the protection. SENDER CLAMP (same confirm-seam PR, UPHOLDS SecMaster D-4 — the allowlist is NOT widened): both register sites send SelfSeedAssetClass.Clamp(assetClass) — case-insensitive member of the D-4 allowlist {Equity,ETF,Index,Currency,Crypto,Commodity} -> canonical spelling, anything else incl null ("Stock"/"ADR"/"REIT"/"Fund") -> "Equity" — because a confirmed, subject-corroborated instrument carrying Gemini's free-form label was silently rejected at register (macro_class_from_untrusted); GIGO fixes the sender, never the allowlist. ONE shared PURE gate wired at both legs so they cannot drift (D-8/D-10 pattern) / GUARD GeminiSelfSeedGate.SelfSeedRefusalReason (shared gate) @ src/Services/GeminiSelfSeedGate.cs:60 + SelfSeedAssetClass.Clamp (shared clamp) @ src/Services/SelfSeedAssetClass.cs:36 — called before self-seed @ DeterministicResolver.TryGeminiResolveAsync src/Services/DeterministicResolver.cs:575 AND ExtractionProcessor.TryGeminiFallbackAsync src/Workers/ExtractionProcessor.cs:1451 / TEST DeterministicResolverTests.gemini_selfseed_refuses_unconfirmed_symbol AND DeterministicResolverTests.gemini_selfseed_refuses_confirmed_ticker_when_subject_mismatches AND DeterministicResolverTests.gemini_selfseed_persists_openfigi_confirmed_symbol AND DeterministicResolverTests.gemini_person_subject_unconfirmed_yields_noresolution AND DeterministicResolverTests.gemini_selfseed_attaches_to_existing_when_confirm_returns_instrument_id AND DeterministicResolverTests.gemini_selfseed_refusal_reason_visible_as_bounded_metric_tag (unavailable row REDs if the outage is laundered into "unconfirmed") AND DeterministicResolverTests.gemini_selfseed_sends_only_d4_allowlist_asset_class_to_register AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_refuses_unconfirmed_symbol AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_persists_confirmed_symbol AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_refuses_unavailable_confirm_with_its_own_reason AND ExtractionProcessorGeminiFallbackTests.v1_gemini_selfseed_clamps_sent_asset_class_to_d4_allowlist AND SecMasterClientTests.confirm_ticker_returns_unavailable_not_unconfirmed_on_503 AND (SecMaster side) IdentifierResolutionServiceTests.should_signal_unavailable_when_openfigi_fails_never_a_null_miss + IdentifiersEndpointsTests.should_return_503_not_404_when_resolution_unavailable
  D-13 runtime-log-level: INTENT a WARN-quiet-by-design collector must be raisable to Information/Debug AT RUNTIME (on-demand deep debugging) with NO restart or redeploy, while steady state stays Warning (prod-quiet). A LoggingLevelSwitch is the SOLE level authority; the operator edits Serilog:MinimumLevel:Default in the RUNNING container and reloadOnChange picks it up live. Serilog.Settings.Configuration 9.0.0 does NOT re-apply MinimumLevel on IConfiguration reload, so an explicit ChangeToken re-parse is what makes the edit take effect; the MEL floor sits at Debug so the bridge does not pre-filter below the switch. The per-source Overrides (DigestService etc. at Information) are independent of the switch and survive — raising Default to Debug lifts every un-overridden source; the raised level lives ONLY in the container's writable layer — revert by editing the key back to Warning (live) OR RECREATE (not restart) the container to reinstate the image's baked-in default; a restart PRESERVES the writable layer and does NOT revert. No compose/mount change (baked-in appsettings is the assumed, acceptable case). / PRECOND operator runs `nerdctl exec sentinel-collector` to edit /app/appsettings.json Serilog:MinimumLevel:Default — no env override for that key exists, so the JSON is authoritative and reloadable. / GUARD RuntimeLogLevel.BuildLogger (ControlledBy = sole authority) + RuntimeLogLevel.CreateSwitch (ChangeToken re-parse on reload) + RuntimeLogLevel.AddRuntimeControlledSerilog (MEL Debug floor) @ src/Telemetry/RuntimeLogLevel.cs / TEST RuntimeLogLevelTests.Runtime_appsettings_edit_raises_then_lowers_serilog_level

SEE: README.md §Reference · Events/src/Events/Protos/observation_events.proto (ObservationEventStream — EXPOSES to ThresholdEngine + CONSUMES from ThresholdEngine for validation) · Events/src/Events/Protos/secmaster.proto (SecMasterRegistry — registration) · src/Semantic/NewsSignalClassifier · src/Services/MacroObservationRouter+EntityResolutionPrepass · src/Workers/ExtractionProcessor · ThresholdEngine src/Workers/ObservationCellProjector · MacroSubstrate MacroObservationRepository · NewsMomentumQueryService (digest)
