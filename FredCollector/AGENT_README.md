# FredCollector — architecture [agent read-first]

PURPOSE: owns FRED series catalog(series_configs)+raw obs(fred_observations point-in-time). Not instrument-master, not sector-owner, not macro-substrate; dual-writes INTO macro_observations(MacroSubstrate).

DATA MODEL + INVARIANTS:
  INV catalog⊥instrument: SeriesId=FRED mnemonic(ToUpperInvariant,<=50 chars), not an instr-id. SignalIdentityId(kebab,<=64)=only SecMaster xref; may be null.
  INV sector-xor-signal: ck_series_config_sector_xor_signal -> AtlasSectorCode IS NULL OR SignalIdentityId IS NULL. both-NULL=legal(newborn pre-tagger); both-set=rejected. Never read as XOR.
  INV AsOf: live+ordinary-backfill->AsOf=date(CollectedAt); ALFRED-vintage->AsOf=realtime_start. Never conflate.
  INV LastCollectedAt: BackfillService advances it; AlfredBackfillService deliberately does NOT touch it (scheduler keys start-window off it; vintage backfill must not move it; also avoids ThresholdEngine re-processing decades of historical revisions).
  INV dual-write-gate: computed each cycle from SignalIdentityId vs SecMaster per-signal `matrix_benchmark` flag; not a per-series is_macro col. Flag is per-signal, orthogonal to Category (cuts across macro/rate/commodity/fx/credit).
  INV ObservationChannel: unbounded Channel.CreateUnbounded; no in-service reader; not live fanout -> memory-growth hazard on long-running deploy.
  INV dot-sentinel: "." value->Value=null row INSERTED fred_observations; macro dual-write skips null; insert itself NOT skipped.

PATHS (distinct code — do not conflate):
  cron [Quartz/DisallowConcurrentExecution per-series]:
    does: incremental fetch -> INSERT DO NOTHING fred_observations -> conditional macro dual-write. The INSERT is the publish (D-3); nothing is pushed.
    does NOT: SecMaster-register; re-tag.
    on-miss: null/empty obs from FRED -> still inserts null row if "." value; macro dual-write skips null value.
  AddSeries [POST /api/admin/series]:
    does: fetch FRED metadata -> derive Frequency->cron -> persist -> schedule -> fire-and-forget BackfillService(24mo,advances LastCollectedAt) -> fire-and-forget RegisterWithSecMasterAsync(gRPC; only if _secMasterClient!=null).
    does NOT: set sector/signal tags.
  gRPC-EventStream [:5001 · ThresholdEngine]: serves fred_observations directly (D-3) — one synthesised SeriesCollected Event per row, EventId="obs-{Id}", one DataPoint each.
    does NOT: read the events table; emit CollectionFailed (unreachable — a failure leaves no fred_observations row); honour eventTypes (accepted and ignored); filter null-value rows out of GetHealth's TotalEvents, which counts every row incl "." sentinels while the stream serves only Value-bearing ones; recover an INVERSION — a row committing AFTER one with a LATER CollectedAt is behind the cursor for good, whatever the tiebreak (D-4).
    on-miss: cursor is composite (CollectedAt, Id) (D-4); a client timestamp re-serves its whole instant, so delivery is at-least-once and TE dedupes on processed_events.event_id.
  taggers [periodic background]: resolve NAICS->AtlasSectorCode + mnemonic->SignalIdentityId -> write-back series_configs.
    on-miss: null/NotFound/transport-fail=tag left unchanged.

PROCESSING macro_observations dual-write gate ("SecMaster owns the matrix-benchmark id-set; classify signal-identity not series; fail closed, serve last-known-good"):
  1. isMatrixBenchmark=SecMasterMatrixBenchmarkClassifier.IsMatrixBenchmarkAsync(config.SignalIdentityId) once per series. null/unresolved SignalIdentityId->not a matrix benchmark.
  2. classifier: 15-min-TTL cache of /api/signal-identities/matrix-benchmarks (per-signal `matrix_benchmark` flag; cuts across categories). transport-fail+populated-cache->last-known-good(no flap); cold-start-fail OR fetch-returns-zero->empty-set fail-closed(no dual-write).
  3. per obs: always INSERT fred_observations(incl null). isMatrixBenchmark AND value!=null->MacroObservationWriter.WriteAsync(upsert last-write-wins; idempotent on triple provenance=source_collector+source_id+obs_time). null->skip substrate only.

DISTINCTIONS:
  fred_observations(verbatim level ALL series, incl null rows) ≠ macro_observations(transformed, matrix-benchmark-flagged only; additive not replacing).
  BackfillService(AddSeries+InitialWorker+manual-admin)=publishes channel+advances LastCollectedAt ≠ AlfredBackfillService=repo-write only, does not publish, does not touch LastCollectedAt. Neither emits a gRPC event: both reach TE by the rows they insert (D-3).
  public.events(15,919 rows, no writer since D-3, no reader ever) ≠ fred_observations(what the stream serves) ≠ ObservationChannel(in-mem, no reader).
  FredCollector does NOT set is_primary: only calls RegisterSeries; SecMaster decides primacy.
  SECMASTER_GRPC_ENDPOINT(registration) ≠ SECMASTER_REST_ENDPOINT(taggers+dual-write-gate): each independently optional, different silent semantics.
  GRPC unset->_secMasterClient=null->register silently skipped, no WARN. REST unset->NullMatrixBenchmarkClassifier wired->no dual-write.
  matrix-benchmark-flagged(SignalIdentityId has matrix_benchmark=true) ≠ non-benchmark(SignalIdentityId flag false/absent) ≠ sector-rolled(AtlasSectorCode set,SignalIdentityId=null) ≠ untagged(both null). matrix_benchmark is per-signal, indep of Category — a benchmark signal may be any category.

CROSS-SERVICE:
  OUT gRPC RegisterSeriesAsync->SecMaster on AddSeries(f-a-f; GRPC unset->silently skipped, no WARN; RPC throws->WARN, does not block).
  OUT sync(cached): REST sector/signal resolve+matrix-benchmark classify(per-signal `matrix_benchmark` flag)->SecMaster.
  OUT dual-write->macro_observations(ConnectionStrings:AtlasData->fallback AtlasDb).
  IN: upstream FRED HTTP (Polly: WaitAndRetry+CircuitBreaker on transient/429).
  FEEDS: ThresholdEngine<-gRPC ObservationEventStream :5001. MCP sidecar: fredcollector-mcp=separate container; ONLY host-accessible surface.

GOTCHAS:
  ✗ per-series is_macro col ✗ read constraint as XOR ✗ SeriesId=instrument-id
  ✗ SecMaster-register=synchronous/authoritative ✗ expect WARN when GRPC unset
  ✗ events table feeds the stream ✗ ObservationChannel=live fanout ✗ CollectedAt is unique ✗ tied groups are small(11 in 7d, 730 per 24-month daily backfill, 100,000 once — size paging for the backfill, not the cron)
  ✗ ALFRED backfill=advances LastCollectedAt ✗ ordinary backfill=true point-in-time AsOf
  ✗ env-vars from appsettings.json(runtime=env only)

DECISIONS:
  D-1 fred-frequency-normalize-at-source: INTENT FRED metadata returns *qualified* long-form frequency strings ("Weekly, Ending Saturday", "Daily, 7-Day", "Daily, Close") — normalise them to the right CollectionFrequency at the single derivation source, else the wrong value propagates to the revision-buffer window (DataCollectionService), the SecMaster registration frequency, and any future frequency-derived cron (GIGO clean-at-source; an exact-match switch silently defaulted every qualified string to Monthly, mislabelling all daily/weekly series). Extension (same defect class, F5): vocabulary with no exact enum member maps least-wrong (Semiannual/SA->Quarterly over-poll, 5-Year->Annual, Not Applicable/NA->Monthly, BW->Weekly); a genuinely UNKNOWN string still defaults Monthly but LOUDLY — Warning with raw string + series id, never silent. / PRECOND AddSeries or MCP add_series onboarding a series from FRED / GUARD SeriesManagementService.ParseFrequency @ src/Services/SeriesManagementService.cs:211 / TEST SeriesFrequencyDerivationTests.AddSeries_derives_correct_frequency_from_fred_metadata + SeriesFrequencyDerivationTests.AddSeries_unknown_frequency_defaults_monthly_and_warns
  note: Frequency does NOT drive the steady-state Quartz cron — DataCollectionScheduler schedules off the stored CronExpression column (a flat `0 0 18 * * ?` for ~76/80 rows), so Frequency's live effect is the revision/fallback window + the SecMaster/MCP frequency label, not cadence.
  D-2 runtime-log-level: INTENT a WARN-quiet-by-design collector must be raisable to Information/Debug AT RUNTIME (on-demand deep debugging) with NO restart or redeploy, while steady state stays Warning (prod-quiet). A LoggingLevelSwitch is the SOLE level authority; the operator edits Serilog:MinimumLevel:Default in the RUNNING container and reloadOnChange picks it up live. Serilog.Settings.Configuration 8.0.4 does NOT re-apply MinimumLevel on IConfiguration reload, so an explicit ChangeToken re-parse is what makes the edit take effect; the MEL floor sits at Debug so the bridge does not pre-filter below the switch. The raised level lives ONLY in the container's writable layer — revert by editing the key back to Warning (live) OR RECREATE (not restart) the container to reinstate the image's baked-in default; a restart PRESERVES the writable layer and does NOT revert. No compose/mount change (baked-in appsettings is the assumed, acceptable case). / PRECOND operator runs `nerdctl exec fred-collector` to edit /app/appsettings.json Serilog:MinimumLevel:Default — no env override for that key exists, so the JSON is authoritative and reloadable. / GUARD RuntimeLogLevel.BuildLogger (ControlledBy = sole authority) + RuntimeLogLevel.CreateSwitch (ChangeToken re-parse on reload) + RuntimeLogLevel.AddRuntimeControlledSerilog (MEL Debug floor) @ src/Telemetry/RuntimeLogLevel.cs:34 / TEST RuntimeLogLevelTests.Runtime_appsettings_edit_raises_then_lowers_serilog_level

  D-3 serve-from-storage-not-push: INTENT a collection reaches ThresholdEngine by being STORED, not by being pushed — the INSERT into fred_observations is the publish, and EventRepository synthesises one Event per row on demand. FinnhubCollector D-2 (quote-path-no-in-process-queue) is the precedent and the scar: a push-shaped buffer with no reader halted that collector's loop for 17 days. All five streaming collectors serve from an observations table; the write to `public.events` served nobody (15,919 rows / 23 MB / 78 a day since 2025-11-21, zero readers in code, views, FKs, scripts, dashboards or ansible — measured 2026-09-20) and both call sites already swallowed their own failures, so a swallowed publish could only ever lose data silently. Retiring the write costs one SaveChanges per series-cycle and loses only the CollectionFailed error code, which D-5 moves to a counter. The table is NOT dropped here (docs/BACKLOG.md). / PRECOND the stream's source is a table the collection path has already committed to; a datum not in fred_observations does not reach TE by any other route / GUARD EventRepository.StreamEventsSinceAsync reads _dbContext.FredObservations @ src/Grpc/Repositories/EventRepository.cs:45 / TEST EventStreamIntegrationTests.GetEventsSince_ReturnsEventsInChronologicalOrder + EventStreamIntegrationTests.GetEventsSince_SynthesisedDataPointCarriesAsOf
  D-4 composite-observation-cursor: INTENT fred_observations."CollectedAt" is NOT unique — one cycle stamps every row it writes with one instant (11 in a 7-day window, 730 for a 24-month daily AddSeries backfill, 100,000 once on 2026-06-02; 76 groups of 100+ all time) — so a scalar `CollectedAt > since` cursor cannot name a position INSIDE a tied group and drops every sibling that commits after the cursor passed it. Measured 2026-09-20 by anti-join against processed_events: 216 of 2,262 streamable rows in 7 days (9.5%) never reached TE. The cursor is the pair (CollectedAt, Id); ObservationCursor.Before(instant) opens BEFORE a whole instant, so a client that checkpoints on a bare timestamp gets the tied group re-served rather than skipped. THE TRADE IS DELIBERATE: delivery is at-least-once, and it is free only because EventId "obs-{Id}" is stable per row and ThresholdEngine's processed_events keys on it — a random EventId (what the other four collectors mint) would turn each repeat into a duplicate insert, so this design does NOT port to them unchanged. WHAT IT DOES NOT FIX, and no cursor over this table can: an INVERSION, where a row commits after one carrying a STRICTLY LATER CollectedAt. 62 such rows in 7 days, median gap 8.0 ms, max 68.9 ms; 12 of the 216 are of this class, so the fix recovers 204 and the 7-day floor is ~12, never 0. The cause is that `CollectedAt` is stamped in app code at cycle start, ahead of both the INSERT and the COMMIT, so the sort key is not monotone in VISIBILITY order — and neither is `Id`, allocated at INSERT and equally free to commit out of order. The table records no commit order at all, so NO choice of sort column closes this; ✗ never "fix" it by sorting on Id, which changes the stream's meaning from collection time to arrival and moves the race rather than removing it. The remedies are a trailing lag re-read (priced against those 730- and 100,000-row groups) or a commit-ordered column — docs/BACKLOG.md. ALSO NOT PINNED, measured by mutation 2026-09-20: 5 of this cursor's 12 sites survive the full 321/128 suite, including BOTH halves of the Between path (StreamEventsBetweenAsync's predicate and GetEventsBetween's open), which still carry the scalar defect latent behind a proto surface with zero callers, and the SubscribeToEvents POLL-LOOP advance, whose loss costs re-delivery rather than rows — the whole set is in docs/BACKLOG.md. / PRECOND ThresholdEngine dedupes on processed_events.event_id; any future client of this stream must too / GUARD EventRepository.StreamEventsSinceAsync composite predicate @ src/Grpc/Repositories/EventRepository.cs:47 + EventStreamService.SubscribeToEvents opening on ObservationCursor.Before(startFrom) @ src/Grpc/Services/EventStreamService.cs:59, then advanced from item.Cursor @ src/Grpc/Services/EventStreamService.cs:79 / TEST EventStreamCursorTests.ResumingMidTie_SkipsNothingAndRepeatsNothing + EventStreamCursorTests.SubscribeToEvents_DeliversSiblingsCommittedAfterTheirInstantWasServed + EventStreamCursorTests.SubscribeToEvents_OpenedAtATiedInstant_DeliversTheWholeGroup + EventStreamCursorTests.GetEventsSince_AtACheckpointInstant_ServesTheWholeTiedGroup
  D-5 collection-failure-counter-replaces-the-row: INTENT retiring the `events` write (D-3) would have deleted the only record of a failed collection's error-code classification — 204 CollectionFailed rows a year — with nobody deciding to. Enumerating what observed it first: no alert, no dashboard, no query, and ThresholdEngine filters to SeriesCollected so it was never delivered either; the counter nearest the site, `fredcollector.series.collected.total{success}`, lives on meter "FredCollector.DataCollection", which Program.cs registers with AddSource and NOT with AddMeter, so it has never reached Prometheus (absent from the live __name__ enumeration, 2026-09-20) and could not stand in. CollectionFailures is a NEW counter on the exported "FredCollector" meter, CollectionPhase.All x CollectionErrorCode.All = 8 series; series_id stays out because 80 series would put the product past the bounded-cardinality bar, and it is already on the Error log and the Tempo span. THE COUNTER ALONE IS NOT THE SIGNAL: an OTeL counter is ABSENT until its first Add and PromQL cannot tell absent from 0, so "no failures" would read identically to "never wired" — MetricWarmupHostedService zero-initialises the whole product at startup, which is what makes A=0 on the dashboard mean something. / PRECOND a mechanism is being removed -> what observed it is enumerated and each survivor pinned before the removal lands; and a counter that replaces a durable record is seeded at startup, never left to appear on first use / GUARD FredCollectorMeter.CollectionFailures @ src/Telemetry/FredCollectorMeter.cs:45 + MetricWarmupHostedService.StartAsync @ src/Telemetry/MetricWarmupHostedService.cs:23, WIRED by DependencyInjection.AddWorkers' AddHostedService<MetricWarmupHostedService> @ src/DependencyInjection.cs:348 (the seeding runs only if that line does), recorded at src/Services/DataCollectionService.cs:252 and src/Services/BackfillService.cs:166 / TEST MetricWarmupHostedServiceTests.Startup_should_register_every_declared_phase_and_error_code_at_zero + MetricWarmupHostedServiceTests.The_composition_root_registers_the_warmup_and_starting_it_seeds_every_series + DataCollectionServiceTests.CollectSeriesDataAsync_should_count_collection_failure_by_error_code

SEE: README.md §Reference (exhaustive series list, endpoint catalog, config/env-var enumeration, Polly backoff numbers, Quartz job-store detail, admin-GET Swagger summary vs impl mismatch, MCP port mapping) · Events/src/Events/Protos/observation_events.proto (ObservationEventStream — EXPOSES gRPC :5001) · Events/src/Events/Protos/secmaster.proto (SecMasterRegistry.RegisterSeries — consumed f-a-f on AddSeries)
