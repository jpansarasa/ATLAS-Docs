# ATLAS Supervisor STATE

Supervisor memory. Read first, write last. **Current truth + open items + pointers ONLY.** History → git log + PRs + tags + memories.

## EACH TURN START HERE
0. **FIRST TURN** — git audit (`worktree list / branch / status / fetch + log HEAD..origin/main`); on `main` & current. [[feedback_git_session_start_discipline]] [[feedback_main_always_current]]
1. Poll NTFY `atlas-claude-reply` (user input first).
2. Re-read this file + the active spec (if any) THIS turn; walk the pipeline backward before dispatching; plan-vs-prod mismatch → STOP + NTFY.

## CURRENT WORK
**Trend-visualization epic — Stages A+B MERGED 2026-07-04; DEPLOY+smoke next, then Stage C.** Spec `docs/superpowers/specs/2026-07-04-sentinel-trend-visualization-design.md` (`1c2ef5e5`) + staged plan `docs/superpowers/plans/2026-07-04-sentinel-trend-visualization.md` (untracked on disk — commit at epic end per PHASE_TAGS). Brainstormed w/ real-data SVG mockups. Redesign Sentinel digest → market-first (KPI tiles → sector×day heatmap index → per-sector `<details>` drill-down: net-lean sparkline + regime strip + top-5 per-signal dual-trend [matrix cell vs news tilt]) + live `GET /sentinel/matrix?range={7d|30d|90d|max}` explorer reusing same blocks. Server-rendered inline SVG, ZERO client JS. hot=red `#e34948`/cold=blue `#2a78d6` CVD-safe. SentinelCollector ONLY, read-only over matrix_cells/sector_regimes/macro_observations.
- **Stage A ✓ MERGED** PR #841 (`65352b9f`): TrendQueryService + TrendSvgRenderer + tests, wired to nothing live. Review-fixed: Escape `"`/`'`, dark-mode CSS vars, provenance test + TWO integration-fixture races Stage A introduced (DDL concurrent-CREATE → `SharedSchemaLock` pg_advisory_lock; DML cross-suite TRUNCATE → merged Trend+MatrixSector into `SharedMatrix_Db` collection, superseded plan's separate-collection detail for those 2 suites).
- **Stage B ✓ MERGED** PR #842 (`4999d1e5`): market-first DigestRenderer (Render()+`trends` param; embed KPI/heatmap/`<details>` drill-downs; empty-matrix notice names `Matrix:ObservationProjectorMode`, HTTP200 never blank-500) + DigestService.BuildTrendsAsync (degrade-to-empty logs Warning+span) + `/sentinel/matrix` endpoint (zero-JS `<a>` range links, max→2025-12-31 floor, OCE-filtered traced-500). Review-fixed: degrade RED-on-unfixed test, OCE filter, logger category, dup-tolerant render. ADDITIVE — old Sector-Heat/Detail sections STILL RENDER (Stage C removes).
- ✓ **DEPLOYED + LIVE 2026-07-04 ~16:49Z** (image `d1854af7`, scoped no-cascade, ZFS snap, clean boot, Loki 0 Error/Fatal /prompts mounted vLLM ok). BOTH surfaces PASS: digest report 182 HTTP200 renders `trend-kpis`+`trend-heatmap`+per-sector `<details>` drill-downs w/ `<svg class=trend-heat-cells>` (real data, no notice); `/sentinel/matrix` 30d/7d/max all 200 (KPI+heatmap, sizes scale). First live trend-chart render CONFIRMED.
- **USER LIVE REVIEW (gate working — caught 2 issues the mockups/tests/smoke missed):**
  1. 🔧 **Drill-down opens EMPTY** — `RenderHeatmapStrip` emits `<details class="trend-heat-row"><summary>…</summary></details>` (EMPTY body); the drill content (spark/regime/dual) is a DISCONNECTED sibling `<details class="trend-drill">`. Clicking a row shows nothing. Tests/smoke checked class PRESENCE not NESTING. ✓ **MERGED PR #843 (`20f40efd`)** — `RenderHeatmapStrip` takes `drillBySector` map, nests drill after `</summary>`; separate `trend-drill` dropped; both digest+/sentinel/matrix via shared RenderTrendBody. 2 nesting guard tests VERIFIED non-tautological (RED on exact sibling regression). review-pr 0-crit/0-imp. **DEPLOY+verify IN FLIGHT `aa4f93b6`** (build → scoped ansible → confirm drill NESTS inside row `<details>` in LIVE html + no `trend-drill` + fresh digest id for user to click). NEXT: give user fresh digest URL → THEN dispatch momentum redesign (off updated main).
  2. 🔧 **"News Signal Momentum" confusing** (PRE-EXISTING section, NewsMomentumDisplay) — bar LENGTH=tilt (all negative now) but COLOR=momentum-direction (green=rising/red=falling/gray=flat) → green bar pointing negative reads contradictory; ALSO Chart.js from CDN (cdn.jsdelivr.net — violates zero-JS/no-CDN, breaks if blocked) + green/red clashes w/ new red/blue. **USER CHOSE: redesign as server-SVG, one language — color=tilt SIGN (red/blue), momentum as separate arrow/delta, drop CDN.** QUEUED — dispatch AFTER nesting fix merges (both touch DigestRenderer → sequence, no worktree-conflict).
- 🛑 **Stage C (cutover) STILL GATED** — do NOT remove old Sector-Heat/Detail/Sector Watch fallback until the new render is confirmed solid (nesting + momentum fixed + user re-confirms). On go: C1 remove old sections + raw-quantity bullets; C2 remove "## Sector Watch" narrative (`src/prompts/digest/narrative.md`, HOST-MOUNTED — deploy refreshes mount) + test updates. Plan L982+. (plan L982+): C1 remove old Sector Heat table + Sector Detail lists + raw extracted-quantity bullets; C2 remove narrative "## Sector Watch" (edit `src/prompts/digest/narrative.md` — HOST-MOUNTED `/prompts`, deploy must refresh mount) + update DigestRendererTests/DigestNarrativeGeneratorTests.
INVARIANT (impl briefs): trend news-tilt reuses digest momentum filter `source_collector='sentinel' AND signal_identity_id IS NOT NULL AND value_numeric BETWEEN -1.5 AND 1.5` (NOT `:sig:`). LESSONS this epic: verify push-marker keys the COMMITTED tree; xUnit doesn't serialize multi-ICollectionFixture InitializeAsync (advisory lock needed even within one collection); **⚠ review subagents SILENTLY `git restore STATE.md` (reverted once this session, recovered) — COMMIT STATE.md at each milestone so the committed baseline stays current and a rogue revert is a no-op.**

**Gemini resolver remediation epic — COMPLETE 2026-07-02.** 6 PRs #835–#840 merged+deployed+verified (alert re-base, D-5 NER-ingress, D-6 V2-direct SubjectEntity, #838 cross-caller burn alert via resolver /metrics, D-7 near-dup collapse, credential-class drop). client_null flood eliminated, cap headroom recovered, burn+starvation alerts live. Detail → git log `38a0d34d`..`51a1862c` + PRs.

Prior (idle): **OKF / LLM-wiki RAG feasibility spike — CLOSED NO-GO 2026-07-02** (tag `okf-rag-spike-done` @ d519fcf1; RELEASES entry; local branch retired, tag-preserved). User decision (their held-back hypothesis, spike-confirmed): the wiki is squeezed out both ways — grounded/structural retrieval is done better + cheaper + more context-efficiently by the existing bge-m3+pgvector RAG (7b truncated 8/14 ingests: whole-page YAML reads exhaust its real 2048-tok slot under `--parallel 4`, the cost a targeted vector retrieval avoids); news alpha is fast-decaying and must NOT accumulate (reinforcement bias) → stays in the numeric benchmark-staleness pipeline (#729). 32b "GO" only cleared bars by burning 32K ctx — orthogonal to the small-model/context-economy goal. Catch #2 (mangled markdown) refuted both arms (format was never the problem, fit was). KEEPERS: the adversarial eval-gate harness pattern (caught a rigged eval-set pre-inference) + measured local 2048-tok slot reality. RAG untouched; main never touched. Baseline tag `pre-okf-rag-investigation` now a no-op marker.

Last epic (done): intent-fidelity enforcement — COMPLETE + merged + spec retired 2026-07-02 (tag `intent-fidelity-done`). Standing discipline canonical in CLAUDE.md `## INTENT_FIDELITY` MECHANICS + hooks + DECISIONS blocks. Also landed 2026-07-02: ASCII doc-style sweep (`## DOC` AGENT_DOC_STYLE), cron→Monitor wake-listener, memory compaction (84→76). Gemini credits refilled.

## STANDING GUARDRAILS
- OBSERVABILITY (user scar tissue — "too many services non-functional due to lack of observability"): never demote a visible signal to Info+metric without a WIRED alert; keep a VISIBLE Warning on any persistent dependency-unavailability; startup banners STAY at Warning (boot-loop visibility). [[feedback_warn_vs_info_by_trigger]]

## OPEN ITEMS (non-blocking)
- **SentinelCollector.UnitTests: parallel-isolation flake (~50% on full runs)** — surfaced during #825. Two DIFFERENT tests fail intermittently ONLY in the full 1720-test parallel run, both a process-global listener capturing a concurrent test's state: `MacroObservationRouterTests.should_count_log_and_tag_error_status_when_qualitative_substrate_write_throws` (`Activity.LastStatus` = Ok not Error) and `ExtractionProcessorArticleEmbeddingTests.should_tag_failed_when_store_throws` (`capture.Results` = {failed,failed} not single). Both PASS in isolation. NOT caused by #825 (only touched ContentNormalizer). Root cause = shared `ActivityListener`/capture not scoped to the test's own activity under xUnit parallelism. Fix: put listener-based test classes in a serial `[Collection]` or filter the listener by a per-test tag. Blocks reliable push-gating (compile.sh `set -e` aborts + no marker on a flake → re-run needed). Separate PR.
- **SecMaster alert thresholds are still INITIAL estimates** — `SecMasterDiscoveryTimeoutsElevated` (timeout-fraction >0.5/30m) + `SecMasterFredSearchFastFailing` (fastfail ≥10/15m/30m); revisit against real traffic, tune if noisy/silent. Metric-name gotcha: OTEL appends `_total` → alert on `secmaster_fred_search_skipped_total` NOT the bare name (promtool won't catch it).
- **SQLitePCLRaw.lib.e_sqlite3 2.1.11 CVE** (NU1903, GHSA-2m69-gcr7-jv3q, HIGH) — TEST-ONLY, surfaced during the OpenApi remediation: pulled transitively by `SecMaster.UnitTests` + `Reports.UnitTests` (SQLite = unit-test DB provider; prod is TimescaleDB). Bump the EF-SQLite test dep; not a runtime exposure.
- **NasdaqCollector dependency line is stale** — `Microsoft.AspNetCore.Grpc.Swagger 0.8.7` → `Swashbuckle 6.4.0` → `Microsoft.OpenApi 1.2.3`. Clean for CVE-2026-49451 (1.2.3 is *below* the affected 2.x range) but an old chain worth a future refresh. (Nasdaq is DISABLED in prod anyway.)

## PARKED — #729 regime news-as-staleness redesign  (NOT current focus; spec = PR #729, DO-NOT-MERGE)
Intent: FRED/OFR benchmark = slow grounding anchor; Sentinel news = fast-decaying coincident perturbation weighted by benchmark STALENESS; measure economic significance, not coverage volume. Phases 1 / 2a / 2b built + deployed (Phase 2b in **shadow**) — #730–#736. Open:
- 🔴 **Phase 2c cutover** (`Matrix:NewsClustering:Mode=Authoritative`) — **HELD.** Embedding coverage still ramping (missing_embedding ~0.57) AND shadow `magnitude_ratio` ~2.5 (>1) ⇒ dedup AMPLIFIES / sign-flips the net (not gentle compression) → materially restructures news. Hold until coverage fills + Phase 3 revisited.
- ◯ **Phase 3** — staleness crossfade `news_weight = g(benchmark_age/cadence)` sigmoid + benchmark-anchored aggregation. The principled fix for the structural news-overweight (news MACRO cells ~2-4× FRED magnitude, net-negative → washes sector differentiation → over-neutral regime). Gated on 2c + backtest.
- ◯ **BACKTEST harness** = the validation layer that unblocks all of #729 (awaiting go). Shadow has no ground truth. Outcome data found: `finnhub_quotes` XLE/XLF/XLV/XLK ~6.5mo + SPY/QQQ + Yahoo EOD backfill for all 11; AV WTI/BRENT/NATGAS to 1986. First gate = establish a realized-sector-return series.
- ⏸ classifier **sign-fix** parked — DO NOT ship off the energy anecdote (n=6 peek showed news directionally right; needs real backtest N) [[project_energy_signal_not_misfire]]. `commercial-paper-stress` de-flagged interim (#733); spread-redesign `DCPN3M − DGS3MO` (add DGS3MO) proposed, awaiting go.

## DEPLOY / RUNTIME
- **Scoped deploy**: `ansible-playbook playbooks/deploy.yml --tags <svc> --skip-tags build -e "scoped_restart=true scoped_services=<svc>"` from `deployment/ansible/` (build image first: `{Project}/.devcontainer/build.sh --no-cache`; `--skip-tags build` uses the fresh `:latest`, avoids stale-cache rebuild). Non-scoped / `[always]` run = compose regen + full-stack restart (incl ~4min vLLM reload). Grafana provisioning is startup-loaded → `--tags alerting|dashboards --skip-tags always` + restart grafana. Scoped restart recreates ONLY the named target — `depends_on` peers (incl. timescaledb) + vLLM stay up, no cascade; fails loudly if a peer drops or the target isn't running after [[project_scoped_restart_cascade_gotcha]]. ⚠️ `autofix-runner/-watcher` timers run a FULL deploy from main every few min → unpushed config reverts; MERGE to hold. Repeated `--no-cache` builds load the host → transient Npgsql cmd-timeouts in other collectors (benign). [[ansible_deploy_stale_image]] [[project_ansible_deploy_restart_behavior]]
- Boot: `atlas.service` + `otel.service` (enabled) bring the stack up; containers `restart: unless-stopped`. EF migration: `nerdctl compose exec -T <svc>-dev dotnet ef migrations add <Name> --context <Ctx> --output-dir Data/Migrations`. Prometheus reload: `sudo nerdctl exec prometheus kill -HUP 1`.
- DB inspect (SELECT only; never DML [[feedback_agent_no_raw_db_fixes]]): `sudo nerdctl exec timescaledb psql -U ai_inference -d atlas_data`. news obs `sentinel.extracted_observations` / `raw_content`; matrix feed `public.macro_observations`; cells `public.matrix_cells`; regime `public.sector_regimes`. SecMaster DB = `atlas_secmaster`.
- Inference: GPU vLLM `Qwen2.5-32B-Instruct-AWQ` 32K (extraction `VllmJson` + classifier); CPU `llama-cpu-rag` (qwen2.5:7b, SecMaster RAG, host port 11438; solo ~29 tok/s post-EXPO-5600), `llama-cpu-embed` (bge-m3), DSL `llama-server` — all memory-bandwidth-bound on the 9960X single IMC (per-request rate divides under concurrency; that's at-spec, not a bug). Trust running `printenv`, not commit narrative.
- Push: git-push-guard blocks direct main push except docs-only; commit + push = SEPARATE bash calls; docs/STATE push → `scripts/claude-mark-verified` then push. Merge gate: `pr-review-toolkit:review-pr <N>` writes `/tmp/atlas-test-markers/pr-reviewed-<N>` keyed to headRefOid; re-run after new commits. [[project_push_guard_main_block]] [[project_pr_review_marker_staleness]]

## SUPERVISOR OPERATING NOTES
- Land substantive work as GitHub PRs [[feedback_work_in_github_not_local]]. **Verify agent DATA vs DIAGNOSIS before surfacing/recording** — for a stuck/looping/slow symptom, pull the OTEL counters FIRST [[feedback_pull_counters_before_loop_hypothesis]] [[feedback_verify_agent_inferences]].
- Code/test/build/deploy → background subagents; worktree-isolate parallel code agents [[feedback_parallel_agents_use_worktrees]]; run `compile.sh` AS FINAL from within the worktree; silent 30-60min usually = mid-compile [[feedback_compile_slow_dont_kill]].
- Autonomy: dispatch/review/fix/merge/deploy autonomous; NTFY `atlas-claude-ask` only on genuine blocker / arch fork / milestone; ACK on receipt. End turns `▶` (autonomous) vs `🛑` (decision needed) [[feedback_signal_autonomous_vs_pause]].
