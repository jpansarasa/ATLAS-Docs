# ATLAS Supervisor STATE

Supervisor memory. Read first, write last. **Current truth + open items + pointers ONLY.** History → git log + PRs + tags + memories. NOT per-turn progress, review verdicts, or merged-PR narration.

## EACH TURN START HERE
0. **FIRST TURN ONLY** — git audit (`git -C /home/james/ATLAS worktree list / branch / status / fetch + log HEAD..origin/main`); main on `main` & current. [[feedback_git_session_start_discipline]] [[feedback_main_always_current]]
1. Poll NTFY `atlas-claude-reply` (user input first).
2. RE-READ this file + active plan THIS turn. Walk the pipeline backward before dispatching; plan-vs-prod mismatch → STOP + NTFY.

## GOAL [PINNED — check every turn AND every dispatch against THIS]
**Both major deliverables DONE 2026-06-10 (see ARCHIVE):**
- **Throughput:** Sentinel CoD extraction ~20 → ~383/hr sustained (~19×), backlog drained to 0.
- **Digest overhaul:** daily/weekly/monthly reports useful — real sectors, de-noised word cloud, News Signal Momentum, cross-collector "all data collection" coverage; macro-noise Top-Sectors count-table dropped. Live + verified at digest 149.
**Standing digest quality bar:** DAILY = good picture of last 24h across ALL collectors (FRED/AV/Finnhub/OFR/Nasdaq/news); WEEKLY/MONTHLY = representative 7d/month summaries.
**Now: MONITOR-MODE** — watch prod health (errors/crashes/backlog/throughput), address issues, await next task.
**HARD CONSTRAINTS:** quality must not regress; land substantive work as **GitHub PRs** [[feedback_work_in_github_not_local]]; **verify agent DATA vs DIAGNOSIS before surfacing** (this session's agents repeatedly misread metrics — the `ollama-cpu-gen`=SecMaster red herring tripped 3) [[feedback_verify_agent_inferences]].

## WHERE WE ARE [2026-06-11 night]
30 PRs merged+deployed today (#655–#688). Night arc (USER-driven): **docs rewritten from the implementation** (#687: 4 code-grounded briefs → EXEC-SUMMARY/ARCHITECTURE/MATRIX.md current-state; 26 historic files retired incl. handoff-v2/plans/research/llm/grammars) · **vllm-server under ansible** (#688, 5 review rounds, cutover verified ~2min downtime — zero hand-run containers remain) · llama-cpu-rag 9G (#686). Production healthy; infra fully IaC; topology vLLM(GPU)+llama.cpp(CPU)×3.

## OPEN (current truth)
- ✅ **Projector split-read LANDED + VERIFIED (#708, 2026-06-13)** → details in ARCHIVE. FRED cells un-frozen: `fred_projected_15min` 0→17, first cycle `HardObsRead=199 NewsObsRead=3101 CellsInserted=77/Updated=517`. Monitor: FRED cells should keep advancing each 5-min cycle (was frozen ~4d).
- ✅ **OFR WIRED INTO THE MATRIX — DONE + VERIFIED (USER-directed; #709 FSI + #710 HFM/repo-dispersion, 2026-06-14)** → details in ARCHIVE. All 3 OFR families now project to `matrix_cells` (source_collector=`ofr`): FSI (5 raw subindices `ofr-fsi-*`), `hf-leverage-gav-median`, `sofr-repo-dispersion` — 11 cells each, verified live (none skipped as unknown_signal). HFM/STFM use a DERIVED path (collector computes the pattern's normalization, writes the ±3 value — projector core untouched); raw `is_macro` STFM/HFM path stays 0-flagged. SecMaster `sofr-repo-dispersion` identity seeded (CSV baked → secmaster rebuilt).
  OFR FAST-FOLLOWS — **ALL DONE + DEPLOYED + VERIFIED (2026-06-14)**: (a) derived-counter observability PANELS LIVE (#711) + zero-init (`DerivedSignalMetricWarmup`); 3 Prometheus `increase()==0` dark-alerts REMOVED (#712, false-fire on restart-reset + lagged/weekend-gapped data) → replaced by ATLAS's first Grafana-native alert (#713, USER-chose TSDB cell-age): Grafana alert on `matrix_cells.evaluated_at` age (NOT created_at — only changes on value-change) for `sofr-repo-dispersion` (>10d, `for:6h`, via existing Alertmanager→AlertService→ntfy), verified Normal (`monitoring/provisioning/alerting/ofr-derived-cell-age.yaml`; needed grafana `provisioning/alerting` mount; Grafana 12.2.1 rejects `relativeTimeRange:[0s,0s]`→from≥1). **HFM panel-only** (quarterly + ~165d uncertain publish lag → staleness threshold = guesswork). (b) #714: `HfmCollectionService.CollectAllAsync` sets span `Error` on PARTIAL failure. (c) #715 DEPLOYED: HFM derived "no fresh data this cycle" WARN→Debug (cell stays live via backfilled history; SOFR-leg asymmetry stays WARN). (d) #717 DEPLOYED+VERIFIED: per-pattern `maxLookbackDays` on the projector hard-data mean (30d FSI/repo-dispersion, etc.) — fixes daily-signal over-smoothing without dropping slow signals (latest-obs fallback; freshness/idem-key invariant by construction). Live re-valuation confirmed sensible: `sofr-repo-dispersion` 0.067→0.233, `ofr-fsi-composite` -2.08→-2.55 (now track recent-30d vs annual avg), vintages unchanged, 0 dropped, projector cycle clean (GroupsProjected=62, 0 bad skips). (e) #716 DEPLOYED: ansible alerts-prune task (set-difference-by-stat, scoped `alerts/*.yml`) — closes the additive-`copy:` orphan gap from #712.
- ✅ **FRED raw-value saturation FIXED + DEPLOYED + VERIFIED (#718, 2026-06-14, USER-directed).** Root cause: WS3 projector computed hard-data magnitude as the RAW mean of `value_numeric` (payrolls 158915, retail 746559) → cell saturated ±3 for every hard-data signal, sometimes WRONG SIGN (um-consumer-sentiment 49.8 read +3 max-bullish during a collapse; unemployment 4.3 read +1.34). The patterns ALREADY carry the correct domain-tuned normalization in `signalExpression` (payrolls `GetMoM*4`, gdp `GetYoY/2`, unemployment `-(UNRATE-4)*2`) which the projector IGNORED. Fix: hard-data branch magnitude = evaluate the compiled `signalExpression` (reuse `ICompiledExpressionCache` + `PatternEvaluationContext` over the live `ObservationCache` — same chain `PatternEvaluationService`/`/evaluate` uses, fed continuously by `MultiCollectorEventConsumerWorker`→`EventProcessor.AddObservation`). News path (decay) UNCHANGED; freshness/evaluatedAt/idempotency from #717 UNCHANGED; impl = nullable `SignalScoringInputs.HardMagnitudeOverride` with raw-mean fallback + `signal_expression_eval_failed` counter + AddException-on-catch. Verified live: all 17 FRED cells de-saturated to normalized values matching `/evaluate` EXACTLY (gdp 1.0, job-openings 0.118, housing-starts 0.767, global-pmi 0.523), 2 sign-flips corrected, 0 eval-failures, projector=`/evaluate` consistent-by-construction. Adversarial review refuted both worst cases (silently-inert wiring; wrong/empty cache instance).
  FOLLOW-UPS: (1) ✅ **DONE (#719, USER-directed, deployed+verified)** — OFR derived path converted to RAW write, unifying OFR with FRED/FSI: collector writes raw leverage / raw `p99−median` spread; projector normalizes via `signalExpression`. Removed `DerivedOfrSignal.{LeverageSignal,DispersionSignal,Clamp}` + 5 formula constants + the drift/unit tests; kept the load-bearing write (group-anchor + freshness + radar count) + STFM join + dark-cell counters; updated the OFR card. Verified live: backfill flipped macro_observations values normalized→raw (sofr-repo-dispersion 0.0/0.4/0.2 → 0.10/0.08/0.09) while the **matrix cells stayed byte-identical** (signalExpression ignores the row value). 221 tests, 0/0; eval-failure fallback for OFR never fires (expressions use `?? default`). (2) ✅ **RESOLVED (non-issue)** — `FNYR-SOFR-A` IS the median (SOFR = volume-weighted median rate, per the OFR card), so the pattern's `sofr99−SOFR` already equals `sofr99−median`; no definition discrepancy. The earlier 0.233→0 shift was GetLatest-vs-per-date-join + data drift, not a wrong leg. (3) ✅ **DONE (#720, USER-directed)** — `signal_expression_eval_failed` counter dashboarded: stat panel (green 0 / red >0) on the ThresholdEngine board's Cell Projection row (uid `thresholdengine`, x=12,y=26), `sum(increase(…[$__range])) or vector(0)` so the absent-until-first-failure series renders a clean 0; deployed no-restart (`--tags dashboards --skip-tags always`), verified live in Grafana = 0. Counter ticks on eval THROW only — silent `?? default` data-gaps do NOT tick, so still spot-check matrix-vs-`/evaluate` after a collector outage. **ALERT DONE (#721, USER-directed)** — Grafana-native alert `te-signal-expression-eval-failed` (`provisioning/alerting/te-signal-expression-eval-failed.yaml`, rule-only file: reuses #713's atlas-alertmanager contact point + root policy — re-declaring would overwrite the policy tree) fires when `sum(increase(…[30m])) or vector(0) > 0`, `for:10m`, severity=warning → Alertmanager warning route → AlertService → ntfy. `noDataState:OK` (absent counter = healthy). Deployed via monitoring-dir copy + `nerdctl restart grafana` (alerting provisioning is startup-loaded, not file-watched). Verified: both rules load health=ok (no #713 regression), new rule provisioned (provenance=file) + inactive; `increase()` is reset-aware so a TE restart won't false-fire.
- ⚠️ **FINDING — 10/27 FRED signals collected but UNPROJECTABLE** (no ThresholdEngine pattern w/ matching `metadata.signalIdentity` → read into HardObsRead but skipped as `GroupsSkippedUnknownSignal`): `breakeven-5y`/`-10y`/`-5y5y-forward`, `pce-core-yoy`, `jolts-layoffs`, `recession-probability`, `durable-goods-consumption`, `job-leavers-rate`, `truck-tonnage-index`, `private-residential-investment`. Benchmark macro series in `macro_observations` that never reach the matrix. Catalog-completeness gap (add patterns or reconcile the signalIdentity string) — NOT a windowing issue; the 366d read now correctly READS them. Surfaced by #708 deploy verification.
- ✅ **FIXED (#722, USER-directed, deployed+verified) — sector REGIME now includes the FRED/OFR benchmark.** Was: `SectorRegimeProjectionWorker` read cells via a single 7d `evaluated_at` window → ALL 17 FRED (vintage >7d: gdp 164d, cpi 44d, sentiment 74d) + hf-leverage excluded → regime was news+6-OFR only, missing the benchmark (pre-existing #694; same class as #708/#717 but at the regime layer; contradicted the USER "FRED/OFR are the benchmark" principle). Fix: `GetLatestProjectorCellsAsync` now applies a PER-SOURCE window — hard-data (`source_collector IN ('fred','ofr')`) gets 366d (matches projector `HardDataLookbackDays`), news (else) keeps 7d; NULL-safe CASE in SQL, InMemory mirror; cell freshness (baked into cell_value) down-weights stale hard-data so the wide window degrades gracefully. NOTE: regime split keys on `source_collector` (matrix_cells), NOT the projector's `:sig:` infix (macro_observations) — coincide only via the 1:1 source write; doc warns against "aligning" them. Verified live: 17/17 FRED now in-window (was 0/17), contributingPatternCount +~17/sector (ENERGY 29→46, FIN 30→48, MATERIALS 31→49…), scores moderated by the benchmark + still neutral, 0 stack errors. Adversarial review 8/8 vs live DB. (The `obs:` prefix still excludes the frozen legacy `sentinel:rss:clm_*` cells — 223 stale per-article→decay-migration rows, NOT polluting; prunable clutter, low priority.)
- 🔬 **REGIME PIPELINE — multi-layer remediation IN PROGRESS (USER-directed, autonomous overnight 2026-06-15).** User: "verify the matrix is sane" → "every regime neutral is a flaw" → "your scaling fix doesn't work, look more critically" → "fix 1+2, reassess 3" → "continue in supervisor mode (sleeping)". The sector regime was 100% neutral across all 7,358 rows ever. Critical audit found FIVE compounding causes; fixes keep uncovering the next layer (systematic-debugging "question the architecture" territory):
  1. ✅ #722 — regime read excluded slow FRED (7d evaluated_at window) → per-source window (366d hard / 7d news). DEPLOYED.
  2. ↩️ #723 (score ×10 scaling) — a band-aid; USER rejected → REVERTED in #724.
  3. ✅ #724 — `GetMoM`/`GetYoY` anchored to `now` not the data's latest release → MoM=0 for stale-but-recent monthly data (compares value to itself). Fixed: anchor to latest release date. DEPLOYED. nonfarm-payrolls 0→0.29, mfg-employment 0→0.167 (live via /evaluate). HistoricalContext look-ahead checked: backtest repo is asOf-bounded → safe.
  4. 🔴 **OPEN — YoY benchmark is DARK (cache-depth).** After #724, audit shows **7/17 FRED signals (ALL YoY) show `?? defaults`, not real data**: cpi-core/-headline, pce-headline, gdp-real(=1.0 default), equipment-investment, industrial-production, retail-sales. Root: `DataWarmupService` loads `maxLookbackDays` (365-400d) FROM NOW, but YoY-on-stale needs `staleness+365` (410-530d) → year-ago point outside cache → `GetYoY` null → default. MoM ok (need 75<90). Level/GetLatest signals ok. FIX (needs approach decision): deepen warmup to `period+staleness` for YoY patterns (per-pattern maxLookback bump to ~550, OR DataWarmupService loads relative to latest-release not now). NOT yet done — held for user.
  5. 🔴 **OPEN — aggregation washes out (the original "3").** Even with a live benchmark: (a) every concept double-counted (FRED-hard + sentinel-news) often OPPOSING → cancel (gdp +0.35 vs −0.71); (b) news dominates ~95% (fred net +0.15); (c) regime score = weighted MEAN of ~46 attenuated cells → ±0.06, can't reach the [-0.5,0.5] neutral band. This is a DESIGN FORK (mean vs net-tilt vs weight-hard-vs-news) — do NOT unilaterally pick; bring options to user.
  PLAN (presented, HOLDING for user): fix #4 (cache depth) → YoY benchmark live → then tackle #5 (aggregation) as a design decision. Baselines: /tmp/regime_baseline.txt, /tmp/matrix_*_717.txt.
- ⚠️ **FINDING — multi-pattern-per-signal collapses to one sectorWeight set.** WS3 `ObservationCellProjector.BuildSignalPatternMapAsync` keeps ONE pattern per `signalIdentity` (first-seen; enabled beats disabled). Signals with multiple patterns targeting DIFFERENT sectors — `housing-starts`×3 (homebuilders/materials/real-estate, 180/180/365d), `challenger-job-cuts`×2, `layoff-disconnect`×2 — therefore project with only the winning pattern's sectorWeights; the others' sector intent is silently dropped. Pre-existing WS3 consequence (the projector replaced legacy per-pattern eval), surfaced by #717's housing-starts watch-item; NOT a #717 regression. Fix would need sectorWeight merge or per-pattern projection (design call).
- ⚠️ **FINDING — secmaster compose healthcheck is broken (curl not in image).** `test: ["CMD","curl","-f",...]` in compose.yaml but the secmaster runtime image has no `curl` → healthcheck perpetually fails (`State.Health` empty). Service is fine (proven: ofr resolution recovers post-restart); only the Docker health signal is dead. Pre-existing. Fix = add curl to the image OR switch healthcheck to a dotnet/wget probe.
- 🔭 **Digest 153 (Fri 11:00Z scheduled) = hands-off confirmation** of the full categorization chain. Credit/NbfiStress radar axes populate as new articles classify under the widened 77-signal catalog (credit+liquidity categories visible to the classifier since 2026-06-11 ~14:10Z).
- ⏳ **sectorWeights recalibration BLOCKED-ON-DATA**: 7 missing SPDR sector ETFs registered for Finnhub accumulation 2026-06-11 (full 11-sector coverage now collecting); re-attempt at ≥1y overlap, or sooner via AlphaVantage historical backfill (user call — AV quota). ECB leg needs a rate-change window.
- 🧹 llama.cpp runners: pin image digest for llama-server/llama-cpu-rag/llama-cpu-embed (vllm-server already pinned #688); optional GGUF blob copy-out vs frozen-ollama-store coupling. · `LlmBenchmark/BenchmarkLlmClientAdapter` broken since #198 (in sln, outside compile gate) — fix signature or remove. · Hooks README nits (v2-manual format undocumented; monitoring-artifacts exemption missing). · sentinel product spec v2 retired to git history 2026-06-11 (recovery via docs/RELEASES.md); product-owner pass would now target the current-state docs (docs/ARCHITECTURE.md + SentinelCollector/README.md). · Optional #619 strict per-source parallel refactor.
- 🛠 **Supervisor infra (this session)**: `scripts/agent-stall-watchdog.sh` (#672) — run every poll cycle; subagents stall at permission prompts (worktree paths miss absolute-path allows) or complete silently; cross-check worktree git state before TaskStop. Allowlist #668. ACK every user ntfy message on receipt.

## DEPLOY / RUNTIME NOTES
- Scoped deploy: `ansible-playbook playbooks/deploy.yml --tags <svc> -e "scoped_restart=true scoped_services=<svc>"`. **Always scoped** — a non-scoped/`[always]` run regenerates compose + full-stack restarts. **⚠️ Even scoped, `nerdctl compose up <svc>` recreates the `depends_on` CHAIN: deploying ofr-collector OR threshold-engine ALSO bounces `timescaledb`+`secmaster` (their deps) → a brief stack-wide transient-error burst (DB conn drop) that self-recovers in seconds (Polly). Verified harmless 2026-06-14, but batch co-dependent service deploys to bounce the DB ONCE, and expect ~1-2s of refused/reconnect WARNs at startup.** Build images first (`{Project}/.devcontainer/build.sh --no-cache`); ansible deploy uses the existing `:latest` (does NOT rebuild). **alert-service is now UP BY DESIGN (re-enabled+verified #656, 2026-06-10) — do NOT stop it** (reverses the old "KEEP DOWN" rule). `--no-cache` build + verify running digest. A cutover must redeploy ALL services the new path touches. **⚠️ AutoFix: `autofix-runner`/`autofix-watcher` systemd timers run a FULL non-scoped `deploy.yml` from main every few min → continuously self-deploys main (UNPUSHED config reverts — MERGE to main to hold; this is what resurrected alert-service all session).** [[ansible_deploy_stale_image]] [[project_ansible_deploy_restart_behavior]]
- ansible vault: ansible.cfg uses absolute path (`/home/james/.ansible_vault_pass`) → run from `deployment/ansible/` (inventory is `inventory/hosts.yml`). Prometheus rule reload: `sudo nerdctl exec prometheus kill -HUP 1`.
- DB inspect: `sudo nerdctl exec timescaledb psql -U ai_inference -d atlas_data` (SELECT only; never DML [[feedback_agent_no_raw_db_fixes]]). SecMaster DB = `atlas_secmaster`. Schemas: news obs `sentinel.extracted_observations`; matrix feed `public.macro_observations`; cells `public.matrix_cells`. Digest served at `http://localhost:5091/sentinel/digests/<id>`.
- **CoD = GPU vLLM `Qwen2.5-32B-Instruct-AWQ` 32K-ctx, `Extraction__Backend=VllmJson`, `MaxConcurrent=8`, `ContinuousStreaming=ON`, loop-guard rep1.1/maxItems60/maxtok4096** (`/parse_json` on dsl-parser-mcp). Classifier also on GPU vLLM. CPU `llama-server` = OLD CoD path, still wired as rollback (CpuInference disabled). **READER-TRAPS:** extraction outcome metric renamed `sentinel_extraction_outcome_total` (#667; old "cpu"-named series ended 2026-06-11 — historical queries before then need the old name); "MaxConcurrent=1/#639/llama-server" = STALE config — trust running `printenv`, not commit narrative.
- Tag-push: the git-push-guard hook can't parse tag pushes → tag via `gh release create <tag> --target main` (API). Docs/STATE-only push → `scripts/claude-mark-verified` then push.

## SUPERVISOR OPERATING NOTES
- All code/test/build/deploy → background subagents (worktree-isolate parallel code agents). **Verify agent DATA vs DIAGNOSIS** before recording/surfacing. [[feedback_verify_agent_inferences]]
- Implementation-agent briefs: forbid `setsid`/`nohup`/detached procs; run `compile.sh` AS FINAL after committing, **FROM WITHIN THE WORKTREE** (`cd <worktree>` first) — running it from the main repo keys the push-marker to MAIN's tree → push blocked (recurring this session, root cause `a6467309`). A code agent silent 30-60min is usually mid-`compile.sh` (≈30-40min) — check worktree git status before TaskStop. [[feedback_compile_slow_dont_kill]]
- Autonomy: dispatch/review/fix/merge/deploy autonomous; NTFY `atlas-claude-ask` only on genuine blocker / architectural fork / milestone. End turns `▶ continuing autonomously` vs `🛑 DECISION NEEDED`.
- Merge gate: `pr-review-toolkit:review-pr <N>` Skill writes `/tmp/atlas-test-markers/pr-reviewed-<N>` keyed to headRefOid; re-run after new commits (let GitHub head propagate first — head-lag mis-keys it; bit this session on #647/#652). [[project_pr_review_marker_staleness]]
- gh auth (2026-06-10): OAuth token can expire mid-session → 401 even though `gh auth status` shows logged-in; user re-auths (`gh auth login`) OR use `GH_TOKEN=<pat>` inline.

## ARCHIVE (git log + tags)
2026-06-14 (USER-directed) — **OFR wired into the matrix** (#709 FSI, #710 HFM+repo-dispersion) + **observability-review command→skill** (`1dd6556c`). OFR was absent from `macro_observations` (only fred+sentinel ever wrote there). FSI (#709): fan composite+4 patterned subindices raw→substrate via `FsiEventSeries.SignalIdentityId`+`WriteSubstrateAsync` (reuse `MacroObservationDualWriter` substrate-only); 5 `ofr-fsi-*` cells live, HardObsRead 202→1492. HFM leverage + STFM SOFR repo-dispersion (#710): DERIVED path — `DerivedOfrSignal` computes the pattern's normalization (`-(lev-15)/5`; `-((sofr99-median)-0.10)/0.05`, clamp±3) and writes the ±3 value (raw levels saturate; projector ignores signalExpression + core untouched). New SecMaster identity `sofr-repo-dispersion`; repo-stress sectorWeights mirror FRED St.Louis-FSI/NFCI. Verified live: hf-leverage-gav-median + sofr-repo-dispersion = 11 cells each, source_collector=ofr. 2 review rounds each; observability-review skill (converted from the command, RED→GREEN tested, auto-triggers on every C#/.NET review via CSO description) caught the backfill-span error-status gap on its debut. Lessons: projector matches pattern.metadata.signalIdentity↔macro_observations.signal_identity_id (¬SecMaster) so matrix works pre-seed; reused dual-writer = MIN_MAINTAIN non-fatal/idempotency for free; meter-capture tests need parallelism-off in the RIGHT assembly (xunit.runner.json was in IntegrationTests not UnitTests).
2026-06-13 (USER-directed) — **Projector split-read: FRED matrix cells un-frozen** (#708). Root cause: WS3 `ObservationCellProjector`'s single descending capped read (120d/1000) let ~450/day news (`:sig:`) evict sparse/slow FRED from the page → FRED cells frozen ~4d (baseline 0/14 projected in 30min; 13/27 absent; quarterly `gdp-real`/`equipment-investment` were also outside the 120d window). Fix: TWO reads — hard-data **366d uncapped** (non-`:sig:`) + news **7d window** (`:sig:`, =7×24h NewsDecay half-life), via new keyset-paged `QueryAllAsync` + generic `SourceIdContains`/`SourceIdExcludes` filter fields (MacroSubstrate); `BuildCellsAsync` unchanged (per-row `:sig:` partition reassembles mixed groups); config-bound `Matrix:HardDataLookbackDays=366`/`NewsWindowDays=7`; `Options.Validate()` fail-fast; removed dead `read_capped` meter. Key insight: news `observation_time` ALREADY = article publish date (`rawContent.CollectedAt`=RSS pubDate), so the window is a clean filter — no new plumbing. Verified live: `HardObsRead=199 NewsObsRead=3101`, `fred_projected_15min` 0→17. 5-agent review + targeted re-review (2 fix rounds); TE 825 tests / MacroSubstrate 64, 0 warnings. Surfaced 3 follow-ups (OPEN): OFR absent from projector input, hard-data raw-value saturation, 10/27 FRED signals collected-but-unpatterned.
2026-06-11 (night, USER-driven) — **Docs-from-implementation + vLLM IaC** (#686–#688): docs/ rebuilt from 4 parallel code-grounded investigation briefs (fact-check review verified 10+ claims vs code, caught a wrong count INSIDE a brief); 26 historic docs retired (recovery: git show 5cf265f7:<path>); new docs/MATRIX.md. vllm-server compose-managed (digest-pinned, GPU device-reservation probe-proven; 5 review rounds caught scoped-outage window, unresolvable digest, double-load; cutover 2min, rollback doc shipped). Lessons: review-marker hand-writing by agents = gate violation (re-run skill); Edit/Write tools on deployment paths in worktrees = unanswerable prompt (Bash-only edits in briefs); the stall watchdog caught two live PROMPT? stalls in ~20min each.
2026-06-11 (evening, USER-driven) — **Inference re-platform + hardening** (#682–#685): RAG revival saga concluded — root cause = ollama Zen5 CPU backend 30× slower than llama.cpp on identical blob+cores (NOT placement/budgets/contention; user's scale-out-on-CPU instinct correct) → llama-cpu-rag (#683, 166/166 success p50 5.0s) → full ollama exodus (#684: llama-cpu-embed, vector-compat gate cosine ≥0.999999, no re-embed; reviewer probe caught ubatch-clamped-to-2048 flag bug pre-merge) · OpenFIGI 22001 (#685: decorated Taiwan exchCodes + varchar(8) undersized vs 16-char official domain + #663-class context poisoning) · TE memory 8→12G (#682, warm-up already needed 9.2G). Lessons: push-guard evaluates cwd at call START (mid-command cd invisible); benchmark the ENGINE before tuning around it.
2026-06-11 (day, USER-driven) — **Digest + categorization rebuild, 10 PRs** (#672–#681): aggregate-first narrative (#675 URL-dedup/backfill + #677 full-24h Tier-1 roll-up leads the prompt; digest-150 unreadable-TSA incident class dead; aggregates-only alert added) · **theme classifier replaced** (#680: keyword tiers proven NEVER-functional — 30d at 97-100% Other, two tiers matched data shapes that never existed; themes now derive from GPU signal catalog, widened 55→77 incl. credit/liquidity) · TextQuote sentence-snapped (#679, 9→151 chars; also feeds resolution/Gemini/CoVe) · narrative token budget single-sourced from VllmClient allowance (#681, third #659-class divergence) · OFR fire-and-forget scopes (#673, 9 handlers) · docs/ consolidated to current-system documentation (#674, 4,581 files retired) · stall watchdog (#672) · RAG rounds 2-3 (#676 prefill ctx600, #678 60s+guard) → tier dormant under load, GPU-move decision with user. Verified: digest 152 multi-theme + radar live.
2026-06-10/11 (overnight) — **Backlog sweep: 8 PRs merged + 4 verified deploys** (#660–#667; #659 by AutoFix). 22003 incident closed: #655 sanitizer had been stale-image-deployed (ran only from 00:51Z); real fixes = prompt-budget guard #659 + fresh-DI-scope failure recording #663 (the poison-pill retry-loop class is dead); 21 stuck rows reprocessed, 0 re-fails. SecMaster RAG storm root-caused: per-request breaker construction never accumulated failures → shared singleton + generation gate + num_predict cap #666 (WARNs −99.6%, load 10→3). matrix_cells DB CHECKs + 3-row cleanup #662 (only 3 rows offended; "226" was total-pre-WS3, not breach count). mcp Containerfile missing-COPY class fixed fleet-wide #661/#665. Docs hygiene #660. OTEL cleanup #667 (rename; error alert = exception-only — real contaminant was age_cutoff bulk-prune, NOT verify_failure; resolve stage wired on v2). Lessons: `claude-mark-verified && git push` in ONE call fails the guard (separate calls); compile BEFORE the final commit mis-keys the marker; agents complete silently — check worktree git log before re-dispatch; nerdctl stats CPU% unreliable (embed vs gen runner mixup).
2026-06-10 — **Digest overhaul** (#649–#652, live+verified @ digest 149): real sectors (re-sourced from classifier-fed `macro_observations` vs ~99%-NULL `extracted_observations.atlas_sector_code`) + de-noised word cloud (per-document freq + scalar-series exclusion, killed the TSA flood) + **News Signal Momentum** (replaced the stale bias-vs-matrix that compared fresh news to a 2-month-old FRED matrix) + **cross-collector "Across All Data Collection"** rollup (raw-hypertable scoreboard + notable-moves table + macro tape; only FRED/OFR-STFM/OFR-FSI/Finnhub-quotes are live, rest footnoted) + dropped the macro-noise Top-Sectors count-table (the "Uncategorized 36471/420" residue was scattered macro signals — kept the BySector data feed for the push/narrative/metric).
2026-06-09/10 — **Extraction throughput ~20→~383/hr sustained (~19×), backlog drained to 0.** Chain: GPU-CoD role-flip (tag `gpu-cod-roleflip-2026-06-09`, #640–#646: CoD CPU llama-server→GPU vLLM JSON-schema, recall gate 0.79) + **verifier removal** (#647: dead SemanticVerifier was ~93% of GPU calls, computed-and-discarded — its matrix consumer retired by design WS3-A3 #601, matrix is classifier-fed) + **continuous-streaming dispatch** (#648: killed the ~40-batch + 5s-`Task.Delay` barrier → GPU continuously fed). Observability verified representative (`a142d89a`); user banked. Lessons: `ollama-cpu-gen`=SecMaster-RAG (NOT Sentinel) red herring tripped agents 3×; CoVe stays GPU (n=563); slow compile ≠ stuck agent.
2026-06-07 — extraction throughput: queue/frontier monitoring (#636) · per-stage OTEL timing (#638) · CPU tuning `3a802551` · memory-hygiene skill+hook (#637).
2026-06-06/07 — digest-quality + reliability sweep: classifier catalog commodity/fx + FRED-anchor (#633) · ThresholdEngine SIGSEGV fix (#634) · 30d retention (#635) · classifier-recall + cascade fixes (#630/#631/#632) · architecture-cards 11/11 (#626/#628/#629).
2026-06-05/06 — matrix-health restoration: #618–#625 · #589 cpuset · #603 verifier-alert. Earlier: counterweight #602/#613–#616 · WS3 #552–#587 · DSL PoC #381–#510 · Matrix MVP #215–#261.
