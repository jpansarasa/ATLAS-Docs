# ATLAS Supervisor STATE

Supervisor memory. Read first, write last. **Current truth + open items + pointers ONLY.** History → git log + PRs + tags + memories. NOT per-turn progress, review verdicts, or merged-PR narration.

## EACH TURN START HERE
0. **FIRST TURN ONLY** — git audit (`git -C /home/james/ATLAS worktree list / branch / status / fetch + log HEAD..origin/main`); main on `main` & current. [[feedback_git_session_start_discipline]] [[feedback_main_always_current]]
1. Poll NTFY `atlas-claude-reply` (user input first).
2. RE-READ this file + the active spec THIS turn. Walk the pipeline backward before dispatching; plan-vs-prod mismatch → STOP + NTFY.

## CURRENT WORK [PINNED — check every turn]

**No open action items outside the #729 redesign below.** Recently shipped (history → git log + PRs):
- This session #777–#782 — SecMaster: 409-on-duplicate, RAG symbol-only prompt + truncation-log demote, RAG prompt differentiation (resolution=symbol / `/ask`=prose), US-primary listing canonicalization, healthcheck curl + `/ask` token cap, quarantine purge (546 LoRA-era garbage dropped). All merged + deployed + verified.
- Earlier #739–#776 — observability remediation, the resolve-entities timeout saga (closed via #764), and the gemini-resolver recurrence-prevention epic.

**Regime news-as-staleness-perturbation redesign.** Spec = **PR #729** (`docs/proposals/regime-news-staleness-redesign.md`) — design-review surface, DO-NOT-MERGE. Nothing deploys without a shadow-run + explicit user go.

**Intent:** benchmark (FRED/OFR) = slow grounding anchor (monthly/quarterly, 30–90d lag); Sentinel news = fast-decaying COINCIDENT perturbation whose weight scales with benchmark STALENESS; measure economic SIGNIFICANCE (distinct occurrences × net direction), NOT press coverage volume.

**5 pieces (phased, each shadow-gated):** (1) daily-observable signals → real FRED/OFR series; (2) news dedup per *occurrence* (not topic); (3) news aggregated by significance; (4) staleness crossfade `news_weight = g(benchmark_age / cadence)`; (5) benchmark-anchored aggregation (replaces the flat net-tilt sum).

**Decisions locked (2026-06-16):** magnitude = **v2** (v1 = dedup + distinct-entity breadth + net-direction); `g()` = **sigmoid** (flat fresh, ramps near next release); cadence = **inferred historical**; dedup = **embedding-cluster first** (fallback `entity+event-type+time-key`); daily-series→sector map = draft spec §8.2, validate/iterate later.

**→ ACTIVE: Piece 1 via Option C — per-signal `matrix-benchmark` flag in SecMaster (single source of truth).**
- *Root cause it fixes:* the sector benchmarks are ALREADY wired — patterns + FRED series + signal-identities + sectorWeights + signalExpressions all exist for oil/yields/dxy/credit (e.g. `oil-price.json` reads `GetLatest("DCOILWTICO")`, ENERGY +1.0). But FredCollector's macro-write gate (`SecMasterMacroSignalClassifier` → `/by-category/macro`) dual-writes ONLY the `macro` category → `rate`/`commodity`/`fx`/`credit` benchmarks never reach `macro_observations`, so news is their only feed. Eligibility cuts ACROSS categories (per-signal: macro 22/33, rate 5/14, credit 8/13, …) → a per-signal flag, not a category-widen, is the right grain.
- *Build plan:* **SecMaster** — `SignalIdentityEntity.MatrixBenchmark` bool (default false) + CSV `matrix_benchmark` column + parser + importer map + EF migration `AddSignalIdentityMatrixBenchmark` (devcontainer `dotnet ef`) + `GET /api/signal-identities/matrix-benchmarks` endpoint. **FredCollector** — repoint classifier `/by-category/macro`→`/matrix-benchmarks`, rename `IMacroSignalClassifier`→`IMatrixBenchmarkClassifier`. v1 flag set = **no-regression superset** (all currently-macro signals + the patterned non-macro benchmarks); OFR untouched v1. compile+test both → PR → deploy (secmaster self-migrates on startup) → **shadow-verify** fred benchmark cells appear for oil/yields/dxy/credit + regime shifts sensibly (te-sector-regime-all-neutral alert guards).
- **AUTONOMOUS supervisor-mode execution of the full spec authorized 2026-06-16** (user merged #729 + "good to do [the spec] in supervisor mode"). Phased, each shadow-gated; NTFY milestones/blockers.
- **✅ PHASE 1 (Option C) DONE + DEPLOYED + VERIFIED (#730, 2026-06-16).** matrix-benchmark flag (SecMaster entity+migration+importer+`/matrix-benchmarks` endpoint) + FredCollector gate repoint. Scoped-deployed (secmaster self-migrated, 53 signals flagged); the previously category-gated FRED benchmarks (oil/yields/dxy/credit/natgas/2s10s/10y-real/fed-funds) now dual-write → projector cells → regime. Verified live end-to-end; regime healthy + differentiated (10/11 non-neutral); 0 errors. (breakevens/2y dual-write but have no pattern → no cell — benign.)
  - **✅ Latent bug it surfaced — FIXED (#731): `hy-credit-spread` (credit-spread-widening) signalExpression was bps but FRED `BAMLH0A0HYM2` is percent → clamped +3 (mis-read calm credit as max risk-on at FINANCIALS weight 1.0). Faithful bps→percent unit fix, hot-reloaded; cell de-clamped 3.0→1.15, `/evaluate` +1.12. Other 7 newly-flowing benchmarks scale correctly (audited).**
  - **🔴 OPEN — held for USER: `commercial-paper-stress` (`(75-cp)/50` over DCPN3M ~5% percent) — same unit class + DESIGN flaw (uses CP rate LEVEL not a spread → can't tell high-policy-rate from stress). NOT yet flowing from FRED (news-sourced until the 18:00Z DCPN3M collection, then mildly mis-flows ~+1.4). NTFY'd the design Q (redesign as spread vs de-flag). If no user answer before ~17:30Z, de-flag (matrix_benchmark=false) as the safe interim.**
- **🔬 Phase 2 — news dedup per OCCURRENCE — IN PROGRESS.** Fork RESOLVED → **B (aggregation-time, non-destructive)**, user 2026-06-16 ("must be B; A is destructive if we ever reprocess"). Phasing: 2a embed-store → **2b projector reads embeddings, clusters per-occurrence, computes deduped news magnitude in SHADOW (flag-gated)** → 2c cutover after shadow validation. Defaults: cosine≥0.85, rolling-24h, rep=highest-confidence, Tilt=confidence-weighted-mean. Pipeline mapped: article→`MacroObservationRouter.TryPlanNewsSignalWrite` writes `{rawContentId}:sig:{signalId}`, value=`Tilt×Confidence`; projector `ObservationCellProjectorCore.ComputeNewsMagnitude` sums per-article `S=Σ vᵢ·decay`, `K·tanh(S/K)` — each article summed independently (no occurrence dedup). **Magnitude CONFIRMED v2-blocked**: `NewsSignal` = Tilt+Confidence only, no headcount/$ → v1 = dedup + net-direction.
  - **User decisions (2026-06-16):** (1) trust rebalance → **WAIT for Phase 4-5** (the principled staleness crossfade; NO interim blunt cap). (2) **build 2b ahead in SHADOW** during the coverage wait.
  - **2b collapse constraint (verified in code):** news rows store `ValueNumeric = Tilt×Confidence` FUSED (`MacroObservationRouter` L187/213); `Trust`/`Payload` null on news path → exact "confidence-weighted-mean + highest-confidence rep" NOT recoverable at the projector under Path B. **v1 = count-normalized mean-within-cluster of (value·decay)** (faithful in spirit, no write-path change). Exact weighting = documented **v2** refinement (plumb Tilt+Conf via the unused `Payload` JSON col — additive). Shadow-only, so safe to ship the approximation + measure.
  - **2b join key (verified):** `news_article_embeddings.raw_content_id` = `rawContent.Id.ToString(Invariant)` = the `:sig:` source_id prefix (ExtractionProcessor L1979-83). Projector extracts prefix-before-`:sig:` → batch-fetch embeddings → cluster per (signal,collector) group.
  - **✅ 2b MERGED (#735, squash `87386774`, 2026-06-16) — deploying Shadow.** Built INLINE (2× bg subagent dispatch died on transient API 529 / 0-token; pivoted to direct edits). Scope: `Matrix:NewsClustering` opts {Mode=Shadow|Authoritative (default Shadow), CosineThreshold=0.85, WindowHours=24}; `ObservationCellProjectorCore.ClusterAndComputeNewsMagnitude` (pure, greedy time-windowed cosine cluster, mean-of-members collapse, TDD'd) + shared DecayWeight/SoftSaturate + `Project(newsMagnitudeOverride)`; `INewsArticleEmbeddingRepository.GetByRawContentIdsAsync` batch read; worker batch-fetches embeddings/cycle (best-effort), clusters per news group, emits `news_clustering_{articles,clusters,missing_embedding}_total` + `_magnitude_ratio` hist + the **join-coverage canary** `news_embedding_fetch_{requested,matched,failed}_total` (distinguishes key-drift from 2a ramp-up — review hardening). Shadow (default) writes flat-sum to cells UNCHANGED. **Reviewed:** 4 agent passes (code/obs/silent-failure + delta re-review) — no Critical/Important; review-pr skill marker set; merged. **✅ DEPLOYED + VERIFIED in Shadow (2026-06-16):** image rebuilt no-cache, scoped-deployed; projector cycle ran (3033 news/74 groups/814 cells), 0 errors, regime differentiated. All shadow metrics populate. **Join canary confirms RAMP-UP not drift:** `news_embedding_fetch_matched=52 / requested=1506` (~3.5% coverage) — matched>0 (join healthy end-to-end) and ≈ total embeddings captured so far (2a only hours old vs 7-day news window). `magnitude_ratio` mean ≈0.997 (≈no compression yet — ~96% of articles are still embedding-less singletons). **Shadow comparison becomes decision-grade as 2a coverage fills the 7-day window (~days).**
  - **2b FOLLOW-UPS:** (1) **✅ DONE (#736, `628a16fb`):** Grafana dashboard row "News Occurrence Clustering (Phase 2b · shadow)" (4 panels: Coverage Compression, Embedding Join Coverage=drift-vs-rampup canary, Magnitude Compression=headline signal, Fetch Failures) + Prometheus P3 alert `ThresholdEngineNewsEmbeddingFetchFailing` (sustained fetch-failure; NO drift alert — would false-fire during ramp-up). Deployed (`--tags dashboards,alerting`), grafana auto-reloaded, alert loaded, verified. Dashboard uid `thresholdengine`. (2) **⏳ 2c cutover = config flip `Matrix:NewsClustering:Mode=Authoritative`** — GATED on (a) embedding coverage filling the 7d window (~days; watch Embedding Join Coverage panel climb 0→~1) + (b) user reviewing the Magnitude Compression shadow signal settling <1. **🔬 2c DEEP-DIVE 2026-06-18 (user "go ahead with A") = NO-GO / HOLD:** (i) coverage still ramping — `missing_embedding/articles ≈ 0.57` (was ~0.96 at deploy), ~57% of clustered articles still embedding-less→forced singletons→can't dedup; gate (a) NOT met. (ii) `magnitude_ratio` settled at **~2.5 (>1), NOT the <1 the gate assumed** — per `ClusterAndComputeNewsMagnitude` (`clusteredS=Σ mean(cluster)` vs `flatS=Σ all`), ratio>1 ⇒ dedup REMOVES redundant coverage that MASKED the true net in the flat sum → news net changes materially, often sign-flips. So 2c materially restructures news, not gentle compression. HOLD until coverage fills AND Phase-3 design revisited (dedup amplifies/flips, not compresses → changes how the staleness-crossfade weights news). Deep-dive prevented a premature regime-changing cutover on non-representative + contrary-to-assumption data. (3) v2: exact confidence-weighted collapse (plumb Tilt+Conf via the unused `Payload` col).
  - **➡️ NEXT after 2c (user-decided 2026-06-16): build Phase 3** (spec Pieces 4+5 — staleness crossfade `news_weight=g(benchmark_age/cadence)` sigmoid + benchmark-anchored aggregation). HOLD until 2b validates/cuts over, THEN build (NOT ahead — Phase 3 weights the news contribution 2b changes). This IS the principled fix for the parked FRED-vs-news trust concern.
  - **🔎 LIVE EVIDENCE the parked imbalance is now degrading the regime (2026-06-18 ~12:15Z — explained, NOT a fresh anomaly; don't re-investigate each cron cycle):** sectors drifted **2-neutral→9-neutral**, sustained since ~11:19Z (was **10/11 differentiated post-#730**; now only REAL_ESTATE=contraction + UTILITIES=expansion). Pipeline HEALTHY — regimes+cells fresh (12:14Z), 0 errors. Cause (verified from `matrix_cells` standing snapshot): news-derived **MACRO** signals (`oil-price`/`cpi-headline-yoy`/`fed-funds-rate`/`gdp-real`/`initial-jobless-claims`/`ust-10y-yield`/`dxy` — the SAME indicators FRED grounds as hard benchmarks) = **39 news cells/sector vs 13 FRED hard**, ~2–4× magnitude, netting **uniformly negative** across all 11 sectors → washes out the cross-sector *differentiation* the tilt depends on → neutral. `signal_identity_id` for sentinel cells is a macro slug, NOT a company entity → **this is double-representation, NOT the junk-surface entity issue** (that's the company-resolution path, P1 above). This is precisely the news over-weighting that **2b dedup (Shadow→2c) + Phase 3 crossfade** are built to fix; magnitude compression is still ≈0.997 (2b not yet compressing). **Holding** per user "wait Phase 4-5, no interim blunt cap." Surfaced via NTFY 2026-06-18.
  - **🔬 2026-06-20 (user "do our signals track the market?") — energy-angle RE-DERIVATION of this same imbalance + 3 new deltas; backtest unblocks the whole effort. (NOTE: this investigation ran fresh BEFORE re-reading STATE — should have walked the plan first [[feedback_walk_pipeline_each_turn]].)** ENERGY reads neutral during a +40% rally (Hormuz/Iran oil supply shock). Root cause = the SAME news over-weighting, but the dig added: **(1) SIGN-INVERSION (additive to Phase 3, NOT covered by crossfade/dedup):** on narrative-laden *market-priced* signals the news SIGN is wrong, not just over-weighted — oil-price news −0.98 vs FRED +0.39; 2s10s −0.77 vs +0.38; dxy/ecb-rate/repo same; ALL 5 sign-disagreers are market-priced/administered (no lag/thumb defense, validating user's point that the defense only holds for lagging official stats — and those (jobless-claims) AGREE in sign, news leading = feature). Classifier scores emotional frame (crisis/recession) not sector-impact direction (nat-gas & 10y-LEVEL signed right; oil & 2s10s-CURVE signed wrong). Crossfade alone won't fix a wrong sign ⇒ **new work item: classifier sign-fix (Sentinel NewsSignalClassifier prompt: sign for sector impact, not mood).** **(2) Divergence-as-signal:** news↔live-price divergence is itself alpha; engine averages it to neutral mush (oil −0.98+0.39→~0), destroying info. **(3) BACKTEST = the missing validation layer for ALL of #729.** 2c held / Phase 3 unbuilt partly because shadow has NO ground truth (magnitude_ratio ~2.5 = uninterpretable). User: "we can validate and backtest, that is the key." Backtest divergence/dedup/crossfade/sign-fix vs REALIZED sector returns → tells us what actually improves *leading power* → unblocks the parked redesign. **Feasibility: signal side OK** (reconstruct divergence from macro_observations: sentinel→2023, fred→2025-10, ~8mo overlap). **OUTCOME-DATA GAP:** `finnhub_candles` EMPTY, `fred_observations` (643k) has NO equity/sector indices; only `finnhub_quotes` (44k per-symbol OHLC snapshots) is a candidate — must verify sector-ETF (XLE/XLK/…) coverage+depth or stand up sector-price collection. **NEXT (awaiting user go): build backtest harness as #729's validation layer; first gate = confirm/establish the realized-sector-return outcome series.**
  - **✅ OUTCOME DATA FOUND (2026-06-20):** `finnhub_quotes` already samples XLE/XLF/XLV/XLK ~6.5mo (78 trading days, 2025-11-28→ = ENERGY/FIN/HEALTH/TECH) + SPY/QQQ/IWM/DIA/VTI; other 7 sector ETFs only since 06-10 (forward-only). `alphavantage_observations` AV/WTI+BRENT+NATGAS daily back to **1986**. Yahoo Finance free EOD (no key, 501pts/2yr verified) = backfill for all 11. Finnhub historical candles premium/EMPTY — not needed. Remaining limit = NEWS density (oil news thin pre-June, dense from June) → backtest power recent-window-limited now, grows forward.
  - **🔭 PEEK DONE (2026-06-20, user "peek first, conserve effort, not 100%") — REFRAMES the energy "miss"; SIGN-FIX PARKED.** n=6 (tiny, NOT conclusive) but `corr(news_oil_tilt, XLE fwd-5d)=+0.69` (bearish-news days → −4.4% avg fwd-5d): news tilt was directionally RIGHT, NOT a contrarian fade. Key correction to the earlier framing: **XLE ~$54 and FELL through June (57.6→53.8; WTI ~106→84) — the Hormuz spike is MEAN-REVERTING; "+40% YTD" = the earlier spike, NOT "rising now."** So ATLAS neutral-and-softening on energy may be CORRECTLY leading the rollover (user's "maybe not wrong"). **⛔ DO NOT ship the classifier sign-fix off the energy anecdote — on this evidence it could be backwards (chasing a fading rally). Revisit only with real backtest N (forward-accumulate, do NOT back-fit a few June days). Divergence-as-signal survives but unvalidated. The STRUCTURAL news-overweight (audit-confirmed) → #729 Phase-3 crossfade remains the real work. [[project-energy-signal-not-misfire]]**
  - **2b worktree:** `.claude/worktrees/2b-clustering` (branch `feat/2b-...`) — cleanup pending post-deploy. TE `.devcontainer/compose.yaml` mounts `../..:/workspace` = worktree root (builds branch code ✓); compile.sh lost +x in worktree → `bash <script>`.
  - **✅ 2a DONE + DEPLOYED + VERIFIED (#732):** per-article news embeddings (`news_article_embeddings` pgvector(1024) in atlas_data; SentinelCollector embeds at ingestion, best-effort). Embeddings accumulating for 2b. **Deploy gotcha fixed:** migrator failed `permission denied to create extension "vector" (must be superuser)` — `compose up` returns 0 even though the one-shot exited 1, so it looked clean. Fixed by provisioning pgvector in atlas_data as superuser (out-of-band, like the timescaledb ext) + re-running the migrator. **✅ 2a IaC follow-up DONE (#734):** `CREATE EXTENSION IF NOT EXISTS vector` (atlas_data, superuser) added to init-db.sh (first-init, before the migrator → fresh-deploy reproducible — load-bearing) + deploy.yml extension task (idempotent re-ensure for existing DBs). Prod already had it (manual unblock); now codified. No deploy needed (applies on next deploy / fresh init).
  - **✅ commercial-paper-stress DE-FLAGGED (#733, user-directed interim):** `matrix_benchmark=false` (CSV → re-import flipped DB flag, 53→52); FredCollector no longer dual-writes DCPN3M. Pattern is unit-bugged + design-flawed (rate LEVEL not spread); redesign-as-spread is an OPTIONAL later task. Still news-sourced. **Spread-redesign PROPOSED to user 2026-06-16 (awaiting go):** classic "paper-bill spread" = `DCPN3M − DGS3MO` (3M CP − 3M T-bill — isolates credit/liquidity stress from the rate level). **DGS3MO is NOT collected** (FredCollector has DCPN3M + overnight SOFR/FEDFUNDS but no 3M T-bill) → must add it. Proposed `signal=clamp(−(spread−0.25)/0.4, ±3)` (0.25%=neutral, 0.65%=−1, 1.05%=−2, ≥1.45%=−3; stress trigger >~0.6%; hist calm 0.1–0.3%, 2008 nonfin >1% / fin 2–3%). Current spread ≈0.1% (calm). Scope on go: add DGS3MO + redesign pattern + re-flag matrix_benchmark=true.
  - **(historical fork, RESOLVED B):** A = ingestion-time dedup (SentinelCollector, before write; 1 row/occurrence; cheaper/real-time but IRREVERSIBLE) vs B = aggregation-time dedup (projector, cluster per-article rows each cycle; non-destructive, tunable/swappable). **Supervisor leans B** (matches "try + fallback if it skews" — keeps raw data, zero-loss re-tune/swap/disable). Defaults if approved: cosine≥~0.85, rolling-24h window, cluster rep=highest-confidence, Tilt collapse=confidence-weighted-mean, fallback key=`signal:sorted-entities:date`, dedup observability counters. Full scope: agent report (transcript) + spec §4 Piece 2/3.

## DEPLOY / RUNTIME NOTES
- **Scoped deploy**: `ansible-playbook playbooks/deploy.yml --tags <svc> -e "scoped_restart=true scoped_services=<svc>"` from `deployment/ansible/`. A non-scoped / `[always]`-tagged run regenerates compose + **full-stack restart** (incl. ~4min vLLM GPU reload). Grafana provisioning (alerting/dashboards) is **startup-loaded** → deploy `--tags alerting|dashboards --skip-tags always` + `sudo nerdctl restart grafana`. Build images first (`{Project}/.devcontainer/build.sh --no-cache`); ansible uses existing `:latest`. ⚠️ Even scoped, `compose up <svc>` recreates the `depends_on` CHAIN — deploying ofr/threshold-engine ALSO bounces `timescaledb`+`secmaster` → brief transient-error burst, self-heals (Polly). ⚠️ `autofix-runner`/`-watcher` systemd timers run a FULL `deploy.yml` from main every few min → unpushed config reverts; MERGE to main to hold. [[ansible_deploy_stale_image]] [[project_ansible_deploy_restart_behavior]]
- ansible vault: ansible.cfg absolute path (`/home/james/.ansible_vault_pass`) → run from `deployment/ansible/`. EF migration: `nerdctl compose exec -T <svc>-dev dotnet ef migrations add <Name> --project src/Data`. Prometheus reload: `sudo nerdctl exec prometheus kill -HUP 1`.
- DB inspect (SELECT only; never DML [[feedback_agent_no_raw_db_fixes]]): `sudo nerdctl exec timescaledb psql -U ai_inference -d atlas_data`. Schemas: news obs `sentinel.extracted_observations`; matrix feed `public.macro_observations`; cells `public.matrix_cells`; regime `public.sector_regimes`. SecMaster DB = `atlas_secmaster`.
- CoD = GPU vLLM `Qwen2.5-32B-Instruct-AWQ` 32K-ctx, `Extraction__Backend=VllmJson`, MaxConcurrent=8, continuous-streaming (`/parse_json` on dsl-parser-mcp); classifier on GPU vLLM too; CPU `llama-server` = disabled rollback. Trust running `printenv`, not commit narrative.
- Push: git-push-guard blocks direct main push except docs-only; commit + push = SEPARATE bash calls; tag via `gh release create <tag> --target main`; docs/STATE push → `scripts/claude-mark-verified` then push. [[project_push_guard_main_block]]

## SUPERVISOR OPERATING NOTES
- Land substantive work as **GitHub PRs** [[feedback_work_in_github_not_local]]. **Verify agent DATA vs DIAGNOSIS before surfacing/recording** [[feedback_verify_agent_inferences]].
- Code/test/build/deploy → background subagents; worktree-isolate parallel code agents [[feedback_parallel_agents_use_worktrees]]. Briefs forbid `setsid`/`nohup`/detached/`-it`/interactive prompts; run `compile.sh` AS FINAL, FROM WITHIN the worktree (else push-marker keys to main → blocked); silent 30-60min usually = mid-compile → check worktree git status before TaskStop [[feedback_compile_slow_dont_kill]].
- Autonomy: dispatch/review/fix/merge/deploy autonomous; NTFY `atlas-claude-ask` only on genuine blocker / arch fork / milestone; ACK every user ntfy on receipt. End turns `▶` (autonomous) vs `🛑` (decision needed) [[feedback_signal_autonomous_vs_pause]].
- Merge gate: `pr-review-toolkit:review-pr <N>` writes `/tmp/atlas-test-markers/pr-reviewed-<N>` keyed to headRefOid; re-run after new commits (let GitHub head propagate first) [[project_pr_review_marker_staleness]].
