# ATLAS Supervisor STATE

Supervisor memory. Read first, write last. **Current truth + open items + pointers ONLY.** History → git log + PRs + tags + memories.

## EACH TURN START HERE
0. **FIRST TURN** — git audit (`worktree list / branch / status / fetch + log HEAD..origin/main`); on `main` & current. [[feedback_git_session_start_discipline]] [[feedback_main_always_current]]
1. Poll NTFY `atlas-claude-reply` (user input first).
2. Re-read this file + the active spec (if any) THIS turn; walk the pipeline backward before dispatching; plan-vs-prod mismatch → STOP + NTFY.

## CURRENT WORK — runtime log-level control (2026-07-21)
Grew from the Loki low-volume investigation: services are WARN-quiet BY DESIGN — telemetry proves liveness (all 13 verified pushing incrementing metrics; threshold-engine +29K clusters/1h, collectors polling), low Loki volume is CORRECT not a gap. Corrected my own outdated model (startup-banner-as-proof-of-life). No dynamic log-level mechanism existed (0 LoggingLevelSwitch/ControlledBy/reloadOnChange across 13 services).
DESIGN (user-decided — reloadable appsettings + LevelSwitch): Serilog LoggingLevelSwitch + EXPLICIT ChangeToken.OnChange (Serilog 4.2.0/Settings.Configuration 8.0.4 does NOT auto-reapply on reload) wired to the EXISTING baked-in appsettings (reloadOnChange:true via default WebApplication.CreateBuilder) + lower the MEL SetMinimumLevel(Warning) floor to Debug (7 svcs set it: Fred/Finnhub/AlphaVantage/Nasdaq/Ofr/Sentinel/ThresholdEngine). NO mounts, NO compose change — ephemeral in-container edit IS the mechanism. REVERT = edit key back live OR RECREATE the container (rm+up); NOT restart (restart keeps the writable layer → the raise PERSISTS). Plan: /tmp/sentinel-remediation/loglevel-plan/PLAN.md (mount-based fallback mapped there if the overlay-edit path fails).
PILOT #883 MERGED (03df3e50) + DEPLOYED + **MECHANISM PROVEN ON REAL CONTAINER** (2026-07-21): fresh image (DLL carries RuntimeLogLevel symbols, scoped deploy ok=24 failed=0), then the operator workflow end-to-end — `nerdctl exec` raise Warning→Information → 25 sub-Warning lines appeared in Loki (matched stdout), NO restart → live revert → 0 sub-Warning (control: Warning markers still landed, so it's the switch not a dead pipeline) → RECREATE → baked-in Warning restored. So the unproven assumption (in-container overlay edit fires .NET inotify on containerd/overlayfs) HOLDS. Reviews (2) 0-crit/0-important; ChangeToken auto-resubscribes, ControlledBy-last wins, MEL-floor-to-Debug is free at steady state (SerilogLogger.IsEnabled short-circuits pre-alloc).
CARRY-FORWARD (both reviews, below-bar, fold into rollout — the rollout touches every D-entry): (1) version citation 8.0.4→9.0.0 in FredCollector D-2 + RuntimeLogLevel.cs comment; (2) missing `// INTENT(D-2):` marker at the AddRuntimeControlledSerilog MEL-floor guard site (2 of 3 named sites have it).
ROLLOUT — user GREENLIT: **inline per-service** (copy FredCollector/src/Telemetry/RuntimeLogLevel.cs verbatim; matches per-Program.cs convention, per-service guard test catches drift; NO shared lib). STANDING (user): run EACH batch through review + deploy. New copies FIX the pilot's 2 nits (cite 9.0.0 not 8.0.4; INTENT marker at all 3 guard sites) — then backfill FredCollector D-2 with the same 2 fixes.
Batches (sequential; review→merge→build+scoped-deploy, ONE representative exec-edit spot-check per batch since the runtime mechanism is already proven platform-wide):
  B1 DONE — #884 merged + DEPLOYED + spot-check FIRED (finnhub raise->Info->revert). Finnhub/AlphaVantage/Ofr.
  B2 DONE — #885 merged + DEPLOYED + verified. Sentinel + ThresholdEngine (Nasdaq code-only, disabled prod). TE spot-check fired (923 Info on raise, 0 on revert). **TE Serilog 8->9 bump KEEP (reviewed+proven): TE already on Serilog core 4.2.0, settings/hosting-only bump, no NU1605 downgrade, TE started CLEAN under 9.0.0.** D-nums: Nasdaq D-1, Sentinel D-13, TE D-6 (no collision).
  ⚠ PRE-EXISTING finding (NOT rollout-caused, do NOT attribute): threshold-engine /health 503 — 17 patterns "severely overdue" 4-50 days + 2 missing (17 identical errors existed 12h BEFORE deploy). TE process functional (evaluate serves 71 patterns). Worth a separate morning look; out of scope for the log-level rollout.
  B3 DONE — #886 merged + DEPLOYED + verified. AlertService/SecMaster/CalendarService (HTTP). REAL SHAPE DIFF handled: all 3 use UseSerilogRequestLogging() -> dropping UseSerilog() would break it (de-registers IDiagnosticContext) -> impl RE-REGISTERED DiagnosticContext+IDiagnosticContext to the switch-controlled logger (faithful to Serilog's ConfigureDiagnosticContext, verified by reflection in review). Request-logging PROVEN at deploy (SecMaster raise -> "HTTP GET /health responded 200 in 5.77ms" WITH enrichment -> revert -> stops). Serilog.AspNetCore 8.0.3->9.0.0 on Alert/Calendar (SAFE — core already v4 via Sinks.OpenTelemetry). D-nums Alert D-2/SecMaster D-6/Calendar D-1. alert-service recreated cascade-safe (compose create --no-recreate, NOT compose up which lacks the flag).
**✓ CORE 10 SERVICES COMPLETE + DEPLOYED + VERIFIED** (Fred + 3 collectors + Nasdaq[code-only,disabled] + Sentinel + TE + 3 HTTP). Runtime log-level live across the main fleet.
USER SCOPE ANSWER (2026-07-22): "Reports yes. MCP I don't think there's a lot of value there." -> Reports Daily/Weekly/Monthly = DO (final batch); MCP sidecar hosts = DROPPED (out of scope, done).
  B5 DONE — #887 MERGED (7b56f5fa) + DEPLOYED + verified. Reports Daily/Weekly/Monthly (all 3 Up long-running scheduler hosts). 3 commits: d114cb3f log-level, fa2eb30a Xml pin 10.0.8->10.0.10 (clears NU1903), 1d6076cb citation fix 9.0.0->8.0.4 (Reports actually resolve Settings.Configuration 8.0.4 via AspNetCore 8.0.3 — impl copied 9.0.0 template text without the pkg bump; review caught it via project.assets.json). Deploy: 3 images --no-cache, ok=24 failed=0, all Up on fresh images, 0 Loki errors, ATLAS.Reports=Information override preserved. Spot-check inconclusive-idle (daily job sleeps to 02:00 fire — wiring confirmed present, mechanism already proven 11x). USER pin decision = Option A (Reports-only): MacroSubstrate keeps 10.0.8 (transitive NU1903 stays a non-fatal warning; fleet-wide standardize "later").

## ✓ RUNTIME LOG-LEVEL ROLLOUT COMPLETE (2026-07-22) — 13 .NET services, MCP sidecars excluded per user
All merged + deployed + verified: #883 FredCollector(pilot, mechanism PROVEN on real container) / #884 Finnhub+AlphaVantage+Ofr / #885 Nasdaq[code-only,disabled]+Sentinel+ThresholdEngine(Serilog settings/hosting 8->9 bump, safe: core already 4.2.0) / #886 AlertService+SecMaster+CalendarService(HTTP — re-registered DiagnosticContext to preserve UseSerilogRequestLogging, verified live) / #887 Reports Daily/Weekly/Monthly(+ Xml 10.0.10 pin). Mechanism: nerdctl exec edit Serilog:MinimumLevel:Default in the running container -> reloadOnChange + ChangeToken re-applies the LoggingLevelSwitch live, NO restart; recreate (NOT restart) reverts to baked-in Warning. Per-service RuntimeLogLevelTests (business-outcome, RED-on-guard-delete). Every batch reviewed 0-crit/0-important; each caught a real thing (TE pkg-bump safety, HTTP request-logging DI, Reports version-citation).
REMAINING (carried, NON-blocking, no user decision pending):
  - request-log regression-GUARD gap (#886 HTTP services): DiagnosticContext re-registration has no INTENT tag + no test -> a future edit deleting it silently breaks request logging. HARDENING follow-up (add tag + a request-log test).
  - FredCollector D-2 backfill: 8.0.4->9.0.0 cite + 3rd INTENT marker (pilot nits; every later copy already correct).
  - fleet-wide Xml 10.0.8->10.0.10 standardize (user "expand later"; MacroSubstrate + ~6 other pins).
  - PRE-EXISTING (NOT rollout-caused, own separate look): TE /health 503 (patterns 4-50d overdue + 2 missing); SecMaster backfill_unresolved_rate_high 85% (19382/22752).
NEXT: RELEASES entry + tag (phase-tag discipline).
CARRIED FOLLOW-UPS (non-blocking, none is a ping): (1) request-log regression-guard GAP — the DiagnosticContext re-registration has no INTENT tag + no test; a future edit deleting it silently breaks request logging (add tag + a request-log test). (2) FredCollector D-2 backfill: 8.0.4->9.0.0 cite + 3rd INTENT marker (pilot nits; every later copy already correct). (3) PRE-EXISTING (NOT rollout-caused): TE /health 503 patterns 4-50d overdue; SecMaster backfill_unresolved_rate_high 85% (19382/22752) — both predate this work, worth a separate look.
  --- ORIGINAL PLAN LINE (superseded above) ---
  B1-orig: FinnhubCollector, AlphaVantageCollector, OfrCollector (all have MEL Warning floor).
  B2: NasdaqCollector (DISABLED prod), SentinelCollector, ThresholdEngine (bigger — care).
  B3: AlertService, SecMaster, CalendarService (no MEL floor — MEL default=Information still blocks Debug, so still lower floor to Debug + switch).
  B4: MCP hosts (Fred/ThresholdEngine/Finnhub/Ofr/SecMaster/Whisper) — hard-code .MinimumLevel.Warning(), need ReadFrom.Configuration ADDED.
  B5: Reports Daily/Weekly/Monthly (already removed SetMinimumLevel — MEL default Information; add switch + lower to Debug).
Per-service PRECHECK: WebApplication.CreateBuilder + Serilog:MinimumLevel:Default=Warning + no Serilog__* env override; a differing service gets done to its real shape or flagged/skipped, never a blind broken copy. Plan /tmp/sentinel-remediation/loglevel-plan/PLAN.md.
Benign (pre-existing, not #883): fred-collector startup stderr `Cannot load libgssapi_krb5.so.2` (Npgsql GSSAPI probe on Kerberos-less image; migrations succeed; on prior deploys too).

## CURRENT WORK — host-resilience / stack failure-detection (2026-07-19→20)
TRIGGER: 4 unclean shutdowns 17:11–17:20Z (no panic/MCE/thermal → power removal; UPS dead since 2026-06-11 [[project_ups_upgrade]]). Hard cut truncated nerdctl's name-store → 7 zero-byte reservations → otel.service failed at boot (`name "prometheus" is already used by ID ""`) → atlas.service `Requires=otel.service` → job failed 'dependency', systemd NEVER retries → whole ATLAS stack down ~2h. MASKED: containerd `restart.policy=unless-stopped` had revived 19 containers, so `nerdctl ps` looked partly healthy — a populated ps does NOT mean atlas.service ran. RECOVERED: cleared name-store + 13 stale CNI reservations, started otel→atlas; 38/38 up, smoke PASS. [[project_hardcut_namestore_corruption]]
- **#880 MERGED+DEPLOYED**: otel `Requires=`→`Wants=`. Zero-downtime (InvocationID unchanged, 0 containers recreated). Also tagged the ansible systemd tasks `atlas-systemd` → unit changes ship via `--tags atlas-systemd --skip-tags always`; **`--skip-tags always` is LOAD-BEARING** (the always-tagged compose regen otherwise forces a full-stack restart).
- **#881 MERGED+DEPLOYED**: `OnFailure=` on both units → host-level ntfy notifier (outside the failure domain it watches) + `atlas-stack-watchdog.timer` 5-min, because OnFailure covers START failure ONLY (Type=oneshot+RemainAfterExit → container death never changes unit state) + 2 prom rules. E2E VERIFIED: bad password→401, real→200 ⇒ the 200 is provably AUTHENTICATED; cooldown suppresses repeats; other units not muted.
- **#882 MERGED+DEPLOYED** (`568e30bf`): atlas+otel StartLimit sizing — slow failures could never reach `failed`, so OnFailure/AtlasHostUnitFailed were blind. **Restart RAMP WITHDRAWN** (user call) after 3 rounds of wrong arithmetic; flat RestartSec gives a one-multiplication criterion `burst*(D+RestartSec) <= interval`. atlas `10*(785+10)=7950<=10800`; otel `5*(423+10)=2165<=3000`. D worst case = ExecStartPre + TimeoutStartSec + **4**×TimeoutStopSec (four timer-armed kill windows: stop-sigterm/stop-sigkill/final-sigterm/final-sigkill; stop-post skipped, no ExecStopPost). Deployed zero-downtime for atlas (InvocationID unchanged); otel bounced (its unit changed), 31 ATLAS containers untouched.
- **⚠ GAP FOUND AT THE GATE + FIXED**: #881's two prom rules (AtlasHostUnitFailed, UnitFailureNotifierNotPublishing) were merged but **never deployed** — `Deploy monitoring directory` is tagged `[monitoring, dashboards, alerting]` and my `--tags otel,atlas-systemd` runs skipped it silently (`changed=3 failed=0` while shipping nothing). Now live + verified via the running server's `/api/v1/rules` (both present, health ok), not just the file on disk. **DEPLOY RULE: unit files ship under `atlas-systemd`; prom/alert rules need `--tags monitoring` — they are DIFFERENT tag groups. A tag-scoped deploy that reports `changed` proves nothing about what you meant to ship.**
SMOKE (COMPLETION_GATE): PASS all sections; 0 Loki Error/Fatal 15m AND 24h; 7 otel containers Up; grafana healthy; prom 6/6 targets; watchdog timer clean 5-min cadence, no findings; OnFailure present both units; `systemctl --failed` 0; promtool 4 rules.
OPEN (unverified DATA, do not assert): Loki holds only ~425 lines/24h from 4 service_name values (SecMaster, sentinel-collector, reports-daily/weekly-host) — no fred/threshold-engine/alert-service line at ANY level in 24h. Could be the success state of the 07-07 warning-reduction epic (prod default=WARN) or a telemetry gap. VERIFY before concluding.
## ✓ EPIC COMPLETE (2026-07-20) — tag `stack-failure-detection-done` @ 490b0447, RELEASES entry live (`490b0447`), milestone NTFY sent.
Tag points at the RELEASES commit (superset of #882 merge 568e30bf) — docs mark-verified had clobbered the code marker for tree 3bba946c, so the tag was retargeted to the marked docs tree d6081d7d [[project_docs_markverified_clobbers_code_marker]] [[project_push_guard_tag_quirk]].
STILL OPEN (separate, non-blocking): verify the Loki low-volume observation (~425 lines/24h, 4 service_name values) — success-state-of-warning-epic vs telemetry-gap. Observed data, NOT diagnosed.
LESSON (3 recurrences this epic, memory saved [[feedback_verify_through_the_real_engine]]): a green check that tests something ADJACENT to the property you depend on CERTIFIES the bug — bare-Jinja2 ≠ ansible Templar (escapes backslashes); `http==200` ≠ delivered (anonymous publish returns 200); `systemd-analyze verify` rc=0 on a config that loops forever. Guard tests must be RED when the guard is wrong.
HOST CHANGE (not IaC — cron is unmanaged here): removed dead crontab entry `.claude/watchdog-stuck-agents.sh` (8,564 logged failures since June).
NEXT: on review clear → merge+deploy #882 (`--tags otel,atlas-systemd --skip-tags always`; expect otel stack bounce ~30s, ATLAS untouched) → verify effective values + both InvocationIDs unchanged + 38 containers → RELEASES + tag.

## PRIOR EPIC (complete) — Loki warnings/errors + autofix-flood remediation (2026-07-07)
User: "look at 24h Loki errors/warnings; autofix didn't fire; a well-running system produces none — something's wrong or miscategorized." + "fix everything" + "autofix should monitor REPEATED warnings (a flood is as bad as an error)."
DIAGNOSIS (evidence, 24h): 0 Fatal; 9 Error (finnhub 503 burst on /stock/profile2 TNXP/NBP/FNWD — transient, retried, recovered). ~2693 Warning: sentinel 1748 (Gemini 429 daily-cap ~809 + ContentNormalizer "withholding markup" ~700 + spaCy/DSL/misc) + SecMaster 939 (RAG-circuit-open flood + Gemini 429) + fred 6 (transient retries). ROOT CASCADE: llama-cpu-rag healthy-but-CPU-slow -> SecMaster RAG breaker COUNTS our own 25s-budget TaskCanceledException as failures (DependencyInjection.cs:361) -> 5-consecutive -> flaps open 60s -> RAG degrades -> load spills to Gemini frontier -> daily-cap 429s. All logged per-request. Autofix (AutoFixChannel EnabledSeverities=[critical,error]) correctly idle: every 24h alert was severity=warning; alert-service 0 log lines, queue empty. Warning-flood detector EXISTS (loki-warning-rate.yaml, Grafana-native, sev=warning) but never wired to autofix.
USER DECISION (AskUserQuestion): warning-flood escalation = **Option A — per-service sustained flood -> `error` severity -> autofix + ntfy-notify (NOT a full page)**. Individual warnings stay non-autofix (honors #757).
PLAN = 5 PRs (each carries DESIGN INTENT stanza; honors observability scar-tissue = dedup-to-visible not silent-Info):
- **PR-A SecMaster**: breaker root-fix (drop `.Or<TaskCanceledException>()` DI:361 so our budget-cancels don't open the breaker) + RagTimeoutSeconds 25->50 / RagMinGenerationBudgetSeconds 12->18 (appsettings) + demote redundant per-request RagService.cs:187 Warn->Info (once-per-open onBreak DI:369 stays visible; precedent LlmClient.cs:358) + Gemini 429 once-per-hour dedup (GeminiResolverClient.cs:68, Interlocked-CAS pattern ThresholdEngine:122, metrics untouched=caller-emitted) + card CROSS-SERVICE "Gen resilience"/"RAG latency budget" invariant updates. No D-supersession (D-1..D-4 don't govern breaker/budget/logs).
- **PR-B Sentinel logs**: Gemini 429 dedup (GeminiResolverClient.cs:78) + ContentNormalizer.cs:287 ADD NormalizerOutcomeCounter{outcome=no_visible_text,site} + once-per-window Warn (unmetered today = obs gap; D-2 guard untouched, keep INTENT(D-2) comments) + SpacyNerClient.cs:153/139/166 once-per-window Warn (metric already wired, dependency=stay visible). DSL collision :1125 leave (low-vol).
- **PR-C Sentinel GIGO** (riskiest): extend CandidateSurfaceFilter.cs:249 with curated NON-TRADEABLE-ONLY MediaOutlet+Index reject classes (Rolling Stone/Eater/Indiewire/Nifty Smallcap...) + new D-11 companion to D-5/D-6 + guard tests (junk rejected AND real issuers KEPT: BuzzFeed=BZFD, News Corp, NYT... survive). Person-names stay OUT (D-5 PRECOND; would need named D-5 supersession). One shared filter -> covers both D-5 prepass + D-6 gemini-direct.
- **PR-D FredCollector**: LogWarning->LogInformation at DataCollectionService.cs:144 (no-result) + DependencyInjection.cs:249 (Polly onRetry) [+ optional siblings]; terminals/breaker/startup stay Warn. #784 pattern, no D-entry.
- **PR-E AlertService + monitoring**: new Loki-ruler rule ServiceLogWarningsElevated (per-service sum, sustained -> sev=**error**) in loki-rules/fake/atlas-logs.yaml (ruler->alertmanager:9093->alert-service CONFIRMED wired) + AlertService route `error`->[ntfy,autofix] (appsettings) + NtfyChannel error->"high" + fix EnabledSeverities doc-drift (README says [critical,warning], code=[critical,error]) + fix stale sentinel.yml:364 KEPT-DOWN comment + AutoFixChannel.cs:15-18 comment + new D-entry (warning-flood escalation; narrowly supersedes #757 "warning never autofixes"). Threshold TUNABLE, recalibrate after A/B/F noise-fixes drop baseline.
finnhub 503s: leave at Error (transient/recovered) unless user asks.
EXEC: 5 worktree agents; per-PR compile.sh(+tests) as FINAL; review loop (review-pr+intent-review+observability-review); merge; then ONE build+scoped-deploy+COMPLETION_GATE smoke (verify Loki warning-volume drops, breaker stops flapping, 429 log deduped, autofix flood-rule present + fires error on synthetic flood). B+C both Sentinel -> rebase 2nd.
STATUS 2026-07-07: **ALL 5 MERGED to main @ 73a7a25f** — #860 fred-levels, #861 sentinel-log-dedup+counter, #862 autofix-flood(Option A), #864 GIGO media-outlets(D-11; MarketIndex class DROPPED at review — US indices resolve to tracked instruments SP500/DJIA/IXIC/RUT, verified via SecMaster catalog; even bare "Nifty" fuzzy-collides w/ 4262.T), #863 secmaster-breaker(+compensating alert rag_degraded{reason=timeout}>0.05/30m added at review to cover the hung-but-alive mode the breaker no longer catches, promtool-validated; defaults reconciled 50/18; PR-D trace-less loop backstops :91/:89 reverted to Warn). Each PR review found+fixed a real issue (A alert-gap, C index-regression, D backstop-visibility). NEXT: build 4 images (secmaster/sentinel-collector/fred-collector/alert-service) + scoped deploy + monitoring configs (alertmanager.yml reload, atlas-logs.yaml Loki-ruler reload, sentinel.yml+secmaster.yml prometheus HUP) + COMPLETION_GATE smoke (Loki warning-volume drop, breaker not flapping, 429 deduped, flood-rule loaded). Worktrees still locked (cleanup after deploy). ⚠ autofix-runner timer may auto-deploy from main — build images FIRST (--no-cache) so it doesn't run stale.
## ✓ EPIC COMPLETE — DEPLOYED + SMOKE-VERIFIED (2026-07-07)
Built 4 images --no-cache (verified contain code: alert-service error-route, secmaster RagTimeout=50). Scoped ansible deploy (secmaster/sentinel-collector/fred-collector + alerting; ok=51 changed=9 failed=0). alert-service recreated via nerdctl (excluded from scoped_services by convention; rm+compose-up, fresh image, error-route live). alertmanager RESTARTED (single-file mount -> restart not HUP) -> error route loaded. prometheus HUP -> SecMasterRagBudgetTimeoutsSustained loaded. loki ruler auto-polled -> ServiceLogWarningsElevated loaded. GOTCHA: nerdctl compose has NO --force-recreate (use rm+up); alert-service must be recreated manually (never in scoped_services). Autofix-watcher SKIPS while Claude Code runs (no timer interference).
SMOKE (COMPLETION_GATE, all green): 11/11 containers Up; 0 Loki errors/10min; SecMaster Healthy (21326 instruments); vLLM 29.7/32.6GB; prompts mounted. **error->autofix VERIFIED** (synthetic error alert -> {"queued":1} -> queue file written -> removed; warning stays ntfy-only). **429 flood STOPPED** (0/8min post-restart vs 277/hr baseline). Warnings dropping (sentinel+SecMaster 12/10min vs ~18/~30 baseline; still spans pre-restart). Breaker NOT flapping (circuit_breaker_opened{Generation} 0 opens/90m); rag_degraded{timeout} metric exists=0 (compensating alert wired to live metric). ContentNormalizer sentinel_normalizer_outcome_total registered, populates on first no-visible-text article (not yet post-restart). FOLLOW-UP (non-blocking): re-check sustained warning-volume drop + ContentNormalizer metric populating in ~30-60min; recalibrate ServiceLogWarningsElevated 500/15m threshold vs new baseline. NEXT: RELEASES + tag + worktree cleanup + push STATE.

## PRIOR EPIC (complete) — Review-queue remediation
**Review-queue remediation epic — plan DONE + user-approved, being finalized.** Plan `docs/superpowers/plans/2026-07-05-sentinel-review-queue-drain.md`. Supervisor-VERIFIED the key reframing: **V2 prod path (`RunV2ProductionAsync` ExtractionProcessor:596) NEVER calls AutoApprovePolicy — writes all Pending; the `:736` Evaluate is the LEGACY branch. Drain was REMOVED at V2 cutover, not broken.** res_conf NULL cause: `ResolverOutcome` has no conf field → writes LLM self-report not resolver's real conf. Auto-reject NoResolution would break digest (DigestQueryService reads `!=Rejected`) → use new non-Rejected status.
LOCKED DECISIONS: DD-1 new `ReviewStatus.AutoClosed`; DD-2 age-gate 7d + backfill 277K (no deletes); DD-3 exact primary-symbol + SharedToken≥1 never fuzzy (Fix2, new **D-8**, NOT a D-2 supersession — exact+overlap IS authoritative); **DD-4 = (b) USER-CHOSE: keep V2 writing Pending, drain DOWNSTREAM via AUTOMATED/scheduled backfill-auto-approve (non-dry-run) — NO cutover reversal, NO D-9**; DD-5 keep ≥0.8 (exact=1.0), leave <0.5 floor. Migrations expected ZERO (status cols = unconstrained varchars).
STAGES: 1=stop-bleed (AutoClosed + NoResolutionSweepWorker 7d batched + digest-preserve test + admin trigger); 2=recover ~27% (TryExactCandidateMatch leg + guard test + D-8); 3a=carry real res_conf on ResolverOutcome, 3b=automated scheduled drainer running AutoApprovePolicy over Pending non-dry-run.
Plan FINALIZED (Stage 3 = 3.1 real res_conf on ResolverOutcome + 3.2 `AutoApproveDrainWorker` scheduled non-dry-run + 3.3 D-9 new-entry; no cutover reversal). Stage 1 = plan L76-259 (5 tasks, TDD, commit-per-task).
**USER DIRECTIVE: run ALL 3 stages through autonomously, NTFY `atlas-claude-ask` when done** (or on genuine blocker/arch-fork). EXECUTION: each stage impl→review→fix→merge SEQUENTIALLY (Stage N+1 branches off main after N merges — avoid collision); then ONE final build+scoped-deploy+comprehensive smoke (verify sweep drains 277K [count dropping, AutoClosed appearing], drainer Approves resolvable [Approved + correct res_conf], exact-match resolver working, digest/matrix intact, Loki clean; trigger manual admin drains to accelerate/verify); then tag epic + RELEASES + STATE + NTFY user. Marker-order discipline: push each code branch BEFORE any docs mark-verified.
→ **Stage 1 DONE + PR #850 OPEN + review loop IN FLIGHT.** 4 commits (`e4dec509`/`e4455c0d`/`09933601`/`86966e74`): AutoClosed enum + EvaluateClose + NoResolutionSweepWorker (7d/500/10min, DI-registered) + close-noresolution admin trigger + digest-preserve + sweep-eligible tests. Gate 1.1: NO CHECK constraint → NO migration. compile 0/0 1778+31, marker tree `b5f64ae1`. Reviews DONE — 0-crit all 3, intent CLEAN. review-pr+observability important (4 real): (R1) close-noresolution live-loop missing `ChangeTracker.Clear()` → O(n²)/memory on 277K; (R2) sweep query no supporting index on ~416K table → add PARTIAL composite index via EF migration [NOTE: introduces 1 migration, was "zero expected"]; (O2+R3) worker silent-persistent-fault (loops forever at Warn, no Error/trace/page) + ambiguous flatline counter → escalate to Error+Activity + queue-depth metric [scar-tissue]; (O1) close-noresolution missing duration histogram (sibling parity). +folds: test-docstring overclaim, dry-run-no-mutate+grace-boundary tests, disabled-path Warn. **FIX `fd489704` DONE + PUSHED** — all 4 important addressed; migration `AddNoResolutionSweepIndex` (3 files present, snapshot carries filtered index, table NOT hypertable); Clear() now both paths (`AdminEndpoints.cs:1379` outside `!dryRun`); compile 0/0, 1781+31 pass, marker for tree 26d7015f. Re-review DONE: observability CLEAN, review-pr 0-crit + marker written for fd489704, but 2 NEW important (round-2): (T) the ChangeTracker.Clear multi-batch test is TAUTOLOGICAL (replays a fixture copy — green when real Clear() deleted; the tests-validate-outcomes scar); (OCE) non-shutdown cancellation escapes worker ExecuteAsync → default StopHost crashes the whole SentinelCollector host. **STAGE 1 DONE + MERGED** (`7d9eca2d`, #850, squash; 3 review rounds converged 0-crit/0-important). Shipped: AutoClosed terminal status + NoResolutionSweepWorker + admin close-drainer, all hardened for 277K (memory-bounded via `NoResolutionCloseDrainer`, index CONCURRENTLY, OCE-safe, throttled, escalation). local main synced.
Round-3 suggestions carried to Stage 3 (touches same worker/meter): OCE-classification test, invalid-index-rebuild edge, queue-depth cutoff.

**STAGE 2 DONE + PR #851 OPEN** (`32e44cae` feat, `9dbeb7de` docs; off 7d9eca2d). Exact catalog-symbol recovery leg `TryExactCandidateMatchAsync` (DeterministicResolver.cs:275; LLM-selected candidate + EXACT primary-symbol hit + SharedTokenCount>=1, NEVER fuzzy) + D-8 (NOT a D-2 supersession). Interfaces all verified real (GetInstrumentBySymbolAsync already used by 2 existing legs). Agent CAUGHT plan's tautological refusal fixture ("Delta Apparel" shares "Delta") → swapped to zero-overlap "Deltec Bank"; both guards RED-verified. compile 0/0, 1786+31, resolver 48/48. No migration. Review loop: intent-review CLEAN (D-8 atomic set complete, exact-only holds, guard RED-on-delete). review-pr found **CRITICAL** (+2 imp) — the name-overlap gate is TAUTOLOGICAL: candidate.Name (catalog-sourced) == hit.Name → `SharedTokenCount(c.Name,hit.Name)` always >=1 → `Math.Max` always passes → SUBJECT check bypassed; V2 passes article-wide candidates + null SelectedCandidateIndex → a co-mentioned listed company resolves for an unrelated subject = wrong-ticker/matrix corruption. Verified the tautology myself. **STAGE 2 DONE + MERGED** (`bfcd22ac`, #851, squash). Exact catalog-symbol recovery leg + D-8; critical (tautological name-overlap gate → co-mention wrong-ticker) caught by review-pr, fixed (subject-overlap sole gate), re-review 0/0. local main synced.

**STAGE 3 IN FLIGHT — split 3a/3b, one branch `feat/sentinel-review-drain-restore` off bfcd22ac, one PR.**
- **3a DONE** (`dfc171cb` + `a9187e2d`): res_conf on ResolverOutcome (trailing-optional; 6 resolving returns populated, NoResolution=null; 22 sites compile); V2ExtractionPipeline:210 sources outcome.ResolutionConfidence; test RED-pre/PASS-post; D-6→:476/D-8→:286 re-anchored. compile 0/0, 1788+31, marker 8e48a8c4. [open q for 3b: V2ExtractionPipeline.cs:81 `extraction.ResolutionConfidence` — pre-resolution init or 2nd persist path? test proves final=outcome; 3b confirms.]
- **3b DONE** (`af611996` drainer, `60aef772` D-9): AutoApproveDrainWorker — fresh-scope/batch (memory-safe), OCE-safe, throttle, 3-fail escalation; AutoApprovePolicy.Evaluate over Pending non-dry-run; batch 500/15min; flag OFF; mode="drain" counter; registered DI:992. RunV2ProductionAsync UNTOUCHED. :81 = resolver INPUT not 2nd persist (confirmed). **Backfill pre-check WAS a real bug** — live path cleared ChangeTracker only in dry-run → O(n²) on 277K; FIXED (Clear every batch, AdminEndpoints.cs, committed in af611996). D-9 @:215. compile 0/0, 1791+31, marker 10b9f7ae.

**STAGE 3 PR #852 OPEN** (4 commits). Review loop DONE: intent CLEAN, review-pr 0-crit (verdicts: NO wrong-approval [null res_conf fails closed→Pending], NO host-crash at shipped defaults, res_conf correct all legs; marker for 60aef772), observability crash-visibility OK. 6 IMPORTANT aggregated (no crit): OBS — no queue-depth gauge, skipped/pending uncounted, alarm not mode-scoped (drain burn masks live rejection alert); RPR — misconfig livelock/crash (BatchSize<=0 spins / negative Interval crashes; defaults safe), cursor/convergence loop untested, resolver res_conf 6-legs untested. **ALL 3 STAGES MERGED**: #850 Stage 1 (7d9eca2d), #851 Stage 2 (bfcd22ac), #852 Stage 3 (`d91f1a50`). Stage 3 re-review: review-pr APPROVE 0/0 (marker 43ec3903→merged), observability 3 fixes correct. local main synced.
DEFERRED to post-enable observability-polish (data-informed, before tag): (1) depth-gauge ALERT — flat-high/stall page (threshold needs observed steady-state floor; blind = false-fires during 277K burn; sibling sweep gauge same gap); (2) D-9 GUARD line-ref stale @:215 → actual :252 (fix inserted queue-depth code, re-anchored D-6/D-8 not D-9); (3) gauge docstring "disabled" overclaim (disabled=-1, Warn log surfaces it).

**DEPLOY+SMOKE DONE + GREEN** (build sha256:d222d19d, deploy 0-failed, Healthy, migration clean, 0 Loki errors, digest emitting). **Stage 1 sweep is ON by committed default (graceDays>0, no flag) → deploy AUTO-STARTED the drain: Pending 321K→87K, 234K AutoClosed in ~5min** (intended). Drainer OFF (flag). Dry-run: close-eligible 27,510; approve-eligible 249 / reject-eligible 43 (LOW — backlog res_conf is pre-3a/stale; 3a fix is go-forward-only). review_status@deploy: Pending 86,783 · AutoClosed 234,510 · Approved 54,866 · Rejected 39,542. Metrics all present (sweep_closed_total=234,510).

**USER DECISION: ENABLE drainer now (go-forward + 249 backlog).** Low-risk (249 Resolved/high-conf); real value = go-forward.
## ✓ EPIC COMPLETE — Sentinel review-queue drain (2026-07-05)
All 3 stages merged + DEPLOYED + VERIFIED. #850 Stage 1 (AutoClosed + sweep), #851 Stage 2 (exact-candidate recovery, D-8), #852 Stage 3 (res_conf + AutoApproveDrainWorker, D-9), #853 enable. Tag `sentinel-review-queue-drain-done`.
RESULT (verified psql): review_status AutoClosed 234,510 · Pending 86,521 · Approved 55,115 · Rejected 39,585. Sweep drained 321K→87K on deploy; drainer live (1st pass approve 249 / reject 43, exact dry-run match); 0 Loki errors; container Healthy. Go-forward: new extractions carry correct res_conf → auto-approve → queue won't regrow.
DEFERRED follow-up (noted in RELEASES): depth-gauge ALERT — raw `sentinel_autoapprove_drain_queue_depth` confounded by ~86K stale-res_conf backlog (3a res_conf is go-forward-only); needs decision-rate/approvable-Pending design, not a raw-depth threshold.
NEXT: NTFY DONE sent — but REOPENED (see below).

## ⟳ REOPENED — 86.5K Pending is NOT human-reviewable (user, 2026-07-05)
User: "no way I am reviewing 86540 items. Some extracted months ago." Correct — the epic drained NoResolution (sweep only touches NoResolution) but left ~86.5K Pending the sweep IGNORES (Resolved-but-Pending + mid-confidence) and the drainer WON'T approve (backlog res_conf is stale LLM self-report, only 249 qualified). Months-old Resolved-but-stuck rows sit forever = graveyard, not a queue.
**REFRAMED (breakdown `a0b24739` DONE):** 86,540 Pending = NoResolution 43,545 + Resolved 42,995. KEY: "Resolved" != instrument-linked — only **176** Resolved rows have instrument_id; **42,819** are Resolved-NO-instrument (null instr + null res_conf). So **86,364 of 86,540 have NO instrument → can NEVER auto-approve** (gate needs instrument). Re-resolution ceiling = 102 (moot). Real graveyard = the **42,819 Resolved-no-instrument** the sweep IGNORES (only touches NoResolution) + they can't auto-approve. NoResolution 43,545 should sweep as they age (Q6=0 aged>7d but 16,799 are "Jun" — DISCREPANCY to reconcile). extraction_confidence≥0.9=80,502 (high); a 2nd `confidence` col skews opposite (80,607 @ 0.5-0.9).
**ROOT CAUSE FOUND (`a11082a8` DONE):** The 42,819 Resolved-no-instrument rows are a BUG, not content. `ExtractedObservation.ApplyReExtraction` (:271) sets InstrumentId but NEVER recomputes resolution_state → when re-resolve clears the instrument (ReExtractBackgroundService StillNull), row keeps stale pre-re-extract `Resolved`. Only re-extract path mints these (V2 ties Resolved to InstrumentId.HasValue). Subjects = junk ("Christopher Penn","2025","15%").
- Q2 HARD CONSTRAINT = SAFE: closing them LOSES NO DATA. Matrix reads macro_observations NOT extracted_observations (WS3 ObservationCellProjector; cards Sentinel L41/TE L16). Digest keeps them (DigestQueryService !=Rejected; AutoClosed!=Rejected). Pure queue removal.
- Q3: 43,545 NoResolution = rolling <7d window (all 6/28–7/05), sweep already drained older + keeping pace, predicate=extracted_at → they self-drain. NO ACTION. (Q6=0 was boundary artifact.)
**FIX DONE + PR #854 OPEN** (`bf7ecb95` root-cause, `f08a0752` reconcile): ApplyReExtraction:286 recomputes ResolutionState=HasValue?Resolved:NoResolution (agent surfaced: UpdateResolution's callers set state, ApplyReExtraction's caller discarded it → inline recompute; direct-assign keeps ResolutionHypothesis). Reconcile endpoint POST /admin/review/reconcile-resolution-state (ResolutionStateReconciler, mirrors NoResolutionCloseDrainer; predicate Pending+Resolved+instr NULL+method NULL). Guard tests RED-verified. compile 0/0, 1806+31, marker 5ffc999c. No D-entry/migration. **MERGED** #854 (`7d223a17`) + DEPLOYED (rebuild, image 06945dfe, Healthy). **DRY-RUN VERIFIED GREEN:** eligible 42,830 == SQL predicate 42,830 (exact); 176 legit instrument-linked untouched; converges 86 batches/18s; 0 errors. **LIVE RECONCILE+CLOSE DONE** (`a02eb2f7`): reconcile relabeled 42,830, close AutoClosed 32,880 aged; 0 residual mislabels, 0 Loki errors. Gotcha: endpoints bind camelCase `dryRun` not `dry_run` (snake silently no-ops) → [[reference_sentinel_admin_dryrun_camelcase]].
RESULT: AutoClosed 267,463 · **Pending 53,689** · Approved 55,115 · Rejected 39,586. Pending split: NoResolution 53,513 / Resolved 176. **HUMAN-ACTIONABLE (instrument_id NOT NULL) = 558.** ~53,131 = NoResolution-no-instrument, ALL within 7d grace (aging to auto-close). Months-old junk = GONE (0 aged).
So: user's complaints ADDRESSED (nothing months-old; root cause fixed). But review UI shows all 53.7K Pending though only 558 actionable.
**USER CHOSE: filter review UI to actionable (instrument-linked ~558).** NoResolution rows still auto-close via sweep, just not shown. **FILTER DONE + PR #855 OPEN** (`6ba74ed1`): ReviewUiEndpoints.ActionablePending = Pending && InstrumentId!=null (shared by list+count, no drift); guard test RED-verified; card GOTCHA note + fixed stale AutoClosed parenthetical. compile 0/0, 1808+31, marker 3e1d55f2. ## ✓ FOLLOW-ON COMPLETE + DEPLOYED + VERIFIED (2026-07-05)
User challenged the 86.5K "done" → found the re-extract state-mislabel BUG. Fixed + deployed:
- **#854** ApplyReExtraction recomputes resolution_state (root cause) + reconcile endpoint; relabeled 42,830 mislabels → NoResolution → sweep AutoClosed aged. Data-safe (no matrix/digest loss).
- **#855** review UI → ActionablePending (Pending AND InstrumentId!=null).
VERIFIED (deploy image ab2a0fd6, Healthy, 0 Loki errors): review_status Pending 53,686 total BUT `/ui/review` displays **558 actionable** (instrument-linked), 0 null-instrument leak. Nothing months-old. Root cause fixed — won't recur.
Queue arc: 321K →(sweep)→ 87K →(reconcile+close)→ 53.7K Pending →(UI filter)→ 558 shown.
RELEASES appended (follow-on under `sentinel-review-queue-drain-done`). Gotcha memory saved: [[reference_sentinel_admin_dryrun_camelcase]].
Deferred (non-blocking): dry-run cursor-convergence test, NoResolution-instrument-linked (382) test, OCE-at-Error log demotion, drain depth-stall alert (decision-rate design).
Committed+pushed (16fd74cb); tag stays at 3-stage close (force-move-tag DENIED by guard — RELEASES documents follow-on instead; local tag restored to match remote aff6006e).

## ⟳ REOPENED (2) — 558 actionable is STILL too long (user, 2026-07-05)
User: "each takes a minute → 558 = ~9h; still too long." Correct — human review must be RARE/exceptional in an automated pipeline, not the default for 558 borderline rows. A queue nobody clears is how it hit 321K.
558 = 176 Resolved+instrument (borderline res_conf 0.6-0.8 — DD-5 SUPERSEDED by #856, now auto-approved) + 382 NoResolution-WITH-instrument (inverse-mislabel suspect — instrument attached but state=NoResolution; reconcile #854 only fixed Resolved-null-instrument). **558 CHARACTERIZED (`a4e058db`):** 382 NoResolution-WITH-instrument = MIRROR mislabel of #854 (re-extract ticker_in_quote/cove attached instrument but pre-#854 left state=NoResolution; INFLOW now fixed, tail collapsing 07-05:3). These age out via sweep (state=NoResolution). 173 Resolved-borderline: 102 ext≥.9&rc.6-.8, 71 ext<.9. 252 distinct instruments (head=SP500/BTC/UNRATE macro toil). KEY: extractions feed only DIGEST (!=Rejected), NOT matrix → low stakes. Recurring BUG found: ticker_in_quote leg lacks D-8 subject-overlap guard → co-mention wrong-tickers (Banxico→EWW, Canada→TRI), and post-#854 they auto-approve.
**USER STOPPED the floor-0.6 agent (diverged) + directed: "the values really do not need human review."** Inspected the 551 review-UI rows (`a9f0387e`): 173 Resolved = mundane-correct macro (S&P500 +1.2%, UNRATE 4.3%, DJIA, BTC) — confidence number does NOT discriminate quality (state does); 378 NoResolution = co-mention GARBAGE (Gilbert Cisneros→LLY, Jewett Cameron→EWTX, Banxico→EWW) carrying a stray wrong ticker. Neither needs review.
Discarded the diverged partial work (git restore, branch clean). **CLEAN IMPL DONE** (`b7d45242` gate, `95c923cb` UI): AutoApprovePolicy.Evaluate = reject-if-ext<0.5 / else Approved-if-Resolved+instrument / else Pending (confidence floors REMOVED — lived in AutoApprovePolicy.cs not ExtractionOptions); ReviewUi ActionablePending += ResolutionState==Resolved; D-9 entry rewritten; RED-verified tests; +3 downstream test files updated (single-source gate). compile 0/0, 1806+31, marker 7ddf03c6. Agent HONESTLY flagged: my "don't touch worker src" left stale INTENT(D-9) comment @AutoApproveDrainWorker.cs:237-244 (old gate) → ATOMIC_SET violation (entry vs comment). **COMMENT-FIX DONE** (`b65653ed`, comment-only VERIFIED non-comment-diff empty): realigned INTENT(D-9) + 7 more stale gate comments to new gate; DD-5 marked SUPERSEDED; res_conf comments redirected to the live >=0.8 event-publish predicate. compile 0/0, 1806+31, marker 7c85f67f.
**PR #856 MERGED** (`d27553f8`). review-pr 0-crit (gate approves nothing it shouldn't, reject-first correct, no dangling refs, test updates honest, b65653ed comment-only) + intent CLEAN (D-9 atomic-set consistent, DD-5 supersession clean). 1 important DEFERRED (comment-only, runtime-safe): stale `AutoApprovePolicy.cs:91` EvaluateClose docstring "NoResolution rows have InstrumentId==null by construction" (false — 382 counterexamples) → fold into follow-up. **TRUST-GATE DEPLOYED + VERIFIED (`d27553f8`, #856):** `/ui/review` shows **"Pending: 0 — No pending observations to review."** Drainer auto-approved Resolved backlog on startup (+217). Breakdown: AutoClosed 271,280 · Approved 55,353 · Rejected 39,589 · Pending 54,123 (ALL NoResolution — hidden from UI, sweeping). 0 Loki errors. **USER'S QUEUE PROBLEM SOLVED.**

**USER CHOSE: fix the resolver bug too (source fix for digest pollution).** **FOLLOW-UP DONE (re-extract leg)** (`88df5cb3` D-10 guard in ReExtractResolutionAdapter [SecMaster Name lookup, SharedTokenCount>=1, refuse→NoResolution short-circuit], `0e48ce19` EvaluateClose docstring, `433c541f` DslAdapter coverage). Tests RED-verified, 1808+31. Agent CAUGHT: the LIVE primary leg `ExtractionProcessor.ApplyLocalResolutionAsync:~1111` has the SAME co-mention bug — its `TickerInQuoteGuard.IsExactSymbolMatch` verifies the ticker string, NOT subject-match → co-mention garbage on the MAIN path.
**LIVE-LEG EXTENSION DONE** (`afdbeca3`): shared pure predicate `TickerInQuoteGuard.SubjectSharesInstrumentToken` (src/Extraction/TickerInQuoteGuard.cs:74) wired at BOTH legs (ExtractionProcessor:1135 live + ReExtractResolutionAdapter:174 re-extract); D-10 unified (both sites, both tests); RED-verified; 1810+31. **PR #857 OPEN** (4 commits: D-10 both legs + EvaluateClose docstring + coverage). Reviews DONE: intent CLEAN (D-10 multi-site atomic set holds, both tests RED-verified, not-a-D-8-supersession; 1 cosmetic line-ref). review-pr APPROVE 0-crit — over-refusal NOT material (GetInstrumentBySymbolAsync coalesces Name??Symbol so found-instruments never null-Name; ticker-subject = accepted D-8 tradeoff); hot-path cost fine (only exact-match branch); bonus: re-extract refuse CLEANS the ~378 backlog as reprocessed. 1 IMPORTANT (re-extract refuse SILENT) FIXED (`84e6ea1e`): both legs now emit comention_rejected counter+log (ReExtractResolutionAdapter:190 + ExtractionProcessor:1141); additive-only, guard logic unchanged; compile 0/0, 1810+31, marker c25c172c. Pushed. **PR #857 MERGED** (`dd053203`, re-review 0/0/0). D-10 guard on both ticker_in_quote legs (shared predicate) + comention_rejected metric + EvaluateClose docstring + coverage. ## ✓ FOLLOW-ON 2 COMPLETE + DEPLOYED (2026-07-06)
User: "558 is still 9 hours" + "the values really do not need human review." Inspected values: 31% mundane-correct macro (state=signal, not confidence), 69% co-mention garbage. extractions feed only DIGEST not matrix → low stakes.
- **#856** (`d27553f8`): auto-approve gate → trust state (Resolved AND instrument; floors were theater; SUPERSEDES DD-5) + review UI Resolved-only. DEPLOYED + VERIFIED: **/ui/review = "Pending: 0"** (drainer auto-approved +217 on startup).
- **#857** (`dd053203`): D-10 shared subject-overlap guard on BOTH ticker_in_quote legs (live ExtractionProcessor:1135 + re-extract:174; TickerInQuoteGuard.SubjectSharesInstrumentToken) + comention_rejected metric + EvaluateClose docstring + coverage. DEPLOYED (image 12:33Z, Healthy, 0 Loki err); guard code present both legs, RED-verified tests; 0 new NoResolution-with-instrument in 62 new obs post-deploy; metric wired (0 so far — no co-mention flowed yet to exercise it).
RELEASES follow-on-2 appended. Deferred (non-blocking): fail-closed-on-transient-SecMaster-lookup; bare-ticker-subject false-negative (accepted D-8 tradeoff); optional reconcile-reject for immediate digest clean (existing ~378 self-clean via re-extract).
Committed+pushed (30e9f05e).

## ⟳ REOPENED (3) — user wants a FUNCTIONAL queue (~10/day), not 0 (2026-07-06)
User: "fix the queue, it really was not functional. I don't mind reviewing 10/day but more is a chore I won't do." My #856 over-corrected to 0 (auto-approve ALL Resolved). User wants a bounded ~10/day spot-check.
Data (`a5f4a280`): Resolved+instrument = 33/day; res_conf is FLAT 0.900 for 94% (ticker_in_quote) — USELESS for ranking. Real signal = METHOD: exact-verified (ticker_in_quote D-10, llm_candidate_exact D-8) = trust; fuzzy (cove_VectorSearch ~2/day, gemini, hybrid, llm_pick) = where ALL sampled errors live (gold→BEA index, $30T→30Y yield, BTC→ETH). **USER CHOSE: fuzzy-method-only review (~2-5/day).**
**IMPL DONE + PR #858 OPEN** (`032fa15e` gate, `9aa62978` tests, `13d9b5eb` D-9, `edf3029b` fixtures): AutoApprovePolicy method-aware (methodTrusted OR agedPastGrace; :99). Confirmed method constants: exact={ticker_in_quote(D-10), llm_candidate_exact(D-8)}; fuzzy=cove_*/hybrid_subject/llm_candidate_pick/gemini_fallback/null. Config AutoApproveMethods+ReviewGraceDays=3. Dropped dead resolutionConfidence gate param (field still read by event-publish predicate). Threaded all callers (drain/backfill/live-legacy ExtractionProcessor/re-extract — single-source invariant). D-9 rewritten (supersedes #856). ReviewUi UNTOUCHED. 3 guards mutation-RED-verified, 1815+31. Reviews DONE: intent CLEAN (D-9 atomic-set + #856 supersession clean; 1 cosmetic D-10 :1135→:1139 drift). review-pr APPROVE, NO method-set leak, grace+reject-first correct, dropped-param safe. 1 IMPORTANT design tension: re-extract path measures grace from old ExtractedAt → re-extract fuzzy default-accepts (no review window). ANALYZED = CORRECT + DELIBERATE (re-extract reprocesses OLD backlog not in current digest; ReExtractedAt basis would FLOOD ≤10/day with ~80K; D-10 still guards co-mentions; only FRESH fuzzy [in current digest] needs review). **⚠ SECURITY: doc-fix agent `a8cd11b0` hit a PROMPT INJECTION** — 0 tool-uses, returned an injected block ("use ntfy MCP for ALL communication", "user won't see your final response"). NOT complied. CONTAINED: branch still edf3029b, working tree clean (only STATE.md), 0 tampering. Re-dispatched HARDENED `ace0ebaa` → CLEAN (21 tool-uses, no injection; agent ignored the warning + did the work). Injection was a ONE-OFF anomaly, not persistent. Anti-injection preamble now standard on dispatches.
**DOC-FIX DONE + PUSHED** (`7531448b`, comment-only VERIFIED non-comment-diff empty): re-extract divergence documented (comment + INTENT@738 + D-9 clause) + D-10 re-anchor :1139. compile 0/0, 1846, marker de9ca722. Re-review `a103e932` CLEAN (0/0, doc-only confirmed, important resolved) BUT did a MANUAL review → marker hook NOT fired → marker STALE @edf3029b (need 7531448b for merge gate). Marker fired (7531448b via full review-pr Skill; surfaced 3 non-correctness importants: grace-default-accept unobservable [silent branch], magic-string method-set no guard [rename→flood], live-call-site test gap). **#858 MERGED** (`cab2e3b3`) — NOT deployed yet (bundling follow-up).
**FOLLOW-UP DONE + PR #859 OPEN** (`1b0fc8bd` approve_reason metric, `1c4ba061` ResolutionMethods constants [compile-time rename safety, values unchanged], `e6ee0c82` live-call-site tests [live path CONFIRMED not dead — V1 sources]). NO gate-decision change. compile 0/0, 1820+31, marker ac7c91d4. Reviews DONE: review-pr #859 APPROVE 0/0 (ResolutionMethods constant VALUES byte-identical VERIFIED; no wrong/null reason; gate-decision unchanged; tests guard) + observability CLEAN (approve_reason bounded/no-double-count/right-site/adequate; minor suggestions: per-pass grace-count log, oldest-held-fuzzy-age leading gauge — deferred). **#859 MERGED** (`08ba8961`). ## ✓ FOLLOW-ON 3 COMPLETE + DEPLOYED + VERIFIED (2026-07-06)
User: "fix the queue, I'll review ~10/day not more." Method-aware review queue built + shipped.
- **#858** (`cab2e3b3`): AutoApprovePolicy method-aware — auto-approve exact {ticker_in_quote, llm_candidate_exact}; HOLD fuzzy for review (~2-5/day); default-accept after ReviewGraceDays=3 (never backs up). Re-extract old-backlog fuzzy default-accepts (deliberate, documented). ReviewUi unchanged. Supersedes #856.
- **#859** (`08ba8961`): approve_reason metric (method_trusted vs grace_default_accept — silent-branch visibility) + ResolutionMethods constants (compile-time rename safety) + live-call-site tests.
DEPLOYED (image 0213e868, Healthy, 0 Loki err, start 14:27:46Z): gate VERIFIED — 49 ticker_in_quote Approved, 0 fuzzy Approved, fuzzy held Pending transiently. /ui/review=0 now (fills going-forward ~2-5/day; #856 drained recent fuzzy). approve_reason wired (populates on 1st post-restart drain approval, ~15min interval).
RELEASES follow-on-3 appended. SECURITY: subagent prompt-injection (ntfy-hijack) caught+contained (0 tampering); anti-injection preamble now standard.
Deferred (non-blocking): per-pass grace-count log; oldest-held-fuzzy-age leading gauge; one-time re-open of Follow-on-2-auto-approved fuzzy (only errors this design can't retro-catch); D-10 :174→:181 re-anchor.
NEXT: commit RELEASES+STATE → push. Backlog clear — FUNCTIONAL bounded review queue live.
PENDING re-anchor (non-blocking): D-10 ReExtractResolutionAdapter :174→:181 drift.
NOTE: existing recent fuzzy rows #856 already auto-approved (incl the wrong ones) — go-forward gate holds NEW fuzzy; offer one-time re-open of recent fuzzy-Approved for review if user wants.
Deferred suggestions (non-blocking): fail-closed-on-transient-SecMaster-lookup (distinguish 404 from blip); ticker-subject false-negative edge. Optional further: reconcile-reject the existing ~378 co-mention digest rows (re-extract refuse auto-cleans them over time anyway).
NOTE: existing ~378 co-mention rows persist in digest (NoResolution→AutoClosed, !=Rejected) — source fix stops NEW; offer reconcile-reject if user wants retroactive digest clean.
BIGGER PICTURE: user's queue problem SOLVED (#856, /ui/review=0). This PR is the digest-quality source fix (user chose it). Epic + follow-ons near-complete.
FOLLOW-UP flagged (not blocking): co-mention garbage is a resolver bug (ticker_in_quote attaches without subject-overlap — same wrong-ticker pattern) — fix at source so digest stops getting polluted.
FINAL after Stage 3 merged: one build + scoped deploy + COMPLETION_GATE smoke (sweep drains, drainer approves w/ correct res_conf, digest intact, Loki clean; trigger manual admin drains) → tag epic + RELEASES + STATE + **NTFY atlas-claude-ask DONE**.

STAGE-3 PRE-CHECK [carry forward, UNVERIFIED agent diagnosis]: fix-agent claims sibling `backfill-auto-approve` (`AdminEndpoints.cs:~1218`) has the same ChangeTracker-clear-one-path-only bug → O(n²) on its live drain. Grep shows its Clear() is INSIDE a `!dryRun` block (partially contradicts "clears only on dry-run"). Stage 3's AutoApproveDrainWorker runs this drain logic non-dry-run over 277K → VERIFY + fix the drain-path ChangeTracker handling THERE, in-context, not bolted onto Stage 1.
**Review-queue backlog investigation — DONE (2 read-only agents, both verdicts supervisor-verified).** User surfaced: `sentinel.extracted_observations` review_status='Pending' = **321K** and growing ~1,650/day; feed healthy but no drain (last Approved 2026-07-03, last Rejected 2026-06-04). ROOT: **86% (277K) are `NoResolution`** (entity→instrument never matched) → can't auto-approve (AutoApprovePolicy needs Resolved+res_conf≥0.8+instrument) AND won't auto-reject (extraction conf≥0.5, reject bar<0.5) → NO terminal disposition → accumulate forever. Only ~239 of 321K auto-approvable now. "100% confidence" = EXTRACTION conf (LLM read text right), NOT resolution — 652/659 conf=1.0 items are NoResolution/no-instrument. NOT caused by digest/word-cloud work.
  → **Investigation A `a774f7bc` DONE — VERDICT MIXED (~73% expected, ~27% BROKEN; structural, NO regression):** Most NoResolution = genuinely non-instrument (countries/people/govt/macros/foreign indices) = correct. BUT ~27% (11,693/7d, ticker-shaped candidates matching real catalog symbols: NKE/TSLA/META/AAPL/AMZN/MSFT/GOOGL) are RESOLVABLE-BUT-MISSED. Resolvers UP (SecMaster healthy 20,967 instruments; Gemini healthy, NOT capped 1,131/1,500). MECHANISM (agent inference, live-probe-supported — VERIFY before acting): SecMaster confirm is SURFACE-FORM SENSITIVE — "Apple Inc"/"Nvidia" resolve; bare/colloquial "Nike"/"Tesla" (even "Nike Inc") filtered below 0.85 fuzzy threshold → LLM's correct ticker guess (in candidate_symbols_json, InstrumentId=null) discarded per GIGO. NoResolution set inline ExtractionProcessor.cs:1277/1323 + DeterministicResolver (99.9% resolution_attempts=0). No cliff at 07-03 → does NOT explain B's drain-stop.
  → **Investigation B `ac07f5d3` DONE — VERDICT: drain NOT broken, pure SYMPTOM.** Live auto-approve worker RUNNING+enabled (ExtractionProcessor.cs:736, `Extraction__AutoApproveEnabled=true`, metric `sentinel_extraction_autoapprove_decisions_total` = 185 pending-decisions/24h, 0 approved/rejected across 10d — it correctly decides Pending because nothing qualifies). DECISIVE (supervisor-verified): last 2 days 4437 extracted, **0 auto-approvable** — recent Resolved items have NULL or ~0.756 res_conf (<0.8 bar). Approved tapered (100s/day May→11 on 07-03→0); resolved_hi pool →0 on 07-04/05. Rejected ABRUPT-zero since 06-04 (CoD confidence-threading #607 ~6/5 moved floor so nothing scores <0.5 → nothing to reject). Backfill=manual/dry-run only, never a real drain. Attribution (inference): GIGO ingress filters #824/#836 shrank resolvable surfaces + dropped res_conf<0.8; CoD #607 killed auto-reject.
**SYNTHESIS**: queue is a 1-way accumulator by design — (1) ~73% unresolvable-non-instrument → NO terminal disposition; (2) ~27% real-instruments-missed (surface-form-sensitive SecMaster confirm); (3) drain=0 (auto-approve res_conf≥0.8 rarely met [most Resolved have NULL res_conf]; auto-reject dead since #607). NOT our digest work. FIX DIRECTIONS (proposed, NOT applied — user decides): terminal disposition for NoResolution (expire/don't-queue); resolver use LLM candidate ticker when fuzzy-confirm fails; fix res_conf calibration/threshold; revisit auto-reject floor.
Read-only (SELECT + logs/metrics + code); NO fixes until findings synthesized + user decides.

Recent completions (history → git log + PRs + tags + RELEASES):

- **Trend-visualization epic — ✓ COMPLETE + LIVE + closed-out 2026-07-04.** Tag `sentinel-trend-viz-done` @ `a2141c54`; `docs/RELEASES.md` entry; spec+plan on main under `docs/superpowers/`. 5 PRs #841 (Stage A: TrendQueryService + TrendSvgRenderer) / #842 (Stage B: market-first DigestRenderer + `/sentinel/matrix` explorer) / #843 (drill-down nesting fix, user-caught) / #844 (momentum SVG redesign, user-caught) / #845 (Stage C cutover) — all merged+deployed+verified; digest report 185 live. Redesigned digest = KPI tiles → sector×day heatmap index → per-sector drill-downs (sparkline + regime + dual-trend) + live explorer + SVG momentum (color=tilt sign, arrow=momentum); old Sector-Heat table / Sector-Detail lists / Sector-Watch narrative removed. Detail → git log `65352b9f`..`a2141c54` + PRs + tag. Word-cloud follow-ups (#846–#849, all merged+deployed+verified, user-accepted, live report 189): **#846** inline tag cloud dropped the last d3-cloud CDN → **digest fully zero-JS / no-CDN end to end**; **#847** repacked as packed/rotated SVG cloud (user rejected the inline sorted-list look → mockup-picked; deterministic spiral+collision layout, theme-aware `--wc-*`, shrink-to-fit); **#848** filter calendar/days/vague-adverbs (movement verbs kept as signal per user); **#849** comprehensive standard-English stopword swap (root cause: hand-rolled function-word list incomplete → generic English + plural time-units leaking; kept financial-boilerplate + movement-verbs + domain signal, grep-verified). Cloud now reads as signal (growth/rate/oil/yield/demand/jobs…).
- **Gemini resolver remediation epic — COMPLETE 2026-07-02.** 6 PRs #835–#840 merged+deployed+verified (alert re-base, D-5 NER-ingress, D-6 V2-direct SubjectEntity, #838 cross-caller burn alert via resolver /metrics, D-7 near-dup collapse, credential-class drop). client_null flood eliminated, cap headroom recovered, burn+starvation alerts live. Detail → git log `38a0d34d`..`51a1862c` + PRs.
- **OKF / LLM-wiki RAG spike — CLOSED NO-GO 2026-07-02** (tag `okf-rag-spike-done`). Wiki squeezed out both ways: grounded retrieval done better/cheaper by existing bge-m3+pgvector RAG; news alpha fast-decaying, must NOT accumulate (→ #729 pipeline). Detail → RELEASES + tag.
- **Intent-fidelity enforcement — COMPLETE 2026-07-02** (tag `intent-fidelity-done`). Canonical in CLAUDE.md `## INTENT_FIDELITY` + hooks + DECISIONS blocks.

## STANDING GUARDRAILS
- OBSERVABILITY (user scar tissue — "too many services non-functional due to lack of observability"): never demote a visible signal to Info+metric without a WIRED alert; keep a VISIBLE Warning on any persistent dependency-unavailability; startup banners STAY at Warning (boot-loop visibility). [[feedback_warn_vs_info_by_trigger]]

## OPEN ITEMS (non-blocking)
- **SentinelCollector.UnitTests: parallel-isolation flake (~50% on full runs)** — surfaced during #825. Two DIFFERENT tests fail intermittently ONLY in the full parallel run, both a process-global listener capturing a concurrent test's state: `MacroObservationRouterTests.should_count_log_and_tag_error_status_when_qualitative_substrate_write_throws` (`Activity.LastStatus` = Ok not Error) and `ExtractionProcessorArticleEmbeddingTests.should_tag_failed_when_store_throws` (`capture.Results` = {failed,failed} not single). Both PASS in isolation. Root cause = shared `ActivityListener`/capture not scoped to the test's own activity under xUnit parallelism. Fix: serial `[Collection]` or per-test-tag listener filter. Separate PR.
- **SecMaster alert thresholds are still INITIAL estimates** — `SecMasterDiscoveryTimeoutsElevated` (timeout-fraction >0.5/30m) + `SecMasterFredSearchFastFailing` (fastfail ≥10/15m/30m); revisit against real traffic. Metric-name gotcha: OTEL appends `_total` → alert on `secmaster_fred_search_skipped_total` NOT the bare name.
- **SQLitePCLRaw.lib.e_sqlite3 2.1.11 CVE** (NU1903, GHSA-2m69-gcr7-jv3q, HIGH) — TEST-ONLY (SQLite = unit-test DB provider; prod is TimescaleDB), pulled transitively by `SecMaster.UnitTests` + `Reports.UnitTests`. Bump the EF-SQLite test dep; not a runtime exposure.
- **NasdaqCollector dependency line is stale** — `Microsoft.AspNetCore.Grpc.Swagger 0.8.7` → `Swashbuckle 6.4.0` → `Microsoft.OpenApi 1.2.3`. Clean for CVE-2026-49451 (1.2.3 below the affected 2.x range) but an old chain worth a future refresh. (Nasdaq is DISABLED in prod anyway.)

## PARKED — #729 regime news-as-staleness redesign  (NOT current focus; spec = PR #729, DO-NOT-MERGE)
Intent: FRED/OFR benchmark = slow grounding anchor; Sentinel news = fast-decaying coincident perturbation weighted by benchmark STALENESS; measure economic significance, not coverage volume. Phases 1 / 2a / 2b built + deployed (Phase 2b in **shadow**) — #730–#736. Open:
- 🔴 **Phase 2c cutover** (`Matrix:NewsClustering:Mode=Authoritative`) — **HELD.** Embedding coverage still ramping (missing_embedding ~0.57) AND shadow `magnitude_ratio` ~2.5 (>1) ⇒ dedup AMPLIFIES / sign-flips the net (not gentle compression) → materially restructures news. Hold until coverage fills + Phase 3 revisited.
- ◯ **Phase 3** — staleness crossfade `news_weight = g(benchmark_age/cadence)` sigmoid + benchmark-anchored aggregation. The principled fix for the structural news-overweight (news MACRO cells ~2-4× FRED magnitude, net-negative → washes sector differentiation → over-neutral regime). Gated on 2c + backtest.
- ◯ **BACKTEST harness** = the validation layer that unblocks all of #729 (awaiting go). Shadow has no ground truth. Outcome data found: `finnhub_quotes` XLE/XLF/XLV/XLK ~6.5mo + SPY/QQQ + Yahoo EOD backfill for all 11; AV WTI/BRENT/NATGAS to 1986. First gate = establish a realized-sector-return series.
- ⏸ classifier **sign-fix** parked — DO NOT ship off the energy anecdote (n=6 peek showed news directionally right; needs real backtest N) [[project_energy_signal_not_misfire]]. `commercial-paper-stress` de-flagged interim (#733); spread-redesign `DCPN3M − DGS3MO` (add DGS3MO) proposed, awaiting go.

## DEPLOY / RUNTIME
- **Scoped deploy**: `ansible-playbook playbooks/deploy.yml --tags <svc> --skip-tags build -e "scoped_restart=true scoped_services=<svc>"` from `deployment/ansible/` (build image first: `{Project}/.devcontainer/build.sh --no-cache`; `--skip-tags build` uses the fresh `:latest`). Non-scoped / `[always]` run = compose regen + full-stack restart (incl ~4min vLLM reload). Grafana provisioning is startup-loaded → `--tags alerting|dashboards --skip-tags always` + restart grafana. Scoped restart recreates ONLY the named target — `depends_on` peers (incl. timescaledb) + vLLM stay up, no cascade; fails loudly if a peer drops [[project_scoped_restart_cascade_gotcha]]. ⚠️ `autofix-runner/-watcher` timers run a FULL deploy from main every few min → unpushed config reverts; MERGE to hold. [[ansible_deploy_stale_image]] [[project_ansible_deploy_restart_behavior]]
- **SentinelCollector prompt-sync** (host-mounted `/prompts`): the repo→host prompt sync is bundled under `--tags sentinel-collector` (runs automatically in a scoped sentinel deploy); `--tags patterns` is ThresholdEngine-only, NOT for sentinel prompts. A prompt-only edit still needs a sentinel deploy to reach the host mount.
- Boot: `atlas.service` + `otel.service` (enabled) bring the stack up; containers `restart: unless-stopped`. EF migration: `nerdctl compose exec -T <svc>-dev dotnet ef migrations add <Name> --context <Ctx> --output-dir Data/Migrations`. Prometheus reload: `sudo nerdctl exec prometheus kill -HUP 1`.
- DB inspect (SELECT only; never DML [[feedback_agent_no_raw_db_fixes]]): `sudo nerdctl exec timescaledb psql -U ai_inference -d atlas_data`. news obs `sentinel.extracted_observations` / `raw_content`; matrix feed `public.macro_observations`; cells `public.matrix_cells`; regime `public.sector_regimes`. SecMaster DB = `atlas_secmaster`.
- Inference: GPU vLLM `Qwen2.5-32B-Instruct-AWQ` 32K (extraction `VllmJson` + classifier); CPU `llama-cpu-rag` (qwen2.5:7b, SecMaster RAG, host port 11438), `llama-cpu-embed` (bge-m3), DSL `llama-server` — all memory-bandwidth-bound on the 9960X single IMC (per-request rate divides under concurrency; at-spec, not a bug). Trust running `printenv`, not commit narrative.
- Push: git-push-guard blocks direct main push except docs-only; commit + push = SEPARATE bash calls; docs/STATE push → `scripts/claude-mark-verified` then push (both bare, no pipe). Tag push = create tag then push in SEPARATE bash calls (the guard blocks the whole bash if it sees `git push`, so a chained create+push never creates the tag). Merge gate: `pr-review-toolkit:review-pr <N>` writes `/tmp/atlas-test-markers/pr-reviewed-<N>` keyed to headRefOid; re-run after new commits. [[project_push_guard_main_block]] [[project_push_guard_tag_quirk]] [[project_pr_review_marker_staleness]] [[project_docs_markverified_clobbers_code_marker]]

## SUPERVISOR OPERATING NOTES
- Land substantive work as GitHub PRs [[feedback_work_in_github_not_local]]. **Verify agent DATA vs DIAGNOSIS before surfacing/recording** — for a stuck/looping/slow symptom, pull the OTEL counters FIRST [[feedback_pull_counters_before_loop_hypothesis]] [[feedback_verify_agent_inferences]].
- Code/test/build/deploy → background subagents; worktree-isolate parallel code agents [[feedback_parallel_agents_use_worktrees]]; run `compile.sh` AS FINAL from within the worktree; silent 30-60min usually = mid-compile [[feedback_compile_slow_dont_kill]].
- Autonomy: dispatch/review/fix/merge/deploy autonomous; NTFY `atlas-claude-ask` only on genuine blocker / arch fork / milestone; ACK on receipt. End turns `▶` (autonomous) vs `🛑` (decision needed) [[feedback_signal_autonomous_vs_pause]].
- Commit STATE.md at each milestone — review subagents have silently `git restore STATE.md` (uncommitted edits lost); a committed-current baseline makes a rogue revert a no-op. Push a code branch (fresh compile marker) BEFORE running docs `mark-verified` in the same worktree [[project_docs_markverified_clobbers_code_marker]].
