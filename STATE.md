# ATLAS Supervisor STATE

Supervisor memory. Read first, write last. **Current truth + open items + pointers ONLY.** History → git log + PRs + tags + memories.

## EACH TURN START HERE
0. **FIRST TURN** — git audit (`worktree list / branch / status / fetch + log HEAD..origin/main`); on `main` & current. [[feedback_git_session_start_discipline]] [[feedback_main_always_current]]
1. Poll NTFY `atlas-claude-reply` (user input first).
2. Re-read this file + the active spec (if any) THIS turn; walk the pipeline backward before dispatching; plan-vs-prod mismatch → STOP + NTFY.

## CURRENT STATE (2026-07-31)
**No active epic.** System quiet + healthy: 0 firing alerts, 0 Loki Error/Fatal (24h), 0 patterns severely-overdue. **No open PRs; issue tracker EMPTY.** main clean + synced at origin. Worktrees/branches pruned to just `main`.
Last thread: the 2026-07-24 power-cycle recovery check surfaced ThresholdEngine pattern-data health false-positives → fixed/unified → the new stall alert surfaced WM2NS → SecMaster frequency-integrity close-out. All merged/deployed/verified/tagged (see Recent completions). Nothing carried is blocking.

## RECENT COMPLETIONS (history → git log + PRs + tags + RELEASES + memories)
- **ThresholdEngine pattern-data health — ✓ 2026-07-24→31.** Tag `te-pattern-data-health-done` @ `0d5198fa` + RELEASES. "Severely overdue" ≠ real staleness: 4 false-positive causes + 2 drifted surfaces + no stall alert. #889 seriesless / #890 FRED daily-on-monthly cron / #891 SecMaster mapping-drift (D-7) / #893 batch-published-FX grace floor `max(3×pubFreq,14)` / #894 slightly-overdue→informational / #896 unify surfaces into `PatternDataHealthEvaluator` / #897 `te-pattern-approaching-severe` alert (closes #895). [[project_te_severely_overdue_causes]]
- **WM2NS publication-cadence override — ✓ 2026-07-31.** Tag `te-wm2ns-publication-cadence-done` @ `d912639a` + RELEASES. #897's alert correctly surfaced WM2NS = weekly-DATED M2 published MONTHLY (FRED H.6, ~22d lag) → derived pubFreq=7 false-flags severe ~3wk/mo. #898 per-pattern `PublicationFrequencyDaysOverride`=30 / #899 `data_overdue_days` gauge honors it (dropped dead `ISeriesFrequencyResolver` dep) / #900 repaired 4 stale --integration tests (retired sahm-rule→sahm-rule-official) / #901 comment-rot sweep. Verified live: overdue −5, threshold 90, Healthy. Rule: RELEASE-cadence coarser than DATA-freq → set the override, never relabel catalog freq (D-7 reverts it).
- **SecMaster blank-frequency INSERT guard — ✓ 2026-07-31** (#902, D-8, closes #892). Persist-boundary reject of blank `source_mappings.frequency` at SaveChanges (mirrors D-5); one choke covers every writer; does NOT fire on D-7 blank-preserve. Scoped to SourceMapping (prod 0 blank/7459) NOT instruments (78% blank, vestigial). Latent defense-in-depth; deployed+verified. Frequency-integrity thread now airtight (D-7 update + D-8 insert).
- **Runtime log-level rollout — ✓ 2026-07-22.** Tag `runtime-loglevel-rollout-done` @ `7b56f5fa` + RELEASES. 13 .NET services (#883–#887, MCP sidecars excluded); LoggingLevelSwitch + ChangeToken re-parse; live `nerdctl exec` raise, RECREATE (not restart) reverts.
- **Host/stack failure-detection — ✓ 2026-07-20.** Tag `stack-failure-detection-done` + RELEASES. #880 Requires→Wants / #881 OnFailure ntfy + watchdog / #882 StartLimit sizing. [[project_hardcut_namestore_corruption]]
- **Loki warnings + autofix-flood remediation — ✓ 2026-07-07.** Tag `loki-warnings-autofix-remediation-done` + RELEASES. #860–#864.
- **Sentinel review-queue drain — ✓ 2026-07-05.** Tag `sentinel-review-queue-drain-done` + RELEASES (+ follow-ons). #850–#859: method-aware queue (fuzzy-only review ~2-5/day); 321K backlog drained.
- **Trend-visualization — ✓ 2026-07-04.** Tag `sentinel-trend-viz-done` @ `a2141c54` + RELEASES. #841–#849 (market-first digest + zero-JS SVG cloud).
- **Gemini resolver remediation — ✓ 2026-07-02.** #835–#840. · **OKF/LLM-wiki RAG spike — CLOSED NO-GO** (tag `okf-rag-spike-done`). · **Intent-fidelity enforcement — ✓** (tag `intent-fidelity-done`; canonical in CLAUDE.md `## INTENT_FIDELITY`).

## STANDING GUARDRAILS
- OBSERVABILITY (user scar tissue — "too many services non-functional due to lack of observability"): never demote a visible signal to Info+metric without a WIRED alert; keep a VISIBLE Warning on any persistent dependency-unavailability; startup banners STAY at Warning (boot-loop visibility). [[feedback_warn_vs_info_by_trigger]]

## OPEN ITEMS (non-blocking)
- **request-log regression-guard gap (#886 HTTP services)** — the DiagnosticContext re-registration (AlertService/SecMaster/CalendarService, preserving `UseSerilogRequestLogging` after dropping `Host.UseSerilog`) has NO `// INTENT` tag + NO test → a future edit deleting it silently breaks request logging at request time. Hardening follow-up: add an INTENT marker + a request-log test.
- **fleet-wide `System.Security.Cryptography.Xml` pin 10.0.8→10.0.10** — Reports bumped at #887 (clears its NU1903 hard-fail); MacroSubstrate + ~6 other pins stay 10.0.8 (transitive NU1903, non-fatal warning). Fleet standardize deferred (user "expand later").
- **SentinelCollector.UnitTests: parallel-isolation flake (~50% on full runs)** — two tests fail only under xUnit parallelism (process-global `ActivityListener`/capture catching a concurrent test's state): `MacroObservationRouterTests.should_count_log_and_tag_error_status_when_qualitative_substrate_write_throws` + `ExtractionProcessorArticleEmbeddingTests.should_tag_failed_when_store_throws`. Both PASS in isolation. Fix: serial `[Collection]` or per-test-tag listener filter. Separate PR.
- **SecMaster alert thresholds are still INITIAL estimates** — `SecMasterDiscoveryTimeoutsElevated` (timeout-fraction >0.5/30m) + `SecMasterFredSearchFastFailing` (fastfail ≥10/15m/30m); revisit against real traffic. Metric-name gotcha: OTEL appends `_total` → alert on `secmaster_fred_search_skipped_total` NOT the bare name.
- **SQLitePCLRaw.lib.e_sqlite3 2.1.11 CVE** (NU1903, GHSA-2m69-gcr7-jv3q, HIGH) — TEST-ONLY (SQLite = unit-test DB provider; prod is TimescaleDB), transitive via `SecMaster.UnitTests` + `Reports.UnitTests`. Bump the EF-SQLite test dep; not a runtime exposure.
- **NasdaqCollector dependency line is stale** — `Microsoft.AspNetCore.Grpc.Swagger 0.8.7` → `Swashbuckle 6.4.0` → `Microsoft.OpenApi 1.2.3`. Clean for CVE-2026-49451 (1.2.3 below the affected 2.x range) but an old chain worth a future refresh. (Nasdaq is DISABLED in prod anyway.)

## PARKED — #729 regime news-as-staleness redesign  (NOT current focus; spec = PR #729, DO-NOT-MERGE)
Intent: FRED/OFR benchmark = slow grounding anchor; Sentinel news = fast-decaying coincident perturbation weighted by benchmark STALENESS; measure economic significance, not coverage volume. Phases 1 / 2a / 2b built + deployed (Phase 2b in **shadow**) — #730–#736. Open:
- 🔴 **Phase 2c cutover** (`Matrix:NewsClustering:Mode=Authoritative`) — **HELD.** Embedding coverage still ramping (missing_embedding ~0.57) AND shadow `magnitude_ratio` ~2.5 (>1) ⇒ dedup AMPLIFIES / sign-flips the net (not gentle compression) → materially restructures news. Hold until coverage fills + Phase 3 revisited.
- ◯ **Phase 3** — staleness crossfade `news_weight = g(benchmark_age/cadence)` sigmoid + benchmark-anchored aggregation. The principled fix for the structural news-overweight (news MACRO cells ~2-4× FRED magnitude, net-negative → washes sector differentiation → over-neutral regime). Gated on 2c + backtest.
- ◯ **BACKTEST harness** = the validation layer that unblocks all of #729 (awaiting go). Shadow has no ground truth. Outcome data found: `finnhub_quotes` XLE/XLF/XLV/XLK ~6.5mo + SPY/QQQ + Yahoo EOD backfill for all 11; AV WTI/BRENT/NATGAS to 1986. First gate = establish a realized-sector-return series.
- ⏸ classifier **sign-fix** parked — DO NOT ship off the energy anecdote (n=6 peek showed news directionally right; needs real backtest N) [[project_energy_signal_not_misfire]]. `commercial-paper-stress` de-flagged interim (#733); spread-redesign `DCPN3M − DGS3MO` (add DGS3MO) proposed, awaiting go.

## DEPLOY / RUNTIME
- **Scoped deploy**: `ansible-playbook playbooks/deploy.yml --tags <svc> --skip-tags build -e "scoped_restart=true scoped_services=<svc>"` from `deployment/ansible/` (build image first: `{Project}/.devcontainer/build.sh --no-cache`; `--skip-tags build` uses the fresh `:latest`). Non-scoped / `[always]` run = compose regen + full-stack restart (incl ~4min vLLM reload). Grafana provisioning is startup-loaded → `--tags alerting|dashboards --skip-tags always` + restart grafana (a bare `--tags alerting` regen cascades a FULL atlas restart, and grafana lives in the SEPARATE OTEL stack so it still needs its own restart to load new alert rules [[project_grafana_alerting_provisioning_reload]]). Scoped restart recreates ONLY the named target — `depends_on` peers (incl. timescaledb) + vLLM stay up, no cascade; fails loudly if a peer drops [[project_scoped_restart_cascade_gotcha]]. ⚠️ `autofix-runner/-watcher` timers run a FULL deploy from main every few min → unpushed config reverts; MERGE to hold. [[ansible_deploy_stale_image]] [[project_ansible_deploy_restart_behavior]]
- **SentinelCollector prompt-sync** (host-mounted `/prompts`): the repo→host prompt sync is bundled under `--tags sentinel-collector` (runs automatically in a scoped sentinel deploy); `--tags patterns` is ThresholdEngine-only (mirrors `config/patterns/` to the host mount; the pattern watcher hot-reloads). A prompt-only edit still needs a sentinel deploy to reach the host mount.
- Boot: `atlas.service` + `otel.service` (enabled) bring the stack up; containers `restart: unless-stopped`. EF migration: `nerdctl compose exec -T <svc>-dev dotnet ef migrations add <Name> --context <Ctx> --output-dir Data/Migrations`. Prometheus reload: `sudo nerdctl exec prometheus kill -HUP 1`.
- DB inspect (SELECT only; never DML [[feedback_agent_no_raw_db_fixes]]): `sudo nerdctl exec timescaledb psql -U ai_inference -d atlas_data`. news obs `sentinel.extracted_observations` / `raw_content`; matrix feed `public.macro_observations`; cells `public.matrix_cells`; regime `public.sector_regimes`. SecMaster DB = `atlas_secmaster` (`source_mappings` / `instruments`).
- Inference: GPU vLLM `Qwen2.5-32B-Instruct-AWQ` 32K (extraction `VllmJson` + classifier); CPU `llama-cpu-rag` (qwen2.5:7b, SecMaster RAG, host port 11438), `llama-cpu-embed` (bge-m3), DSL `llama-server` — all memory-bandwidth-bound on the 9960X single IMC (per-request rate divides under concurrency; at-spec, not a bug). Trust running `printenv`, not commit narrative.
- Push: git-push-guard blocks direct main push except docs-only; commit + push = SEPARATE bash calls; docs/STATE push → `scripts/claude-mark-verified` then push (both bare, no pipe). Tag push = create tag then push in SEPARATE bash calls (the guard blocks the whole bash if it sees `git push`, so a chained create+push never creates the tag; and a tag pointing at an OLD commit needs that commit's tree marked — checkout + mark-verified it first). Merge gate: `pr-review-toolkit:review-pr <N>` writes `/tmp/atlas-test-markers/pr-reviewed-<N>` keyed to headRefOid; re-run after new commits. [[project_push_guard_main_block]] [[project_push_guard_tag_quirk]] [[project_pr_review_marker_staleness]] [[project_docs_markverified_clobbers_code_marker]]

## SUPERVISOR OPERATING NOTES
- Land substantive work as GitHub PRs [[feedback_work_in_github_not_local]]. **Verify agent DATA vs DIAGNOSIS before surfacing/recording** — for a stuck/looping/slow symptom, pull the OTEL counters FIRST [[feedback_pull_counters_before_loop_hypothesis]] [[feedback_verify_agent_inferences]].
- Code/test/build/deploy → background subagents; worktree-isolate parallel code agents [[feedback_parallel_agents_use_worktrees]]; run `compile.sh` AS FINAL from within the worktree; silent 30-60min usually = mid-compile [[feedback_compile_slow_dont_kill]].
- Autonomy: dispatch/review/fix/merge/deploy autonomous; NTFY `atlas-claude-ask` only on genuine blocker / arch fork / milestone; ACK on receipt. End turns `▶` (autonomous) vs `🛑` (decision needed) [[feedback_signal_autonomous_vs_pause]].
- Commit STATE.md at each milestone — review subagents have silently `git restore STATE.md` (uncommitted edits lost); a committed-current baseline makes a rogue revert a no-op. Push a code branch (fresh compile marker) BEFORE running docs `mark-verified` in the same worktree [[project_docs_markverified_clobbers_code_marker]].
