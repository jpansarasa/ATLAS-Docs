# ATLAS Supervisor STATE

Supervisor memory. Read first, write last. **Current truth + open items + pointers ONLY.** History → git log + PRs + memories. WRITE_GATE: product/phase truth, deploy-todos, open-asks — NOT per-turn progress, review verdicts, or merged-PR narration.

## EACH TURN START HERE
0. **FIRST TURN ONLY** — git audit (`git -C /home/james/ATLAS worktree list / branch / status / fetch + log HEAD..origin/main`); main checkout on `main` & current. [[feedback_git_session_start_discipline]] [[feedback_main_always_current]]
1. Poll NTFY `atlas-claude-reply` (user input first).
2. RE-READ this file + active plan/relevant docs THIS turn (not "read earlier"). Walk the pipeline backward before dispatching; plan-vs-prod mismatch → STOP + NTFY.

## GOAL [PINNED — re-read + check every turn AND every dispatch against THIS, before acting or surfacing any subagent plan]
**Objective:** maximize Sentinel CoD extraction THROUGHPUT (today ~20 art/hr, ~4-day backlog) with **NO quality/recall regression**.
**User's chosen approach (role-flip, 2026-06-08/09):** **GPU runs CoD; CPU runs CoVe** — distribute the LLM load across BOTH devices.
**HARD CONSTRAINTS — a plan/dispatch violating ANY = STOP, do not surface, re-align:**
- Use BOTH GPU + CPU; **NEVER consolidate all tasks onto one device** (GPU was a prior bottleneck; co-locating CoD+CoVe on the GPU starves CoVe). Distribute.
- ≥30B model, 32K context; extraction quality/recall must NOT regress.
- Land substantive work as **GitHub PRs** (user reviews there, not ntfy/local); once a direction is DECIDED, BUILD it — don't pre-gate on validation (move checks into shadow/canary). [[feedback_work_in_github_not_local]]
**CHECK each turn:** does this turn's action + any subagent plan match the goal + constraints — not merely "is it technically valid"? Drift-cause 2026-06-09: a subagent's "co-locate on GPU" inverted the distribute goal; I surfaced it without checking vs this. Don't repeat.

## WHERE WE ARE [2026-06-07]
Sentinel→matrix counterweight, digest-quality, and the crash/disk fixes are ✅ DONE + LIVE. Matrix = **signal × sector** (Sentinel news = fast-decaying LEADING counterweight to lagging FRED hard data); both halves flow. Digest healthy (LlmStatus Ok, 23 sig×11 sector, FRED anchor 165 cells, commodity/FX recognized). System stable; repo pristine (main only). [[project_matrix_feed_classifier]]
**Active focus:** Sentinel extraction **THROUGHPUT** — CPU 30B CoD is model/DRAM-bound at ~20 art/hr (queue ~3 days behind, draining slowly). Truncation experiment CONCLUDED (H1 failed, abandoned per user 2026-06-08). **New lever: revisit ALL CPU/model tuning parameters** (decode-focused; the prior "CPU tuning exhausted" read is being re-examined now that `=1` eliminated self-contention).

## OPEN (current truth)
- 🏗️ **ARCHITECTURE: GPU↔CPU role-flip — PROMISING, decider in flight.** Eval DATA: GPU **99.5% IDLE** (CoVe ≈17 GPU-sec/HOUR, 0% util, 2.3GB free) while CoD is **4.3 days behind** on CPU (11.3 art/hr); CoD ~600× heavier than CoVe. Literal 2-model swap = **VRAM-impossible**; viable path = **route CoD → the existing idle GPU vLLM (Qwen2.5-32B-AWQ)** → projected **~170–330 art/hr (15–30×)**, drains <1 day; CoVe→CPU trivial (1.5s/art). GATING UNKNOWN: 32B-AWQ DSL-CoD quality (qwen3-30b-a3b = benchmarked champion 0.75 recall; 32B-AWQ unproven). **A/B `a0e59436` DONE: reuse-32B-AWQ-for-CoD FAILS** (recall **0.28 vs 0.75** gold — skips NUM pass on ~30% arts, judge-validated; serial **12 tok/s** GBNF-masked 6.3× = ~20–26 art/hr serial = only ~3× CPU, NOT 15–30×). Grammar-port = FREE (vLLM `structured_outputs:{grammar}` accepts GBNF unmodified via xgrammar). **REFRAME:** cheapest path dead; real lever = **CHAMPION qwen3-30b-AWQ on GPU + BATCHING** (CPU `--parallel 1` can't batch; serial # undersells the GPU edge). **GPU batched VERDICT `af8b2108`: DEAD as-is.** C=1=16.5, C=4=20.1, C8/16≈14–20 art/hr — **saturates at C=1**; GPU 100% util but NOT the bottleneck = **GBNF masking (xgrammar) is CPU-bound per-step → serializes the GPU, nullifies batching** (per-stream 14→4.1 tok/s @C=4). GPU-CoD ≈ CPU + 4–15× latency ⇒ role-flip DEAD *with current grammar*. **BLOCKER = SOFTWARE → UNLOCK FOUND (`a7216356`, BREAKTHROUGH):** masking NOT fundamental — the **9.2KB DSL grammar's free-text wildcard spans** (`[^\n]{0,255}` etc.) force full vocab-mask recompute/token. DATA: unconstrained 70 tok/s; complex-GBNF 10.4 (ALL 4 backends equally slow = backend-swap dead end); **JSON-schema `response_format` = 70.9 tok/s single, batched C4/C8 = 607/713 art/hr; simple-GBNF = 703 art/hr.** ⇒ **GPU-CoD UNLOCKS to ~700 art/hr (~35× vs ~20) IF output reshaped DSL→JSON-schema (or simplified grammar) → ROLE-FLIP REVIVES.** SPEED proven. **QUALITY GATE ✅ PASS (`a2176ea7`):** GPU-JSON-CoD clean-23 recall **0.777** (median 0.857) ≈ 0.75 gold; **NUM-skip FIXED** (required `value` field → 679 NUMs 100% valued, 98% verbatim, NO fabrication — low precision = over-extracts real nums gold missed); speed HELD 72 tok/s. all-30=0.669 dragged by the runaway-repetition loop — **NOW SOLVED (`ac1cdcc9`):** root cause = JSON arrays lack `maxItems` → temp=0 argmax repeats one object to the 16K cap (sampler-only guards stochastically unreliable; STRUCTURAL cap is the fix). **Guard = `repetition_penalty=1.1` + `maxItems≈50–60`/array + `max_tokens≈4096`** → closes 5/5, all-30 recall **0.669→0.762 ≈ clean 0.777**. ⇒ residual CLOSED, role-flip quality FULLY validated (no caveats). ⇒ **ROLE-FLIP VALIDATED: GPU-JSON-CoD ~700 art/hr (~35× vs ~20) + 0.777 recall on the EXISTING 32B-AWQ** (no VRAM-freeing / champion / new model needed). NTFY'd 2026-06-09. **NEXT = USER-GATED ROLLOUT:** (1) DSL→JSON CoD output + prompt, (2) `dsl-parser-mcp` JSON parser (`CpuCod__DslParserEndpoint`), (3) loop-guard, (4) GPU backend wiring (compose: CoD→vLLM, CoVe→CPU), (5) prod re-validate (verifier n≥72) → scoped deploy. **USER GO 2026-06-09 → STAGED ROLLOUT** (plan `a15bac5c` → `docs/plans/gpu-json-cod-rollout-2026-06-09.md`): **PLAN ✅** (6 PRs: P1 `GpuJsonExtractionService`+`InferenceBackend.VllmJson`+loop-guard / P2 `dsl-parser-mcp /parse_json`→same DocumentAst / P3 compose+group_vars CoD cfg / P4 shadow-runner+n≥72 harness — **all additive** / **P5 CUTOVER=user-gate**: `Backend=VllmJson`+raise `MaxConcurrent 1→8-16` / P6 drop CPU path). **USER CORRECTION 2026-06-09: DISTRIBUTE — don't pile everything on GPU** ("was the bottleneck before"). The co-locate-on-GPU default INVERTED the user's intent. Corrected: GPU=CoD (heavy, ~700/hr); **CoVe MOVES to CPU** (off the GPU so it isn't starved behind the CoD batch = user's original flip). **GATING: CoVe-on-CPU quality** — GPU 32B-AWQ = verified CoVe champion (71.53% agreement n=137); CoVe is model-sensitive (~30B swaps regressed −15 to −53pp). Does freed `qwen3-30b` hold ≥71.53%? **→active validation `a592eefa`**. Holds → balanced flip viable; fails → bring user the tradeoff (don't silently degrade verification). Concurrency raise at cutover still LOAD-BEARING. Other defaults: maxItems clip OK, MaxConcurrent start 8. **EXECUTION STARTED 2026-06-09** (user frustrated w/ validation-spinning + wants build-IN-GITHUB): **PR-1 → ✅ #640 MERGED** (main `13cc3207`; review added a `salvage_empty` silent-data-loss guard; branch `feat/gpu-json-cod-backend`; additive GPU-JSON CoD backend `InferenceBackend.VllmJson`+`GpuJsonExtractionService`+loop-guard, INERT until backend-flip; + plan-doc revision to DISTRIBUTED flip CoVe→CPU; 1525+6 tests pass). NEXT: review-pr #640 → merge; PR-2 ✅ #642 MERGED (main `fea73133`; `/parse_json` → same DocumentAst + `json_no_cod_arrays` guard, 97/97) → **PR-3 ✅ #643 MERGED** (main `6553a4f4`; GPU-CoD config inert). **ADDITIVE STACK ✅ COMPLETE** (#640 backend / #642 parser / #643 config / #644 shadow — all MERGED to main `8baa518b`, all inert, prod unchanged; review caught+fixed 2 silent-data-loss + 1 shadow-not-detached defect pre-merge). **RECALL GATE ✅ PASS** (`a38276b`, offline n=80 on the production-wired path — real vLLM 32B-AWQ + real `/parse_json` + loop-guard rep1.1/maxtok4096/maxItems60: **macro recall 0.79 ≥0.75**, median 0.92, 52% ≥0.9, long-tail 10 arts <0.5; 0 judge errors). ⇒ role-flip VALIDATED end-to-end. **✅ USER GO 2026-06-09 ("go for it") → CUTOVER EXECUTING** (PR-5 ✅ MERGED main `f29d3396` → **DEPLOYING `af8795aa`**: ZFS snapshot + REBUILD --no-cache [deployed image predates #640-644 GPU-CoD code] + scoped deploy + immediate smoke[backend-active/GPU-CoD-flowing/no-errors] → **NO-GO `af8795aa`: GPU-CoD backend LIVE + generating, but extractions 404 — `dsl-parser-mcp` image STALE (13d, no `/parse_json`; #642 sidecar never redeployed). ZFS snap `nvme-fast/timeseries@pre-gpu-cod-cutover-20260609` ✓; old sentinel img `f199f8e8` staged. FIXING ¬rollback `a9a72308` (rebuild+scoped-redeploy dsl-parser-mcp → re-smoke). LESSON: a cutover must redeploy ALL services the new path touches (sentinel-collector AND dsl-parser-mcp).** → **FIX GO `a9a72308`: dsl-parser-mcp rebuilt+redeployed (`/parse_json` 200, 97 tests), GPU-CoD extractions FLOWING ~180/hr (~12× CPU, climbing), GPU 99%, 0 err, no crash loop.** Downstream smoke `afb52674` (throughput/backlog-drain + matrix cells + **ntfy-digest-not-zeroed** check) running. **FOLLOW-UP (data): ~79 articles 404-stuck from the deploy-gap window — 404 classified PERMANENT (ExtractionProcessor.cs:599-602) + queue excludes `processing_error IS NOT NULL` → won't self-recover; re-admit via CODE path (clear processing_error for the 404 cohort), ¬raw DML.**) → **SMOKE `afb52674` ✅ CUTOVER FULLY HEALTHY**: matrix cells populating (352/30m), digest CLEAN (real confidence 0.90-0.95, ZERO zeroed rows — no digest-zeroing recurrence), no mass-reject, no downstream errors. Throughput ~90 art/hr (~846 obs/hr, ~6× CPU, climbing); backlog 6480 drain TBD → **RE-CHECK ~1h (≈21:30Z): backlog inflecting down? rate→~700/hr? if plateau, raise concurrency 8→16**. **🎉 GPU-CoD ROLE-FLIP LIVE 2026-06-09 ~20:21Z.** **79-RECOVERY `a178ee73`:** cohort=`sentinel.raw_content WHERE processed_at IS NULL AND processing_error LIKE '%404 (Not Found)%'`(79); **latent bug found:** `/admin/reprocess` nulls ProcessedAt but NOT ProcessingError → can't re-admit ANY failed article. **DECIDED BUILD** 1-line fix (`/admin/reprocess` also clear ProcessingError + RetryCount=0) → invoke w/ 79 ids. **BUNDLE deploy w/ next sentinel-collector deploy** (¬interrupt healthy cutover). Fix ✅ **#646 MERGED** (main `47e0e4d5`, deploy-ready). **1h RE-CHECK `a777b620`: backlog DRAINING (~244/hr net, ~20h ETA) BUT throughput ~350/hr = 50% target, FEED-STARVED (GPU ~10% cap, 8 slots UNsaturated → concurrency 8→16 tune MOOT; dispatcher/feed = bottleneck, not GPU). ⚠️ POSSIBLE DOWNSTREAM BREAK: `macro_observations` stalled (26, all `without_extraction_job`) vs 225+ GPU extractions — CONFLICTS the earlier smoke (cells populating). → **`ab02cf5b` VERDICT: DOWNSTREAM HEALTHY** (extracted_obs 1334 → macro_obs → matrix_cells 506 all FLOWING; "1h-re-check severed" was a MISREAD — `without_extraction_job` NULL is NORMAL for the news path [all 769 sentinel macro_obs ever], "stale" obs = GPU grinding a months-old backlog w/ fresh writes). **CUTOVER GENUINELY DELIVERING to matrix.** REAL follow-ups: **(1) FEED-STARVED** ~350/hr=50% target (dispatcher ¬saturating GPU's 8 slots → fix the FEED, ¬concurrency); **(2) quality-flag ✅ BENIGN `a14fe561`** (degenerate descr = source-text VALUE quotes [value/unit cols well-formed]; statistically IDENTICAL pre/post CPU; matrix classifier reads ARTICLE-TEXT excerpt ¬obs-rows → NO downstream impact; consistent w/ 0.79 recall gate; no regression, no fix). **3 post-cutover scares all cleared by data-verification (smoke/1h/quality) — [[feedback_verify_agent_inferences]] earned it.** **THROUGHPUT CEILING `ad01dcf2`: NOT feed-starved — GPU SATURATED** (100% util, 4-7 running/0 waiting; **~7.7 GPU calls/article** = CoD + SemanticVerifier fan-out [sector-tag/event + claim-verify/claim] + entity-resolve + classifier, all GPU; ~37s/art → ~327/hr). Concurrency-tune MOOT (keep 8; 16 useless). **CORE GOAL ✅ SOLVED (~25×, backlog ~20h drain).** ~700/hr (~35×) needs CODE opt: batch the verifier fan-out (~5 of 7.7 calls)→1-2 + cut cod_llm p95=218s runaway tail = SEPARABLE PROJECT → **🛑 USER CALL (pursue vs bank ~25×).** Low-urgency: reprocess #646 + 79-recovery = 1 brief deploy when convenient. **LESSON: 2 agents misread (10-min smoke + 1h re-check); the data reconciliation got it — verify inferences. [[feedback_verify_agent_inferences]]** (PR-5: flip `Extraction__Backend`=VllmJson + raise `MaxConcurrent` 1→8 → ~700 art/hr ~35×; rollback = flip back + concurrency=1 [CPU path stays wired until PR-6] + ZFS snapshot; smoke MUST verify matrix cells AND ntfy digest [[project_cod_cutover_zeroed_digest]]). → PR-6 drop CPU path (post-soak). → PR-3b (CoVe→CPU) → PR-4 (shadow) → PR-5 (cutover=USER CHECKPOINT) → PR-6 (drop CPU path). CoVe-on-CPU `a592eefa` → **FAILS: −40 to −50pp** (CPU candidates 21–31% vs GPU champion 71.53% agreement, n=137 scorer-validated). Root: the 71.53% is bound to the **AWQ+vLLM+GPU stack** (even same-family CPU q4 = 31%) — calibration, not prompt-fixable; speed/volume fine. ⇒ **CoVe→CPU = ~40pp verification regression (violates no-quality-regression GOAL).** BUT CoVe is tiny (~0.5% GPU duty) → co-residing on GPU (prioritized) does NOT overload it. **🛑 NTFY'd user the CoVe-PLACEMENT FORK:** (a) keep CoVe on GPU, tiny+prioritized [REC]; (b) accept −40pp CPU-CoVe; (c) hunt a CPU-servable verifier. **RESOLVED 2026-06-09 → KEEP CoVe ON GPU.** Binary reframe (user's NIAH point, `aa96fd50`) shrank the gap to ~7pp at n=137 BUT that was small-sample noise (26 pos); **n=563 confirm `a8ea4904` REVERSES it: CPU recall 0.657 (lets 34% of hallucinations through) vs GPU champ recall 0.910 / F1 0.864 — CPU-CoVe NOT viable.** CoVe stays GPU = only acceptable verifier; tiny (~0.5% GPU duty, prioritized) so NO bottleneck; CPU keeps classifier+embed (both devices used). **PR-3b (CoVe→CPU) DROPPED** from rollout. (Corrected the optimistic n=137 read to user via NTFY.) GPU-CoD PRs (1,2,3,4) proceed regardless. CPU-CoD fix `a8a38698` → also PR. **PROCESS LESSON: all substantive work → GitHub PRs (user reviews there, not ntfy/local); don't pre-gate decided directions on validation.** Cutover smoke MUST verify matrix cells AND ntfy digest (prior cutover zeroed digest). **NEXT: ship CPU-CoD fix `a8a38698` first (file overlap), then P1.** Artifacts `/tmp/sentinel-remediation/json-cod-quality/`. 🔁 **RECURRING: runaway generations** (some arts → 4,700+ tok, ~1,000–1,190s) = throughput TAIL-DRAG on BOTH HW (also P1) → separable lever: repetition-penalty / max_tokens-cap / prompt-fix.
  - ⚠️ **A/B 0.28 may be UNFAIR to 32B-AWQ** (user-flagged): GRAMMAR transferred (93% parse-clean) + model CAPABLE (aggregate NUM 632>524) — failure = it SKIPS the NUM pass on ~30% arts under the **qwen3-tuned** v8 prompt (prompt-elicitation is model-specific; format generalizes, elicitation often doesn't). A/B applied qwen3 prompt as-is — may be wrong qwen2.5 chat-template / unfair prompt. **QUEUED (after `af8b2108`, to avoid GPU contention): fair-shot retest** — correct qwen2.5 template + targeted NUM-pass prompt tweak → re-measure recall. ≥0.75 → reuse-32B-AWQ-CoD back on table (work DID transfer); <0.75 → real model gap → champion-on-GPU path. Premise-correction: NewsSignalClassifier is on CPU 7b, NOT GPU. Real cost: GBNF→vLLM-guided port. **USER EXPANDED 2026-06-08 → optimal model×task×HW search** (test other CoD-bench models + NEW models on BOTH CoD & CoVe; find best combo). **PROD-PAUSE AUTHORIZED for tuning tests.** →survey+eval-matrix plan `a61128a8` (→ `docs/plans/model-task-hardware-optimization-2026-06-08.md`). In flight: A/B `a0e59436`, speculative `ab17ed30`.
- 📐 **Model-opt plan ✅** `docs/plans/model-task-hardware-optimization-2026-06-08.md`. **KEY reframe:** GPU is COMPUTE-idle but **VRAM-FULL** (29.8/32GB = 32B-AWQ verifier) → a NEW GPU-CoD model needs VRAM-freeing; **reusing 32B-AWQ for CoD (1 model, both tasks) is VRAM-free** = A/B `a0e59436`. Floors: CoD recall ≥0.61 (qwen3-30b gold, full-ctx 0.61/0.75); CoVe ≥71.53% agreement vs Foundry-Claude (n=137, ±5pp). New CoD candidate: **GLM-4.7-Flash** (on-disk, MIT, MoE arch-peer of champion). **Exp seq:** (1) GLM-4.7-Flash CPU-CoD A/B (win-or-neutral); (2) **verifier-shrink (Qwen2.5-14B-AWQ on CoVe n=137) = VRAM UNLOCK** (frees ~17GB); (3 cond.) GPU dedicated-CoD pool + CPU long-article pool = both saturated (optimum). NOTE: speculative bench `--threads 12` contends w/ prod `--threads 24` (same 24-core) → relative speedup still valid; honor disjoint-CCD for next CPU bench. Next-experiment choice gated on A/B outcome.
- 🎯 **PRIMARY (2026-06-08): CPU/model tuning for throughput.** Recon ✅ → plan `docs/plans/cpu-tuning-throughput-2026-06-08.md`. DATA: model=`qwen3-30b-a3b-q4_K_M`; **decode-bound ~8–10 tok/s** (855 tok/art, p50 120–375s/art; aggregate art/hr figure window-deflated by P1 downtime → per-art decode is the clean metric). HW=TR-9960X 4×6-core CCDs; **RAM 4/8 channels (½ BW) but decode is LATENCY-bound (MoE expert-gather), not BW-bound**; prior `3a802551` tuning = WEAK/UNVERIFIED (target tok/s not met → "CPU exhausted" was never established; validates revisiting). **Speculative decoding TESTED (`ab17ed30`): NOT a free win.** Vocab-compat ✓ (Qwen3-1.7B/0.6B q4 drafts, byte-identical vocab; DSL accept 1.7B@nmax8=76%), BUT **byte-identical gate FAILED** — batched-verify logits flip near-tied argmax → diverges from greedy even at top_k=1 (NOT exact on b9544). ⇒ speculative = output-changing config needing a RECALL A/B; gain noisy ~1.3–3.7× (BW-free) → ~1× (DRAM-saturated). Build note: needs `--spec-type draft-simple`. **Box is MEMORY-BW-BOUND**; ollama-cpu-gen (classifier 7b) saturates shared DRAM → CPU-CoD lever = isolate that contention. Speculative now a FALLBACK (pending prod-paused clean speedup + recall gate), behind the GPU-CoD path. Other levers queued: thread/affinity (ollama SMT-contention), `-ctv q8_0`, `--mlock`, short-pool routing; DRAM-fill deferred (latency-bound). **NTFY results + proposed prod config before applying.** Prereq for non-spec levers: restore `scorer.py` (only in worktrees) as the recall guardrail.
- 🔬→🛑 **CoD truncation CONCLUDED + ABANDONED (user):** H1 FAILED (verified — recall ≤0.23 vs full 0.77, **96% TRUE material loss**; unbankable). Negative result `docs/benchmarks/cod-truncation-2026-06-07/results-p1.md`; harness reusable as a recall-A/B guardrail; cleanup TODO: archive `feat/cod-trunc*` branches + remove leftover `agent-*` worktrees. ↓ below = experiment play-by-play (historical, compress later): **STATUS: P0(a) harness ✅ built+verified** (branch `feat/cod-truncation-harness`, 6 commits f2b76ce9→fde0b882, 8 files intact; reuses prod prompt `cod_dsl_v8_baseline.txt`+grammar `cod-dsl-v2.3.gbnf`+llama-server `/completion`+dsl-parser `/parse`/`/verify` = fidelity proven; 5-art e2e ran real CoD+GPU judge). Threshold refined ≥4000→**≥1500 CoD-tok** (agent DATA: wire median ~547 tok, only ~4/60 ≥1500 — corpus is shorter than spec assumed). `cod_runner` PID172574 still draining 48-var sweep (shares prod llama-server slot). **ECONOMICS ✅ CONFIRMED (clean exclusive-slot benchmark) → PROCEED.** Truncation saves **73–94% wall-clock/article** (long 1052s→72s; short 195s→53s). Reframe: win is **DECODE-driven** (decode=68–100% of compute; 9.3K prefix caches to ~0.2s warm) — less input→fewer obs→less output→less decode; no rambling regression. Throughput payoff PROVEN; the recall sweep's job = is the dropped output **material vs boilerplate**. **P0 ✅ + consolidation ✅** — `feat/cod-truncation-harness` SWEEP-READY (4 branches merged 0-conflict, verified `7bacc153`+merges; validated scorer + NextData clients + cod_runner 1800s timeout; smoke clean). P0(b) matching 100% (36/36) but on ONE article (74600) → **judge spot-audit folded into P1**. Win is decode-driven → robust even on short articles (73% on 1371-tok). **P1 COMPLETE — ❌ H1 FAILS (VERIFIED) → ESCALATED (esc#2, awaiting user direction).** Truncation recall ≤0.23 vs **healthy-full 0.77** (median 0.857; raw-full 0.61 = 7 pipeline failures, NOT matcher error). Verification manually adjudicated 133 misses: **96% TRUE content loss** — wire news carries material numeric facts body-wide, not front-loaded; matcher trustworthy (P0(b) holds, false-miss ≤4%); hallucination recall-irrelevant; "100% material" tautological (baseline = value-centric obs). ⇒ the proven 73–94% throughput win costs ~72% material recall = **UNSAFE for news** (the strongest truncation case). NTFY'd Q: (a) abandon trunc / (b) still test P2 analytical / (c) pivot (smaller model | >10K-tail-only); my lean (a)+(c)-lite. **Experiment direction PAUSED pending user.** Fix-before-followup (≠ verdict): 7 full-depth pipeline bugs (≥10K-tok timeout, runaway-gen parse-err, judge ctx-overflow). [[feedback_verify_agent_inferences]]
- ✅ **PROD RESTORE COMPLETE.** PR #639 (`MaxConcurrentExtractions 4→1`) merged `e4257764` + scoped-deployed; **sentinel-collector BACK UP, `=1` live** (container+startup confirmed), alert-service stayed down, **self-contention timeouts ELIMINATED** (0 in 25-min post-deploy), extraction resumed. **But backlog still CLIMBING (new RSS > 1-slot drain) → CORE THROUGHPUT PROBLEM UNSOLVED** (=1 removed waste, added NO capacity; truncation FAILED). Caveat: deploy-agent measured pre-fix self-contention ~0.2–0.6/min (the cited 26k/48h "~9/min" likely counted broader timeout types). Git repaired: local main=origin/main; `backup-main-980584ca` kept. **TODO cleanup:** 4 leftover `agent-*` worktrees + consolidate experiment branches (`feat/cod-truncation-harness`+econ+nextdata+matching+p1-pilot) → one PR or archive.
- 🔧 **Prod side-findings from econ benchmark (agent DIAGNOSIS — verify before fixing):** (a) ~~harness skipped prod's `NextDataExtractor` → prod feeds ~24K-tok prompts~~ **REFUTED + fix landed** — NextData only hits investing.com `_article` (fewer tok); harness already matched prod for searxng; corpus genuinely ≤~8K tok; (b) ✅ **CONFIRMED (file:line + telemetry):** prod `Extraction__MaxConcurrentExtractions=4` (compose:883, overrides appsettings default 1) vs llama-server `--parallel 1` → 3 reqs queue/slot → C# per-req timeout (900–1800s) fires → **bypasses Polly** → app requeues → **26,469 timeouts/48h (~9/min) + 10 PERMANENT article failures (data loss)**. Violates documented invariant `ExtractionOptions.cs:122`. **FIX ✅ done + PR'd:** commit `5db88164` (group_vars `sentinel_max_concurrent_extractions` 4→1 + `LlamaServerClient.cs:420` comment fix; compile clean, 1519+6 tests pass) → **PR #639 open** (pushed; push-guard OK). **HELD for post-P1 (deploy restarts sentinel-collector = steals P1 slot):** review-pr #639 → merge → scoped deploy `--tags sentinel-collector` (restarts prod w/ =1) → verify timeout drop. Likely a separate higher-value win than truncation. NOTE: "~20 art/hr = model-bound" baseline may be partly THIS churn → **re-baseline throughput after deploy**.
- **Throughput baseline (concluded — context for the experiment):** CPU `qwen3-30b-a3b` CoD ≈ 20 art/hr; tuned config LIVE (`3a802551`: `--parallel 1` + flash-attn + ubatch 2048 + KV-q8 — kept for the 32K-ctx quality fix; aggregate throughput ~flat regardless of CPU knobs). Monitoring LIVE: #636 `queue_depth`/`frontier_lag` + #638 per-stage (`cod_llm`/`slot_wait`/tokens) + Grafana "Extraction Queue Health" panels. HW maxed (4-channel TRX50, healthy, no failure). Fallback levers if the experiment fails: GPU-offload / smaller-model (vs ≥30B rule) / accept-lag. [[project_atlas_inference_topology]]
- **Codify the core-dump `LimitCORE=512M` drop-in into deployment IaC** — manual host file (`/etc/systemd/system/containerd.service.d/core-limit.conf`), lost on rebuild.
- **Recalibrate flagged natgas/ECB sectorWeights** vs observed betas (optional, hot-reloadable).
- **Hard-data natgas/ECB → matrix wiring** — the hard-data pattern signals evaluate but don't produce `matrix_cells` (DHHNGSP/ECBDFR not pushed to `macro_observations`); matrix entry is news-`:sig:`-only.
- Verifier verdicts not persisted (blocks auditing verifier strictness).
- #622 doc-sweep (`~/.ansible_vault_pass` refs); hooks README "commit-hash-keyed" → "head-keyed" wording.
- break-map doc `docs/sentinel-matrix-break-map-2026-06-05.md` premise SUPERSEDED → update or retire.
- 🐛 **Prod CPU-CoD 900s-timeouts ROOT-CAUSED + fix ready** (`ac1cdcc9`): = the runaway-repetition loop — `LlamaServerClient.cs:435 LlamaCompletionRequest` sends only temp+`n_predict`, **NO `repeat_penalty`/`min_p`** (llama.cpp default 1.0 = no guard) → looped article runs to `n_predict` → timeout + wasted slot. **FIX = add `repeat_penalty`=1.1 + `min_p`=0.05** to that record (+ optionally bound GBNF block-count). Low-risk live-reliability win. **USER GO 2026-06-09 → ✅ MERGED #641** (main ff'd `a487135d`; review CLEAN; `repeat_penalty=1.1`+`min_p=0.05` both LlamaServerClient sites) → **✅ DEPLOYED + LIVE `af8ca5b6`** 2026-06-09 15:03Z (clean scoped deploy: fresh image `f199f8e8` confirmed running, sentinel-collector healthy, alert-service stayed down). **SMOKE ✅ `a586cb9a` (n=22 post-deploy): 0 timeouts (pre=2), 0 new exceptions, cpu p99 987s→154s declining, ~15 art/hr no stalls** — **RE-CHECK ✅ CONFIRMED `ae50493a` (n=66, 2h42m): 0 timeouts, 0 runaways, trigger 74923 absent, p99 987s→92-244s settled (740s worst = legit long art, watch). #641 deploy gate CLOSED.** Fast-follow option: `a8a38698`'s bonus `ExtractionOptionsValidator` (rep≥1.0, minP∈[0,1]) saved `/tmp/a8a38698-leftover.patch`. [`a8a38698` HUNG ~2h in a runaway A/B (un-guarded arm looped to cap) that MONOPOLIZED the prod llama-server slot + starved the backlog — KILLED + prod freed; re-dispatched impl-only. **Recurring: agents' detached setsid/nohup procs run away → forbid them + cap any bench with strict timeout in every brief.**]
- Optional: #619 strict per-source parallel refactor.

## DEPLOY / RUNTIME NOTES
- Scoped deploy: `ansible-playbook playbooks/deploy.yml --tags <svc> -e "scoped_restart=true scoped_services=<svc>"`. **Always pass scoped flags** — a non-scoped/`[always]` run regenerates compose + resurrects `alert-service` (must STAY DOWN, user) — **incl. `--tags dashboards` (does a `compose up`): `sudo nerdctl stop alert-service` after every dashboards deploy.** `atlas_repo_path` defaults to main checkout → keep it current; verify running digest + `--no-cache` build. [[ansible_deploy_stale_image]] [[project_ansible_deploy_restart_behavior]]
- ansible vault: ansible.cfg uses the absolute path (`/home/james/.ansible_vault_pass`, #622) → plain `sudo ansible-playbook` resolves it. Prometheus rule reload: `sudo nerdctl exec prometheus kill -HUP 1`.
- DB inspect: `sudo nerdctl exec timescaledb psql -U ai_inference -d atlas_data` (SELECT only; never DML [[feedback_agent_no_raw_db_fixes]]). SecMaster DB = `atlas_secmaster`. Schemas: news obs `sentinel.extracted_observations`; matrix feed `public.macro_observations`; cells `public.matrix_cells`.
- vLLM = `Qwen2.5-32B-Instruct-AWQ`, 32K ctx (classifier + GPU). CPU CoD = llama-server (**`--parallel 1`, 32K-ctx/slot, flash-attn on, ubatch 2048, KV-q8**, in `deployment/ansible/group_vars/all.yml`). cpuset: llama-server 0-23, ollama-cpu-gen 24-39, ollama-cpu-embed 40-47. IaC changes must be COMMITTED in the same step (uncommitted drift blocks the next deploy's pre-flight).

## SUPERVISOR OPERATING NOTES
- All code/test/build/deploy → background subagents (worktree-isolate parallel code agents). Verify agent DATA vs DIAGNOSIS (trust data, independently check inferences) before recording/surfacing. [[feedback_verify_agent_inferences]]
- Autonomy: dispatch/review/fix/merge/deploy autonomous; NTFY `atlas-claude-ask` only on genuine blocker / architectural fork / milestone (SINGLE-LINE bodies). End turns `▶ continuing autonomously` vs `🛑 DECISION NEEDED`.
- Merge gate: see SKILL `MERGE_GATE` — the `pr-review-toolkit:review-pr <N>` Skill (with a valid cwd) writes `/tmp/atlas-test-markers/pr-reviewed-<N>`; re-run after new commits. Push to main BLOCKED → branch+PR always; selective `git add -- <paths>`. [[project_pr_review_marker_staleness]]

## ARCHIVE (git log + tags)
2026-06-07 — extraction throughput: queue/frontier monitoring (#636) · per-stage OTEL timing #638 (V2-path + real `timings`) · CPU tuning `3a802551` (`--parallel 1`+flash-attn+ubatch+KVq8, 32K-ctx restored) · bottleneck read = model-bound CPU 30B ~20/hr · memory-hygiene skill+hook (#637).
2026-06-06/07 — digest-quality + reliability sweep: classifier catalog `commodity/fx` + digest FRED-anchor 90d lookback (#633) · ThresholdEngine Roslyn-SIGSEGV serialize+pre-warm (#634, +containerd `LimitCORE=512M` cap) · 30-day `raw_content` retention freed 11.1GB (#635) · classifier-recall natgas/ECB legs + SecMaster-prune/Fred-delete-cascade fixes (#630/#631/#632) · supervisor SKILL.md sharpening (#627) · architecture-cards 11/11 services (#626/#628/#629).
2026-06-05/06 — matrix-health restoration: #618 signal→pattern · #619 grounding resolver · #620 23505 · #621 weights · #622 vault · #623 digest VIEW · #624 FRED reconciliation · #625 classifier-recall · #589 cpuset · #603 verifier-alert. Earlier: counterweight #602/#613/#614/#615/#616 · WS3 #552–#587 · DSL PoC #381–#510 · Matrix MVP #215–#261 · digest plumbing #607–#611.
