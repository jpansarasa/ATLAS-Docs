# ATLAS Supervisor STATE

Supervisor memory. Read first, write last, each turn. This file holds **current truth + pointers**. Arc/iteration history lives in git log + phase tags (see ARCHIVE), NOT here — keep it lean so the live numbers don't get buried.

## EACH TURN START HERE
0. **FIRST TURN OF SESSION ONLY** — git audit before any dispatch: `git worktree list`, `git branch`, `git status`, `git fetch origin && git log --oneline HEAD..origin/main`. Flag stale worktree-agent branches, detached HEADs, dirty main, behind-main drift. Safe cleanup direct; destructive (worktree remove, branch/stash delete) needs user approval. Per [[feedback_git_session_start_discipline]] + [[feedback_git_verify_each_step]] + [[feedback_main_always_current]].
1. Poll NTFY `atlas-claude-reply` (user input first).
2. Re-read `docs/plans/atlas-dsl-poc-plan.md` §5/§6/§7/§8 + this file (RE-READ, not "I read it earlier").
3. Walk plan §8 pipeline backward from this turn's target.
4. Plan vs production mismatch → STOP + NTFY architectural, don't dispatch.

**Canonical pipeline (deployed, matches plan §8):**
SentinelCollector → CPU CoD extraction (qwen3:30b-a3b on `llama-server`, v8 prompt + v2.3 GBNF) → deterministic verifier (`dsl-parser-mcp`, v2_3_1) → `DslToMergedExtractionAdapter` → GPU semantic tier (`EntityResolver` cascade SecMaster CoVe→OpenFIGI→Gemini; `ClaimVerifier` on vllm-qwen2.5:32b; `SectorTagger` NAICS) → `SignalFilter` (§8.2 safety gate) → gRPC `MatrixCellEnrichment` → ThresholdEngine `MatrixCellSentinelWorker` → `matrix_cells`.

Dispatching without re-grounding → [[feedback_walk_pipeline_each_turn]]. Citing benchmark scores as production capability → [[feedback_benchmark_not_production]].

## WHERE WE ARE NOW [2026-05-28]
- **DSL PoC plan §0–§8 COMPLETE + DEPLOYED.** Phase 5 (matrix integration + CPU CoD cutover) merged at `ff72eb8f`, tag `dsl-poc-phase5-done`. **First true Phase-5 activation in prod 2026-05-28 20:41 EDT** (prior `:latest` images were Jan code). Pipeline verified live end-to-end on rss traffic; `matrix_cells` write + audit REST work.
- **CURRENT ISSUE — low matrix-cell yield.** Run9 demo: 2 cells from 1/10 articles. **Root cause (diagnostic-verified): inter-stage WIRING gap — NOT extraction, NOT prompt, NOT architecture.** The CoD-validated CLAIM blocks (`adapterOutput.ClaimInputs`) are orphaned at `CpuDslExtractionService.cs:243`; the semantic tier (`SemanticVerifierService.cs:302-332`, `MatrixCellEnrichmentPublisher.cs:213-266`) re-derives degenerate `predicate:"reported"` claims from flat NUM rows → GPU verifier returns None (`gpu_verifier_none`) + resolver exhausts on NUM-derived subjects (`entity_resolution_exhausted`). The good claims never reach the verifier. **Fix in flight (see IN FLIGHT #2).**

## CPU CoD EXTRACTION QUALITY — VALIDATED GOOD  [reference — read carefully, do NOT call it low]
The cutover proceeded *because* extraction is good. Three scoring conventions (this is what I kept conflating):
- **scored / pooled** = quality on cells that produced output → the TRUE extraction quality (~0.85–0.89).
- **penalized** = empty-doc / parse-fail cells counted as 0 → UNDERSTATES (corpus edge cases drag it down).
- **n=9** = small Arm-B subset → OVERSTATES scale (v8 was 0.907 on n=9 but 0.7826 penalized on n=72). Always cite **n=72**.

Authoritative n=72 figures:
- **ABSOLUTE SOTA = v2.3 stochastic, pooled 0.8733.**
- β v12.1: scored **0.887** / penalized 0.8257.
- v15 (word + chunked) T=0: **0.8261** — deterministic ceiling.
- v8 baseline: penalized **0.7826** — lowest mature prompt.
- n=9 trajectory (subset, not scale): v3 0.790 → v4 0.808 → v5 0.782 → v6 0.895.

**Production recommendation (prior benchmarking): v15 leading, v2.3 runner-up.**
**Deployed in prod: v8 baseline prompt + v2.3 GBNF.** Diagnostic confirms v8 already emits `source_entity` and v2.3 grammar permits it → the prompt is NOT the yield problem; the current fix needs **no prompt swap**. (Upgrading the deployed prompt to the v15/v2.3 family is a separate, optional gain — not the fix.)

## IN FLIGHT [2026-05-28 PM]  — items 1–3 touch the prod stack: sequence those (harness #4 is worktree-isolated)
1. **n=10 acceptance demo** (agent) — running against the deployed (pre-fix) pipeline = baseline yield. Harness allow-list on run9's 10 corpus IDs; restores rss-queue posture after.
2. **Wiring fix — PR #552 OPEN; C1 fix DONE → re-review dispatched (agent a0f0).** Original review (a75c): gates 1/4/5 PASS; 2/3 FAIL on C1 (empty-CoD doc with NUMs but zero CLAIM blocks fell through to legacy NUM-synthesis → fabricated `reported` triples). **C1 fix (a819, commits `eecbd8c0`+`105e16e7`):** discriminator flipped `{Count:>0}` → `ClaimInputs is not null` in BOTH verifier + publisher (invariant verified: legacy=null, CoD adapter always non-null incl empty list) → empty CoD = skip CoVe + zero synthesized claims; legacy null path preserved. +S1 no-fabrication tests, I1 (skip Stage-1 per-NUM resolution in CoD mode, matrix keying verified intact), I2 comment. compile.sh 0-err, **1347/1347 unit + 6/6 integ**. Re-review (a0f0) confirming gates 2/3 now PASS before merge. NOT deployed.
3. **threshold-engine RAM 4G→8G** — QUEUED (user). Ansible compose-template mem limit + scoped `compose up -d threshold-engine`.
4. **CoD→GPU-CoVe acceptance harness — DONE, PR #553 OPEN** (`docs/benchmarks/cod-cove-acceptance/`, reusable/expandable). **Result: VERIFIER HEALTHY** — GPU-CoVe = **97.1% (100/103)** on coherent verbatim-validated CoD claims (3 misses NOT verifier defects). **Confirms the run9 gap is the orphaned-`ClaimInputs` WIRING, not the verifier.** OPEN/unverified: harness flagged some benchmark CLAIM cells as degenerate triples (one predicate fanned across many subjects, e.g. 29149) which CoVe correctly REJECTS — check at PR #553 review whether that's genuine CoD claim structure or a harness reconstruction artifact (per the interpretation guard); does NOT change the headline. Used `claude-mark-verified` (Python-only, no .NET compile). **Architecture: CoD extracts (true/false/null — doesn't matter); CoVe is THE judge; the wiring's only job is to deliver CoD's output to the judge.** Prod bug = the wiring usurped the judge by feeding synthesized NUM-row claims instead of CoD's real ones, so CoVe was judging fiction. **Acceptance (NOT a rigid pass-rate):** (1) the fix/harness delivers CoD's genuine CLAIM blocks to CoVe; (2) CoVe renders real verdicts — confirm true / reject false / null→nothing-to-judge, all correct; (3) end-to-end cells flow ≫ 0, consistent with ~80% extraction (vs run9's dead 0–1/10). A CoVe rejection of a genuinely-unsupported claim is CORRECT, not a bug. The only bug class is the wiring failing to deliver CoD's real output to the judge.

5. **EntityResolver cascade — DIAGNOSED (agent a806), NOT yet fixed.** Cascade is structurally exhaustive; `entity_resolution_exhausted` fires honestly only after all 5 stages (CoVe→composer→OpenFIGI→macro→Gemini); Gemini reachable on the ENT path (default-ON). TWO impl-gaps vs plan §7.2: **(A — high-impact, PRECISION-SENSITIVE):** SecMaster upstream-discovery hits score a flat **0.50** but prepass/composer `MinConfidence`=**0.8** (`EntityResolutionService.cs:635` vs `ExtractionOptions.cs:437`) → discovered instruments dropped before reaching the resolver; the exhaustive fan-out's output is unreachable. **DO NOT auto-loosen** — the 0.8 floor is a deliberate symbol-hallucination defense (symbol-id remediation arc); needs post-fix-demo data + USER decision. **(B — clean):** discovery is sequential + only on local Count==0 (`CatalogService.cs:103-120`); plan §7.2 wants parallel `Task.WhenAll`. Bundle B if A proceeds.

## OVERNIGHT PLAN [2026-05-28→29, autonomous; user asleep; notification-driven]
GATE: do NOT implement cascade Fix A autonomously (precision/safety). Deploy only AFTER the current n=10 demo finishes (recreating sentinel-collector mid-demo corrupts it).
1. ✓ **#552 MERGED** (squash `f531f101`, 2026-05-29). All 3 toolkit reviews (a880 code / a0942 silent-failure / abb7 tests) → **mergeable, 0 critical / 0 important-defect** (merge-guard satisfied by the toolkit run). Local main fast-forwarded; feat branch deleted; only STATE.md dirty.
   - **FOLLOW-UPS (non-blocking, off new main):** (a) test-hardening (abb7): add interleaved blank-subject index-drift symmetry test + empty-predicate `"reported"` fallback test + adapter NUM-only-non-null-empty test — behavior is correct, these regression-proof it. (b) **YIELD #2** (a0942 MEDIUM): #552's `BuildClaimsFromCodBlocks` sets `ParentEvtBlockId=null` → CoD claims LOSE the legacy EVT-sector-tagger fallback → a claim whose subject doesn't resolve drops as `sector_unresolved` (observable). Precision-vs-recall, same class as cascade Fix A → bundle into the user yield decision; do NOT auto-fix. Tempers post-fix expectation (gpu_verifier_none should clear; entity_resolution_exhausted + sector_unresolved remain).
2. **git-hygiene post-merge:** `git checkout main` → `git pull --ff-only` → commit STATE.md to main (supervisor-owned) + push → sweep leftover harness worktree (`agent-a5838e…`). Verify each op individually per [[feedback_git_verify_each_step]].
3. ✓ DONE — n=10 demo **baseline: 1 cell / 1 article (31430 searxng)**, run9-comparable (run9 was 2/1 FOMC). ~9/10 die at `entity_resolution_exhausted` + `gpu_verifier_none` (demo log: "no GPU verifier wired on CoD path" = the PR #552 gap); 152 obs emitted across the set (extraction healthy, dies at gates). Posture restored CLEAN (compose byte-identical sha `a31cd94b`, alert-service DOWN, only STATE.md dirty). One transient 60s SecMaster prepass timeout on 27560 (warm-up after recreate). **Deploy now UNBLOCKED** (demo done).
4. ✓ DISPATCHED (deploy+demo agent): **deploy** merged fix — build `sentinel-collector` from `f531f101` + scoped `compose up -d` (NOT ansible full restart; alert-service DOWN) → bundled with the post-fix n=10 demo (step 5). **RAM 4G→8G DEFERRED** — blocked on the ansible-scoped-restart infra gap: a durable mem_limit change must come from the ansible template, but the playbook's `[always] systemctl restart atlas` cycles the GPU + starts alert-service; direct compose.yaml edit is a HARD_STOP; `nerdctl update --memory` would drift from IaC. threshold-engine was stable at 4G in the baseline demo (no OOM). → **SURFACE to user as a decision** (fix ansible scoped-restart / accept a one-time full restart / keep deferring).
5. ✓ DONE — post-fix demo (agent af75): deploy verified (new image `.Created 23:12:50`, fix live, claims confirmed reaching the CPU DSL verifier). **RESULT: 0 cells / 0 of 10 articles — REGRESSION from baseline 1/1.** **`gpu_verifier_none`=90 did NOT clear** (headline); `entity_resolution_exhausted`=2; SectorTagger **180s TIMEOUTS** observed; 196 obs emitted (way up vs baseline). **Claim-wiring fix NECESSARY but NOT SUFFICIENT** — claims now reach the GPU verifier but the verdict isn't landing in SignalFilter's GPU-support field → still `gpu_verifier_none`. **HANDOFF GAP #2** (verifier-verdict → SignalFilter on the CoD path) and/or GPU timeout under load. (Baseline's 1 cell was the OLD synthesized path slipping one through; the fix unified all claims onto the verifier path, exposing gap #2.) NB: CoVe harness proved the verifier = 97.1% standalone, so the model works — production returns no verdict ⇒ wiring or timeout. TE no OOM (3.69/4G). Posture restored CLEAN (compose sha `a31cd94b` match). Leftover worktree `agent-a5838e` (PR #553 branch) → sweep at #553 merge.
   - **⚠ VERIFIED (direct Prometheus `increase[2h]` over the post-fix window — I queried it MYSELF because af75 and a459 CONFLICTED):** `matrix_filter_dropped`: **`gpu_verifier_none`=117 DOMINANT**, `entity_resolution_exhausted`=50. `claim_verifier_support`: full=54 / partial=3 / **none=117** (gpu_verifier_none ≈ support-none, 1:1). **TRUE picture:** the GPU verifier IS called + returns verdicts (NOT a timeout, NOT the verdict-wiring gap claimed above — both WRONG); it judges **~67% of CoD claims as `none`/unsupported** → 117 dropped → 0 cells. **a459's "#552 worked / gpu_verifier_none cleared" was WRONG** (it read the BASELINE window 00:42–01:55, not the post-fix demo ~03:13–04:10). af75 directionally right (gpu_verifier_none dominant) but its "verifier returns no verdict" was wrong. **`entity_resolution_exhausted`=50 is SECONDARY — do NOT make the entity-res wiring fix primary.** OPEN QUESTION (diagnosis a-noneverdict dispatched): are the 117 `none` CORRECT (verifier rightly rejecting degenerate/incoherent CoD claims — harness 41.9% degenerate vs 97.1% coherent) OR a PRESENTATION gap (coherent claims wrongly rejected, evidence_span/context not reaching the verifier)? — that's the fork. **LESSON: verify agent telemetry reports myself when they conflict / windows are ambiguous.**
   - **✓ FORK RESOLVED (diagnosis a009): (b) PRESENTATION/WIRING GAP — single code site.** `DslToMergedExtractionAdapter.cs:392` (`BuildClaimInputsFromClaims`) sets `EvidenceSpan = rawObject` (the bare 1–4-word `object:` slot, e.g. "virtually"). NO source-sentence assembly on the CoD path → verifier gets a fragment, can't confirm → `none`. The CoVe harness scored 97.1% ONLY because it injected the source SENTENCE (`_enclosing_context` ±240). 85/146 object slots ≤4 words. Tally: (a) genuinely-degenerate correctly-None = 25/28 sampled misses; (b) evidence-starvation = the DOMINANT lever (applies to ALL ~100 coherent claims); (c) prompt/threshold = 0. **FIX DISPATCHED (agent a9fb, branch `feat/dsl-poc/claim-evidence-span-from-source`):** set `EvidenceSpan` = source-text window around the verbatim object (reuse `V2ExtractionPipeline.SnippetFor` / mirror harness `_enclosing_context`); keep rawObject for audit; fix the byte-equiv Python reference (`run_phase4_7_production_pipeline.py:453`). Impl-only, NOT architectural — judge is sound, just feed it the span. Expect cells to flow post-fix. Secondary noise (NOT the driver): SecMaster prepass 60s timeouts + 2 ent_res drops.
   - **✓ FIX DONE — PR #554 OPEN** (`442d2e20`): `EvidenceSpan` = source-sentence window (`BuildEvidenceWindow`, sentence-snapped ±240, byte-equiv to harness `_enclosing_context`); **ALSO fixed `ObjectText`** (was the `{subject} {predicate}` label → now the real object; `RawObjectSlot` kept for audit — the real object never reached the verifier either). Python reference (`run_phase4_7_production_pipeline.py:453`) fixed. compile.sh **1348/1348 unit + 6/6 integ** (1 pre-existing flaky test, passed final clean run). STATE.md verified INTACT after the agent's stash/restore (grep + wc). **PIPELINE: review #554 (ad18 code / ac20 tests) → merge (toolkit gate) → build sentinel-collector → scoped recreate → re-run n=10 demo → measure cell yield → NTFY result.** Phase5-acceptance result files dirty (demo artifacts) → resolve at git-hygiene.
     - ac20 (tests) BACK: **MERGEABLE** — 3 required checks PASS (`.NotBe` anti-regression guard present; no lingering broken assertion; fallback tested). Non-blocking fast-follows: direct `BuildEvidenceWindow` unit test, multi-`\n`/object-at-edge test, stale doc-path comment.
     - ad18 (code) BACK: **MERGEABLE** — 300k-case fuzz confirms the evidence-window is byte-equiv to the 97.1% harness; ObjectText=real object wired. BUT found an **Important §8.2 SAFETY HOLE that #554 INTRODUCED**: non-verbatim object → `BuildEvidenceWindow` fallback returns `rawObject` → `EvidenceSpan==ObjectText` → trips `ClaimVerifier.IsSelfReferential` → auto-grounds **`Full` (0.7) WITHOUT verification** → unverified claim → matrix cell (violates "matrix never receives a value not anchored to source"; ~13% of copy slots non-verbatim). Won't ship; would contaminate the demo with false-positives. **✓ aba5 FIX DONE (#554 `f3afbd73`):** option-(a) — `BuildEvidenceWindow` returns empty on non-verbatim → `ClaimVerifier` short-circuits empty→`None` (+`NoSourceEvidenceMarker`, skips vllm) BEFORE the self-ref check; legit self-ref preserved; +`Trim` hardening, +2 doc-comments fixed, +`CounterCollector` test-isolation race fix. compile.sh **1351/1351 unit + 6/6 integ**. **Re-review dispatched (a084 code / a483 silent-failure) on the new tree (merge gate re-triggered by the new commit).** PIPELINE: re-review clean → merge #554 → deploy → CLEAN demo → NTFY cell yield.
     - a483 (silent-failure) re-review BACK: **PASS / ship-it** — short-circuit fail-safe + observable (distinct `no_source_evidence` tag + marker), empty-span ONLY from the intended fallback, legacy path gated by `HasClaimShape`, no swallowed exceptions, 64/64 affected tests. a084 (code) BACK: **MERGEABLE** (0 crit/0 imp, all 5 gates confirmed, §8.2 hole closed, 82 tests). **✓ #554 MERGED (squash `87ee9748`).**
   - **MERGE-GUARD LESSON:** the gate marker is set by INVOKING `/pr-review-toolkit:review-pr <N>` (the SKILL), NOT by dispatching the review agents directly — burned a failed-merge retry on both #552 and #554 learning this. Going forward: invoke the skill once before merging a supervisor PR.
   - **✓ DEPLOY + CLEAN n=10 demo DISPATCHED (a061)** — full fix on main (`87ee9748`). PAYOFF TEST: cells should flow now that the verifier gets real source-sentence evidence (97.1% on this format). Telemetry rigor required (reset-safe `increase()` + DB cell count); I will verify the headline myself via Prometheus after a061 reports → then NTFY the yield + remaining parked decisions.
   - **✓ a061 POST-#554 RESULT (Prometheus-VERIFIED by me, increase[90m]):** **#554 WORKED at the verifier layer** — `claim_verifier_support` full+partial ≈ **42–46 (was 0 post-#552)**; verifier now verifies coherent claims. The ~54 `none` = the new `no_source_evidence` guard (non-verbatim object → None by design, correct §8.2). **BUT still 0 cells — blocker MOVED to `entity_resolution_exhausted` ≈ 44–46:** every verified-good claim drops because its SUBJECT resolves to `NoResolution` (Phase 4.4 cascade missed). Image `.Created` 05:10Z, demo 05:12–06:02Z, TE no OOM (3.69/4G), posture restored clean. **GAP #3 (entity resolution) — thesis again: verifier stage now validated, failure is the NEXT handoff.** DECISIVE OPEN Q (diagnosis a-entdiag dispatched): of the ~46 dropped subjects, how many GENUINE instruments that SHOULD resolve (→ resolver wiring fix a459 empty-RAG-candidates, OR a806 Fix-A 0.8-floor [precision, user]) vs NON-instruments (parties/agencies/concepts → correct §8.2 drop, pipeline working on non-instrument-heavy corpus). Fork = fix-vs-corpus-artifact → then NTFY.
   - **✓ FORK RESOLVED (diagnosis a8e2): (a)-SIGNIFICANT — a resolver ROUTING BUG, NOT a corpus artifact.** Distinct subjects a=10/b=22; by weight (a)≈35 obs dominated by **Jenoptik AG (26 obs, art 27772, LISTED, catalog-present)**. **Root cause (a806 family):** the EntityResolver cascade's name-capable stage calls `/api/identifiers/resolve?**ticker**=` → 404s on company NAMES; SecMaster's NAME-based `/api/semantic/resolve?q=` resolves them all (Jenoptik→`0ZPV.LON`, Meta→META, Russell2000→RUT, S&P500→SP500, DAX→ETF) but **the cascade never calls it** = EXACTLY the user's "SecMaster finds most via search" point. a459 (empty RAG) secondary. **FIX DISPATCHED (agent a98c, branch `feat/dsl-poc/entity-resolver-name-route`):** add the SecMaster name-resolve stage GATED by the EXISTING 0.8 floor + instrument-type (so non-instruments — the "Bank of Canada"→BMO mis-fire — still drop). Recovers catalog-PRESENT instruments → cells. **Catalog-ABSENT instruments (Adyen/LVMH/CAC40/FTSE100/Stoxx600/WTI) need upstream discovery = Fix-A (precision, USER decision) — still parked.** PIPELINE: a98c → review (INVOKE the skill!) → merge → deploy → demo → cells. **3 handoff gaps fixed: (1) ClaimInputs delivery #552, (2) evidence-span starvation #554, (3) resolver wrong-endpoint a98c.**
   - **✓ a98c DONE — PR #555 OPEN (`b721e09a`):** Stage 2.6 `SecMasterNameResolver` → `GET /api/semantic/resolve?q=`, placed between ticker/alias and Gemini; **precision-gated** (binds only on SecMaster definitive `Resolution` + 0.8 vector floor `NameResolveMinScore`; "Bank of Canada"→BMO @0.62 → drops, tested); `enableDiscovery=false` (catalog absences = deferred Fix-A); new `GroundingSource.SecmasterName` + `secmaster_name` tag. compile.sh **1356 unit + 6 integ**, 5 new tests pass. a459 deferred (name route resolves the blocks without it). **Review dispatched: skill `/pr-review-toolkit:review-pr 555` invoked (gate marker) + a36e (code) / ab74 (silent-failure).** PIPELINE: review clean → merge #555 → deploy → demo → CELLS (the payoff).
     - ab74 (silent-failure) BACK: **ship-ready on §8.2** — gate correct, BoC→BMO misfire DOUBLE-blocked (server 0.8 fast-exit returns null + client floor). No silent failures. **1 HIGH (test-coverage gap):** `SecMasterNameResolver` wire-deserialization UNTESTED (the 5 tests mock the interface). If server JSON drifts, `TopVectorScore`=`VectorMatches[0].Score` silently → null → vector match BYPASSES the 0.8 floor → latent FALSE-POSITIVE regression no test catches. +MEDIUM (`VectorMatches[0]`==bound coupling; no-context weaker precision) +LOW. **DECISION pending a36e: add `SecMasterNameResolver` wire-shape tests (stub HttpMessageHandler + canned JSON) BEFORE merge — protects the §8.2-critical seam, cheap — vs fast-follow.**
     - a36e (code) BACK: **MERGEABLE** — all 5 gates verified against the LIVE SecMaster server (client 0.8 floor ≡ server 0.8 bind threshold = exact complements, NO §8.2 gap; wire shape cross-checked CORRECT). 0 crit/0 imp. **DECISION: MERGE now** — both reviews confirm the code is correct + §8.2 holds NOW (a36e verified the wire deserialization); ab74's HIGH wire-shape test is FUTURE-drift protection (server+client same monorepo, versioned together) → **fast-follow**; the demo empirically confirms §8.2. **✓ #555 MERGED (squash `46e1e3a3`)** — clean (skill invoked BEFORE merge → no failed retry). **✓ DEPLOY + PAYOFF n=10 demo DISPATCHED (ad77)** — all 3 handoff fixes on main; brief demands a cell-subject SAMPLE + `entity_resolution_exhausted` delta + `secmaster_name` binds.
     - **✓ ad77 POST-#555 PAYOFF RESULT (Prometheus-VERIFIED by me):** **#555 WORKED** — `entity_resolution_exhausted` **46→~11**; `secmaster_name` GROUNDED **3** genuine instruments (BoC→BMO @0.754<0.8 correctly DROPPED — NO false-positive); **33 claims now PROCEED past SignalFilter (was ~0)**; verifier full=36. **BUT still 0 cells — blocker MOVED 2 stages down to the SECTOR TAGGER (4th handoff):** all 33 proceeded claims die at `calculator_skipped{sector_unresolved}=33` because the SectorTagger LLM call TIMED OUT **20/20** — it contends with the main DSL extraction for llama-server's SINGLE CPU slot (`--parallel 1`, CPU) + expires at `SemanticPipelineTimeoutSeconds=180`; `SignalMagnitudeCalculator` won't emit a cell without a sector_code. Image `.Created` 02:36Z, demo 06:37–07:32Z, TE no OOM, posture restored clean. **DECISIVE Q (diagnosis a-sectordiag dispatched):** SectorTagger on the WRONG backend (CPU llama-server vs GPU vllm where ClaimVerifier runs → clean CONFIG fix removing contention) OR inherent CPU contention (→ topology DECISION: raise timeout / dedicated slot / deterministic fallback)? Then NTFY. **4th handoff — thesis holds: 3 stages validated (#552/#554/#555), failure is the next handoff (sector tagging).**
   - **✓ a891 VERDICT: (a) wrong-backend DI MISCONFIG → clean fix, NOT a topology decision.** SectorTagger injects the AMBIENT `IOllamaClient` (follows `Extraction:Backend=LlamaServerDsl` → CPU llama-server, the contended `--parallel 1` slot); the ClaimVerifier has its OWN vllm typed client (why verifier=36 worked, sector died). Plan LOCKS sector-tagging to GPU vllm (topology rule PR #386 "immovable" + §7.2 + §3.3); the `arm=vllm_qwen2_5_32b` metric label already CLAIMS vllm — wiring contradicts it. vllm headroom ample (16 seq slots, 0% util, fixed VRAM pre-alloc). **FIX DISPATCHED (a583, branch `feat/dsl-poc/sector-tagger-vllm-backend`):** point the SectorTagger's `IOllamaClient` at `Extraction:VllmEndpoint` (mirror the ClaimVerifier DI ~485-491); extraction STAYS CPU. RESTORES plan topology. **Likely THE FINAL fix for cells>0.** PIPELINE: a583 → review (INVOKE skill) → merge → deploy → demo → CELLS. **4 handoff gaps, all wiring/config: #552 claim-delivery, #554 evidence-starvation, #555 resolver-endpoint, a583 sector-backend.**
   - **✓ a583 DONE — PR #556 OPEN (`552665a3`):** DI rewire — SectorTagger now gets a dedicated `SectorTaggerVllm` named HttpClient → `Extraction:VllmEndpoint` (GPU vllm), mirroring ClaimVerifier; extraction STAYS CPU; `arm=vllm_qwen2_5_32b` label now accurate; +binding regression test. compile.sh **1357 unit + 6 integ**. Review dispatched (skill `/pr-review-toolkit:review-pr 556` invoked for gate + a862 code). PIPELINE: review clean → merge #556 → deploy → demo → CELLS (the payoff).
     - a862 (code) BACK: **MERGEABLE** (0 crit/0 imp; confirmed SectorTagger structured-output works on `VllmClient` = same `response_format`/`json_schema` path the ClaimVerifier uses; circuit-breaker/timeout mirror ClaimVerifier; extraction unchanged). **✓ #556 MERGED (squash `7f8b7003`)** — **ALL 4 FIXES NOW ON MAIN.** **✓ FINAL PAYOFF demo DISPATCHED (a88d):** deploy main + re-run n=10; expect the ~33 proceeding claims to pass sector-tagging (now vllm, no timeout) → **CELLS>0** (genuine instruments, no false-positives). I'll verify via Prometheus+DB → NTFY the cell yield. If cells flow: **CoD→verifier→resolver→sector→matrix end-to-end WORKING.** Post-demo wrap-up: git-hygiene (commit STATE, resolve dirty `phase5-acceptance/*`, sweep `agent-a5838e` worktree, merge PR #553) + deferred test-hardening + surface parked decisions (Fix-A, RAM-bump).
     - **⚠ a88d MID-STOPPED** (Monitor/PID-poll anti-pattern — launched the demo detached + "stood by"). But the demo is VALID + LIVE: running on the CORRECT #556 image (`.Created` 07:54Z, post-merge 07:52Z), PID 781436 → `/tmp/sentinel-remediation/phase5-demo-final/harness.log` (appends `HARNESS_EXIT=` on exit). **matrix_cells 18→19 mid-run — an early cell (PROMISING; allow-list-scoped so likely a genuine demo cell).** Allow-list left SET + compose modified (a88d didn't restore). **Recovery a2fe dispatched:** file-poll `harness.log` for `HARNESS_EXIT` (NOT Monitor) → measure (DB cells + subject SAMPLE + Prometheus SectorTagger-success / `sector_unresolved`-delta) → RESTORE posture (clear allow-list, compose byte-identical == `a31cd94b…`, alert-service DOWN). → I verify (Prometheus+DB) + NTFY the cell yield.
     - **a2fe ALSO mid-stopped** (same background+standby anti-pattern). Demo still running on its own (will finish ~08:51Z, writes `HARNESS_EXIT`; DB has the result regardless). **Attempt 3 = aa82** dispatched with a FOOLPROOF foreground-blocking-poll (`timeout 540 bash -c 'until grep -q HARNESS_EXIT …; do sleep 10; done'`, sequential blocking Bash calls, NO background/Monitor/standby). **RECURRING LESSON (memory-worthy at wrap-up):** subagents default to background+"await notifications" for long waits → orphaned on subagent turn-end; must FORCE foreground blocking sleep-poll. If aa82 also fails: the demo completes anyway → I measure via my own Prometheus+psql + dispatch a scoped restore.
   - **✓✓ aa82 DEFINITIVE RESULT (block-waited correctly, measured, restored):** demo exit 0, window 07:56–08:40Z, verdict FAIL. **0 NEW cells** — the "19" was a COINCIDENTAL 02:48Z PRE-demo cell; my "+1 = first demo cell" read was WRONG (corrected via `created_at`). **#556 CONFIRMED WORKING:** SectorTagger 20/20-timeout → **~78 success**, `sector_unresolved` **33→0**. **Cells STILL 0 — bottleneck moved to NON-WIRING factors:** (1) CPU DSL extraction TIMEOUTS — **3/10 articles failed at llama-server 900s** + verifier hard-fails; (2) filter dropped survivors — `gpu_verifier_none` +46 (no_source_evidence guard + verifier-none), `entity_resolution_exhausted` +8, **filter_proceeded=0** (vs #555 run's 33 — big run-to-run variance from the 3 extraction timeouts). Posture RESTORED clean (compose canonical sha; allow-list was on the harness's extraction path, never persisted to compose; alert-service DOWN). TE no OOM.
   - **═══ FINAL VERDICT (autonomous fix chain COMPLETE) ═══** 4 handoff/wiring gaps all FIXED + verified working (#552 claim-delivery, #554 evidence, #555 resolver-name-route, #556 sector-vllm). Pipeline STRUCTURALLY COMPLETE. Remaining 0-cells = **CAPACITY** (CPU extraction throughput / 900s timeouts — the dominant lever + variance driver) + **PRECISION** (strict verifier/§8.2 — working correctly) + **CORPUS** (R2/R3 macro/political-heavy, thin on resolvable instruments) — NOT wiring. **STOPPED auto-fix chain; surfaced to user (NTFY).** USER DECISIONS: (1) CPU throughput (async/chunking — architectural); (2) Fix-A (0.8 floor, catalog-absent); (3) instrument-heavier corpus; (4) RAM-bump / test-hardening / PR #553 merge. **WRAP-UP TODO: git-hygiene (commit STATE, sweep `agent-a5838e` worktree, resolve dirty `phase5-acceptance/*`, merge PR #553) + recompress this overnight thread in STATE.**
   - **⚠ CORRECTION (a88d deeper root cause — SUPERSEDES the "capacity/corpus" framing above):** the DOMINANT 0-cells blocker is SPECIFIC + fixable: `DslToMergedExtractionAdapter.BuildEvidenceWindow` (the #554 fix) sets evidence_span only if the CLAIM `object` slot is VERBATIM (Ordinal `IndexOf`) in source; else empty → `ClaimVerifier` short-circuits `no_source_evidence` → Support=None → `gpu_verifier_none` drop. **45/87 (52%) of CoD `object` slots are NON-verbatim (CoD paraphrases objects)** → >half drop here. = #554's evidence-anchor precision-vs-recall tension at scale. **5th BLOCKER = evidence-anchor too strict for CoD's paraphrased objects.** Fix (PRECISION-SENSITIVE — touches §8.2/auto-Full guard): (a) anchor on the claim's source provenance / parent EVT source sentence [clean if the DSL provides it]; or (b) fuzzy/normalized object locate [risky] — WITHOUT re-opening the self-referential auto-Full hole. **→ SURFACED to user (precision decision; user owns §8.2); did NOT auto-relax.** Secondary: CPU 3/10 extraction timeouts (capacity). aa82 also flagged a possible verifier-support→filter disconnect (full/partial claims still gated) — verify IF the anchor fix doesn't resolve it.
   - **DEFERRED TEST-HARDENING (one fast-follow PR):** #552 interleaved-blank-subject index-drift test; #554 direct `BuildEvidenceWindow` snap/multi-`\n` tests; #555 `SecMasterNameResolver` wire-shape tests (stub HttpMessageHandler + canned JSON — ab74 HIGH). All protect already-correct behavior against regression.
6. **NTFY user** (atlas-claude-ask) with: post-fix demo yield, remaining drop breakdown, and the cascade **Fix A decision** (precision tradeoff, data-grounded). Verify PR #553 degenerate-slice source → merge harness.

## DEPLOY / RUNTIME NOTES
- Deploy via scoped `nerdctl compose up -d <svc>`, NOT `ansible --tags`: ansible's `[always] systemctl restart atlas` does a full down/up that cycles the vllm GPU + starts alert-service (violates alert-service-down). **Deferred infra item:** ansible needs a scoped/per-service restart mode.
- **alert-service STAYS DOWN** (user; output not useful now).
- Verify lesson: `nerdctl images` CreatedSince is unreliable for buildkit images — use `inspect .Created`.
- PoC test window ended; the deploy restarted the pipeline services + collectors + markitdown. Some MCPs (whisper/ofr/fred/finnhub) may still be stopped — restart as the workload needs.
- Pre-existing (not Phase 5): threshold-engine pattern-data CCSA/ICSA unregistered + ~6 patterns overdue.

## OPEN ASKS (user decisions)
- **Next workstream after the wiring fix lands**: re-demo to confirm yield, then scope Phase 6 — residual `entity_resolution_exhausted` after the fix is genuine catalog miss (data axis, not wiring); audit-provenance `rawContentId=null`; CPU throughput borderline for sync extraction.
- **AutoApprove re-engage** (symbol-id remediation): flip `Extraction__AutoApproveEnabled=true` once true grounding rate ≥90%; needs fresh readiness recon.
- **Statement-level CoVe (#329) + Gemini Google-Finance fallback (#328)** — shipped default-OFF; operator decision to activate.
- **TEI bge-m3 GPU experiment** — optional; does GPU-served bge-m3 beat CPU baseline (~216ms p50 / ~1474ms p95)?
- **Finnhub API key rotation** — deferred; circuit breaker (PR #326) bounds the 403 cost.

## DEFERRED FOLLOW-UPS (non-blocking)
- README-TEMPLATE 6-variant router verification (PR #543).
- `vector_search_duration_ms` old metric name aging out of Prom retention.
- Embedding cache hit-rate alerting (~57% live) if the ceiling stalls.
- readme-consistency skill: sub-component template detection; S5 `IConfiguration` regex; `:` vs `__` key equivalence; Jinja port resolution.
- AtlasSectorCodes compile-time exhaustiveness if a sector is added.
- Parking lot: benchmark llama.cpp vs vLLM-on-CPU on the existing corpus (separate workstream).

## CONTEXT / POINTERS
- DSL PoC plan (current driver): `docs/plans/atlas-dsl-poc-plan.md`; Phase 4 sub-plan: `docs/plans/atlas-dsl-poc-phase4-gpu-semantic-verifier.md`.
- Benchmark scores (authoritative): `docs/benchmarks/cod-2026-05-17/results/**/NOTES.md` (v8 / β v12.1 / v2.3 / v15).
- Matrix MVP plan + handoff: `docs/atlas-matrix-mvp-plan.md`, `docs/atlas-matrix-handoff-v2.md`. Acceptance criteria: `docs/llm/ACCEPTANCE_CRITERIA.md`.
- Phase 5 iteration history: `git show dsl-poc-phase5-done:docs/plans/atlas-dsl-poc-phase5-matrix-integration.md`.

## SUPERVISOR OPERATING NOTES
- All code/test/build/deploy work → background subagents (never direct). STATE.md / plans / CLAUDE.md = supervisor-owned (annotate ≤30 lines/turn; full rewrites only when directed, as here).
- NTFY: publish `atlas-claude-ask`, poll `atlas-claude-reply`. Templates: `.claude/skills/supervisor-mode/templates/`.
- Git: selective `git add -- <paths>`; never `-A`/`-u`/`.` with agents in flight.

## ARCHIVE (recoverable via git log + phase tags — not carried here)
- **DSL PoC Phases 0–5**: PR chain #381–#510 (Phase 0 instrumentation → Phase 1 disambiguation → Phase 2 GBNF → Phase 3 v2/word-grounding schema → Phase 4 GPU semantic verifier → Phase 5 matrix integration + CPU CoD cutover). Prompt arc v3→v15 + n=72 scale-outs. Tags `dsl-poc-phase{4-7,5}-done`; RELEASES.md.
- **Symbol-identification remediation** (2026-05-15/16): rich RAG embeddings v5 + top-N retrieval + CoVe grounding + Gemini catalog-of-last-resort + similarity floor. Plan `docs/plans/symbol-identification-remediation.md`. (AutoApprove Phase 5 still HOLD — see OPEN ASKS.)
- **Matrix MVP Epics 1–6** (2026-05-10/11, PRs #215–#261): TE schema rework, MacroSubstrate `macro_observations`, matrix-storage MVP, REST/MCP surfaces, sector×phase dashboards. All merged.
- Push-guard hook fix (PR #388, tree-hash-keyed markers). Supervisor-mode skill v2 + PLAN_GROUNDING + STATE anchor (PR #511).
