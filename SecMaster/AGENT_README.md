# SecMaster — architecture [agent read-first]

PURPOSE: identity+classification(NAICS/ATLAS-sector)+source-routing. Not price/obs (atlas_data=cross-DB read-only).

DATA MODEL + INVARIANTS:
  Instrument 1-N SourceMapping · 1-N Alias · 1-1 Embedding(bge-m3,unique(InstrumentId,Model)); ->FK AtlasSector(11)
  SignalIdentity = macro handle (FOMC/CPI), not an Instrument; sector=null always.
  AtlasSectorCode/RollupVersionId = plain cols, not DB-FK (real FK->atlas_sectors on naics_sector_rollup).
  INV identity⊥coll: 0 SourceMappings->ResolveAsync=SourceResolution{Warning} not NotFound. "CatalogInstrumentMissingSector" fires whenever AtlasSectorCode=null.
  INV sector-gate⊥asset-class: AtlasSectorCode req only Equity; Indicator/Economic/Rate/Commodity/Currency/Crypto=null-sector valid. Never gate non-Equity (~87% catalog dropped).
  INV asset-class-canonical: asset_class stored as ONE canonical spelling, normalized at the persist boundary (D-5: case-fold + clear-synonym merge fx->Currency/Interest Rates->Rate/treasury->Rate); consumers compare by exact value. Distinct/ambiguous long-tail (Economic Indicator/Credit/Monetary/fred_series) stays PASSTHROUGH — never auto-merge (Economic Indicator is a consumer sentinel). In-app Equity sector gate (InstrumentMeetsSectorGate) is already OrdinalIgnoreCase — the divergence bit value-exact consumers (ops gap query, SearchAsync filter, AV-promote), not that gate.
  INV lazy-load: self-seeds from SEEN; never bulk-preload.
  INV is_primary: FredCollector is_primary=SOLE trust signal, not asset_class str. empty FRED=ANOMALOUS.
  INV Embed 1-N: unique(InstrumentId,Model), not 1-1. entity-default="nomic-embed-text" DRIFTS; runtime=bge-m3. never infer active model from entity-default.

PATHS (distinct code — do not conflate):
  resolve-entities [HTTP /api/resolve-entities · Sentinel->EntityResolutionService]
    does: NER surface+articleCtx->instr+NAICS+sector; dedup(surface,kind); isolated DbCtx+timeout; self-seeds.
    does NOT: serve collectors; serve ThresholdEngine; gate matrix entry.
    on-miss: EnqueueForReview(idempotent open(surface,kind); gated ReviewQueueEnabled)+spanErr; candidate=null; batch survives.
  ResolveBatch [gRPC :5001 · ThresholdEngine->ResolutionService]
    does: symbol->best active SourceMapping. RANK: freq/maxLagDays(filter) -> PreferCollector SHORT-CIRCUITS sort -> [S]OrderByDesc(is_primary)->ThenBy(priority asc)->ThenBy(lag asc).
    does NOT: NER; discovery; OpenFIGI; self-seed.
    on-miss(no instr)=NotFound; on-miss(instr,0src)=SourceResolution+Warning(identity+sector+naics).
  register [gRPC :5001 · collectors; fire-and-forget]:
    GUARD (D-4 allowlist, shared predicate gRPC+REST): untrusted collectors may register ONLY equity-shaped classes {equity,etf,index,currency,crypto,commodity}; ANY macro-shaped class from untrusted (fred_series/fx/treasury/economic/rate/liquidity/...) ->reject(macro_class_from_untrusted); equity-shaped symbol len<2->reject(symbol_too_short). Trusted (FRED/OFR/BLS) bypass the macro gate.
    proto ALIAS_MATCH=3 exists; C# never emits -> dead-on-wire.
    metadata["alias"]->series_id alias ensure (post-success, best-effort): Symbol gets UPPERCASED + resolution is case-SENSITIVE, so collectors alias the verbatim consumer id (mixed-case OFR mnemonic, FH/{sym} stream id); conflict (alias resolves elsewhere) -> skip+WARN, never steal; failure never fails the registration (outcome appended to Message).

RESOLUTION MODEL ("fuzzy proposes, authoritative confirms"):
  local(.95/.85/.75)->[ticker]secmaster(.95)->edgar(.90)->OpenFIGI[.85-.90]->signal-alias(.90)->discovery-PROPOSES->CONFIRM(OpenFIGI->Finnhub->Gemini)->persist+embed|review-queue.
  UNCONFIRMED->null. NotFound="tried everything", not "not in table".
  GEMINI leg = PAID grounded-search LAST RESORT — company-name->ticker:exchange ONLY, gated BEFORE the call (D-1); skips counted secmaster_entity_resolution_gemini_gate_skipped{reason}. ✗send-every-fallthrough: ungated it drained a $100 prepay in 6d, hid 12d behind a green /health (2026-06-30). [[INTENT_FIDELITY]] [[GIGO]]
  ContextFactor: articleCtx non-empty AND canonical does not overlap(suffix-norm)->score=0->conf=0->ALWAYS<MinConf 0.8->DROPPED null, not down-ranked.

DISTINCTIONS:
  resolve-entities(HTTP,EntityResolutionSvc,news) ≠ ResolveBatch(gRPC,ResolutionSvc,series) — different consumers/code.
  symbol-forward(ResolveSymbol) ≠ LookupSource(collector-id-reverse; DORMANT — integration-tests only).
  MATRIX gated by NewsSignalClassifier(signal-dim); resolve-entities=sector-dim only, does NOT gate entry.
  FILTER(freq/lag/preferCollector) ≠ SORT(is_primary->priority->lag). PreferCollector=sort-bypass not filter.
  ContextFactor=0->DROPPED ≠ "scored lower"; identity/classification indep of collection/source-mapping.

CROSS-SERVICE: collectors->register(f-a-f); ThresholdEngine->ResolveBatch(sync); Sentinel->resolve-entities(sync).
  OUT: collectors REST, OpenFIGI, Gemini, llama-cpu-embed(embeddings), llama-cpu-rag(RAG generation). FEEDS: ThresholdEngine matrix via sector grounding.
  BOTH runners = llama.cpp llama-server since 2026-06-11 — NO ollama container remains (ollama exodus; topology = vLLM GPU + llama.cpp CPU). Generation (llama-cpu-rag, qwen2.5:7b q4_K_M): /completion (raw prompt, no chat template; truncation=stop_type:limit or legacy stopped_limit — `stop:true` even at cap, NOT a signal). Embeddings (llama-cpu-embed, bge-m3): OpenAI-style /v1/embeddings {input}->data[0].embedding; SAME GGUF blob as the retired ollama runner, vectors interchangeable with existing pgvector rows (cosine >=0.999999 on prod texts) -> NO re-embed; model field=informational(one model/process). Llm__* names + ILlmClient = engine-agnostic config seam, not engine-pinned. Gen-swap rationale (rag-cpu-scaleout bench): ollama 0.24/0.30 bundled CPU runner decoded same GGUF at 0.2-1.3 tok/s vs llama.cpp ~24 tok/s (~30x ENGINE gap; HT/contention theory REFUTED, second-order +/-30%).
  Gen resilience: breaker=process-SINGLETON(5 consecutive SERVER faults [5xx/conn]->open 60s; SERVER-fault-ONLY — our budget/caller cancels [TaskCanceledException] are NOT counted, they are bounded by the RAG budget CTS not the breaker, else healthy-but-slow flips to a 60s hard outage that flaps+floods) + in-flight gate(4 = runner --parallel slots; cancelled-while-queued never sent) + n_predict cap(64); NO retry on generation — runner computes abandoned non-streaming generations to completion, so re-sends/unbounded fan-in = self-sustaining saturation.
  RAG latency budget (measured 2026-06-11 on llama-cpu-rag, embed island pegged): p50 4.8s solo / 8.7s @c2 / 11.6s @c4 (max 12.5s), prefill ~150 tok/s (~3.3s of solo wall = CPU floor for dense 7b), decode ~24 tok/s (19 @c4) -> ctx cap 600 est-tokens (per-candidate split, snippets clipped, not candidates dropped) + one-sentence answer (21-30 tok) + 50s budget (~4x the c4 worst-burst above; headroom for the 7b CPU runner under SUSTAINED 4-slot queuing — the prior 25s ~2x-bench was too tight once production queue-wait stacks above the bench); queued-budget guard: if <RagMinGenerationBudgetSeconds(default 18s; invariant min-budget<timeout) remain after vector search, abstain(rag_degraded{reason=insufficient_budget}), never send the doomed generation. Budget>solo safe: gate caps fan-in, breaker bounds persistent failure. Throughput ~21 gen/min at gate 4 covers historical 17/min storms.

GOTCHAS:
  ✗ bulk-preload ✗ backfill-rows-to-green ✗ NotFound="not-in-table" ✗ gate-non-Equity-sector
  ✗ Embed-1-1 ✗ trust-entity-default-model ✗ AtlasSectorCode/RollupVersionId-as-FK
  ✗ ALIAS_MATCH-emitted(dead-wire) ✗ macro-class-from-untrusted-collector(D-4 allowlist not blocklist; fred_series/fx/treasury are macro)
  ✗ freq/lag/prefer-as-sort ✗ LookupSource-live-prod ✗ ContextFactor=0-as-lower
  ✗ send-non-company-to-Gemini (D-1: PAID last-resort; ungated=$100-drain 2026-06-30) [[INTENT_FIDELITY]] [[GIGO]]
  ✗ compare asset_class case-sensitively in a NEW consumer w/o canonicalizing input (D-5: canonical at write, but harden — see SearchAsync/AV-promote) ✗ merge a DISTINCT/ambiguous value into AssetClassNormalizer.Synonyms (only CLEAR synonyms; Economic Indicator[sentinel]/Credit/Monetary/fred_series stay passthrough — merging=reviewer-approved schema decision)
  ✗ Polly-breaker-in-per-request-policy-selector # fresh state each request = never opens (7b retry-storm bug)
  ✗ re-sort SearchAsync by symbol "for stability" (D-9: that ORDER BY IS the fuzzy resolution decision; alphabetical resolved Morgan Stanley->CAF)
  ✗ assume the fuzzy tier inspects past candidate [0] # it reads [0] only — a high-confidence row at [1] is never seen
  ✗ add an OR branch to the SearchAsync predicate without a trigram index on that column # one unindexed branch = no BitmapOr = full-catalog seq scan

DECISIONS:
  D-1 gemini-last-resort-earned: INTENT frontier Gemini = PAID RARE exception, EARNED only when all cheap sources (OpenFIGI->Finnhub) failed on a plausibly-hard company name — THERE it pays dividends; ungated call-on-miss = #823 drain ($100/6d) + silent wrong-ticker corruption / PRECOND kind==CompanyName AND plausible-name (no money, no abbrev(EPS,IPO...), no markup, no code-slug, no boilerplate); defense-in-depth: gemini-resolver (host systemd :9300, outside repo) re-gates server-side (_company_gate) + fail-closed daily CALL cap / GUARD IdentifierConfirmationService.ShouldResolveViaGemini @ src/Services/IdentifierConfirmationService.cs:229 / TEST IdentifierConfirmationServiceTests.junk_surface_never_reaches_the_paid_gemini_api
  D-2 fuzzy-proposes-authoritative-confirms: INTENT discovery top-match = PROPOSAL only; resolves+self-seeds ONLY when an authoritative source grounds it in a real FIGI/ticker; unconfirmed->null, never floored-score (old flat 0.50 = 51%-exhausted bug; wrong-neighbour WULF->USAF would corrupt catalog+matrix) / PRECOND confirmation.Confirmed before resolve or persist / GUARD EntityResolutionService.TryResolveViaUpstreamDiscoveryAsync @ src/Services/EntityResolutionService.cs:877 / TEST EntityResolutionServiceTests.should_keep_candidate_null_when_confirmation_returns_unconfirmed_anti_hallucination — RED-on-delete held JOINTLY w/ exhaustive-switch backstop (:889 default-throw->catch :922-930 degrades null): single-line guard delete keeps boundary outcome BY DESIGN (layered defense); test REDs on the entry's real failure mode (floored-score/self-seeded resolution), not guard-line presence
  D-3 notfound-means-exhausted: INTENT NotFound = "tried everything", not "not-in-table" — the contract that lets callers stop at <=1 SecMaster + 1 external call; a true miss = SIGNAL -> review queue (idempotent surface+kind), never silent-null / PRECOND every cascade leg missed incl confirm-cascade+Gemini / GUARD EntityResolutionService.EnqueueForReviewAsync @ src/Services/EntityResolutionService.cs:659 / TEST EntityResolutionServiceTests.should_enqueue_to_review_queue_when_full_fail_through_exhausted_incl_gemini
  D-4 register-ingress-trust-gate: INTENT catalog integrity at register ingress — untrusted/LLM collectors can never legitimately publish macro data (LoRA-era: 365 hallucinated macro rows Dec25–May26; then GeminiFallback tagged fred_series[82,e.g. A824RE1A156NBEA="Mark Rutte"]/fx/treasury sailed past the old 2-item Economic BLOCKLIST); reject at the SOURCE, never backfill-clean later. ALLOWLIST not blocklist: closed-by-default so any new macro-ish label a future model invents is rejected without a code change / PRECOND untrusted collector (NOT in TrustedMacroCollectors) => assetClass MUST be in EquityShapedAssetClasses {equity,etf,index,currency,crypto,commodity} (else macro_class_from_untrusted) AND equity-shaped symbol len>=2; trusted (FRED/OFR/BLS) bypass; ONE shared predicate covers gRPC register + REST /api/instruments / GUARD RegistrationService.EvaluateGuard @ src/Services/RegistrationService.cs:179 / TEST RegistrationServiceGuardTests.should_reject_macro_class_from_untrusted_collector (RED-on-revert-to-blocklist; +should_accept_macro_class_from_trusted_collector +should_accept_equity_shaped_class_from_untrusted_collector)
  D-5 asset-class-canonical-at-write: INTENT one canonical spelling per asset_class so value-exact consumers (ops "Equity missing sector" gap query asset_class='Equity', catalog SearchAsync filter, AV-promote type-map, QueryAnalyzer->SmartRouter) never split on a variant — normalize at the persist choke in TWO layers: (1) case-fold (equity/Equity etf/ETF commodity/Commodity, born at SentinelCollector .ToLowerInvariant + Gemini lowercase DTO) + (2) CLEAR-synonym merge of a placeholder into its canonical class (fx->Currency; Interest Rates->Rate; treasury->Rate — each verified via the actual rows: FRED FX series / FRED rate series / CMT-yield series DGS30+GS10). Distinct/ambiguous long-tail stays PASSTHROUGH by design and MUST NOT be merged: Economic Indicator = consumer sentinel (D-4 macro-guard set + AdminEndpoints.DedupeFredhijacks + HybridResolutionService StrictMode pre-filter all match the exact string); Credit/Monetary/fred_series (mixed bag) = reviewer-approved data call, not a guess. Also REJECTS blank/whitespace/null LOUDLY (GIGO): NOT NULL admits "" and EvaluateGuard has no emptiness check, so a blank class would persist a row invisible to every asset_class='X' consumer — refuse at the source, never store it. Migrations NormalizeAssetClassCase (case) + ConsolidateAssetClassTaxonomy (fx/Interest Rates) + AddTreasuryAssetClassSynonym (treasury) heal existing rows; synonym registry = AssetClassNormalizer.Synonyms (add only CLEAR synonyms). DRIFT COUNTER secmaster_asset_class_canonicalized_total{canonical=<result>} bumps only on a recognized-class CHANGE (tag = resulting spelling, bounded ~12; never the raw input) so a post-deploy smoke test confirms the normalizer is live / PRECOND every InstrumentEntity persist (Added|Modified) supplies a NON-BLANK class; blank/whitespace/null throws InvalidOperationException{symbol,state} / GUARD AssetClassNormalizer.ApplyTo @ src/Data/AssetClassNormalizer.cs:132 (wired SecMasterDbContext.SaveChanges[Async]) / TEST AssetClassCanonicalizationTests.should_find_a_synonym_registered_instrument_by_its_canonical_class_filter (synonym layer, incl treasury; + should_treat_a_lowercase_equity_identically_to_a_canonical_one_in_the_equity_sector_gap_query for the case layer + should_reject_a_blank_or_null_asset_class_at_the_persist_boundary for the reject) + AssetClassNormalizerTests.should_map_clear_synonyms_to_their_canonical_class + AssetClassCanonicalizedMetricTests (counter fires on change, silent on passthrough/unrecognized-trim)
  D-6 runtime-log-level: INTENT a WARN-quiet-by-design service must be raisable to Information/Debug AT RUNTIME (on-demand deep debugging) with NO restart or redeploy, while steady state stays Warning (prod-quiet). A LoggingLevelSwitch is the SOLE level authority; the operator edits Serilog:MinimumLevel:Default in the RUNNING container and reloadOnChange picks it up live. Serilog.Settings.Configuration 9.0.0 does NOT re-apply MinimumLevel on IConfiguration reload, so an explicit ChangeToken re-parse is what makes the edit take effect; the MEL floor sits at Debug so the bridge does not pre-filter below the switch. REQUEST-LOGGING PRESERVED: this service calls app.UseSerilogRequestLogging(...) with a custom GetLevel (client-cancel -> Warning, not Error), whose middleware resolves the concrete Serilog DiagnosticContext from DI. AddRuntimeControlledSerilog replaces Host.UseSerilog (which would register it), so Program.cs registers the DiagnosticContext explicitly (concrete + IDiagnosticContext forwarder) wired to the same switch-controlled logger — drop that and request logging throws at request time; the custom GetLevel is untouched. The raised level lives ONLY in the container's writable layer — revert by editing the key back to Warning (live) OR RECREATE (not restart) the container to reinstate the image's baked-in default; a restart PRESERVES the writable layer and does NOT revert. No compose/mount change (baked-in appsettings is the assumed, acceptable case). / PRECOND operator runs `nerdctl exec secmaster` to edit /app/appsettings.json Serilog:MinimumLevel:Default — no env override for that key exists, so the JSON is authoritative and reloadable. / GUARD RuntimeLogLevel.BuildLogger (ControlledBy = sole authority) + RuntimeLogLevel.CreateSwitch (ChangeToken re-parse on reload) + RuntimeLogLevel.AddRuntimeControlledSerilog (MEL Debug floor) @ src/Telemetry/RuntimeLogLevel.cs / TEST RuntimeLogLevelTests.Runtime_appsettings_edit_raises_then_lowers_serilog_level

  D-7 register-updates-frequency-on-drift: INTENT a collector's frequency CORRECTION must propagate to an existing SourceMapping. register is otherwise INSERT-ONLY for an existing (collector,source_id) — the common re-register/backfill path early-returns "already exists" without touching Frequency — so a corrected collector frequency (fred 07-18 FixSeriesConfigFrequency fixed FEDFUNDS Daily->Monthly in the collector + SecMaster fred-catalog) NEVER reached the source mapping; ResolveBatch kept serving the stale "daily" and ThresholdEngine derived pubFreq=1 for monthly FEDFUNDS -> normal ~2-month value read as 59d "severely overdue" (false page). ResolveBatch/PrimarySource.Frequency IS the authority downstream consumers read (SecMasterFrequencyResolver -> pattern PublicationFrequencyDays), not the fred-series catalog. / PRECOND existing mapping AND request.Frequency non-blank AND differs case-insensitively -> update via UpdateSourceMappingAsync; blank/whitespace request frequency NEVER clobbers a good stored value (GIGO); unchanged (incl case-only) -> no write. Does not touch the D-4 trust gate (runs after it, trusted+untrusted alike once past ingress). / GUARD RegistrationService.RegisterCoreAsync @ src/Services/RegistrationService.cs:277 / TEST RegistrationServiceFrequencyTests.should_update_source_mapping_frequency_when_collector_corrects_it
  D-8 sourcemapping-frequency-blank-reject: INTENT sibling to D-7 — PrimarySource.Frequency (the primary SourceMapping) is the authority ResolveBatch/SecMasterFrequencyResolver serves -> TE pattern PublicationFrequencyDays. The NOT-NULL frequency column admits "" and the register INSERT paths for a SourceMapping (RegistrationService.cs:338 + :400, HTTP + gRPC; :378 is the InstrumentEntity write, deliberately unguarded) assign request.Frequency verbatim, UNLIKE the D-7 UPDATE path which never clobbers a good stored value with a blank — so a blank on a NEW mapping persists and then throws in ISeriesFrequencyResolver.FrequencyToDays("") at TE pattern-load, silently disabling the pattern (same GIGO class as the FEDFUNDS drift, #892; latent today — collectors default, so this is the defense-in-depth net at the single persist choke, not a per-INSERT re-gate). Scoped to SourceMappingEntity ONLY, deliberately NOT InstrumentEntity: instruments.frequency is ~78% blank in prod (18715/23940 — optional/vestigial, not the resolution authority), so guarding it would reject updates to most instruments; source_mappings.frequency is 100% populated (0 blank/7459) so an Added|Modified reject breaks no legacy row. / PRECOND every SourceMappingEntity persist (Added|Modified) supplies a NON-BLANK Frequency; blank/whitespace/null throws InvalidOperationException{collector,source_id,state}. Does NOT fire on the D-7 blank-preserve-existing path (that persists the good stored value, never a blank). / GUARD SourceMappingFrequencyGuard.ApplyTo @ src/Data/SourceMappingFrequencyGuard.cs (wired SecMasterDbContext.SaveChanges[Async] + the TestDbContext faithful double) / TEST SourceMappingFrequencyGuardTests.should_reject_a_blank_frequency_at_the_persist_boundary (+should_persist_a_source_mapping_with_a_non_blank_frequency)

  D-9 search-orders-by-relevance-not-symbol: INTENT InstrumentRepository.SearchAsync's ORDER BY *is* the fuzzy-tier resolution decision, because HybridResolutionService reads candidate [0] and returns it (a high-confidence match at position 1 is NEVER examined). Alphabetical order made that decision INCIDENTALLY: "Morgan Stanley" resolved to CAF (Morgan Stanley China A Share Fund) while MS came back INSIDE the tier's LIMIT 5, at index [4] (of the 10 matching rows the window returned CAF, EDD, IIF, KOF, MS) — it was never EXAMINED, because the fast-exit reads [0] and nothing else, so a correct row sitting anywhere below the top is invisible regardless of window size and RAISING THE LIMIT FIXES NOTHING (widening SearchAsync(query, null, 5) to 20 still reads only [0]); the ordering is the sole repair; "Intel Corp"->0R24.LON; "JPMorgan Chase & Co"->CMC.DE (a Frankfurt line whose ticker root collides with an unrelated US issuer, so the canonicalizer's issuer gate correctly refuses to repair it). A wrong winner here writes a WRONG INSTRUMENT into the macro matrix, and catalog junk-names reinforce it through vector similarity. TWO PATHS reach this method and they are NOT equally guarded: (a) ICatalogService.SearchAsync -> CatalogService.cs:56, consumed by EntityResolutionService.TryResolveViaUpstreamDiscoveryAsync @ EntityResolutionService.cs:826, which IS D-2 gated (unconfirmed proposal -> null, never self-seeded); (b) HybridResolutionService.ResolveLocalAsync's fuzzy fast-exit @ HybridResolutionService.cs:112, reached from EntityResolutionService.ResolveOneAsync @ :457, which returns a ResolvedEntity @ :472 with NO authoritative confirmation at all — on that path the ORDER BY is the ONLY thing between a news surface and a wrong instrument. So this entry does NOT relax D-2 (path (a) still refuses to resolve or self-seed on similarity alone) but "nothing here resolves without authoritative confirmation" is FALSE for path (b) and always was. CONSEQUENCE of relevance ordering, and it belongs in the record: a better-ranked [0] passes IsHighConfidenceMatch more readily than the alphabetically-first row did, so the fast-exit fires MORE often and MORE resolutions take the ungated path (b). / PRECOND rank = exact-symbol(0) -> exact-name(1) -> prefix(2) -> trigram-only(3), ties broken by GREATEST(similarity(symbol),similarity(name)) DESC (Math.Max emits GREATEST: 2 similarity() calls per row, not 4), then bare symbol before a dotted venue line (cross-listings share an issuer name verbatim, so tier AND score tie — SEVEN rows carry the exact name "JPMORGAN CHASE & CO"; nine is the LIKE 'JPMORGAN CHASE%' count), then symbol ASC for a total order. Bare-preference sits AFTER the score so a genuinely better foreign match is never displaced by a weak bare one. Country is deliberately NOT a tiebreak: it demotes only AFFIRMATIVELY non-US rows, so it rewards missing data (TM country=JP loses to 7203.T country=null). / LATENCY accepted, not mitigated — measured on prod (24,221 active rows, LIMIT 5, warm cache): relevance ordering forecloses the early-stop the old ORDER BY symbol got from the symbol btree, so a DENSE substring must now materialise EVERY match — "%ms%" 0.16ms -> 48.7ms (2,148 matches), "%inc%" 0.08ms -> 59.8ms (6,578 matches). The cost is the Seq Scan producing those matches, NOT the top-N heapsort: strip the similarity terms entirely and "%ms%" still costs 40.4ms of its 48.7ms. No index can restore the early-stop — gin_trgm_ops cannot serve an ORDER BY (only gist_trgm supports KNN) and nothing produces rows in relevance order — so the only mitigations shrink the match set. ACCEPTED because a 1-2 char surface reaches this method only AFTER the exact symbol/alias tier missed (i.e. it is not a ticker in the catalog) and a 2,148-row "contains ms" set carries no relevance signal to rank; the SELECTIVE surfaces that are the real hot path pay ~+6.6ms and nothing more ("Morgan Stanley", 10 matches: 31.3ms -> 37.9ms), and THERE the Sort genuinely does see ~10 rows — that claim holds only for selective surfaces, never for short/common ones. If revisited: a prefix-anchored predicate is provably result-identical whenever it yields >= limit rows (tiers 0/1/2 ARE exactly the prefix set and tier 3 sorts strictly after all of them) and measured 0.78ms for "ms" via a BitmapOr on the symbol+name trigram indexes — not taken here because the recall cliff at exactly 3 characters and the buried tier<->predicate invariant cost more than the 48ms they save on junk queries. / INDEX Description is in the WHERE for recall (thematic queries like "semiconductor" match 79 rows via description ALONE) but is NOT ranked; it needed its own trigram index because Postgres only builds a BitmapOr when EVERY OR branch is indexable — one unindexed branch degrades the whole hot-path predicate to a full-catalog seq scan (15923 buffers/42.3ms -> 104 buffers/0.574ms with all three indexes in the BitmapOr). idx_instruments_description_trgm ships in migration AddInstrumentDescriptionTrigramIndex and is NOT yet applied on prod, which is why the "Morgan Stanley" figures above still show a Seq Scan on both sides. / KNOWN LIMIT deliberately NOT addressed here: "Microsoft Corp" still ranks MSF (country=DE, sector NULL) ahead of MSFT. Real, and not a ranking-key defect — it belongs to the US-primary/listing-equivalence workstream (ForeignListingNormalizer / CanonicalizeToUsPrimaryAsync) and needs its own D-entry. / GUARD InstrumentRepository.SearchAsync @ src/Data/Repositories/InstrumentRepository.cs:229 (the ORDER BY) + the reciprocal INTENT(D-9) marker at the consuming fast-exit @ src/Services/HybridResolutionService.cs:104 — the coupling spans two files and neither end means anything alone / TEST InstrumentSearchRelevanceTests, one fixture per ORDER BY key, each mutation-verified RED when its key is deleted: should_rank_a_name_prefix_match_above_a_higher_scoring_mid_string_match (TIER — the ONLY fixture that can catch a tier-CASE deletion; every other one holds a row scoring similarity=1.0, which lets the score key reproduce the same winner and masks the tier entirely) + should_rank_the_basf_se_issuer_above_the_differently_named_india_subsidiary (SCORE) + should_prefer_the_bare_us_primary_over_a_same_name_foreign_listing (BARE-SYMBOL). Regression pins that fix relevance-vs-alphabetical but discriminate no single key: should_resolve_morgan_stanley_to_ms_not_the_alphabetically_first_fund + should_rank_the_exact_name_match_above_a_prefix_match_that_sorts_first_alphabetically + should_rank_tractor_supply_first_for_bare_tsco_in_the_fuzzy_candidate_list; should_not_fast_exit_the_fuzzy_tier_when_the_best_candidate_is_only_a_weak_substring_match pins the fast-exit's confidence gate; HybridResolutionServiceTests.should_ignore_a_high_confidence_candidate_below_position_zero_in_the_fuzzy_tier pins WHY order is load-bearing. Real Postgres required (ranking is SQL; a mocked repository is handed a pre-ordered array and CANNOT go RED on a revert) — which is why SecMaster/.devcontainer/compile.sh runs the integration suite in the DEFAULT gate: behind --integration it never ran before a push while mark-tests-passed.sh wrote the marker anyway, the same hole that left four tests silently red from #231 to #900.

SEE: README.md §Reference (API endpoints, config tables) · Events/src/Events/Protos/secmaster.proto (SecMasterRegistry+SecMasterResolver gRPC contracts) · EntityResolutionService.cs:1180(ContextFactor) · ResolutionService.cs(ranking) · RegistrationService.cs:179-197(EvaluateGuard)
